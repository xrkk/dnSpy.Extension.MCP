# 协作与提交

- 每次 `spawn_agent` 必须显式设置 `fork_turns: "none"`，并提供必要背景、边界及路径。
- 每完成一个功能点或修复点，运行相应验证后立即提交一次；相关实现、测试和文档归入同一逻辑提交，不把多个无关修复积压到任务末尾。
- 提交前检查暂存 diff，只暂存本功能点内容；共享文件按片段分组。不得覆盖其他工作，不自动 push。
- 提交说明记录验证结果及未解决限制；提交不等于正式验收通过。只读取证无需空提交，临时协作文件按既有忽略规则保留。
- 本用户规则取代旧任务中的“不 commit”要求；在途旧任务不得与主会话同时操作索引，待交接后由单一提交方执行。
- 主会话派发并安排 luna+max 监听后，不主动查询 GLM 进度；等待 luna+max 唤醒后验收和续派。减少重复进度更新。
