# 审核处理 P03-CHANGE-002：检查点镜像哨兵编码

- 文档类型：审核处理记录（作者/执行方）
- 创建日期：2026-09-11
- 会话角色：作者/执行方
- 被审文档：`PLAN/2026.09.11/2026.09.11-02-方案变更-P03-检查点镜像哨兵编码.md`（v1 → 处理后升 v2）
- 审核记录：`PLAN/2026.09.11/2026.09.11-03-审核-P03-CHANGE-002-检查点镜像哨兵编码.md`，blob `42ef6e532f3f7ac21f3c08707b991a1c4c6d2020`
- 审核方：sub-plan-reviewer（隔离上下文第三方）

## 逐项处理

| 审核项 | 级别 | 处理 | 落点 |
|---|---|---|---|
| AUD-001 关闭机理 RID 错位缺口 | 问题（决定性） | 采纳。v1"materialize 幂等跳过"确实使 B（哨兵@36）与 C（墓碑清理后 RID 35 空洞→写者补 dummy 行）不对称。v2 改为归一算法：移除空置写者墓碑（`dummy.{GUID}` 补洞行）+ 移除既有哨兵 + 以 `TypeDefUser` 表尾确定性重加；持有占位成员行的 `dummy_ptr` 保留（维持删除态双写确定性机制）。已按基线/删除/恢复三态及删除后再删除逐态推演 A=C=T(实+哨兵@尾)。 | 方案 v2 §2.1-2.3、§5 |
| AUD-002 Interface+Extends 非常规形状 | 问题 | 采纳。哨兵形状改为 `NotPublic|Abstract|Sealed` 类 + Object 基类（与 dummy_ptr 同类常规 metadata），不再使用接口+显式基类组合。 | 方案 v2 §2.3 |
| AUD-003 后续 Pxx 操作与哨兵交互未声明 | 建议 | 采纳。声明哨兵与写者墓碑同待遇：不隐藏、不特殊拒绝、无白名单；指纹投影排除仅用于 roundtrip/导航比较。 | 方案 v2 §3 |
| AUD-004 表计数/引用尾诊断常驻 | 建议 | 采纳。轮次 38 已将 `tables_pre/post`、`refs_tail` 诊断加入 spike；纳入验证计划 §4.3 作为标准输出保留。 | 方案 v2 §4.3 |

## 通过项确认

P-1（问题定位与实证链一致）、P-2（PLAN-CHANGE 升格程序合规）、P-3（§5.1/§8.1/§8.2/CON-009/011/016/024/NON-010/011/fallback 无冲突）、P-4（"P03 未发布、无既有 image SHA 迁移"诚实）、P-5（备选①②③排除成立）、P-6（验证计划主体足以防断言弱化）——维持，无需动作。

## v2 实施验证失败与 v3 升版（2026-09-11 追加）

- v2 按本记录处理后实施并经 VM 双架构验证：TypeDef 表确实归一（T35/T35），但红门禁仍红。新增最小机制探针（SPIKE-PROBE）与删除态镜像诊断（SPIKE-B）证明：图形类型的 corlib 伪基类经类型遍历路径按值去重（45→45 不追加），而 dnlib `ReUseDeletedParamRows` 内部创建的 dummy_ptr 基类在写出早期阶段无条件追加第二行 System.Object（16+46 双行）；引用表行数从源文件继承、图形操作不可脱落——写出期归一原理性无法对称 A(TR45)/C(TR46)。
- 按方案第 6 节既定规则执行：回退 v2 哨兵代码（NormalizeCheckpointSentinel/IsCheckpointSentinel/投影排除），方案升版 v3（采用审核方 AUD-001 关联评估中的备选④"操作期墓碑"），证伪证据与 v3 机理写入方案 v3 §2/版本记录。
- v3 已送原审核方增量复核（`2026.09.11-05-审核-P03-CHANGE-002-v3-增量.md`，blob `7a8ef9add902fbf8ab0d5c79b2b7df1bc08ab292`）：结论**有条件通过（可实施）**。

## v3 增量审核处理（2026-09-11 第二批）

| 审核项 | 级别 | 处理 | 落点 |
|---|---|---|---|
| AUD-101 无 Object 行模块机理不闭合/文档矛盾 | 前置 | 采纳。修正 §1 表述（fixture 的 Object 行在 RID 16，追加源于 dnlib 内部路径无等价检查而非"无 Object 行"）；§2.2 显式前提声明；无 Object 行模块归 PLAN-CHANGE-001 兜底。 | 方案 §1/§2.2 |
| AUD-102 sealed 类含 abstract 虚方法风险 | 前置 | 采纳。宿主方法改为 `Private|Static|HideBySig` 常规私有静态形状；类型保持 `NotPublic|Abstract|Sealed`。 | 方案 §2.1；`EditDeletedRowsTombstone.AcquireParamHost` |
| AUD-104 残留 v2 文字 | 前置 | 采纳。§4/§5 标题与结论、§3 可逆性描述全部改为 v3 口径。 | 方案 §3/§4/§5 |
| AUD-105 回退未发生 | 前置 | 采纳并作为实施第一步执行：`NormalizeCheckpointSentinel`/`IsSentinelMarker`/`IsCheckpointSentinel` 及投影引用、spike 哨兵引用全部移除（机械核对工作区无 `CheckpointMarker`/`IsCheckpointSentinel` 残留）。 | 实施 |
| AUD-103 method_remove/type_remove 命中墓碑的兜底 | 建议 | 采纳。§3 显式声明该入口由 PLAN-CHANGE-001 兜底。 | 方案 §3 |
| AUD-106 宿主形态未经结构验证 | 建议 | 采纳。`EditStructuralValidator` 以显式登记豁免跳过墓碑整型（引用登记注释），样本类型不满足墓碑形状谓词。 | `EditStructuralValidator.Validate`；方案 §3 |

实施中补充发现并修复：本会话新建（RID=0）的参数行不存在于元数据表，删除不产生空洞，跳过墓碑重挂（避免同会话指纹引入无谓墓碑）；恢复识别强制宿主名与行 rid 匹配（负例 `--tombstone-gate` 曾实际捕获"仅查 d 前缀"的缺口后补齐）。

## 结论（v2 时点，已被上节取代）

- v1 审核的全部问题与建议均已处理并升版 v2；作者复评闭环（方案 v2 §5）。
- AUD-001 的修复实现若与 v2 推演不符，以既有红门禁断言为最终判定——断言原文不变，失败即回退重新登记，不在实现层静默调整。**（该判定路径已实际触发并执行。）**
