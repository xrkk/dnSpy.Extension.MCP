# P03 检查点、提交、恢复与会话内历史审核循环熔断处理

- 文档类型：第三方定向复审处理记录（不可变）
- 创建日期：2026-09-04
- 会话角色：作者/执行方
- 所属 Pxx / OUT：`P03` / `OUT-003`
- 原复审：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.09.04/2026.09.04-07-复审-P03-检查点提交恢复与会话内历史.md`，blob `3026a671a82af681704e4c1d51723d66efc1a757`
- 原结论：`AUD-004` 复开并熔断；审核循环熔断
- 处理前方案：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.09.04/2026.09.04-01-实施子方案-P03-检查点提交恢复与会话内历史.md` v2，blob `e1176baed06b211415a005ba0477831d6f832dcb`
- 最小 Spike：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.09.04/2026.09.04-08-Spike-P03会话关闭与恢复最小状态机.md`，blob `41c19be8bea8b6c6541f266855a110289b3b5dfb`
- Spike 模型：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.09.04/P03-session-close-state-machine-spike.v1.json`，blob `a51d2a34060e8bfc0f6f53aa85c258116df31142`
- 处理后方案：同路径 v3，blob `ab4306d777dd4dcf15d4684f0a8abdd07c415755`
- 总纲/需求：总纲 v1 blob `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a`；需求 v0 blob `22574a9054a5016e8a2899f0508d0069e7742843`
- 本地基线：分支 `feature/p01-public-contract-vm-gate`，HEAD `2b24049c12e5d5e02de10c1271614fde98b03f21`
- 结论：同意熔断，根因归类为方案抽象错误；最小 Spike 已完成并被 v3 消费，熔断解除到“重新作者自评”入口，方案仍 `未审核, 不得实施`
- 不可变规则：本记录创建后不得修改。

## 1. 引入门禁与判断

定向复审、v2、历史审核链、总纲、需求和代码事实身份均匹配；审计真实持久化且没有操作 Win10VM。第三方指出的两个可执行缺口成立：P02 没有产品级 unknown 恢复入口，跨会话 terminal 查询也没有 Interface 且不属于 `RACC-019`。继续给原状态表加文字会违反熔断规则，因此不执行第三轮文字补丁。

处理选择是完成独立最小状态机 Spike，而不是新增高风险远程 live 修复或跨会话 terminal 索引。Spike 以 15 条转换穷举 close/live/inverse/cleanup/finalize/recovery/restart；只读机械校验全部通过。

## 2. AUD 处理

### AUD-004：同意复开和熔断；以 Spike 解除

- 根因分类：方案抽象错误。v2 把 DNMCP_TEST emergency seam 误称为 P02 产品恢复，又把内部 session terminal cache 误当成跨会话验收 surface。
- 上游决定：沿用 P02 已审核语义——`live_state_unknown` 只查询/阻断，产品恢复建议为重启 dnSpy；不发布远程 unknown 修复。跨会话断线结果只由 status/history/live/head/package/temp 事实证明，不发布 terminal 查询。
- Spike 证据：模型 15/15 transition ID 唯一；checkpoint/partial 只来自 verified post-live；unknown 允许动作始终仅 `query`；committing 只投影 cleanup-only recovery；forward failure 不可 retry；跨会话 terminal=false。
- v3 修改：版本记录；§4.1 明确 unknown 查询/重启；§4.4 把 terminal 限定在存活 session；§6 inverse failure 把 temp 降级为 residual，删除产品恢复承诺；§7 unknown 无 recovery ID，重启丢失内存状态且 residual 不自动采信；§8.1 复用；ACC-019/024 删除 terminal/product unknown recovery oracle，改为既有事实读回及断言后 test-only cleanup。
- 最小闭合：已满足。产品动作、owner、状态、残留、重启代价和验收 surface 均与现有 P01/P02 Interface 一致，不再要求不存在的调用。

`AUD-001/002/003/005` 的关闭结论继续继承；v3 不改变 source identity、temp 随机身份、容量/ZIP、navigation/finalize partial，unknown temp 变 residual 仍按 `AUD-003` 统一计量且永不由 `cleanup_temp` 删除。

## 3. 实际差异与影响

v2→v3 只包含：

1. 方案升 v3 并引用 Spike；
2. unknown 从“产品显式恢复后清 temp”改为“query/block/restart，temp residual”；
3. session terminal 从笼统保存改为仅会话存活期间有界保存，不提供跨会话查询；
4. ACC-019/024 用五类公开 state surface 和文件/指纹事实取代 terminal/unknown-product-recovery；
5. 明确 DNMCP_TEST emergency cleanup 只用于验收后的 fixture 清理，不算产品能力。

受影响：`RACC-019`、IMP-003/004/008/009、ACC-019/024、unknown/reconnect/cleanup 交界。继承：其余八项 RACC/ACC、Pxx 边界、身份、包/容量、三态、导出、legacy、双 TFM/双架构和文档要求。

全局不变量：单协调器/单事务；只有 live success 才 checkpoint/partial；unknown 不伪装、无远程恢复；不重放；temp 精确删除或 residual 计量；旧 session 失效并释放名额；源和旧 package 不覆盖；无数据库/持久任务/后台恢复；不实施 P04-P09。

## 4. 批次和下一入口

原批次已使用一次全面复审和一次定向复审，且以熔断结束；不得在原批次继续送审。P03 v3 + Spike + 本处理记录形成新的审核批次身份。下一步必须从作者完整自评和需求保真门禁重新开始；若自评通过且高风险触发成立，再以新批次派遣一次全面审核。当前不得进入实施。

本记录不修改原复审，不给 P03 v3 授予审核通过状态，不修改业务代码或 Win10VM。
