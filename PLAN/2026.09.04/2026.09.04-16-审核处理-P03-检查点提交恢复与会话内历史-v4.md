# P03 v4 全面审核结果引入与作者闭环

- 文档类型：第三方审核处理记录（不可变）
- 创建日期：2026-09-04
- 会话角色：作者/执行方
- 所属 Pxx / OUT：`P03` / `OUT-003`
- 原审计：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.09.04/2026.09.04-15-审核-P03-检查点提交恢复与会话内历史-v4.md`，blob `34e2502a8e9b778b225faa90caa05664af59fd7d`
- 审计结论：`存在不同意见`
- 处理前方案：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.09.04/2026.09.04-01-实施子方案-P03-检查点提交恢复与会话内历史.md`，v4，blob `216356483f7dbc8991ce90e2ed89c46207ce7fdc`
- 契约修订后、状态切换前方案：同路径，v5，blob `04306d9474d1ce5b3076f5277b32a4ba1ea9b5d5`
- 处理后方案：同路径，v5，blob `675c6b820085a8ce12afdc12bb1b09fd14a80688`
- 处理轮次：v4 全新批次首次全面审核引入
- 总纲：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.08.31/2026.08.31-02-总纲-dnSpy-MCP结构化程序集编辑.md` v1，blob `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a`
- 需求：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.08.31/2026.08.31-01-需求提炼-dnSpy-MCP结构化程序集编辑.md` v0，blob `22574a9054a5016e8a2899f0508d0069e7742843`
- 仓库基线：分支 `feature/p01-public-contract-vm-gate`，HEAD `2b24049c12e5d5e02de10c1271614fde98b03f21`
- 审核性质：首次审核后的作者自评闭环
- 当前状态：`已审核, 可以实施`
- 不可变规则：本记录创建后不得修改；自身 blob 在创建后计算，不写入正文。

## 1. 身份和漂移核对

审计对象、总纲、需求、P01/P02 前序提交与实施记录、两份 Spike 和 03..14 历史链均可精确复核。审计绑定后没有业务代码变化；工作区新增/修改仅为本次可归因的 P03 v5 方案和处理记录。受保护的 `PLAN/dnSpy-MCP.md`、`PLAN/2026.08.26/独立核验/round-8/`、`tests/spikes/` 未修改、未暂存、未回退。Win10VM 未操作。

## 2. 逐条处理

### AUD-001 — 同意，高风险

- 问题成立：v4 列出六个 commit barrier，但唯一 P03 schema 未明确承接现有 `edit_test_barrier`，实现只加插点会被 P02 name 枚举先拒绝。
- 修改位置：v5 §3.2、IMP-001/009、ACC-019、审核重点。
- 修改：`p03-tool-schemas.json` 现在必须包含 P02 全部 6 个既有测试工具，barrier 保留三个旧 name 并增加六个 commit name；action 固定 `arm|snapshot|release|reset`，结果为 `name/owner/entered/released`，未知名零变化拒绝。运行时和测试都只读这一份合并 schema，旧 P02 schema 不再成为第二事实源。
- 闭合证据契约：九个 name schema 正反例、六个新插点 entered，以及 x86/x64 close 矩阵均进入 ACC-019。

### AUD-002 — 同意，高风险

- 问题成立：当前旧 rename 同步修改 definition 与 same-module TypeRef/MemberRef，P02 单定义 update 不能表达该语义；在 registry 外修改会破坏 checkpoint/inverse 唯一路径。
- 修改位置：v5 §5.1、§10、IMP-002/007/009、ACC-020、审核重点。
- 修改：冻结只由 `LegacyEditAdapter` lowering、不能由公共 `edit_apply` 输入的内部 `legacy_symbol_rename, kind_version=1`。信封按 token 固定顺序记录 definition 和全部同模块 ref 的 old/new 值，forward 逐项核对后修改，inverse 严格逆序；任何漂移或故障共同恢复且不建 checkpoint。旧 counters 保留。
- 边界：仅覆盖现有 type/method/field rename 兼容行为，不扩为任意引用行或 raw metadata 编辑，不侵入 P04。
- 闭合证据契约：三类 closed-generic fixture 均执行 old call→checkpoint→Undo→Redo→export→reload→execute，并覆盖中途故障共同恢复和公共 apply 拒绝内部 kind。

### AUD-003 — 同意，高风险

- 问题成立：旧静态套件要求无路径 save 备份并覆盖源，与 `CON-016` 和 P03 安全默认导出互斥；v4 没有声明该预期变化和旧响应投影。
- 修改位置：v5 §10、IMP-005/007/009、ACC-014/020、审核重点。
- 修改：明确需求强制的兼容变化。无路径 save 写 ArtifactRoot 唯一默认输出，不覆盖源、不产生 backup；显式路径也不得指向源/package/temp。旧字段固定为 `saved_to=output.path`、`bytes_written=output.length`、`backup_path=null`，追加 source/hash/file/history/checkpoint/warning 字段。
- 测试边界：只定点更新 `tests/fixtures/run-tests.ps1` 的旧 save 断言为源 SHA/file ID 不变、默认路径和字段正确；其他 generic rename、执行、错误及静态断言不得放宽。
- 闭合证据契约：无路径/显式路径、旧输出存在/失败、源身份、旧字段和 history/checkpoint 独立读回均进入 ACC-014/020。

## 3. 实际 diff 与影响

方案 v4→v5 的契约差异只有：

1. 合并现有 edit test schema 并扩展 barrier 九名称；
2. 新增一个内部、非公共输入的 legacy rename composite kind；
3. 冻结旧 save 的安全兼容变化、结果投影和定点测试更新；
4. 扩充既有 IMP/ACC，不增删或重编号稳定 ID。

受影响通过项：`IMP-001/002/003/005/007/008/009`、`ACC-014/019/020/024` 的 schema、legacy 和测试细节。未受影响继承项：状态机、unknown/cleanup、三态 migration、source resolver、随机 temp、ZIP/容量、session close 业务终态、atomic export、P01/P02 owner/debug gate、其余六个 ACC 和全部非目标。

全局不变量未变：单 coordinator、单 history package、单 store seam、单 schema 事实、全部 live 写经同一 plan；无第二写状态、AsmEditor、raw edit、数据库、远程 unknown recovery、跨会话 terminal API 或后序依赖。

## 4. 引入后完整作者复评

- 范围/追溯：修改均落在 `OUT-003/P03`，对应 `REQ-017/018`、`CON-007/008/009/016/018/020/024`、`RACC-019/020`；没有需求变化或总纲边界变化。
- ID：`IMP-001..009` 和九个 ACC 稳定唯一，正反向映射完整。
- 可实施性：barrier schema/name/结果/插点、rename envelope/顺序/inverse、save 路径/字段/测试更新均已唯一冻结，没有让实现者选择互斥方向。
- 独立性：P03 只依赖已完成 P01/P02；内部 rename kind 属 P03 旧兼容迁移，不要求 P04；全部 ACC 可在 P03 完成后独立执行。
- 状态/数据/安全：v4 已闭合的 unknown、cleanup 和 exact navigation 未被修改；新增 composite 仍走同一预写/live/inverse/checkpoint 状态机；save 不提供覆盖源 fallback。
- 测试/兼容：九 barrier 确定性 close、三类 rename、save 有意变化均有正反例；旧套件只允许定点更新一项，不能将范围外失败当通过。
- 需求保真：应承接 9 / 完整 9 / 未落点 0 / 语义弱化 0 / 不可验收 0 / 失效或复核条件缺失 0；异常 ID 无。

## 5. 再次审核逐项判定

默认采用首次审核后的作者自评闭环。逐项检查再次第三方审核触发条件：

- 未跨越两个独立项目，也未改变跨项目或多业务共享契约；修改只在当前插件 P03 和其测试/文档边界。
- 不涉及不可逆迁移、凭据、隐私或外部权限；方案尚未实施且所有改动可回退。
- 不改变总纲总体架构、核心提交数据流、并发一致性、恢复/切换策略；barrier 只补齐测试契约，rename composite 是既有 legacy 语义进入已冻结 operation registry，save 是 `CON-016` 已决定的安全策略投影。
- 核心路径可由当前 P03 单提交隔离和回退，影响面可由工具名、内部 kind 和一处旧测试断言机械限定。
- 没有多个仍有证据的可行方向；三个最小闭合条件都由需求和现有行为唯一确定。
- 项目约定和用户均未要求本次修改后再次第三方审核。

因此不命中再次审核条件，不输出复审模式。本记录同时作为“首次审核后的作者自评闭环”，方案状态可设为 `已审核, 可以实施`。

## 6. 结论

三条意见全部接受并闭合；原审计保持不可变。P03 v5 已完成首次第三方审核引入和作者完整复评，无待复审项，取得本流程既有授权下的实施资格。

结论：**方案已修改并由作者自评闭环，无需再次第三方审核，状态为已审核，可以实施。**
