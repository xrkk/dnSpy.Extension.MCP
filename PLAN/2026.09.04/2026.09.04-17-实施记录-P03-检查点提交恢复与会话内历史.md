# P03 检查点、提交、恢复与会话内历史实施记录

- 文档类型：实施记录（持续追加）
- 创建日期：2026-09-04
- 会话角色：作者/执行方
- workflow_id：`dnspy-edit-master-01a050b5`
- 所属 Pxx / OUT：`P03` / `OUT-003`
- 实施方案：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.09.04/2026.09.04-01-实施子方案-P03-检查点提交恢复与会话内历史.md`，v5，blob `675c6b820085a8ce12afdc12bb1b09fd14a80688`，状态 `已审核, 可以实施`
- 总纲：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.08.31/2026.08.31-02-总纲-dnSpy-MCP结构化程序集编辑.md` v1，blob `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a`
- 需求：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.08.31/2026.08.31-01-需求提炼-dnSpy-MCP结构化程序集编辑.md` v0，blob `22574a9054a5016e8a2899f0508d0069e7742843`
- 首次审核祖先：`2026.09.04-03-审核-P03-检查点提交恢复与会话内历史.md`，blob `67033bb9d1b6ae430f2582a044df693f265db84a`
- 当前版本闭环审计：`2026.09.04-15-审核-P03-检查点提交恢复与会话内历史-v4.md`，blob `34e2502a8e9b778b225faa90caa05664af59fd7d`
- 当前版本闭环处理：`2026.09.04-16-审核处理-P03-检查点提交恢复与会话内历史-v4.md`，blob `1d97d80342b789b8a7718b6e8d067a153489e2ca`
- 实施授权：用户通过长程正文明确授权，状态 `进行中`
- 实施执行方：Codex 主会话
- 分支：`feature/p01-public-contract-vm-gate`
- 实施基线 HEAD：`2b24049c12e5d5e02de10c1271614fde98b03f21`
- 前序完成：P01 `236d023c15990bd8240f0f8af98c8b8f8cad3c04`；P02 `f1ca384633c285a0b777921ea685b5178adfc914`
- 轮次：1

## 1. 边界和受保护现场

任务边界：方案 IMP-001..009 明列的 `Editing/`、六个旧写工具/路由、项目嵌入契约、`dnspy_mcp/`、`tests/edit/`、一处旧 save 回归定点更新、双语 README/MCP 资源/完整与定向提示词，以及本 P03 文档链和长程状态。

实施前工作区非干净。受保护且不得修改、暂存、提交或回退：

- `PLAN/dnSpy-MCP.md` 的既有 tracked 修改；
- `PLAN/2026.08.26/独立核验/round-8/` 的既有 untracked 内容；
- `tests/spikes/` 的既有 untracked 内容。

P03 方案/审计链及长程状态是当前任务可归因现场。staged 为空。决策关键业务代码相对审核基线未漂移。所有 VM 动作必须由 AI 通过已连接 Win10VM MCP 执行；若该 MCP 断开，立即停止 VM 动作并通知用户，不自行恢复。

## 2. IMP 状态

| ID | 方案位置 | 状态 | 实际落点 | 验证动作/结果 | 证据 | 备注 |
|---|---|---|---|---|---|---|
| IMP-001 | §11 | 进行中 | 待实施 | 待验证 | 待生成 | 契约、schema、工具和状态 |
| IMP-002 | §11 | 未开始 | 待实施 | 待验证 | 待生成 | history/package/replay/legacy rename |
| IMP-003 | §11 | 未开始 | 待实施 | 待验证 | 待生成 | commit/recovery/session lifecycle |
| IMP-004 | §11 | 未开始 | 待实施 | 待验证 | 待生成 | history navigation/三态 |
| IMP-005 | §11 | 未开始 | 待实施 | 待验证 | 待生成 | store/export |
| IMP-006 | §11 | 未开始 | 待实施 | 待验证 | 待生成 | lineage/divergence/accept |
| IMP-007 | §11 | 未开始 | 待实施 | 待验证 | 待生成 | legacy 六入口迁移 |
| IMP-008 | §11 | 未开始 | 待实施 | 待验证 | 待生成 | Python/docs/prompts |
| IMP-009 | §11 | 未开始 | 待实施 | 待验证 | 待生成 | tests/dual-arch/regression |

## 3. ACC 状态（轮次 61 终态复核：全部通过）

每项 ACC 的技术缺口已在轮次 2-60 全部闭合，双架构（x64/x86）证据日志逐字节一致；§10.1 取证目录 `p03-evidence-20260912-r3-x64/r1-x86`（019/025）与 `p03-evidence-20260913-r1-x64/r1-x86`（013/014/020/024/029）各含 case.json/driver.log/actions.jsonl/summary.json 且全部 `status=pass`。

| ID | 状态 | 证据锚点（轮次 / §10.1 目录） |
|---|---|---|
| ACC-011 | 通过 | 分支/显式目标/Ctrl+Z 隔离（47/48）；validated 自动导航拒绝 restore+undo 双入口并入 EDIT-ACC-013 矩阵 B4/B6（56；`p03-evidence-20260913-r1-*\EDIT-ACC-013`） |
| ACC-012 | 通过 | 单 baseline/payload 去重/唯一解析常驻套件；分页+异源 mvid 反例 `--capacity-resolution`（58；`EDIT-ACC-029` 取证目录同探针） |
| ACC-013 | 通过 | 分类矩阵 harness `--dual-tool-classification`（55，§8 版本判定修复）；集成确认/拒绝+迁移子 exact（56；`EDIT-ACC-013` driver.log `578626f1...` 双架构一致） |
| ACC-014 | 通过 | 导出动作 harness 常驻（46）；MCP 形状 EXPORT_BLOCKED 双向（57 并入 EDIT-ACC-020 C7）；真实 Adapter 文件锁→aborted_temp_cleanup→cleanup_temp（60；`EDIT-ACC-014` `b10e9636...`） |
| ACC-019 | 通过 | 六相位×会话删除+16 名额（52/53；`p03-evidence-20260912-*`）；listener 重启交错（54，`cb736004...`）；stream 关闭与 DELETE 同源终结事实等价记录（P02 ACC-002 传输层语义） |
| ACC-020 | 通过 | 六入口 MCP 形状回归+泛型 MemberRef 完整性+受约束 revert+save 投影（57；`EDIT-ACC-020` `46bc16b9...`）；Ldc_I4_S 操作数缺陷修复后 force_return 通过 |
| ACC-024 | 通过 | commit/undo/redo/restore 注入矩阵 31/17+会话关闭交错（49/50）；导航 forward/inverse 故障+§6 终态语义（59；`EDIT-ACC-024` `7bd0d76b...`）；五类 partial 机制 store 常驻 |
| ACC-025 | 通过 | diverge/accept 端到端 16/16（52；`p03-evidence-20260912-r3-x64/r1-x86\EDIT-ACC-025`） |
| ACC-029 | 通过 | 恶意 ZIP/单 baseline 常驻；22 提交容量曲线/瞬态双份/residual 窗口（58；`EDIT-ACC-029` `265382c5...` 双架构一致） |

产品观察登记（不阻断验收，按总纲回流机制跟踪）：①diverged 错误与 restore assess 不携带 live 指纹（AI 无法自举接纳/应用，轮次 49/50）；②带外删除 ArtifactRoot 后 begin 归类 EDIT_INTERNAL_ERROR 而非谱系缺失诊断（轮次 53）；③UIA 重应用设置即监听器重启、取消本地覆盖复选框不停已运行服务器（轮次 54，编排事实）；④open_files 对 C:\Tools\mcp-repo 路径失败（轮次 48，P01 工具策略，非 P03 范围）。

## 4. 状态变化日志

| 轮次 | ID | 原状态 | 新状态 | 原因 | 证据 |
|---|---|---|---|---|---|
| 1 | IMP-001 | 未开始 | 进行中 | 审核闭环、实施授权和前序门禁均满足；从声明式契约开始 | 方案 v5；处理记录 16 |

## 5. 提交与清理

- 提交：待全部 IMP/ACC 通过后，仅提交可机械归属文件/hunk。
- Win10VM：尚未操作。
- 临时文件/进程：尚无 P03 实施副作用。

## 6. 2026-09-05 恢复实施追加（轮次 2）

用户“构建后暂停”之后明确发送“恢复”，恢复 P03 IMPLEMENT；长程授权仍为进行中。方案 v5 blob 和处理记录 16 blob 重新计算，分别仍为 `675c6b820085a8ce12afdc12bb1b09fd14a80688`、`1d97d80342b789b8a7718b6e8d067a153489e2ca`。分支与 HEAD 未变，未提交任何实现；上方初始状态表保留作为历史，当前状态以本节追加为准。

| 轮次 | ID | 原状态 | 新状态 | 实际落点和检查 | 结论边界 |
|---|---|---|---|---|---|
| 2 | IMP-001 | 进行中 | 进行中 | 内嵌 21 个 schema（13 产品、8 测试）；契约校验退出 0；legacy 入口补齐 unknown/busy 守卫；partial operation kind 与 checkpoint 的 legacy kind 分离 | 不代表所有状态矩阵已通过 |
| 2 | IMP-002 | 未开始 | 进行中 | History/package/replay 和内部 legacy rename 已有实现；严格逆序和故障回滚测试已运行；泛型引用枚举问题已定位并修正 | payload、完整 inverse、容量等仍待完整验收 |
| 2 | IMP-003 | 未开始 | 进行中 | commit/recovery/session lifecycle 已有实现 | close × failure 矩阵未完成 |
| 2 | IMP-004 | 未开始 | 进行中 | history/navigation/三态已有实现 | LCA/inverse 和 migration 全矩阵未完成 |
| 2 | IMP-005 | 未开始 | 进行中 | Windows/in-memory store、owned temp/readback/export 已有实现 | 首次运行发现 injected store 仍访问生产 snapshot；已修正，待复测 |
| 2 | IMP-006 | 未开始 | 进行中 | lineage/source binding/accept 已有实现 | 全部身份反例未完成 |
| 2 | IMP-007 | 未开始 | 进行中 | 六旧入口 Adapter；删除旧 snapshot/直接写；IL 返回保留 SerializeBody/return_behavior；name 空白拒绝 | 完整旧工具回归未完成 |
| 2 | IMP-008 | 未开始 | 进行中 | Python 便利接口 12 项测试退出 0 | README/资源/提示词尚待同步 |
| 2 | IMP-009 | 未开始 | 进行中 | 契约校验、P03StoreHarness 构建和 x64 隔离运行 | 双架构/九 ACC 未完成 |
| 2 | ACC-020 | 未开始 | 进行中 | closed-generic method 的 forward/inverse 顺序、definition/reference 故障恢复、drift 零部分变更在 x64 harness 输出 PASS | 仅子集，不是 ACC-020 完成 |

### 本轮验证及证据

- `python3 tests/edit/validate_p03_contract.py`：退出 0，9 ACC / 22 公共 operation / 13 产品工具 / 8 测试工具，checks_failed 空。
- `python3 -m unittest tests.python.test_edit_client -v`：退出 0，12/12。
- 任务边界内 `git diff --check`：退出 0。全工作区检查另报告受保护 `PLAN/dnSpy-MCP.md` EOF 空行；该原有用户修改未处理。
- 专用 dnSpy 工作树双 TFM build 曾因 net48 的数组 `Reverse` 扩展绑定为 void 失败；改用显式 `Enumerable.Reverse` 后 `dotnet build dnSpy.Extension.MCP.csproj -c Release --no-restore` 退出 0、0 warning、0 error。随后 generic resolver 和测试 store 修正需重新确认最终双 TFM。
- `dotnet build tests/edit/P03StoreHarness/P03StoreHarness.csproj -c Release --no-restore`：多次退出 0；宿主只构建，运行在 Win10VM。
- VM 测试目录：`C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r1`；首次传输 archive SHA-256 `7467e0b659197b9d7b3f77e54fd9262efe10ee2080b4f9f4088e3d1dde2fa5a5`，解包前验证一致；之后仅更新该隔离目录依赖 DLL。
- `result-x64.log`：harness exit 1，反向 rename 校验失败；`result-x64-r2.log`：精确定位 `reference_missing_or_drifted`；`result-x64-r3.log`：rename 子集 PASS，随后 injected Store 无生产 snapshot 导致 capability error，harness exit 1。这些失败不写成通过。
- 根因依据：本机 dnlib 4.5.0 XML 文档 `M:dnlib.DotNet.ModuleDef.GetMemberRefs` 明确 generic 参数引用不缓存、每次新建。当前实现优先同 token 的实际 attached 方法体引用，同时保留稳定缓存表行；不能把原始磁盘投影当实时可变对象。
- 本轮修改指纹：`EditLegacyRenameOperation.cs`（generic 修正后）`1138125ad00481b5701b2ee3c353c753452f1c86db4238eac54fd0c58672cc07`；`EditTransactionCoordinator.cs` `eac3b43031aecba51d73fa47f75235d9c02e5d3565071b50960648ca8360a76d`；`McpTools.LegacyEdit.cs` `b1f7f250749bb7b2f2bfa90fd5bb37a7f8750548567826396064c8aaf2948afa`；harness Program `331dfc8bed75b679489bb49a8faaa4d697850cad53bc8b35d9b63097fd5580f0`。后续变动以新轮次指纹覆盖其证据适用范围。

### VM 安全与恢复检查点

Win10VM MCP 本轮持续可调用，专用 `C:\Tools\dotnet10-x64` 含 Desktop/Core 10.0.11；未改网络、防火墙、快照或管理服务，未启动 dnSpy、未覆盖已部署插件和源样本。测试二进制及日志保留在上述隔离目录作为证据，进程为同步退出的 harness。若管理通道断开立即停止 VM 操作并通知用户，不自行恢复。

当前检查点：P03 IMPLEMENT，继续修正/运行 store harness、完整 inverse/payload/LCA、接口兼容及全部 ACC；不具备提交或推进 P04 的条件。

### 16:08 追加复验

- injected store 修正只作用于显式测试构造器，生产缺失 settings 仍拒绝，不新增生产 fallback。
- `result-x64-r4.log`：rename PASS，随后 package invalid。检查发现全局 `WhenWritingNull` 省略包 schema 必填的 nullable source identity 和 root parent 字段；为对应 DTO 字段显式设置 `JsonIgnoreCondition.Never`。多 root 也在 `Single` 之前机械拒绝。
- `result-x64-r5.log` 与 `result-x86-r5.log`：rename 顺序/故障/漂移子集均 PASS；包预写/读回已走过；均在 `live graph switch` 断言失败，exit 1。原因是整图替换不更新 `ModuleDefMD.ResolveToken` 的既有缓存。该未完成实现不符合 §8.1 的操作级 LCA inverse/forward 要求；继续在 IMP-002/004 内修正，禁止以弱化 token 查询断言来通过。
- 最终双 TFM build：退出 0、0 warnings、0 errors。net48 DLL SHA-256 `66895444e3267c809fed300ec3c9e3f6032858e3a266a6b29723383ac727b4fd`；net10 DLL SHA-256 `1c8e3a0511377e164225f4bd0961bcc387ed77bd082a13ff6b929aa2e0f248d2`。仅发布构建，非验收发布。
- `Editing/EditHistoryModule.cs` 当前 SHA-256 `cf40aa9069a43b12fb36c9f89dd797b000ac54182e0ba257a6fa32ab191c6496`。
- 当前无 VM 管理断连、无外部阻碍，P03 仍在实施；整体 harness 未通过，不提交，不部署 dnSpy，不推进 P04。下一验证点：操作级历史导航及完整可执行 inverse，保持原 token 身份、附件与失败恢复的断言。

## 7. 2026-09-05 16:52 恢复实施追加（轮次 3）

本轮是实际代码及隔离程序验证，不是 dnSpy 窗口/MCP 联动验收。用户询问窗口时已明确说明：未启动 dnSpy、未部署 P03 插件。前一轮属于 progress：编译及失败测试提供了可复核的新证据。当前所有 IMP 仍为进行中，九项 ACC 均未完整通过，禁止提交或推进 P04。

### 修改、失败与最小复验

- IMP-002/004：删除本次未完成的整图切换实现，改为 `EditHistoryNavigationPlan` 的 LCA 逐 operation inverse/forward；预演在 detached graph，实时应用保留对象/token 身份，失败恢复使用完整实时指纹。新增 `EditOperationRegistry.Inverse.cs` 目前只覆盖 update/body/parameter/legacy rename 子集，add/remove 仍明确未实现，不能称为完整 inverse 注册表。
- IMP-002：方法体操作的 payload 按 SHA 去重并落为唯一 ZIP entry；读回展开后才交给操作注册表；恢复包增加实际重放检查；读取 entry 使用声明长度的有界缓冲区。完整 CRC/容量/恶意包矩阵尚未完成。
- checkpoint semantic 使用既有 `ComputeRoundtrip`；实时冲突、回滚和部分提交 pre/post 仍使用完整 `Compute`，并保留独立的完整输出 image SHA。没有用语义相等替代 image 相等。
- `result-x64-r6.log` 的原导航子集通过；r7 的 payload 测试失败，r8/r9 定位第二次事务私有副本失败。r9 原始证据：round0 live/copy header=`True/8`，round1=`False/1` 对 `True/8`，write_mutated=False，semantic_equal=True。`EditWorkspace` 在 private copy/rebuild 保留实时方法头后仍执行完整指纹检查；r10 已越过该失败。
- r10/r11 定位 live image 不同；r11 分类 exact、semantic_equal=True、image_equal=False，长度均 10752，首差异 0x100。r12 证明写出布局相关；尝试再次写出后 r13 的 payload 通过，但普通 rename 仍失败，因此丢弃“固定多写一次”的不充分实现。
- 最终 P03 `WriteCheckpointImage` 保留 `PreserveRids|PreserveExtraSignatureData|KeepOldMaxStack`，不保留源对象的 heap offset 历史；baseline/目标/replay/live image/export 共用该输出路径。P02 `Write` 的 PreserveAll 路径未替换，原始源文件不写。证据为 dnlib 4.5.0 XML `MetadataFlags.PreserveRids/PreserveStringsOffsets/PreserveUSOffsets/PreserveBlobOffsets/PreserveExtraSignatureData` 的定义和 r14 双架构实际结果；决定详见 DEC-008。

### 已运行命令和证据身份

- 专用构建目录 `/home/adminn/tmp/dnSpyEx/Extensions/dnSpy.Extension.MCP`：`dotnet build dnSpy.Extension.MCP.csproj -c Release --no-restore` 与 `dotnet build tests/edit/P03StoreHarness/P03StoreHarness.csproj -c Release --no-restore` 均退出 0，0 warning/0 error；前者覆盖 net48/net10.0-windows。
- VM 仅通过已有 Win10VM MCP 更新 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r1` 中两个 DLL，并分别运行 `C:\Tools\dotnet10-x64\dotnet.exe`、`C:\Tools\dotnet10-x86\dotnet.exe`，参数为该目录 `P03StoreHarness.dll` 和 `C:\Tools\mcp-repo\tests\fixtures\bin\TestIL.dll`。
- `result-x64-r14.log`、`result-x86-r14.log`：各退出 0，四行 PASS 分别为 legacy rename 顺序/故障/漂移、update inverses、payload history（3 checkpoints/1 baseline/1 payload）、store/navigation/readback fault/duplicate manifest/token identity。两个日志 SHA-256 均为 `e8bf96315b173ba1f4ea213d0355d3e04b669dd1d58693eb7346faa5ad1db590`。测试同时断言 live/replay 完整字节相等、输出重载后字节稳定、第二次创建/重建 private 完整指纹不变。
- VM 插件 DLL SHA-256 `df6c29a9ae0e99ced9d4d3f3b4128c89bb1217779e5e9fcc9998c401c88b0dc4`；harness DLL `21fd8f3f2d14e19719896eca70aa0553927fb1ee9d701243c64cfd507f637f08`。
- `python3 tests/edit/validate_p03_contract.py` 退出 0：9 ACC、22 advertised operation kinds、13 product/8 test tools；这只证明声明契约，不证明所有 operation 实现已完成。
- `python3 -m unittest tests.python.test_edit_client -v`：12/12、退出 0。`git diff --check -- Editing tests/edit/P03StoreHarness`：退出 0。
- 本轮源 SHA-256：Workspace `d0dd682d07918770c94cbd31ec684570ca1ebd6dd24c8bbc523ead9848c37f4e`；HistoryModule `7ee174d093c031811011df9a1776ea480b43635fe2d608f179a21391167b8b9e`；NavigationPlan `7bdc1a5b513d7358fb44d58a66411416a6c1d9264e2e947cc94421fb22e563a0`；Inverse registry `9e372a7fc1153b6fc0bd07e51b07006b0fda83b8323208f8b534ffffe0a8f56c`；harness Program `a91e189cb494f985943ab290d2a67bcea81f67659fbc08d3f92a692f65ca2888`。

### 未完成和恢复检查点

- 必须继续：22-kind 的所有 add/remove 与附件/token 完整可序列化 inverse；当前 replay_parent_prefix 存储必须核对是否真正满足 §5.1 的完整语义记录，不能用子集通过掩盖。
- 私有方法头复制目前以 token 映射，新增 Rid=0 对象/删除 tombstone/多个新方法的情形还需扩展验证；现有 fixture 通过不覆盖这些边界。
- heap 布局重建需补充 opaque signature、资源、unused heap 依赖与全部结构编辑/旧工具执行回归；不得据 r14 宣称所有不可信样本行为不变。验证若证明影响规范性来源，必须回流方案层而非静默放宽。
- 仍需完整 source identity/alias、unknown/partial/cleanup、session close、三态真实工具版本、CRC/容量/竞态、双架构 dnSpy+Python client 联动、README/资源/第三方完整与定向提示词和共同回归。
- 当前检查点 `P03_V5_ISOLATED_R14_SUBSET_PASS`；本轮未暂存/提交、未部署 dnSpy、未改 VM 服务/网络/防火墙/快照。harness 同步退出，隔离 DLL/日志保留作证据。无管理断连，无外部阻碍，继续 IMPLEMENT。

## 8. 2026-09-05 17:25 恢复实施追加（轮次 4）

前轮属于 progress，代码、构建和真实双架构测试均有新增证据。本轮继续 IMP-002/004/009，所有 IMP 和 ACC 的整体通过状态不提升；P03 尚不具备提交资格，P04 尚不可推进。

### 本轮实现与逐步反证

- 新增 `parameter_add/remove`、`generic_parameter_add/remove` 的尾部 inverse，以及 `method_body_replace` 的原本无 Body 状态。参数删除记录 owner/index/type、是否存在 ParamDef、名称/属性/token/附件位置；泛型记录 owner/index/name/flags/token 和方法 arity/calling convention。类型/方法/字段/属性/事件的 add/remove 十类仍未实现完整 inverse。
- r15 x64/x86：同一 loaded ModuleDef 内的新尾部 inverse 子集通过，包含完整指纹和原 cached 参数/泛型对象身份。并非跨重载或所有22类通过。
- r16/r17：跨输出重载时参数恢复失败；原 ParamDef token `0x08000017` 成为 `dummy_ptr.{GUID}::mp000017()` 的 `p000017`、sequence=0 占位行。实现严格核对占位 type/name/signature/参数 flags/附件后才将该行按记录放回原 owner/index；正常 owner 行仍拒绝，恢复动作自身的 Undo 放回完整占位状态。
- r18/r19：参数跨重载通过，泛型失败。原 `0x2a000002` 已归属 `TestIL.ObfuscatedDelegate`1`，不能从另一类型挪用；这是 dnlib `PreserveRids` 不涵盖 GenericParam 的真实重排。恢复以已记录 owner/index 的空槽为目标，保留仍 detached 的原对象或构造缺失行，不改变其他类型的 cached 行；再次输出重载验证原 RID 排序恢复。
- r20/r21：参数和泛型的跨重载语义恢复通过；r21 x64/x86 还验证重复 inverse 零变更拒绝、inverse 自身 Undo 恢复完整 pre-state、其他类型对象不被挪用、再次发射后 GenericParam RID/owner 正确。两个 r21 日志 SHA-256 均为 `71203de13ed1343dcef32ac955d36d95bef5c7d105cd8013a4ac45b37e0bc0be`。
- r22 的新增重复 image 断言失败：删除参数后重复写出的完整字节不一致。本机 `ilspycmd -t dnlib.DotNet.Writer.PreserveTokensMetadata /home/adminn/.nuget/packages/dnlib/4.5.0/lib/netstandard2.0/dnlib.dll` 证实 `CreateDummyPtrTableType` 直接使用 `Guid.NewGuid()` 生成占位类型名；缺失 TypeDef 也使用随机 GUID。它不是原样本语义。
- `WriteCheckpointImage` 的实验性实现改为先 materialize，再只为本次新增、原模块不存在的 writer placeholder 按 RID 生成无碰撞确定名称，再重建 heap；不修改原样本中恰好匹配 dummy 命名的类型，不直接修改 raw metadata/PE 字节。r23 x64 的重复 image 断言通过。
- **r24 的更强契约仍失败**：删除→输出→重载→inverse 后，完整产物字节仍不等于旧检查点；失败断言 `restored checkpoint image equals original checkpoint parameter_remove`，退出 1。旧占位表布局仍残留。r21/r23 的局部通过不能证明 exact 历史恢复；不允许把该断言删掉或只比语义来绕过。

### 当前证据及未完成方向

- VM 仍只操作 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r1`；r15..r24 日志均保留。r24 日志 SHA-256 `de2fe95060baca44664d217a6b27b4753f0f1b0d0c5f7dae3178bc7aa5c528f6`。
- 当前 VM 插件 DLL SHA-256 `f2496d66d45670dffa2b0f630d9928a3c180a2cf4b099b84b71786562f87da23`；harness `d73f01e0a26926922d7e2940b17ceee65db08ed1f304effc4433429c653943a9`。
- 当前源 SHA-256：Inverse registry `bed2ea6f65678c0e55861b7917d90e4e6ac2bd2b7bf09402644de70f8586decd`；Workspace `c08126f1972293187bb33bcf9332211213461acff1d9f861d010a1b935324431`；Program `8fa0a5562a19b8b1aac6a42641f0af029bbfa62f5791d4dcd0c16d1f81b29f3b`。
- `git diff --check -- Editing tests/edit/P03StoreHarness` 退出 0；临时 `[DEBUG-p03-*]` 探针已删除，失败/通过原始日志保留；net10 harness 构建退出 0。
- 不能继续假定 PreserveRids 产物在跨重载删除/恢复后与 baseline 完全相同。下一步需以 r24 为 red-capable loop，验证统一产物编码与内部历史对象定位；任何契约/核心恢复方向改变须按流程登记 PLAN-CHANGE 并回流作者自评/必要审核，不能在实现中静默变更或要求用户重新定义通过标准。
- 同时仍需解决：new definition 的 Rid=0 与实时 token 查询、事务 object_id 在跨 checkpoint/reload 的映射、完整 inverse 的持久语义与当前 replay_parent_prefix schema 是否闭合、其余十类 add/remove、全部九 ACC 和真实 dnSpy MCP 联动。上述都是未完成实现/验证，不是外部阻断。
- 授权保持进行中，分支/HEAD 未变，没有 staging/提交，没有 P03 dnSpy 部署或启动，没有操作 VM 服务/网络/快照，没有管理断连。当前恢复点 `P03_V5_RELOADED_EXACT_IMAGE_FAIL`，继续 IMPLEMENT 的最小技术验证，不推进 P04。

## 9. 2026-09-05 17:39 编码与历史引用反例（轮次 5）

- 上一用户答疑轮只核实 dnSpy 进程为零并解释测试范围，不算实现进展。本轮新增可执行反例及双架构证据，属于 progress；所有 IMP/ACC 的整体状态不提升。
- 当前测试仍是 `P03StoreHarness`，不是 dnSpy/client 验收。管理 MCP 上传仅覆盖隔离目录的 harness DLL；没有部署插件、启动 dnSpy、修改服务/网络或恢复虚拟机。
- `TestImageEncodingSpike` 的 `--image-spike` 模式先比较参数/泛型删除→保存→重载→inverse 的完整输出：参数 preserving_exact=False、去掉本实验新增占位类型后的 canonical_exact=True；泛型两者 True。它只证明这两个 fixture 的编码差异，不证明通用 canonical writer 保真。
- 本轮新增真实 `ApplyPersisted` 反例：移除 `TestIL.Members.score` 后 canonical 写出并重载，原另一字段 token `0x04000006` 变成 `0x04000005`。随后将原 token 交给现有 `field_update` 历史操作，实际改名的是另一字段，而预期字段未变。反例完成后调用该操作的 Undo 并验证错误字段名称复原；所有修改仅位于已隔离的内存 ModuleDef。
- x64/x86 均输出 `SPIKE stale-persisted-token=wrong-field-mutated restored=True`，退出 0 表示反例被成功证实，不表示产品通过。正常 r24 全 image 恢复失败断言保留，未削弱。
- 构建命令：专用 dnSpy checkout 内 `dotnet build tests/edit/P03StoreHarness/P03StoreHarness.csproj -c Release --no-restore`，退出 0、0 warning/0 error。运行操作：分别以 `C:\Tools\dotnet10-x64\dotnet.exe` 与 `C:\Tools\dotnet10-x86\dotnet.exe` 执行隔离目录 `P03StoreHarness.dll C:\Tools\mcp-repo\tests\fixtures\bin\TestIL.dll --image-spike`。
- 持久原始证据：`C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r1\image-spike-x64-r2.log` 及 `image-spike-x86-r2.log`；两者 SHA-256 均为 `244c5a4bebc9ed58163b2dbe5cbd895d1703dedbe9fbfe67f402d7ae427b9db0`。VM harness SHA-256 `c5b04378e7fcaed44d2e29ba1cb81a78c6d957424f359795cd14b27bd4677741`；本轮 Program.cs SHA-256 `42fffc3ef84314148bc66a8cc084bc6b879d50f3d5b8cf9c810cc88f8f12d83a`；作用域 diff-check 退出 0。
- 结论：排除“直接把 WriteCheckpointImage 换成 WriteCanonical 即完成修复”。`EditHistoryModule.Replay` 当前保留原 token，`EditOperationRegistry.ParseOperand` 的 body token 也直接 ResolveToken；恢复和操作目标/操作数引用必须一起验证。P03 §8 已要求重放时确定的 token/稳定身份，不能依赖未经映射的历史 raw token。下一步在该已要求的稳定身份范围内验证定位及发射一致性，完整 inverse/全部类型/附件仍未完成；若需要改变包契约或恢复策略，则先登记 PLAN-CHANGE 并回流，不静默替换生产路径。
- 本轮仅修改测试和记录，无生产修复、提交或 P04 推进。恢复检查点为 `P03_IMAGE_SPIKE_STALE_TOKEN_WRONG_TARGET_PROVEN`；无外部阻碍，授权继续进行中。

## 10. 2026-09-05 17:47 定义位置与引用表对照（轮次 6）

- 上轮有可执行反例和原始证据，分类 progress；本轮只深化已确认需要的稳定身份验证，没有更改生产包契约或宣称 IMP/ACC 通过。
- 新 `--locator-spike` 实验：在 TestIL 内删除 score、改名所属类型、添加嵌套类型和两个同名但不同类型的零 RID 字段；按当前操作前的类型层次/成员位置、参数 sequence、泛型 number 建立索引。canonical 写出/重载后全部 194 个定义的地址集合、table 和文本表示对应；通过既有 object_id 输入应用 field_update，只改变第二个同名字段，再 Undo 验证恢复。该地址只描述操作前状态，不声称跨增删仍是同一永久 ID。
- r1 x64/x86 以上子实验通过，原日志 `locator-spike-x64-r1.log` / `locator-spike-x86-r1.log` SHA-256 均为 `6bac799a74419b959ddacdc4468004b17559911ebd962618aebf32fc6fb81517`。
- 第二个对照保留 TypeRef/MemberRef/StandAloneSig/TypeSpec/MethodSpec 的 RIDs 和 ExtraSignatureData，但不保留定义表 RIDs；使用安装的 dnlib 4.5.0 XML 中相应 MetadataFlags 定义作为 API 依据。106 个既有引用表行的 token/文本表示在输出重载后保持，且删除态输出重复写出完整字节相等。这里只覆盖当前 fixture 的既有引用，不宣称 opaque signature、未使用引用、新建引用、embedded PDB 或任意恶意样本均已验证。
- r2 x64/x86 在原 `RestoreParameterTail` 失败（历史参数 token 已不是原对象）；日志 SHA-256 均为 `019ce132030edc7ff7c9e9a38cafbd892c1fdc343ffe58cac46e6308761b8120`。r3 保留并断言该失败及完整 pre-state 不变，然后只在实验里将 owner 解析为操作前位置对应对象，并把缺失参数行交给现有零 RID materialization 分支；原 token 单独保留作实验来源，不把它冒充新表指针。完整恢复输出与删除前逐字节一致。
- r3 双架构输出 `definition-slots=194 ... correct-target=True restored=True` 和 `retained-reference-rows=106 stable-image=True raw-inverse-rejected=True slot-inverse-parameter-restore-exact=True`，两进程退出 0。证据目录仍为 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r1`，`locator-spike-x64-r3.log` / `locator-spike-x86-r3.log` SHA-256 均为 `2389d3edf2faa5791965636d3ee66c90a4c4efe56b80be4f1c2abf6cc631bb63`。
- 本轮仅新增 `tests/edit/P03StoreHarness/Program.cs` 测试逻辑，SHA-256 `ca38312abc89d9ee95822ea6f6633a035d0ed107fe964fc7890377fc1953a985`；VM harness SHA-256 `899116d895f2c6226eb4e9e80da3fc56ace8399e9a75997fdbe51dde68e532aa`。专用 checkout 的 harness Release build 退出 0、零警告/错误；范围内 diff-check 退出 0。执行分别使用既有 x64/x86 dotnet，参数 `P03StoreHarness.dll <既有TestIL.dll> --locator-spike`，没有执行样本入口。
- 结论与下一步：已有证据支持把定义位置解析与引用表编码作为统一历史内部处理的候选，但还不能替代正式实施。必须验证连续操作前位置更新、inverse 全语义持久化、token provenance 与实时 object identity、body 引用/legacy 精确引用行、附件和原样本 dummy 命名。若细化改变已冻结包形状/恢复策略，先登记 PLAN-CHANGE 并完成相应闭环，再修改生产路径。正常 r24 失败保留，全部九 ACC 及真实 dnSpy/client 联动仍未通过。
- VM 管理未断连，无服务/网络/快照操作、无 dnSpy 部署/启动、无提交；无外部阻碍。检查点 `P03_LOCATOR_REFERENCE_SPIKE_SUBSET_PASS`，授权进行中，P03 继续，不推进 P04。

## 11. 2026-09-05 17:53 私有副本多个新方法修正（轮次 7）

- 本轮在 IMP-002 的私有副本保真边界内修正实际代码；不是包格式或产品恢复契约变更，不更改审核方案版本。上一轮实验产生新证据，为 progress。
- 新回归模式 `--workspace-new-methods` 在既有 fixture 中添加两个 RID=0 的方法，分别设置不同 InitLocals/MaxStack，再实际调用 CreateForTesting 和 RestoreCommittedState，要求 live、private、rebuilt 的完整指纹相等且两个方法头分别保持。
- 修正前 x64 r1 退出 1：CaptureBodyHeaders 的 ToDictionary 抛重复键 100663296（0x06000000）。该原始日志保留于隔离目录 `workspace-new-methods-x64-r1.log`。
- EditWorkspace 的方法头临时映射由 raw token 改为类型层次/成员位置；仅用于无编辑的私有复制/重建，不作为持久历史 ID。缺失位置/Body 仍拒绝，原完整指纹守卫保留，不影响公开 token 或包字段。此项解决既有零 RID 键碰撞，不等于历史稳定身份已完成。
- 修正后 x64/x86 `workspace-new-methods-*-r2.log` 均退出 0，SHA-256 均为 `7f4a9952814111d2ef8cdc124e65a88477af28d80a75fb4e16045c800871ec49`。测试纳入常规 harness，并保留单独运行入口以免上游失败掩盖其结果。
- 双 TFM Release build 退出 0、零警告/错误；harness build 退出 0；P03 声明契约验证退出 0（仅 schema 声明，不代表22种操作通过）；作用域 diff-check 退出 0。
- 常规 r25 双架构重新运行：legacy/update/tail 子集通过，仍在旧的 parameter_remove 完整 image 恢复断言退出 1，日志 SHA-256 均为 `de2fe95060baca44664d217a6b27b4753f0f1b0d0c5f7dae3178bc7aa5c528f6`，与 r24 同根因，未削弱断言。
- 本轮源 SHA-256：Workspace `2d555bbdd2015005da9355cd5256df6d9eee620f7a5a9083717104462169317f`；Program `74f26bccd2ab2d662e94eaa8b525f95c05cc719def6e118915e1ce9aa0d31a1a`。VM 仅更新隔离目录两 DLL并运行 dotnet harness，未部署/启动 dnSpy，无管理断连或服务/网络/快照操作。
- 所有 P03 IMP/ACC 整体仍未完成，未提交、不推进 P04。恢复点 `P03_WORKSPACE_ZERO_RID_HEADERS_FIXED`；下一步继续历史操作定位/完整 inverse 的统一契约与实现，原 exact-image red 测试仍为硬门禁。授权进行中，无外部阻碍。

## 12. 2026-09-05 17:57 内部定义定位器（轮次 8）

- 上轮实际修复私有副本并验证，分类 progress。本轮按 P03 §8 的重放稳定身份及 IMP-002/004 新增内部 `EditDefinitionAddress`，尚未接入生产包读写，不宣称主问题解决。
- Capture/Resolve 覆盖 type/nested type/method/field/property/event/ParamDef/GenericParam；类型成员使用当前位置，参数用 sequence、泛型用 number。Capture 验证对象确已挂接且解析回同一引用；路径解析拒绝非规范数字、负数、溢出、未知段、错误父类型、越界、重复 sequence/number；类型 owner 循环检查避免捕获死循环。没有名称 fallback、公开工具或 token 输入变化。
- 定位器只适用于调用者已核对的操作前图；不是永久 ID。串联历史时每步的 pre-state 绑定与 inverse 恢复对象完整语义仍需实现，不允许单独解析旧位置后盲目修改 live。
- 独立测试索引对 194 个定义分别校验生产 Capture 与 Resolve，另覆盖空/不完整路径、负数、前导零、加号、溢出、越界、未知段、字段下再索引字段及未挂接方法，既有 106 引用与参数 exact 恢复实验继续通过。
- x64/x86 `locator-spike-*-r4.log` 退出均为 0，SHA-256 均为 `2389d3edf2faa5791965636d3ee66c90a4c4efe56b80be4f1c2abf6cc631bb63`；日志目录仍为 VM 隔离目录 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r1`。构建 harness 和双TFM Release 均退出0、零警告/错误；作用域 diff-check 退出0。
- 源SHA-256：EditDefinitionAddress.cs `b96fc1b26a65a50920fd4714951c277d7c9fa0b528dab849636bf6c57ba06158`；Program.cs `ca9fb5e74129ae07061c7aa8b0d94b8cce8c395069d2fc99dcf92c60b6337a63`。测试日志文本与r3一致不能单独证明新增断言，须同时绑定本轮源指纹和本轮构建/运行记录。
- 当前仍是未完成的内部实现。包序列化、引用 lowering、完整 inverse、实时身份与全部验收未闭环；涉及冻结包契约或恢复策略改变时仍须先走 PLAN-CHANGE。未部署或启动dnSpy，未改VM管理服务/网络/快照，无管理断连、无提交、无外部阻碍。检查点 `P03_DEFINITION_ADDRESS_PRIMITIVE_VERIFIED`，授权进行中，不推进P04。

## 13. 2026-09-05 18:00 新增引用撤销反例（轮次 9）

- 上轮定位器实现和验证为 progress。本轮核对 `EditTypeSigParser.Resolve`，确认 P02 已支持通过签名编辑创建 TypeRefUser；既有106引用的实验不足以支持保留全部引用RID的生产方向。
- 在 `TestReferenceEncodingSpike` 增加初始不存在 System.Uri 的机械前置，改方法参数类型为 System.Uri→保留引用写出→重载→恢复原类型。实际输出 `preserving-exact=False canonical-exact=True`：保留引用RID使不再被使用的新增引用残留，不能回到旧检查点完整image；非保留输出在该fixture上恢复一致。
- x64/x86 `locator-spike-*-r5.log` 均退出0（反例成立，不是P03通过），两日志SHA-256均为 `b241e648d00742ffcd593d580cc8df62c26f77fc578ae3a76562a910f6222b73`，位于既有VM隔离目录。Program.cs SHA-256 `e96983512343d17f5c650e27cc30cf35e8498c0411c2180575dc4dd9bafe42fd`；harness build退出0、零警告/错误。
- **排除的方向**：不得把“定义位置+保留所有引用RID”直接升级为生产编码契约。r3/r4仅证明固定引用集下的子集，不再支持对新增引用的推广；定义位置Capture/Resolve本身的证据未失效。
- 下一步需统一处理非定义引用的语义定位或发射映射，不能继续在每种删除上累加占位表清理；同时补齐inverse完整语义而非当前parent-prefix摘要。正式变更仍按冻结包/恢复边界回流；本轮未修改包schema或生产writer，未降低exact标准。
- 当前所有P03验收仍未完成，无提交/部署/启动dnSpy，无管理断连或VM配置变更，无外部阻碍。检查点 `P03_REFERENCE_PRESERVATION_COUNTEREXAMPLE_PROVEN`，授权进行中，不推进P04。

## 14. 2026-09-05 18:05 五类新增定义inverse（轮次 10）

- 上轮新增引用反例改变后续编码选择，为progress。本轮按既有IMP-002/004补齐type_add/method_add/field_add/property_add/event_add的可序列化compiled inverse；没有改变公开工具、包schema或Pxx边界。
- inverse保存add_kind、操作前owner地址、追加位置及预期名称；应用要求恰为该尾位置、名称一致，然后经同一registry的reject_if_referenced删除，不整图替换。删除成功移除该对象的事务map绑定；inverse自身Undo恢复相同对象引用、附件位置及原map绑定。该内部动作仍须由上层操作前指纹保护，不把位置/名称当成充分的跨状态身份。
- 测试模式 `--definition-add-inverses` 覆盖上述五类（type为nested新增），逐项序列化/反序列化inverse，验证完整graph指纹、map移除、重复inverse零变更拒绝、inverse自身Undo原对象身份与map恢复、再次inverse回到原指纹。顶层type、跨checkpoint/reload及组合操作尚需补测。
- x64/x86 `definition-add-inverses-*-r1.log` 均退出0，SHA-256均为 `9444c1f112f2e3b1831143156eaab76b6d84ec550c81d1096854712ad2f8d2f6`。VM证据目录仍为 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r1`。harness与双TFM Release build退出0、零警告/错误；作用域diff-check退出0。
- 源SHA-256：Inverse.cs `1283ce4dc8953952798409e3b63aef24fcc78ffb0d95ccf888b7ee972feb36da`；Program.cs `456c4435a2ee17e2b5a862e85d4ac9f913fb9ee9b2d75387202dfb5d93b35091`。
- 仍未完成五类definition_remove的完整语义恢复、compiled inverse包持久化、引用lowering/编码及全部9 ACC/真实dnSpy联动；新增支持不等于22类已验收。未部署/启动dnSpy、未改管理服务/网络/快照、无断连、无提交；授权继续，检查点 `P03_FIVE_DEFINITION_ADD_INVERSES_SUBSET_PASS`，不推进P04。

## 15. 2026-09-05 18:10 零RID引用守卫修正（轮次 11）

- 上轮五类inverse落地及验证为progress。本轮核对删除守卫发现Same把非零table prefix当作已分配RID，导致同表的两个零RID新对象被错误视为相同。
- 新 `--zero-rid-reference` 用例创建两个零RID静态字段及只读取第一个字段的方法，删除未被引用的第二个字段。修正前x64 r1实际退出1（FieldRemove的引用检查拒绝）。
- Same仅将实际ReferenceEquals视为相同，或在两者RID均非零且完整token相等时判同；不降低真实引用拒绝。修正后x64/x86专项均通过：未引用字段可删除、Undo恢复完整graph、真实引用字段仍零副作用拒绝。该直接依赖修正是既有身份守卫实现纠错，不改变公开/方案契约。
- `zero-rid-reference-*-r2.log` SHA-256均为 `87d08e21174edae81fef5adebed697992f1ccd8c511a1b202f51f98e3bd89dcd`，两进程退出0。常规r26双架构legacy/update/tail/五类新增definition/零RID引用子集通过，仍在原parameter_remove全image恢复断言退出1；`result-*-r26.log` SHA-256均为 `8de7ef82d7618d007a17aa38ff6ed13ac88c0e4eb11c242d714e2731c66f133d`。
- 日志位于既有VM隔离目录；仅上传两DLL、运行harness，无dnSpy部署/启动、无VM管理服务/网络/快照修改，无断连。harness与双TFM构建退出0，零警告/错误；作用域diff-check退出0。
- 源SHA-256：Registry.cs `81d8910123bcd749f3ff9126992760dc1e146eff0581d74d7db56c09cf6956ef`；Program.cs `981c17156173cfe49d182d0be54a81721f581de6b19d1af05d227155db674738`。
- 当前删除守卫允许的完整恢复数据不能简化：字段可有constant及复杂FieldSig；property/event可有全部accessor/other methods及signature；方法可有参数/泛型附属数据与完整body；type删除守卫不等于整个子树均无附属数据。后续完整inverse必须据此实现，不能仅复用公开add字段然后宣称等价。
- 检查点 `P03_ZERO_RID_REFERENCE_GUARD_FIXED`；全部九ACC和主image恢复仍未完成，未提交、不推进P04，授权进行中，无外部阻碍。

## 16. 2026-09-05 实现偏离登记与恢复表示复核（轮次 12）

- 前轮零RID守卫实际修复及验证为progress。当前代码/工作区与轮次11一致，无新增外部运行；本轮核对包schema、SerializeOperations、ValidateOperationEnvelope及PlanNavigation，进入明确的表示闭环工作，不再用局部inverse通过替代持久化完成。

### PLAN-CHANGE-001：完整inverse与操作对象身份的实现偏离

- 责任层级：P03，OUT-003，IMP-002/004/009；影响包消费的IMP-003/005/006/007及ACC-011/012/013/014/019/020/024/025/029沿调用依赖复核。P01/P02已提交结果、Pxx顺序、公开工具范围、exact验收标准不改变。
- 上游原契约：P03 v5 §5.1要求每个operation具有可执行、可序列化inverse，删除对象保留完整语义和token/attachment位置；§8要求操作级LCA inverse/forward以及重放时确定的token/稳定身份。总纲/需求不授权parent-prefix摘要替代完整逆操作。
- 当前偏离证据：EditHistoryModule.SerializeOperations只生成strategy=replay_parent_prefix、parent_checkpoint_id/prefix_operation_count和少量引用；checkpoint-package.schema.json的inverse additionalProperties=false不允许完整compiled state；ValidateOperationEnvelope强制该摘要；PlanNavigation忽略stored inverse正文，重新从forward编译。此为尚未发布的P03实现缺口，不能声称v5已经落地。
- 关联技术证据：r24/r26完整image恢复失败；image-spike-r2旧token错改字段；locator-r5新增TypeRef在保留引用RID时撤销仍残留。既有定义地址实验不能证明非定义引用/完整签名恢复闭环。
- 必须达到的新实现契约（不降低上游要求）：包实际承载且执行完整inverse；每一步定义和非定义引用都绑定被验证的操作前状态，不把来源token直接当成重载后身份；原token保留作来源/身份恢复数据；删除恢复包含当前守卫允许的完整签名、常量、附件和子树。包、replay、live导航使用同一表示及校验，不保留独立摘要恢复路径或暗中fallback。
- 需要补齐而尚未冻结的纯内部表示：非定义引用/复杂签名的可重放描述、删除子树的完整语义载荷与当前对象复用策略、正反向载荷去重。不得在未闭合时直接改生产schema宣称通过；如具体表示需要调整v5已冻结字段/恢复策略，正式升版并按作者自评及具体触发条件执行必要审核，授权保持。
- 隔离与回滚：未发布、未部署dnSpy、未提交P03，所有现有WIP保留；从现在起暂停受影响的包持久化/导航接入修改及其通过判定，允许只读核对和隔离最小验证。已验证的定位器/零RID守卫子集不因此失效，但不能作为包级通过证据。不存在需恢复的线上checkpoint或用户源文件。
- 恢复条件：完整表示有可执行序列化往返证据（含新增引用、复杂签名、删除/恢复及连续操作），作者逐条对照§5.1/§8闭合；任何规范性调整完成对应资格闭环后才能接入生产包路径。无需用户重新授权或缩减需求。
- 当前无外部资源阻碍；工作流继续内部表示验证，不标记整体blocked/complete。检查点 `P03_INVERSE_REPRESENTATION_DEVIATION_REGISTERED`，不提交、不部署、不推进P04。

## 17. 2026-09-05 18:16 PLAN-CHANGE-001 写出映射最小验证（轮次 13）

- 上轮登记实际实现偏离并改变接入门禁，为progress。本轮仅增加隔离实验，不修改已暂停的生产包/导航路径。
- 根据dnlib4.5.0安装XML的ModuleWriterEvent.MDEndCreateTables、ModuleWriterEventArgs.Writer和Metadata.GetToken(Object)，在表建立完成时查询已实际写出的FieldDef、MethodDef及IL引用的MemberRef映射。
- `--writer-token-map` 先删除前置字段造成RID重排，再观察三种对象的输出token；逐项验证重载后token解析到相同文本对象、live原token未被改写、加观察回调的完整输出与无回调的WriteCanonical逐字节相等。字段token确实发生重排。
- x64/x86 `writer-token-map-*-r1.log` 均退出0，SHA-256均为 `380ec6e8cc4221788076baa6d2d08cb245428e96bc9ecbcd63b0e5274380b271`，位于既有VM隔离目录。Program.cs SHA-256 `a969798b16f829862eed3a2f56bfe76abd74a258ef69ba08372f7bcf6596e9de`；harness构建与diff-check退出0。
- 证据限定：证明已写出定义/MemberRef可观察映射，不证明任意未使用引用查询无副作用、去重后的token到对象映射一一对应、复杂签名/TypeSpec/MethodSpec/新增引用及删除inverse完整语义。后续必须区分实际写出对象与强行追加未使用引用，不能用GetToken自动添加引用来掩盖missing-binding。
- PLAN-CHANGE-001仍未闭合，生产接入暂停而内部验证继续；无部署/启动dnSpy、无提交、无VM管理变更或断连、无外部阻碍。检查点 `P03_WRITER_TOKEN_MAP_SPIKE_SUBSET_PASS`，P03未完成，不推进P04。

## 18. 2026-09-05 写出映射边界及测试现场变化（轮次 14）

- 上轮写出映射实验为progress。本轮以安装dnlib4.5.0的ilspycmd只读结果确认：Metadata.GetToken调用AddMDTokenProvider；NormalMetadata.GetRid(TypeRef/MemberRef)仅TryGetRid，不创建行。新增独立unused TypeRef及两个实际被字段引用的等价TypeRef。
- 第一次运行未进入harness：旧隔离目录缺失runtimeconfig，两个架构退出-2147450749。只读清单确认旧r1目录仅剩本次P03StoreHarness.dll和两份失败日志，原依赖及历史测试日志均已不在；原因未知，未推断为快照恢复，也未恢复管理服务。原日志路径现不可复核；此前记录的结果/hash保持历史事实，但不能作为当前可读取原始文件证明，正式验收须重跑。
- 保留r1现场，创建此前不存在的新目录 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2`，通过仍正常的Win10VM MCP放置本机harness构建目录全部顶层DLL、deps/runtimeconfig共22个文件；只读既有TestIL，不操作dnSpy安装目录、VM服务/网络/快照。
- 新目录 `writer-token-map-x64-r3.log` / `writer-token-map-x86-r3.log` 均退出0，SHA-256均为 `6b5e5638f9dff5279e84dacd07cc99cedf56b186ebe3e0edcfbf57da00aca137`。原始关键输出如下（两架构相同）：

```text
SPIKE writer-token-map definition+memberref=True image-unchanged=True live-token-unchanged=True remapped-field=True
SPIKE GetRid unused=0 duplicates=many-to-one exact-image-unchanged=True
```

- 结论：GetRid能不改变该fixture完整image地报告unused=0；两个不同但等价的引用对象映射到同一输出RID。因此不能使用token→单个live对象的反向字典任意择一，legacy逐引用对象/原token语义仍需单独保真；不能对未使用引用调用GetToken补行并冒充纯查询。
- Program.cs SHA-256 `5b6b2efcca039b7e76196d310c2cf5a7eb6194bd9fa50d6795b4189d1d6619e6`；harness构建及diff-check退出0。生产包/导航接入依旧暂停，PLAN-CHANGE-001未闭合。无管理断连、无部署/启动dnSpy、无提交；后续测试使用新r2目录且每次先核实依赖。检查点 `P03_WRITER_MAPPING_MANY_TO_ONE_VERIFIED`，P03未完成，授权进行中。

## 19. 2026-09-05 18:25 引用身份图序列化验证（轮次 15）

- 上轮映射边界验证为progress。本轮仅测试PLAN-CHANGE-001需要的显式引用身份图：3个TypeRef节点、5个根引用；每节点独立ID，名称/namespace保留UTF8字节，嵌套scope指向节点ID。对同值不同对象不合并，对同对象多次引用保持共享。
- `--reference-identity` 实际JSON序列化→反序列化→新TypeRef对象构建，验证distinct-equal、sharing、nested-scope和FullName序列均保留；循环scope拒绝。此probe明确只支持fixture既有corlib scope，不是完整生产codec；AssemblyRef/ModuleRef/TypeSpec/MemberRef/MethodSpec/复杂签名及完整inverse仍待验证，不能把有限scope作为产品限制。
- 运行前新r2隔离目录runtimeconfig/deps/插件/dnlib存在检查通过，上传仅harness DLL。x64/x86退出均0；日志 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2\reference-identity-x64-r1.log` 及x86同名日志，SHA-256均 `6358d8da36cd97bf379d2da167158acde144f8d54cbcd2333a2433f1487dd716`。
- 两架构原始输出：`SPIKE serialized-reference-graph nodes=3 roots=5 distinct-equal=True sharing=True nested-scope=True cycle-rejected=True`。Program.cs SHA-256 `bd05ff5f0e8ab702299623e3fea80ee482e4df1dcff1720fff1bc99591598e5c`；harness构建及diff-check退出0。
- 当前没有生产包接入、部署、启动dnSpy或提交，没有管理断连/恢复。PLAN-CHANGE-001未闭合，授权进行中；检查点 `P03_REFERENCE_IDENTITY_GRAPH_SPIKE_PASS`，P03及全部验收仍未完成。

## 20. 2026-09-05 18:29 复杂签名结构化快照（轮次 16）

- 上轮引用身份图验证为progress。本轮新增test-only SignatureSnapshotProbe，依据dnlib4.5.0公开ArraySig/GenericInstSig/CModReqdSig构造器，将自定义必需修饰符、泛型实例、三维数组的sizes/lower bounds显式编码到JSON节点，保留引用名称UTF8字节。
- 实际创建复杂FieldSig→保存基准→捕获JSON→删除字段→写出重载→反序列化重建签名和字段。SigComparer一致、rank/sizes/lower bounds逐项一致，恢复后的完整canonical文件字节等于基准。没有在产品公开任意raw signature编辑。
- x64/x86 `--signature-snapshot` 均退出0，原始输出 `SPIKE signature-snapshot modreq+generic+array-bounds=True json-roundtrip=True delete-reload-restore-exact=True`。新r2目录的 `signature-snapshot-*-r1.log` SHA-256均为 `423e690adaa56269746cc72de17981e4cf919c586a9db82d49221b6376571564`。
- SignatureSnapshotProbe.cs SHA-256 `88bebfbe1525a01260b6025458bf791dafba04545d7e8fd6f3f69968306fdd74`；Program.cs `dfeb1c2520cbf34ff1ff25349920d4f51a6cc0ab44da8766a51c14f0238f5549`；harness build和diff-check退出0，零警告/错误。
- 边界：测试只覆盖该复合签名、corlib引用scope及一个无附属数据字段；不代表完整FieldDef/type subtree、所有签名类型、opaque尾数据、跨程序集scope或身份图合并codec已完成。完整inverse及PLAN-CHANGE-001仍需闭合，生产包接入继续暂停。
- 操作前新r2依赖检查通过；仅更新隔离harness DLL，不部署/启动dnSpy、不改VM服务/网络/快照，无管理断连或提交。授权进行中，检查点 `P03_COMPLEX_SIGNATURE_SNAPSHOT_SPIKE_PASS`，P03未完成。

## 21. 2026-09-05 18:42 浮点常量恢复表示验证（轮次 17）

- 前一用户问询通过实际进程查询取得 `DNSPY_PROCESS_COUNT=0`，改变了对外测试进度说明：dnSpy未启动，此前均为隔离harness而非client→dnSpy MCP验收。本轮恢复后只执行尚未运行的常量快照实验，没有把专项通过记为任何ACC完成。
- test-only ConstantSnapshotProbe用r4/r8与原始位值十六进制串进行JSON往返，覆盖12个负零、正负无穷、不同NaN payload及次正规数。实际添加常量字段→保存基准→删除→写出重载→由快照重建，逐项验证常量位值及完整canonical文件字节相等。
- 双架构实际命令为各自 `C:\Tools\dotnet10-{x64|x86}\dotnet.exe` 执行新r2目录的 `P03StoreHarness.dll`，输入既有 `C:\Tools\mcp-repo\tests\fixtures\bin\TestIL.dll --constant-snapshot`。两个进程退出0；原始输出均为 `SPIKE float-constant-snapshot cases=12 negative-zero+infinity+nan-payload+subnormal=True json-roundtrip=True delete-reload-restore-exact=True`。
- 日志 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2\constant-snapshot-x64-r1.log` 与x86同名日志SHA-256均 `946c90a5cfcc08c1c4edf7f00cd207d9cd44039560a09b248d8cf6845a4da6fd`。源ConstantSnapshotProbe.cs SHA-256 `c9fd1d18a6ca0535114602d9edc8cee4e7795d8fd4689859714f7d91dc8a9e2e`；Program.cs `16f0d6edba358bb9626f5bb182ba113d65001a304db3e7d16195580960731450`。作用域diff-check退出0。
- 表示结论只限浮点常量样本：使用位值串可避免JSON数值对非有限数和NaN payload的损失；生产InverseConstant仍未采用完整编码。本轮没有解决完整inverse、定义子树、跨操作身份和原parameter_remove文件恢复失败；PLAN-CHANGE-001保持打开，生产包/导航接入继续暂停。
- VM管理连接正常，先检查隔离依赖再仅上传harness DLL，无dnSpy部署/启动、无管理恢复/网络/快照修改、无提交。P03所有9项ACC仍未完成，授权进行中，检查点 `P03_FLOAT_CONSTANT_SNAPSHOT_SPIKE_PASS`。

## 22. 2026-09-05 18:49 实际编译逆操作的保真回归（轮次 18）

- 上轮浮点表示实验为progress。本轮回到实际 `CompileInverse -> JSON -> Apply -> ApplyCompiledInverse` 路径，新增 `InverseFidelityRegression`，不是以实验codec替代生产inverse。目标为P03 §5.1完整可重放语义、IMP-002/004、ACC-011/012及PLAN-CHANGE-001；普通字段改名作为有效对照。
- 三项有具体代码依据的假设全部复现：FieldType.FullName不能承载复杂签名；UTF8String.String不能保留无效UTF8原始字节；InverseConstant的JSON数值不能编码NaN。每例使用独立加载的只读fixture内存图，先记录完整canonical image、名称bytes、signature、float bits和同一字段对象；恢复必须逐项一致。
- 实际命令：x64/x86 dotnet10执行新r2目录 `P03StoreHarness.dll C:\Tools\mcp-repo\tests\fixtures\bin\TestIL.dll --inverse-fidelity`。两进程均退出1（不能由外层PowerShell退出0改判）。两架构共同原始结果：

```text
PASS compiled-inverse-fidelity ordinary-name-control
FAIL compiled-inverse-fidelity complex-signature stage=inverse error=EditDomainException: Structural validation failed
FAIL compiled-inverse-fidelity raw-name-bytes stage=compare error=InvalidOperationException: Name UTF8 bytes changed
FAIL compiled-inverse-fidelity nan-constant stage=serialize error=ArgumentException: .NET number values such as positive and negative infinity cannot be written as valid JSON.
Compiled inverse fidelity failures=3/4
```

- 日志 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2\inverse-fidelity-x64-r1.log` 及x86同名日志，SHA-256均 `21a1a1f0e3ebd7ad8673377aa2a27c36d6d21fe7583fb96ac9fb20ddc86547e3`。异常全文保留在日志；未接受运行库建议的AllowNamedFloatingPointLiterals作为修复，因为它不能证明NaN payload保真。
- 新回归还加入常规Main中TestUpdateInverses之后，不允许最终运行跳过；加入常规入口后再次构建成功，0 warnings/errors，尚未重新运行整套。专项实际已运行。源InverseFidelityRegression.cs SHA-256 `e80f75a0dd1b90047c98f6c24e12ae003be5c5e6c77f37e46ef2f72332ee1509`；最终Program.cs `d6e5d47fa1e4bcb59b8da77e22baac34d3757c86ce8a90c684e087bf8ba4d66f`；diff-check退出0。
- 结论变化：完整恢复缺口不仅是五类删除和包消费，当前更新inverse也存在真实语义丢失。此前简单更新子集通过只保留其明确样本范围，不能继承为全部更新保真。下一恢复表示必须统一覆盖更新/删除，保留原始名称字节、结构化签名和常量位值；不能仅补删除或启用宽松JSON后解除PLAN-CHANGE-001。
- 生产包/导航保持暂停；无生产修改/部署、无dnSpy启动、无服务/网络/快照修改、无管理断连、无提交。所有9个ACC仍未完成，授权进行中。检查点 `P03_COMPILED_INVERSE_FIDELITY_RED`，本轮为取得能改变下一实现方向的失败证据，不是验收通过。

## 23. 2026-09-05 19:01 结构化签名候选与身份绑定接口（轮次 19）

- 上轮实际inverse三项失败为progress。本轮只在隔离harness新增StructuredSignatureCodec候选与Probe，不接入暂停的生产包/导航、不改公开schema。对应PLAN-CHANGE-001、§5.1更新和删除必须保留完整语义。
- 类型节点显式记录类型类别、泛型编号/owner、引用ID、子签名、数组rank/sizes/lower bounds、ValueArray size、Module index及函数指针调用签名；调用节点记录约定字节、arity、返回值、普通/哨兵后参数（保留null与空数组区别）、ExtraData字节。通过bind/resolve回调使用引用对象身份，不将FullName或写出后的token当作唯一对象ID。未实现跨重载绑定图，测试resolve只返回原图已绑定对象。
- 基于安装dnlib4.5.0 XML核对API；第一次编译13 errors，实际证明部分被XML记录的构造器非公开及NonLeafSig.Next只读。改用公开构造器/属性、保留约定高位并检查回读约定相等；循环反例通过FnPtr/MethodSig.RetType构造。未使用反射/私有字段或增加fallback。复编译退出0，0 warnings/errors。
- `--structured-signature-codec` 双架构均退出0，18个类型样本、129个调用签名（含fixture全部字段/方法/属性签名和五类手工调用样本）JSON捕获→恢复→再捕获相等，保持绑定ID。经实际field_update修改复杂字段后，用候选恢复签名，原modifier对象ReferenceEquals及完整canonical文件字节相等；递归函数指针签名捕获拒绝。
- 两架构原始输出：`SPIKE structured-signature-codec types=18 calls=129 json-shape+reference-identity=True field-update-restore-exact=True cycle-rejected=True`。新r2目录 `structured-signature-x64-r1.log` 和x86同名日志SHA-256均 `f9a7c20f013109f5544925235065296f702d9886eb2c2481099a50d9bbdc1e76`。
- 源StructuredSignatureCodec.cs SHA-256 `5f776b0be0e6779caaa367acb20739094f634964ec84ae943f1f4cc3107104e0`；Probe.cs `d6473239669df0a4f4d7f69178307e4c9ef0a0b5d724485ad58f4839bad855ea`；diff-check退出0。只更新VM隔离harness DLL，依赖预检通过，无管理断连/恢复，无dnSpy启动/部署，无提交。
- 证据边界：18样本不是全部可能签名；候选DTO尚无生产输入schema/预算验证，Restore没有面向恶意DTO的完整结构检查；没有验证跨重载引用图、signature实例共享、全删除子树、连续checkpoint或生产inverse。轮次18三个生产失败及parameter_remove image失败没有关闭。下一步仍需完整表示与资格闭环，不能用此候选直接绕开PLAN-CHANGE-001。全部9个ACC未完成，检查点 `P03_STRUCTURED_SIGNATURE_CANDIDATE_VERIFIED`，授权进行中。

## 24. 2026-09-05 19:11 签名恢复的独立字节判据（轮次 20）

- 上轮签名候选实现与验证为progress。本轮补强原先capture/restore/recapture同源判据：通过dnlib4.5.0 `SignatureWriter.Write` 独立比较原/恢复签名字节。测试用ISignatureWriterHelper按原引用对象身份分配一致的TypeDefOrRef编码，仅为比较提供稳定引用编号，不修改真实metadata token、不写模块引用表。
- 18个类型样本及129个调用签名均通过独立字节比对；另外故意改变数组下界、泛型arity，两项均导致dnlib输出不同，证明判据能识别这两类语义丢失。已有JSON形状/绑定身份、完整字段更新恢复image、循环拒绝断言保留。
- 本机构建首次请求未执行：自动权限审核因连接失败拒绝。只读核对同步源hash/csproj后经同一审批机制重试通过，构建0 warnings/errors。VM依赖检查首次自动审核超时、工具明确允许一次重试，重试成功；这是审批服务问题，不是VM管理断连，没有执行任何管理恢复。
- 双架构专项进程均退出0。新r2目录 `structured-signature-x64-r2.log` 和x86同名日志SHA-256均 `4a428ae5c785d6278ff7645e083bac22e532b00c3f7d1a8829090859c4d1d755`。原始输出：`SPIKE structured-signature-codec types=18 calls=129 json-shape+reference-identity=True independent-dnlib-bytes=True negative-controls=2 field-update-restore-exact=True cycle-rejected=True`。
- StructuredSignatureCodecProbe.cs SHA-256 `a71d49d07a90c95ef33097b6c37b4fdb6f5391aaffc08e84f043d51e8c8034e0`；diff-check退出0。只更新隔离harness DLL，无生产代码/schema/导航修改，无dnSpy启动部署或提交。
- 轮次19的边界完整保留：未验证跨重载引用图、完整删除子树、生产逆操作；轮次18三项失败和参数恢复image失败仍开放，全部ACC未完成。PLAN-CHANGE-001保持打开；检查点 `P03_SIGNATURE_INDEPENDENT_BYTES_VERIFIED`，授权进行中，不推进P04。

## 25. 2026-09-05 19:22 跨重载引用图候选（轮次 21）

- 上轮独立签名字节判据为progress。本轮新增test-only ReferenceGraphCandidate及ReloadProbe，将StructuredSignatureCodec的bind/resolve连接到显式引用节点。按对象身份分ID，不按FullName或写出RID合并；源token仅保留来源信息，不作为恢复后的目标。
- 候选节点覆盖AssemblyRef（UTF8 name/culture、version、attributes、key/token、hash）、ModuleRef、嵌套TypeRef scope、TypeSpec/MemberRef/MethodSpec及当前模块/definition地址绑定。附属CustomAttributes目前明确拒绝，未静默丢失，不能把这一实验限制升级为产品范围。恢复独立分配引用对象并拒绝循环；本轮未执行循环/恶意DTO覆盖。
- 测试在三个字段中构造共享TypeRef、同值但不同对象TypeRef、嵌套scope及非corlib AssemblyRef；捕获签名和12-node图为JSON，删除三个字段→写出→新ModuleDefMD重载→从JSON恢复。验证共享/不同对象/不复用原source对象、assembly FullName/hash、三字段恢复后完整canonical image等于原基准。另检查MemberRef/MethodSpec共享、TypeSpec/ModuleRef及definition绑定；这些额外引用未写入IL，不能据此宣称其可执行语义或独立image恢复已验收。
- 首次编译MemberRefUser构造器签名不匹配退出1；改用公开Name/Signature/Class属性后构建退出0，0 warnings/errors。没有反射或产品改动。
- `--reference-graph-reload` x64/x86进程均退出0；原始输出 `SPIKE reference-graph-reload nodes=12 fields=3 distinct+shared+nested=True assembly-identity=True member+methodspec+typespec+moduleref=True exact-image=True`。新r2目录 `reference-graph-reload-x64-r1.log` 及x86同名日志SHA-256均 `cc3fdb4697343227a85a0efdbf01b0a5da7e9a89c28642843d22efdfc9b90fe4`。
- ReferenceGraphCandidate.cs SHA-256 `3008c755755cd5f2a8ce776d9728f3bfe878a83d0446b615f3e8e8bace57f921`；ReloadProbe.cs `304c9d73e98c6dc1f3c8fc7490a348b764703eb60f9bc5104ba22367c08edc5c`；diff-check退出0。VM依赖预检通过，仅上传harness，无dnSpy启动/部署、无管理断连/恢复、无提交。
- 新证据闭合有限签名根引用跨重载重建，不闭合与重载后存活引用的复用、删除definition完整附属子树、引用CustomAttributes、恶意输入预算、连续操作身份或包消费。轮次18生产inverse三失败及parameter_remove失败未关闭；PLAN-CHANGE-001、全部9ACC仍开放。检查点 `P03_REFERENCE_GRAPH_RELOAD_CANDIDATE_PASS`，授权进行中，不推进P04。

## 26. 2026-09-05 19:29 存活引用复用与错误绑定拒绝（轮次 22）

- 上轮跨重载候选为progress。本轮在同一test-only图候选增加显式surviving bindings；调用者从未删除字段的已知位置提供TypeRef/嵌套parent/AssemblyRef整条作用域绑定，候选不按名称猜测、不按source token解析目标。
- Restore先解析全部节点，再以同ID重新捕获实际对象的描述并比较，只有source token作为历史来源字段不参与重载相等判断。未知绑定、额外引用边、两个独立节点被绑定成同一对象、描述不符均拒绝；校验阶段不把候选附加到target，不修改live图。该再捕获校验是候选完整性检查，不能替代生产期望指纹/绑定入口证明。
- 新增GraphSurvivor字段不删除。另三个字段捕获→删除→写出重载→恢复后，首字段/泛型字段与survivor持有同一TypeRef对象；同值独立TypeRef仍不同。外部scope及完整canonical image保持一致。错误名称绑定和distinct节点合并绑定均抛InvalidDataException，拒绝后target完整image不变。
- x64/x86 `--reference-graph-reload` 均退出0；新r2目录 `reference-graph-reload-x64-r2.log` 和x86同名日志SHA-256均 `d4b770d5c1ac969ffcc29d5ababaed55232e24786c4cc457e870b52f61d97737`。原始输出：`SPIKE reference-graph-reload nodes=12 fields=3 distinct+shared+nested=True assembly-identity=True member+methodspec+typespec+moduleref=True surviving-identity=True wrong+collapsed-rejected=True exact-image=True`。
- ReferenceGraphCandidate.cs SHA-256 `d97ea22cb1fb8bc56204584d0ccc63a00a2f44a0f1675abd94cf984dc79d368f`；ReloadProbe.cs `dc82f9407379ac4e9e1e053ca9dffce166c08a51154bef96ec995f9d1b734212`。构建退出0、0 warnings/errors，diff-check退出0。依赖检查通过，仅上传隔离harness，无dnSpy启动部署、无VM管理断连/恢复或提交。
- 边界仍包括：通用operation-prestate引用入口生成尚未实现，本例手工从已知survivor字段定位；附属CustomAttributes、完整definition子树、恶意DTO预算及循环校验、连续操作/包消费未闭合。轮次18生产inverse三失败和参数恢复image失败仍开放；没有改变9个ACC完成状态或解除PLAN-CHANGE-001。检查点 `P03_SURVIVING_REFERENCE_BINDING_VERIFIED`，授权进行中，不推进P04。

## 27. 2026-09-05 19:39 特性解析态/原始态快照候选（轮次 23）

- 上轮存活引用验证为progress。本轮用安装dnlib4.5.0 XML与ilspy实际CustomAttribute代码确认RawData仅在未解析态存在；解析态参数不能直接替换成blob后冒充内存语义保真。新增test-only AttributeSnapshotCandidate/Probe，不接入生产包/导航。
- 节点保留constructor引用、RawData/解析态、参数TypeSig、命名字段/属性标志、UTF8名称、参数顺序及原始值类型。float/double存位值，UTF8保存bytes，string保存UTF16 code units；覆盖数组与CAArgument值。parsed构造器可记录BlobOffset；raw构造器无同等公开offset设置，本轮raw样本offset=0，非零原始offset尚未验证，不隐瞒此边界。
- 第一轮双架构退出1、dnlib writer报Infinite recursion：样本错误地把object构造参数的CAArgument.Type也写Object并嵌套CAArgument。实际Writer.WriteElem Object分支递归使用value.Type；修正为CAArgument.Type=实际装箱String、ctor参数仍Object。只改测试数据，不移除独立字节断言。原失败日志 `attribute-snapshot-*-r1.log` SHA-256均 `660368ef8ef7a1ad1bac37ebae6c666ecfe3f3734c657f900e2b01ed1ea7f793`。
- 修正后x64/x86均退出0：JSON捕获恢复再捕获一致，constructor ReferenceEquals、IsRawBlob保持，CustomAttributeWriter独立bytes一致。parsed样本含int、NaN payload、原UTF8、int数组、object装箱string及命名bool；raw样本为未解析bytes。本轮不证明所有ValueNode分支、跨重载特性图或可执行程序集已验收。
- 原始成功输出：`SPIKE attribute-snapshot parsed+raw=True nan-bits+utf8+array+boxed+named=True constructor-identity=True independent-dnlib-bytes=True`。新r2 `attribute-snapshot-x64-r2.log` 与x86同名日志SHA-256均 `395775616588463b3ceefdf8f2e80577dc63ba5a3129646ac137cf9a3f3e7dcf`。
- AttributeSnapshotCandidate.cs SHA-256 `70e2de52c9d60bbefec8cddcb182dd4d3f0f1c91305c36806fcba380e4d6562c`；Probe.cs `1ed0097d9e08ea22a0d1deed987a13f3da328bb5f47e4b5922ae7ccb2e3eb555`。构建0 warnings/errors、diff-check退出0。仅更新隔离harness、只读fixture；无dnSpy部署启动、无VM管理断连/恢复、无提交。
- 组合恢复仍待处理：reference上的特性构造器可能再次引用该scope，附属边循环不等同非法TypeRef scope循环；不能不加区分沿用图候选的递归拒绝。通用定位、完整删除子树、预算、包接入及生产inverse回归未闭合。全部9ACC未完成，PLAN-CHANGE-001保持打开；检查点 `P03_ATTRIBUTE_SNAPSHOT_CANDIDATE_VERIFIED`，授权进行中。

## 28. 2026-09-05 19:45 引用图与特性组合恢复（轮次 24）

- 上轮特性候选为progress。本轮在test-only ReferenceGraphCandidate增加有序CustomAttributes节点：先恢复结构引用、再给新建引用附加特性，最后完整描述复核。surviving引用不改动特性；module/definition只作为外部anchor，禁止节点携带待附加特性，避免借恢复改写既有对象。
- reference属性的constructor边可以返回已建立的引用对象，不和TypeRef.ResolutionScope结构循环混同。source token和attribute BlobOffset只作来源数据，在重载描述匹配时不视为实际身份；字段/引用的语义、附属顺序和raw/parsed区别仍比较。通用来源位置映射尚未完成，此规则未接入产品格式。
- 新 `--reference-attribute-cycle` 将一个TypeRef附加两条共享constructor的特性（parsed空参、raw bytes），constructor.MemberRef.Class指回该TypeRef。捕获JSON→删除唯一字段→写出重载→组合恢复，确认顺序/raw状态/共享constructor/回指关系及完整canonical image均相同。另将TypeRef scope改为自身，Restore拒绝且目标image不变。此为元数据保真实验，不证明该样本的运行时AttributeUsage语义。
- x64/x86新例均退出0，r2目录 `reference-attribute-cycle-*-r1.log` SHA-256均 `b033f47df05967552ebc3664f918184a720b92d8f0268e020e6df70a98003e0a`。输出 `SPIKE reference-attribute-cycle parsed+raw+order=True constructor-sharing+cycle=True scope-cycle-rejected=True exact-image=True`。
- 同批复跑 `--reference-graph-reload` 双架构均退出0，`reference-graph-reload-*-r3.log` SHA-256均 `d4b770d5c1ac969ffcc29d5ababaed55232e24786c4cc457e870b52f61d97737`，原存活引用/错误与合并绑定拒绝/image断言未退化。
- ReferenceGraphCandidate.cs SHA-256 `ffee8e99a843bcbf4f82687b09db7071e3479572c98954b57b9eb00da05b47f1`；ReferenceAttributeCycleProbe.cs `6611b309975d84632362013580e9dfbcdfe5a2eac001cc66f47b4d0552c9cb40`。构建0 warnings/errors、diff-check退出0；依赖检查通过，仅隔离harness更新，无dnSpy启动部署、无VM管理断连/恢复、无提交。
- 该样本的特性组合闭合，但通用anchor生成、完整删除definition子树、预算/恶意输入、所有附属分支、连续checkpoint、实际inverse及包消费仍未闭合。原生产失败未关闭，9ACC均未完成，PLAN-CHANGE-001保持打开；检查点 `P03_REFERENCE_ATTRIBUTE_COMPOSITION_VERIFIED`，授权进行中。

## 29. 2026-09-05 19:52 字段删除完整状态样本（轮次 25）

- 上轮组合恢复为progress。本轮test-only FieldDeletionSnapshotProbe按Registry.HasAttachment(FieldDef)实际允许范围捕获owner结构地址、position、来源token、原Name bytes、attributes、完整FieldSig/ExtraData、Constant.Type/value及InitialValue。复用现有引用图/签名/原值候选，不用公开field_add降低恢复字段集合。
- 两字段分别含无效UTF8名称、modreq/二维数组边界/签名尾数据，以及NaN常量；均经实际Registry.Apply(field_remove)删除并验证owner/map解绑，随后写出重载、JSON重建并按原位置插入。直接检查Name/ExtraData/常量类型位值及完整canonical image相等。来源token样本为零RID，尚不证明已有非零token/cache恢复；新图对象创建不证明live同对象Undo。
- x64/x86 `--field-deletion-snapshot` 均退出0。r2目录 `field-deletion-snapshot-*-r1.log` SHA-256均 `ef0737b0fb6d20a5f8d900566d8076551a6e42625b17eecaa8f5318d75c70392`；原始输出 `SPIKE field-deletion-snapshot actual-registry-removal=2 raw-name+signature-extra+array-bounds+nan=True positions=True reload-restore-exact=True`。
- FieldDeletionSnapshotProbe.cs SHA-256 `7a41e05461a1991eab865abdb029719467bd634da83a70ff3d13330c9a1ec59e`；AttributeSnapshotCandidate.cs仅把原值capture/restore改为internal以便复用，SHA-256 `a7bd3f26260dc34894f5a58a6fd92b29af92643ce98f4a346f5ac1c8af61222d`。构建0 warnings/errors、diff-check退出0，依赖检查通过；仅隔离harness更新，无dnSpy部署启动、无管理断连/恢复、无提交。
- 证据仅为两字段完整状态样本；全部字段边界/常量类型、live对象/token、其他definition删除、通用anchor、包消费和生产inverse仍未闭合。轮次18生产三失败及参数image失败未关闭，全部9ACC未完成，PLAN-CHANGE-001保持打开；检查点 `P03_FIELD_DELETION_SNAPSHOT_VERIFIED`，授权进行中。

## 30. 2026-09-05 20:05 属性与事件删除恢复样本（轮次 26）

- 恢复前重新核实虚拟机 `DNSPY_PROCESS_COUNT=0`，明确当前没有 dnSpy MCP 联动验收；此前状态答复不构成实现进展。本轮运行已构建的 test-only AccessorDeletionSnapshotProbe，不部署产品，不启动或恢复管理服务。
- 样本捕获属性/事件 owner 地址、位置、来源 token、名称字节、attributes、完整属性签名/ExtraData/constant、事件类型及所有 accessor/OtherMethods 的有序引用。实际 Registry.Apply(property_remove/event_remove) 删除后确认方法定义保留，写出重载，再从 JSON 恢复属性/事件；验证多 getter/setter 顺序、共享 OtherMethod 和存活方法对象复用，以及完整 canonical image 相等。
- VM 隔离目录依赖预检通过，仅更新 P03StoreHarness.dll。x64/x86 `--accessor-deletion-snapshot` 均退出 0；日志 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2\accessor-deletion-snapshot-x64-r1.log` 及 x86 同名日志 SHA-256 均 `6ff3d314ae63baca3a75a9dcce765829939c21b1a8004fde037bbf7f9e7bd1aa`。
- 原始输出：`SPIKE accessor-deletion-snapshot actual-removal=property+event multi-accessors+order+constant+extra=True surviving-method-identity=True reload-restore-exact=True`。源 AccessorDeletionSnapshotProbe.cs SHA-256 `2c9f6e62e6ccb6ee56ee426d9d6fcde375811fca0ca392085ef5c567cbd27782`。
- 这是元数据保真样本：多 accessor 及简化方法签名不代表运行时有效属性/事件行为；零 RID、新增引用的样本不关闭 live token/cache、通用引用定位、完整 method/type 删除子树或包消费。生产 inverse 三项回归及参数 image 失败仍未关闭，全部 9 ACC 未完成，PLAN-CHANGE-001 保持打开，未提交。
- HEAD 和 P03 v5 blob 与既有基线一致；全工作区 diff-check 唯一输出为受保护的 PLAN/dnSpy-MCP.md:93 EOF 空行，本轮未修改该文件。授权进行中，检查点 `P03_ACCESSOR_DELETION_SNAPSHOT_VERIFIED`；P04 不推进。

## 31. 2026-09-05 20:11 方法删除的参数与泛型附属状态（轮次 27）

- 上轮属性/事件双架构验证为 progress。本轮重新核对生产 CompileInverse 和 HasAttachment：旧更新 inverse 的 FullName/.String/普通 JSON 数值问题未消失；method_remove 只拒绝方法自身 CA/ImplMap/security/overrides/semantics，允许参数/泛型的附属数据。按此真实边界新增 test-only MethodMetadataDeletionProbe，未修改暂停的包/schema/导航。
- 样本含零 RID runtime 方法、原始 UTF8 名称、GenericMVar owner 回指、签名 ExtraData、非 sequence 顺序 ParamDefs（含返回参数）、参数常量及特性、泛型 flags/约束及两层特性。通过实际 Registry.Apply(method_remove) 删除，canonical 写出重载，先在离线重载模块原位置创建方法壳，再解析 owner 引用并从 JSON 恢复签名/参数/泛型附件。直接断言 owner 绑定新方法、列表顺序、名称字节、尾数据、附件存在，最后独立比较完整 canonical image。
- harness 构建退出 0、0 warnings/errors。x64/x86 `--method-metadata-deletion` 均退出 0；隔离目录 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2\method-metadata-deletion-x64-r1.log` 及 x86 同名日志 SHA-256 均 `a6c637f2caaf497807b9707493162161b1d25b6b2d174e41d5c2a5704294d98c`。
- 原始输出：`SPIKE method-metadata-deletion actual-removal=True generic-owner-rebound=True parameter-order+constant+raw-name+signature-extra=True parameter+generic+constraint-attributes=True reload-restore-exact=True body+marshal=not-covered`。源 MethodMetadataDeletionProbe.cs SHA-256 `2fe43e2b00339778059e216300fb2f5d9922f8a80aff20f59750255880d725ad`；作用域 diff-check 退出 0。
- 证据边界：无方法体和 marshal，且不是运行时执行测试；没有验证完整方法删除、type 子树、原对象/token/cache 的 live 恢复、连续操作或生产包消费。方法壳只写入 detached reload，不是生产 live 失败恢复策略。所有生产失败、PLAN-CHANGE-001 和 9 个 ACC 继续开放。
- 只同步专用构建目录和 VM 隔离 harness，未部署/启动 dnSpy，无 VM 管理断连/恢复，无提交；授权进行中。检查点 `P03_METHOD_METADATA_DELETION_VERIFIED`，不推进 P04。

## 32. 2026-09-05 20:17 marshal 状态与方法删除组合（轮次 28）

- 上轮方法元数据恢复为 progress。本轮按 dnlib4.5.0 本地 XML MarshalType API 新增 test-only MarshalSnapshotCandidate/Probe：显式编码 simple/raw/fixed string/safe array/fixed array/array/custom/interface 八类，保留数值缺省哨兵、原 UTF8 字节、raw bytes、类型引用和 parsed/raw 子类型，不把全部对象统一降为 blob。
- 14 个样本首次双架构测试退出 1：默认 CustomMarshalType 的必需 UTF8 字段为空，dnlib writer 报 `UTF8String is null`。保留该样本为明确负例，原/恢复对象均必须精确报此错误，其余 13 项继续要求独立 MarshalBlobWriter 字节一致；不吞掉其他错误。失败 r2 日志 SHA-256：x64 `9ac177207d38790b76f1acaa0da7a9043a4f1399bacd686cae782fa0dcb450ee`，x86 `0d6070450e786eab7aa4add2eccbb7c95385e9758840335ce7a1b54c3ec1810e`。
- 修正测试判据后 x64/x86 `--marshal-snapshot` 均退出 0：`samples=14 classes=8 parsed+raw+sentinels+reference+utf8=True independent-dnlib-bytes=13 invalid-default-custom-rejected=1`。隔离目录 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2\marshal-snapshot-*-r3.log` SHA-256均 `2f59422f0e7dfd4464df3babc0a25c929da27630d960d646f3ae5f367c331279`。
- MethodMetadataDeletionProbe 的参数加入 RawMarshalType 和 ArrayMarshalType，经实际 method_remove→JSON→离线重载恢复，两个架构完整 canonical image 及直接子类型断言通过。`method-metadata-deletion-*-r2.log` SHA-256均 `1646f30b79463549d7baf2e58eeb735bcd50be7d30e6dc63e61c5c298a8ec7d5`，输出明确 `raw+parsed-marshal=True body=not-covered`。
- 源 SHA-256：MarshalSnapshotCandidate `90b82048180260f515dcde114ddda2ffef9076bfd395eb368b588d6b2008e8a3`，MarshalSnapshotProbe `4cc4d97c8170fc4cc62920841e1d9fed83b6e7b5ae503fbd3d6de96d18b731dd`，MethodMetadataDeletionProbe `50f53752be92cf8b42b16cf6096875175ac8a0f32823e96b2a4f2076b8cfb59d`（首次组合运行后仅修正顶部注释，复编译已包含）。两次构建均退出0、无 warnings/errors；作用域 diff-check 退出0。
- 边界：不是运行时 marshal 执行验证，未覆盖全部值组合、类型引用跨重载存活复用、恶意 DTO 全校验、方法体、type 删除子树、live token/cache、连续 checkpoint 或生产包。没有关闭原 inverse/image 回归或 PLAN-CHANGE-001；9ACC 全部未完成。只更新隔离 harness，无 dnSpy 部署启动、管理断连/恢复或提交。授权进行中，检查点 `P03_MARSHAL_SNAPSHOT_VERIFIED`，不推进 P04。

## 33. 2026-09-05 20:22 IL 方法体图候选（轮次 29）

- 上轮 marshal 组合验证为 progress。本轮按本地 dnlib4.5.0 XML 公共 API 新增 test-only BodySnapshotCandidate/Probe。记录 InitLocals/KeepOldMaxStack/HeaderSize/MaxStack/LocalVarSigTok、局部变量类型、opcode/offset、标量位值、metadata/signature 引用、局部/参数引用、分支/switch 和 EH 边界/catch 引用。恢复先创建全部新指令，再绑定边；不把旧 Instruction 引用放进新 body。
- 在同一 fixture 模块中遍历 81 个 HasBody 方法，逐个捕获 JSON→恢复→再捕获相等，检查无旧指令复用，最终与替换前完整 canonical image 比较相等。另构造指向列表外指令的 branch，Capture 精确拒绝 `Detached instruction`，测试后恢复原方法体。
- x64/x86 `--body-snapshot` 均退出0；原始输出 `SPIKE body-snapshot fixture-methods=81 json-shape=True distinct-instructions=True exact-image=True detached-target-rejected=True`。隔离目录 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2\body-snapshot-*-r1.log` SHA-256均 `7523e8aefe5d02091456326280c1bce754de16b6eeda0cb8f0c8628abb8f5973`。
- 源 SHA-256：BodySnapshotCandidate `f9d935b3d247dd7ba4b6a3e4f6e6cd8d82c42405200da04a1fcb6e88752213fc`，BodySnapshotProbe `8196c38586032c1c2eb324259b2312bb34d90ee41f8979f224d4ff64a1485a6d`。构建退出0、0 warnings/errors；作用域 diff-check 退出0。
- 证据边界：同模块存活 metadata 引用由原对象字典返回，不是跨重载引用恢复或实际 method_remove 的组合；81 个方法不等于全部 operand/EH 种类已覆盖。PdbMethod/SequencePoint 明确拒绝；obsolete Local.Name/Attributes、MetadataBodySize 来源信息、native body、恶意 DTO/预算及所有指令合法性尚未完整处理，不能声明完整 body codec。这里的实验限制不改变产品需求。原生产 inverse/image 失败、PLAN-CHANGE-001 及全部9ACC继续开放。
- 仅专用构建和隔离 harness 更新，无 dnSpy 部署启动、VM 管理断连/恢复或提交。授权进行中，检查点 `P03_BODY_SNAPSHOT_CANDIDATE_VERIFIED`；下一步需方法删除跨重载组合和缺失分支验证，不推进P04。

## 34. 2026-09-05 20:28 方法删除与 IL 跨重载组合（轮次 30）

- 上轮81方法体同模块验证为 progress。本轮将 BodySnapshotCandidate 接入 MethodMetadataDeletionProbe 的 JSON 状态，方法改为 IL；样本包含参数/局部引用、重复 switch 目标、leave/catch、外部 Console.WriteLine MemberRef、NaN payload，并保留已有参数/泛型/特性/marshal/原始名称/签名尾数据。
- 初轮构造样本时两架构退出1：直接给 MethodSig.Params 添加类型未同步 dnlib ParameterList，访问 method.Parameters 越界，尚未进入删除。按本地 API 文档在源和恢复签名准备后显式 UpdateParameterTypes；不改变恢复断言。`method-metadata-deletion-*-r3.log` SHA-256均 `6385f820f90afd25523f09e89444d92fff50f5b09de11ae9aa04e300a70e2be1`。
- 修正后实际 Registry method_remove→写出重载→方法壳/签名/附属/Body JSON 恢复，直接验证新参数/局部对象、switch 双边同目标、EH HandlerStart、新方法 generic owner 和 NaN 位值，完整 canonical image 相等。x64/x86进程均退出0，`C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2\method-metadata-deletion-*-r4.log` SHA-256均 `632e062ed204bc21b75e86880705afc11a8eea8643732a78900a7f429584b7a1`，新增输出 `body-parameter+local+switch+catch+external-call+nan=True reload-restore-exact=True`。
- 源 MethodMetadataDeletionProbe.cs SHA-256 `d795c0d9d6027695c3e7fe2e7a486ed8a8344d6391385d4f44c23dcce3f5304e`；两次构建退出0、无 warnings/errors，作用域 diff-check 退出0。仅专用构建与 VM 隔离 harness 更新；无管理断连/恢复、dnSpy 部署启动或提交。
- 边界：此为完整样本组合，不是所有合法方法/operand/EH/native/PDB 状态或运行时执行验收；尚未覆盖删除type子树、通用引用入口和live对象/token/cache复用、连续checkpoint、生产包schema/inverse消费。原生产三项inverse回归与参数image失败未关闭，PLAN-CHANGE-001及9ACC保持开放。授权进行中，检查点 `P03_METHOD_BODY_DELETION_COMPOSITION_VERIFIED`，不推进P04。

## 35. 2026-09-05 20:36 嵌入符号序列点恢复（轮次 31）

- 上轮方法删除组合为 progress。本轮核对需求 CON-003/015、NON-002/015 和 P02 私有副本契约：混合模式编辑已排除，但嵌入符号必须维护，不能把候选 PDB 拒绝当作产品范围。新增 test-only SequencePointSnapshotCandidate/Probe，并接入 BodySnapshotCandidate；暂不支持的 PdbMethod scope 和 document custom debug info 仍明确拒绝。
- 节点保存指令位置、可见/隐藏行列、按对象身份分配的文档索引、URL UTF16、language/vendor/type/checksum GUID 和 checksum bytes。两点共享文档，恢复新文档对象并保留共享；没有通过名称合并不同文档。当前只在单 body 内绑定文档，跨方法共享尚未实现。
- 初轮双架构在首次写出（恢复前）退出1，NullReferenceException 位于 dnlib Metadata.AddCustomDebugInformationList。直接反编译固定 dnlib4.5.0 的 PdbDocument、Metadata 和 SymbolDocument，确认 string/default 构造器不初始化 customDebugInfos，writer 直接读 Count；public PdbDocument(SymbolDocument) 会初始化集合。改用公开 SymbolDocument adapter，未反射/修改依赖、未加吞错fallback。失败 `sequence-point-snapshot-*-r1.log` SHA-256均 `6242c4475b500c2348ce83f3038bb4d6be271b01dccedfa877f2964906dfe6e4`。
- 复验 x64/x86均退出0：JSON body恢复形状相等、可见与隐藏点共享新文档、checksum一致、完整含嵌入 PDB 的 module image 相等；从内存 image 重载后实际找到两序列点和checksum。隔离目录 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r2\sequence-point-snapshot-*-r2.log` SHA-256均 `1d3b9b785629b24f5fa772f49198f27372b786235374c43fafd8fa1f5439d4ce`。输出 `points=2 shared-document=True visible+hidden+checksum=True exact-embedded-image=True reload-points=2 standalone-pdb-created=False`；最后字段只指样本写出全在内存且符号内嵌，不包含构建工具自身调试文件。
- 源 SHA-256：SequencePointSnapshotCandidate `7c594dc3b308de9dde9fe862ad7afde410ed09dd72b5b063edf3aeb3d8a34777`，Probe `34dc4b80ea37784042e5d0bdb6d41440c165224801603d6a211dae177b811153`，BodySnapshotCandidate `890f463f298b27b70bb6fc75299088a371877b3e076a71ee88b6a4e7b7903f09`。两次构建均0 warnings/errors、退出0，作用域diff-check退出0。
- 边界：未覆盖 scope/import/local/constant/custom debug data、跨方法共享、实际删除带符号的方法后重载、所有 IL/元数据分支、live对象/token/cache与生产包接入。原inverse/image回归与PLAN-CHANGE-001保持开放，9ACC未完成。仅专用构建与隔离harness，无dnSpy部署启动、管理断连/恢复或提交；授权进行中，检查点 `P03_SEQUENCE_POINT_SNAPSHOT_VERIFIED`，不推进P04。

## 36. 2026-09-05 20:43 嵌套符号作用域与测试目录缺失（轮次 32）

- 上轮序列点恢复为 progress。本轮 test-only PdbScopeSnapshotCandidate 保存 scope 的指令边界、子scope、local索引/名称/flags、常量名称/类型/值、namespace顺序，接入 BodySnapshotCandidate，同时区分无PdbMethod与有PdbMethod但空scope。imports/custom debug info暂时拒绝，不把此限制升级为产品需求。Capture拒绝scope循环、未挂接边界/local；Restore预算及完整恶意DTO覆盖未完成。
- 扩展序列点样本为两层scope、隐藏local和int常量37。JSON恢复后直接验证scope指令边界及local绑定；完整embedded image相等，重新加载程序集后实际找到隐藏local和常量。此测试不包含PDB imports/custom信息或跨方法共享。
- 首次r2目录运行失败于dotnet启动，未执行测试：P03StoreHarness.runtimeconfig.json缺失，退出 -2147450749。查询发现该目录只剩本轮上传DLL和两份失败日志；先前依赖及历史日志均已不在，原因未确定。两架构运行时和fixture存在，Win10VM管理MCP全程可用；没有进行管理/网络/服务/快照恢复。保留r2现状，在新隔离目录 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r3` 上传22个构建依赖，上传退出0。r2启动失败日志SHA-256均 `354cfef3247c2c3fb38c8784ad7fabbd06985f08c68a64a36ade801d44b3455a`。r2历史原始日志目前不可读取，先前回复和hash不替代最终验收复跑证据。
- 新r3目录 x64/x86 `--sequence-point-snapshot`均退出0；`sequence-point-snapshot-*-r1.log` SHA-256均 `b3eb1d03ac9030bccc8018aff10620cf34e324c729c7289e67c23a2e3433c789`。新增输出 `nested-scope+local+constant=True exact-embedded-image=True reload-symbols=True`。样本内存写出，不产生样本独立PDB文件。
- 源SHA-256：PdbScopeSnapshotCandidate `08dad8f9148606042be2e5dd7906c9bec5d2d85b39a2aeec0f07477be23e91e9`，SequencePointSnapshotProbe `c18d079aa78dac623fd7a1a63da5adb5fd72b2e2e2c03b0b4bcfbd1ac19652fd`，BodySnapshotCandidate `5f542f05b8a8c311998102646989c41097194c2ea5bc3d543ce446c5b91decd2`；构建退出0、0 warnings/errors，作用域diff-check退出0。
- 该样本闭合基本scope/local/constant恢复，不关闭PDB剩余数据、所有删除子树/通用live身份、生产inverse与包消费；原回归和PLAN-CHANGE-001保持开放，9ACC未完成，无dnSpy部署启动或提交。授权进行中，检查点 `P03_PDB_SCOPE_SNAPSHOT_VERIFIED`，不推进P04。

## 37. 2026-09-05 20:50 Portable PDB import scope 图（轮次 33）

- 上轮基本 PDB scope 恢复为 progress。本轮基于固定 dnlib4.5.0 的本地 XML 和反编译 API，新增 test-only PdbImportSnapshotCandidate。按 PdbImportScope 对象身份建图，保留 Parent、imports顺序、alias/namespace UTF16及 AssemblyRef/ITypeDefOrRef 引用；覆盖九种公开 import 类，不按文本合并scope或引用。
- Restore先分配全部scope，再连接parent和九种import，最后逐scope检查parent环；缺引用、类型不符、未知kind或parent自环明确拒绝。PdbScopeSnapshotCandidate改为引用body级import graph；同一import scope被父/子PdbScope共享时恢复为同一对象。
- 先以三种import的版本运行x64/x86，均退出0，日志r2 SHA-256均 `a633c89a4b044aa6dd8636f736c38be3d99f3eee4103817c7b5658d6f8a4197a`。随后扩到所有九种（ImportNamespace/AssemblyNamespace/Type/XmlNamespace/AssemblyReferenceAlias及四种Alias），加parent自环负例；正常 JSON/embedded image/reload断言保持。
- 最终x64/x86 `--sequence-point-snapshot`均退出0；输出 `points=2 shared-document=True visible+hidden+checksum=True nested-scope+local+constant=True import-kinds=9 shared-parent+cycle-rejected=True exact-embedded-image=True reload-symbols=True standalone-pdb-created=False`。`C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r3\sequence-point-snapshot-*-r3.log` SHA-256均 `3224e0cca1cc900c7235e887884c4b0e3e46e72d7dc2abafed8a58aff697218a`。
- 源SHA-256：PdbImportSnapshotCandidate `6042bdc22ec17c7eb723278ccf70061e4aaf46878e1f87754a1a1971c97a1add`，PdbScopeSnapshotCandidate `38c7215c2a097b12b05b0c2a1aa9a34cc1016eae12fc25abb2e83e61df0b4598`，BodySnapshotCandidate `a78e1a27483478c48fa1ce4ad34c387bcc5126e028a5f6ec7442f6f816153982`，Probe `39f55778f75a59099276fa1f1f38b5833c5c357011861c8ee0a0c1eadefeaa59`。两次构建退出0、0 warnings/errors；作用域diff-check退出0；HEAD不变。
- 边界：PdbCustomDebugInfo仍未编码；body间共享document/import graph、完整方法删除带PDB的跨重载组合、恶意DTO预算和生产inverse/package仍未闭合。九类样本不等于所有值边界。原生产回归、PLAN-CHANGE-001和9ACC保持开放。仅隔离harness更新，无dnSpy部署启动、管理断连/恢复或提交；授权进行中，检查点 `P03_PDB_IMPORT_GRAPH_VERIFIED`，不推进P04。

## 38. 2026-09-05 20:55 Portable PDB 标量自定义信息（轮次 34）

- 上轮 import graph 验证为 progress。本轮从固定 dnlib4.5.0 本地源码确认 embedded portable writer 的接受集合，新增 test-only PortablePdbCdiCandidate/Probe，先覆盖无方法/指令/文档图边的12类：Unknown、两种EnC map、portable tuple names、default namespace、dynamic local flags、embedded source、SourceLink、compilation metadata references/options、EnC state map、primary constructor blob。
- DTO 对所有字符串保存UTF16 code units、所有blob克隆、未知CDI保留Guid、dynamic保留bool位序、编译引用保留name/aliases/flags/timestamp/image size/MVID、options和state保持有序。Restore按具体公开类型重建，未知kind拒绝；没有将有结构的类型降为通用unknown blob。
- 测试把12类依次挂入ModuleDef.CustomDebugInfos，捕获JSON与初始embedded image，清空并恢复，再捕获JSON相等、完整embedded image逐字节相等；从image重载后验证12项/12种Kind均存在。x64/x86 `--portable-pdb-cdi`均退出0；输出 `scalar-kinds=12 ... json-shape=True exact-embedded-image=True reload-kinds=12`。`C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260905-r3\portable-pdb-cdi-*-r1.log` SHA-256均 `626d18b9f83adcbc230c0f17a3d8a10c462aaf8d30d28614941a13c1329893f9`。
- 源SHA-256：PortablePdbCdiCandidate `18a357d865cbea944421756d02a35c272ac1083158038d92209b41820c705f5d`，Probe `d4bbfa654aab2a0154d802d64a576c0efe55b93846bb8e637a6a50b43195264f`；构建退出0、0 warnings/errors，作用域diff-check退出0。
- 边界：未覆盖StateMachineHoistedLocalScopes、Async/Iterator方法边及TypeDefinitionDocuments；CDI尚未接入document/import/scope/local/constant/type/method等所有owner；Windows PDB专属kind由现有embedded writer拒绝，需以事务基线转换后的实际portable图为准。恶意DTO预算、完整删除/跨重载/live身份及生产inverse/package未闭合。原回归、PLAN-CHANGE-001和9ACC保持开放。
- 仅专用构建和VM隔离harness更新，无dnSpy部署启动、管理断连/恢复或提交；授权进行中，检查点 `P03_PORTABLE_PDB_SCALAR_CDI_VERIFIED`，不推进P04。

## 39. 2026-09-05 21:09 Portable PDB 图结构 CDI 初轮失败与 VM 等待（轮次 35）

- 上轮标量 CDI 验证为 progress。本轮新增 test-only 图结构候选，覆盖 StateMachineHoistedLocalScopes（含 synthesized null/null scope）、AsyncMethod 的 kickoff/catch/跨方法 yield→breakpoint 边、IteratorMethod kickoff 边及 TypeDefinitionDocuments 文档图。引用以方法对象绑定 ID + 指令索引描述，不用来源 token 或文本方法名替代身份；游离 hoisted/async 指令明确拒绝。
- 初轮 harness 构建成功，0 warnings/errors。只上传 DLL 到既有 r3 隔离目录后，x64/x86 `--portable-pdb-graph-cdi` 均执行到重载检查并退出 1，错误同为 `Graph CDI kind missing after reload`。日志 `portable-pdb-graph-cdi-*-r1.log` SHA-256 均为 `c2ed4a9fba7cc7321a08b4272b29595c3c89310d3ac298ad18c2fbb2afd030c0`。失败是保留证据，不记为任何 ACC 通过。
- 只读反编译固定 dnlib 4.5.0 的 `dnlib.DotNet.Writer.Metadata` 后确认：AsyncMethod 和 IteratorMethod 都会为 CDI 拥有者写 `StateMachineMethod` 行。初轮探针把两种 CDI 同时挂到同一个 MoveNext 方法，构造了冲突关系。候选探针已改为三个不同方法，其中 Async 和 Iterator 使用不同拥有者；同时修正 hoisted scope 捕获，非 null 但不属于 owner body 的指令不再误编码成 null 哨兵。
- 修正后专用副本再次构建成功，0 warnings/errors。当前源 SHA-256：PortablePdbCdiCandidate `82823d350c3d046d98c69cdbebd8f8d3f4f6d4be2da9547901dd10aada52ca2e`；PortablePdbGraphCdiProbe `5d8ea2ba085f52e8372123cc0833adfc9f4cceef86090b7255b81d525da64e77`；Program `b8b99c4a482c3b04326d7490c9e7360441f7eb302d148d3b3ade1f57a2fc42f1`；构建 DLL `c1282f194ba65c3609086318df98173c12093fe77e97a13d50d2a80dbed22f00`。
- 上传修正版前 Win10VM MCP `192.168.204.149:28787` 返回 `No route to host`。按用户固定约束立即停止全部 VM 操作，没有重试、重连、重启服务或修改网络；等待用户恢复。宿主机因缺少 `Microsoft.WindowsDesktop.App 10.0` 不能运行该 WindowsDesktop harness，未安装新运行时替代 VM 证据。
- 恢复检查点：上传上述已构建 DLL 到现有 r3 目录，先运行 x64 诊断；若通过再运行 x86，并保留新的日志/进程退出码/hash。修正尚无 VM 运行证据，不能关闭图结构 CDI 缺口。dnSpy 未启动，本轮仍不是 client→dnSpy MCP 验收，无部署或提交。
- PLAN-CHANGE-001、生产 inverse/package/navigation、完整 owner 附件、预算/恶意 DTO、删除子树和全部 9 ACC 继续开放。最后已验证检查点仍为 `P03_PORTABLE_PDB_SCALAR_CDI_VERIFIED`；待办检查点为 `P03_PORTABLE_PDB_GRAPH_CDI_VM_REVALIDATION_PENDING`，P03 未完成，不推进 P04。

## 40. 2026-09-11 新端点交接验收与图结构 CDI 复验（轮次 36）

- 环境事实：总纲已升 v2，测试 VM 管理连接冻结为 `Win10VM-dnSpy`（`http://192.168.204.240:28787/mcp`）；旧 `192.168.204.149` 端点阻碍已被替代，本轮未向其重试。本会话工具清单实际出现 `Win10VM-dnSpy` 工具，客户端配置端点身份精确匹配，MCP 实际可调用（VM hostname `DESKTOP-3FI41GR`）。
- dnSpy 版本门禁：GitHub API 实时确认官方最新稳定 Release 为 `v6.6.0`（2026-06-20 发布、非 prerelease，`releases/tag/v6.6.0`）。VM 内 `C:\Tools\dnSpy\dnSpy.exe`（x64）与 `dnSpy-x86.exe`（x86）均为 FileVersion 6.6.0.0 / ProductVersion v6.6.0；仓库 build/release/verify 三工作流 `DNSPY_REF` 均为 v6.6.0；绑定检出 `/home/adminn/tmp/dnSpyEx` 位于 tag `v6.6.0`（e2bada2）。全部一致，无需更新、无需重部署；VM `C:\Tools\dnSpy\bin` 当前无已部署插件 DLL，与 P03 尚处隔离 harness 阶段一致。
- VM 现场重核对：`C:\Tools\dnspy-mcp-edit-tests` 整体不存在（旧 r1/r2/r3 隔离目录及历史日志全部丢失，印证交接警告，历史 hash 仅为不可复核的既往事实行）；dotnet10 x64/x86 运行时均 10.0.400；fixture `C:\Tools\mcp-repo\tests\fixtures\bin\TestIL.dll` 在（SHA `5F4B6D832A741E6E402684B4793006DCBBABD1783D64C17AF6B9B29ECBE997EE`，与当前本地仓 TestIL `5c916bac...` 不同，属旧版布局的历史证据基础，本轮继续使用以保持可比）。
- 新隔离目录 `C:\Tools\dnspy-mcp-edit-tests\p03-harness-20260911-r1`：经主机 `192.168.204.1` 临时 HTTP 文件服务由 MCP PowerShell 下载，archive 22 文件、SHA `0243b560d50b6a3440376adde93b015821cb871567062243232ca580101583b1` 解包前校验一致；轮次 35 修正版 harness DLL（`c1282f19...22f00`）指纹与本地专用构建逐字一致。
- 图结构 CDI 复验（待办检查点闭环过程）：轮次 35 修正版 r1 x64 仍退出 1，但失败特征变化为 `missing=StateMachineHoistedLocalScopes,AsyncMethod,IteratorMethod; loaded=TypeDefinitionDocuments`——方法级 CDI 全部在重载后丢失，类型级存活。反编译本地 dnlib 4.5.0 定位根因：写出端 `Metadata.AddMethodDebugInformation` 在方法无序列点时直接返回、不写 MethodDebugInformation 行；读取端 `PdbState.InitializeCustomDebugInfos` 经 `PortablePdbReader.GetMethod` 附加方法 CDI，而 GetMethod 对无 MDI 有效行的方法返回 null。真实编译产物中 async/iterator 方法必有序列点，探针补上该真实形态（owner/iteratorOwner 各挂序列点），不属于弱化断言。
- 修正后 r2 双架构 `--portable-pdb-graph-cdi` 均退出 0：`kinds=4 hoisted+synthesized+async-cross-method+iterator+type-doc=True owner-sequence-points=True json-shape=True exact-embedded-image=True reload-kinds=True`。日志 `portable-pdb-graph-cdi-x64-r2.log` / `portable-pdb-graph-cdi-x86-r2.log` SHA 均 `728d7be26e65a420c421b0abebfb40f21527c486ff5c721d77676612c9634acb`。
- 常规套件重基线：`result-x64-r1.log` / `result-x86-r1.log` 均退出 1，legacy-rename、update-inverses、ordinary-name-control 通过后停在轮次 18 登记的 `InverseFidelityRegression` 3/4 生产失败（complex-signature / raw-name-bytes / nan-constant），两架构日志 SHA 均 `1cbd50caee20591f7afbb2753c839f64a47f034f0089550b790446f190e08e45`，与历史记录逐字一致——新 VM 复现既有红门禁，不是回归。该红门禁正是 PLAN-CHANGE-001 的动机，继续按其登记的恢复条件推进。
- 本轮源/产物 SHA-256：`PortablePdbGraphCdiProbe.cs` `7b3bcd3e1fbd3a04d53cf2922bb41a9f6bdfd7ddaa743aa6f99d557cec2b2305`；新 harness DLL `3c037fd7c4e724cf73861228abe7fd2e085921b0ded2bcd88f1354d6b76be57f`。专用检出构建退出 0、0 警告 0 错误；探针为独立模式，不影响常规套件其他用例。
- 表示层新边界（并入 PLAN-CHANGE-001 表示工作输入）：方法持有图结构 CDI 但无序列点时，嵌入 Portable PDB 往返后该 CDI 必然丢失（dnlib 读写两侧共同约束）；生产恢复/导出表示不得假设此类状态可经 PDB 往返保真。
- 边界不变：本轮全部为隔离 harness 证据，不是 client→dnSpy MCP 验收；未部署/启动 dnSpy、未修改 VM 服务/网络/快照、无管理断连、无提交、不推进 P04。临时 HTTP 文件服务仅用于向隔离目录传输构建产物，已在本轮结束关闭。检查点 `P03_PORTABLE_PDB_GRAPH_CDI_VERIFIED`；PLAN-CHANGE-001、其余图/删除/引用/live 身份、包消费和全部 9 ACC 继续开放。

## 41. 2026-09-11 轮次18生产inverse红门禁关闭（轮次 37）

- 上轮新端点交接验收与图结构 CDI 复验为 progress。本轮按 PLAN-CHANGE-001 登记的恢复方向修改生产 inverse 代码并经 VM 双架构验证，不是仅测试代码变更；公开工具 schema、包 schema、导航路径未动（包仍不消费 inverse 正文）。
- 生产实现：新增 `Editing/EditStructuredSignatureCodec.cs`，把轮次 19/20/23/24 已验证的结构化签名候选提升为生产类型（类型/调用签名节点、bind/resolve 身份回调、循环拒绝、ExtraData 字节），同时删除测试副本 `tests/edit/P03StoreHarness/StructuredSignatureCodec.cs`，10 个探针文件改用生产类型——生产与测试自此共用同一表示，不再有两份 codec。net48 不可用的 `ReferenceEqualityComparer`/`SingleToInt32Bits` 改为本地实现。
- `CompileInverse` 的 `field_update` 改为发射特殊化 `field_state`（同 `parameter_state` 模式，不经公开 forward schema）：`name_utf8` 原始字节（base64）、`attributes`、`field_signature` 结构化签名节点、常量改为位值编码（r4/r8 十六进制位串、char 码位）；EditWire 的 WhenWritingNull 会丢弃 JSON null，故旧常量为空用显式 `constant_null` 标记。`ApplyCompiledInverse` 新增 `field_state` 分支 `RestoreFieldState`，恢复保留原对象身份并提供原值 Undo。
- **身份绑定新事实（重要）**：dnlib `CorLibTypes.CreateCorLibTypeRef` 通过 `module.UpdateRowId` 给用户对象分配伪 RID——每个模块的 corlib 类型引用都带非零 RID 但不在表缓存中，`ResolveToken` 不解析回该实例（实证 `token:0x01000035 -> <null>`，该 ID 属 fixture 的 System.Int32 corlib 引用）。binder 修正为：仅当 `ResolveToken(token)` 返回**同一实例**时才持久化 token 绑定，否则注册为会话内合成对象绑定（`inverse-ref-N`）；跨重载持久化零 RID/伪 RID 绑定仍属未完成的包表示工作，代码中已注释声明。
- VM 双架构验证（隔离目录 `p03-harness-20260911-r1`，harness DLL `5514a0a7...eca10b`、生产 net10 DLL `7f447ebb...583e2`，与上传逐字一致）：
  - `result-x64-r5.log` / `result-x86-r5.log`：`compiled-inverse-fidelity` **4/4 PASS**（ordinary-name-control、complex-signature、raw-name-bytes、nan-constant）——轮次 18 登记的三项生产失败全部关闭；legacy-rename、update-inverses、tail-inverses、definition-add inverses、zero-RID reference guard 全部 PASS；套件推进至历史最深，最终停在 r24 已登记的 `restored checkpoint image equals original checkpoint parameter_remove`（既有开放问题，非回归）。两架构日志 SHA 均 `074e6012f1eecbc819a6707358fe4d9761aa229a1a74a5c0ee12bb3d828159df`，exit 1 由该既有红门禁产生。
  - 提升后生产 codec 专项 `--structured-signature-codec` 双架构均退出 0：18 类型/129 调用签名、独立 dnlib 字节、2 负例控制、field-update 精确恢复、循环拒绝全部保持。
- 宿主验证：双 TFM Release 构建 0 警告 0 错误（net48 DLL `79471e43...2c34e9`、net10 DLL `7f447ebb...583e2`）；`python3 tests/edit/validate_p03_contract.py` 退出 0 PASS（9 ACC、22 operation、13 产品/8 测试工具声明不变）；`python3 -m unittest tests.python.test_edit_client` OK。
- 本轮源 SHA-256：`EditStructuredSignatureCodec.cs` `a127e5f0e5a2ac0597ab503cc99a5942404c2703c247f92e96bc3c527a8f3670`；`EditOperationRegistry.Inverse.cs` `a7a4555e4656599582ea94ce5e22ebd373c8404b954af53e14aaaed546fd037b`；探针 `ad36de28583b14508ed7544aad4f0f7c1d95b479bfaae38d904797e90192a799`。调试期间两轮临时诊断输出已删除，中间失败日志（r2/r3/r4 复合）保留于隔离目录。
- 边界与未完成：`field_state` 仅覆盖 `field_update`；type/method/property/event 更新仍走 FullName 文本 inverse、五类 definition_remove 完整子树 inverse、跨重载零 RID/伪 RID 绑定持久化、`parameter_remove` 精确镜像（r24 门禁）、包消费与导航统一、全部 9 ACC 仍开放；PLAN-CHANGE-001 整体保持开放，本轮只闭合其登记的更新保真缺口中 field_update 部分。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_FIELD_UPDATE_INVERSE_FIDELITY_GREEN`。

## 42. 2026-09-11 墓碑清理与 r24 门禁边界实证（轮次 38）

- 上轮 field_update 保真闭环为 progress。本轮针对 r24 红门禁（`restored checkpoint image equals original checkpoint parameter_remove`）实施生产修复并取得决定性定位；红门禁本身未关闭，失败仍精确停在同一断言，套件其余全部保持通过。
- **dnlib 机制实证（反编译 dnlib 4.5.0 PreserveTokensMetadata）**：`ReUseDeletedParamRows` 把被删参数槽写成 `p{rid:X6}` 占位行，由**表尾追加**的 `mp{rid:X6}` 方法行持有，宿主为 `CreateDummyPtrTableType` 的 `dummy_ptr.{GUID}` 类型（基类 = `module.CorLibTypes.Object.TypeDefOrRef`，Abstract）；定义表（TypeDef/Method/Param/Event/Property）行数由 `MemberDefDict.SortDefs` 按**活对象最大 RID** 计算（可随图形清理收缩）；而 `CreateEmptyTableRows` 对引用表（TypeRef/MemberRef/StandAloneSig/TypeSpec/MethodSpec）按**源文件行数**保留行位（图形操作永不收缩）。
- **生产修复一（墓碑结构清理）**：`RestoreParameterTail` 在参数行移回真实 owner 后，严格验证空置的占位结构（mp 仅含本参数、无 body/特性/ImplMap/Override/泛型参数；dummy_ptr 类型仅含该方法、无任何其他成员/接口/特性）后将其从图形移除，Undo 与失败恢复按容器层级精确还原。依据是 mp 方法和 dummy_ptr 类型都在表尾追加，移除不留 RID 空洞。验证生效：spike 表计数 pre/post 从 T34/M86/P30+残留变为 **T34/M86/P30 完全一致**（此前墓碑类型/方法直接进入产物）。
- **生产修复二（参数签名表示）**：`parameter_tail_restore` 的 `parameter_type` FullName 文本改为 `parameter_signature` 结构化签名节点（复用已提升的 `EditStructuredSignatureCodec` 与身份 binder，恢复优先走 codec，文本路径保留为后备）。`InverseBinder` 新增 `corlib:` 专用绑定键：dnlib `CorLibTypes` 引用带 `UpdateRowId` 伪 RID、永不解析回自身，专用键使空 objects 映射（跨重载恢复）也能自足解析；token 绑定仍要求解析回同一实例。此修复消除解析器为外部类型新建重复 TypeRef 行的旧路径。
- **剩余差异精确定位（spike 诊断输出）**：`tables_pre=T34/M86/P30/TR45` vs `tables_post=T34/M86/P30/TR46`，`refs_tail_post=...,46:System.Object`——唯一差异是**一行 TypeRef**：B（删除态镜像）写出墓碑类型基类时追加的 System.Object 引用（fixture 原 45 行 TypeRef 中无 Object 行），经引用表文件行继承持久存在于恢复态产物；firstdiff=0xa8，长度相同，canonical_exact=True，generic_parameter_remove 全部精确通过（preserving_exact=True）。
- **结构性结论（PLAN-CHANGE-001 边界细化）**：在当前 PreserveRids 检查点编码内，删除态写出为保 RID 空洞所必需的墓碑结构会引入基线中不存在的引用表行，而引用表行位从源文件继承、不可经图形操作脱落；因此"删除→写出→重载→inverse 恢复后产物逐字节等于原检查点"在引用表缺少该行（如无 System.Object 引用行的模块）时结构性不可达。候选关闭方向均属已登记的统一产物编码/历史引用表示变更（基线预置引用行、引用表编码策略调整、或历史引用改用地址/写出映射表示），须按轮次 24 规则走 PLAN-CHANGE 登记、作者自评与必要审核后实施，不在实现层静默变更或以弱化断言绕过。
- VM 双架构验证（`p03-harness-20260911-r1`，harness `e4804550...9a83e`、生产 net10 DLL `b49dde6f...24692`）：`result-x64-r7.log`/`result-x86-r8.log` 与修改前逐字节一致（SHA 均 `074e6012...159df`，唯一失败仍为该既有断言，无回归）；`image-spike-x64-r4.log`/`image-spike-x86-r4.log` SHA 均 `dc74a9c9...c5a0a`，含表计数/首差偏移/TypeRef 尾部明细诊断。spike 诊断属 test-only 代码，保留供后续轮次取证。
- 宿主验证：双 TFM Release 构建 0 警告 0 错误（net48 `425e7845...27fca`、net10 `b49dde6f...24692`）。本轮源 SHA-256：`EditOperationRegistry.Inverse.cs` `784836705fc4abf2e7d13ca004e3fd38564154397dd99ddba1a5dab4a4da1a21`；`Program.cs` `0da4fd680152d6793cfccc21e1708d78b7d17d5ad984162cadcc967746498c62`。
- 边界与未完成：r24 门禁保持开放且已具备精确边界证据；其余更新种类、definition_remove 子树、跨重载身份持久化、包消费与导航统一、全部 9 ACC 继续开放；PLAN-CHANGE-001 整体开放。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_TOMBSTONE_CLEANUP_REFERENCE_ROW_BOUNDARY`。

## 43. 2026-09-11 P03-CHANGE-002 审核闭环与 r24 红门禁关闭（轮次 39）

- 本轮按方案变更流程完成 P03-CHANGE-002 的完整闭环：v1 登记 → sub-plan-reviewer 审核（AUD-001~004，`2026.09.11-03`）→ v2 修订处理（`2026.09.11-04`）→ v2 实施 **VM 实证证伪**（最小探针 SPIKE-PROBE：图形类型 corlib 伪基类经类型遍历路径按值去重 45→45；SPIKE-B：dnlib `ReUseDeletedParamRows` 内部 dummy_ptr 基类在写出早期阶段无条件追加 16+46 双 System.Object 行；引用表行数从源文件继承不可脱落——写出期归一原理性无法对称 A(TR45)/C(TR46)）→ 按方案既定规则回退 v2、升版 v3（操作期墓碑=审核方备选④）→ 原审核方增量复核（AUD-101~106，`2026.09.11-05`，blob `7a8ef9add902fbf8ab0d5c79b2b7df1bc08ab292`，结论**有条件通过**）→ 前置/建议逐项处理（见 `2026.09.11-04` 第二批表）→ 实施 → **双架构验证全部通过**。
- **v3 实现**：新增 `Editing/EditDeletedRowsTombstone.cs`（标记/形状谓词、惰性墓碑创建 `dnspy.mcp.edit/DeletedMemberRows`，静态类形状；宿主方法 `d<rid:X6>` 取 `Private|Static|HideBySig` 常规形状）；forward `ParameterRemove` 对真实 RID 行把被删 ParamDef 重挂到墓碑宿主（保留原 rid/名称/sequence/attributes），零 RID 行（本会话新建、无表行）跳过；Undo 镜像移回并经 `ReleaseParamHost` 清理空置宿主/墓碑；`RestoreParameterTail` 识别改按墓碑形状并强制宿主名与行 rid 匹配，移回后清理空置结构（轮次 38 清理基础设施复用）；`EditFingerprint.Projection` 排除墓碑（`Compute` 不排除）；`EditStructuralValidator` 以显式登记豁免跳过墓碑整型（AUD-106）。
- **机理闭环（spike 实证）**：删除态 B=T35/M87/P30/**TR45**（单墓碑+宿主，零引用行追加）；恢复态 C 与基线 A **逐字节相等**（`preserving_exact=True firstdiff=0xffffffff`，T34/M86/P30/TR45 全等）；基线与恢复态均无墓碑残留。
- **验证结果（VM `p03-harness-20260911-r1`，harness `8c641d0f...`、生产 net10 DLL `5f9277c0...`、net48 `16d274f1...`）**：
  - 常规套件 `result-x64-r11/r12.log`、`result-x86-r11/r12.log` 均 **exit 0**、日志 SHA 均 `5f679c00c007b512443bcf2da8a750f15fe055967f0736868c00282ad2ac09fd`——**r24 红门禁（reloaded-tail-inverse parameter_remove）转绿，且 payload-history、p03-store-harness 等全部用例首次完整通过**。
  - 专项负例 `--tombstone-gate` 双架构 exit 0（`tombstone-gate-*-r2.log` SHA `374667f4...c321b`）：墓碑唯一、宿主名=rid、void() 签名、TypeRef 行数不变、**异形状宿主拒绝且零突变**、恢复逐字节相等、双循环复位。负例开发期间实际捕获并修复"识别仅查 d 前缀未匹配 rid"缺口（`tombstone-gate-*-r1.log` 保留失败证据 SHA `e62eeccc...61997`）。
  - spike `image-spike-x64-r9.log` SHA `a4a563ee...3d87`；SPIKE-PROBE/SPIKE-B 常驻诊断保留。
  - 宿主：双 TFM Release 0 警告 0 错误；`validate_p03_contract.py` 退出 0；`test_edit_client` OK；AUD-105 机械扫描哨兵残留计数 0。
- 本轮源 SHA-256（节选）：`EditDeletedRowsTombstone.cs` `b1f67b34...83ffc`；`EditOperationRegistry.cs` `4ac47f81...f682e`；`EditOperationRegistry.Inverse.cs` `57304b16...beda6`；`EditFingerprint.cs` `33c65f1f...575c4`；`EditStructuralValidator.cs` `1ea0dbf3...73621`；`EditWorkspace.cs` `2d555bbd...9317f`（哨兵回退后恢复至轮次 7 形态）；`Program.cs` `41be9a24...f805d`；方案 v3.1 `3204fed2...c5a30`；处理记录 `a6c234ab...657d`。
- 边界与未完成：无 System.Object 引用行的模块仍不闭合（AUD-101 前提，归 PLAN-CHANGE-001）；`method_remove`/`type_remove` 命中墓碑宿主（AUD-103，PLAN-CHANGE-001 兜底）；field/method/event/property 中部行删除的同类空洞按同机制后续扩展；其余更新种类 inverse、definition_remove 子树、跨重载零 RID/伪 RID 绑定持久化、包消费与导航统一、全部 9 ACC 继续开放；PLAN-CHANGE-001 整体开放（本变更闭合其"引用表行继承导致的镜像不对称"子项）。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_R24_IMAGE_GATE_GREEN_CHANGE002_CLOSED`。

## 44. 2026-09-11 全部更新种类 inverse 完整状态化（轮次 40）

- 上轮 r24 门禁关闭为 progress。本轮把轮次 37 建立的 `field_state` 完整状态模式扩展到**全部剩余更新种类**，属 PLAN-CHANGE-001 登记的更新保真缺口收尾；不改公开 schema、包格式或墓碑机制。
- **生产实现**（`EditOperationRegistry.Inverse.cs`）：`type_update`→`type_state`（name/namespace UTF8 字节、attributes、base_signature 结构化节点或 base_null 标记）；`method_update`→`method_state`（name 字节、attributes、impl_attributes、return_signature、has_this）；`property_update`→`property_state`（name 字节、attributes、整 PropertySig 结构化 CallNode、getter/setter 引用或 null 标记）；`event_update`→`event_state`（name 字节、event_signature、add/remove/raise 引用或 null 标记）；`generic_parameter_update`→`generic_state`（name 字节、flags）；`parameter_update` 的 `parameter_state` 升级（name_utf8/parameter_signature，保留文本后备，且仅在 forward 实际触碰时恢复——修复 forward 只改 name 时后备无条件读 parameter_type 的崩溃）。全部经已提升的 `EditStructuredSignatureCodec` 与身份 binder；访问器引用统一走 `InverseReference`。
- **身份绑定统一**：本轮实证捕获 `InverseBinder`（局部计数器）与 `InverseReference`（独立静态计数器）各自生成 `inverse-ref-0` 键并在同一 objects 映射内互相覆盖（property 案例：签名引用被 getter 覆盖 → "Wrong reference kind"）。统一为单一 `NextInverseBindingKey` 生成器（共享计数器 + 映射内查重）；`InverseReference` 对零 RID 会话新建对象从抛异常改为注册会话内合成绑定（与 binder 一致；跨重载持久化仍为 PLAN-CHANGE-001 开放项）。
- **回归扩展**（`InverseFidelityRegression.cs` 重构为 kind-aware runner）：从 4 个 field 案例扩至 **10 案例**，新增 type/method/property/event/parameter/generic 更新各一，覆盖无效 UTF8 名称字节、CModReqd+数组边界复杂签名、访问器置空恢复、ParamDef 物化等。开发期间回归实际捕获并修复：subject 未注册、基线在 subject 创建前捕获、fixture 方法名依赖（改为自建访问器）、event forward 对象序列化循环、双计数器键冲突（VM 日志 r13-r17 保留全部失败证据）。
- **VM 双架构验证**（`p03-harness-20260911-r1`，harness `c884deca...`、生产 net10 DLL `4048cc56...`、net48 `947f2211...`）：`result-x64-r18.log`/`result-x86-r18.log` 均 **exit 0**、日志 SHA 均 `3e30b166ad6071ac93c128f653e4147c1194bac072d90028aefda4e5a0bb848a`——compiled-inverse-fidelity **10/10 PASS**，其余全部用例（含 r24 门禁、payload-history、store harness）保持通过；`--tombstone-gate` 双架构 exit 0 且 SHA 与轮次 39 一致（`374667f4...c321b`，零回归）。
- 宿主：双 TFM Release 0 警告 0 错误（清理未用 `Old` 局部函数后）；`validate_p03_contract.py` 退出 0；`test_edit_client` OK；diff-check 干净。
- 本轮源 SHA-256：`EditOperationRegistry.Inverse.cs` `667c3600d65bedca993cd8ba5bd860b6b8bddeff64e02da3a75996ca56f13ebb`；`InverseFidelityRegression.cs` `cf3a7159e6b274ed95584a890cc149be97074290dd2a446e67abd671a476dfb9`。
- 边界与未完成：至此 22 个公开 operation kind 中全部更新类（field/type/method/property/event/parameter/generic/body）具备完整状态 inverse；仍开放：五类 definition_remove 完整子树 inverse、field/method/event/property 中部行删除墓碑扩展（§2.6 机制）、跨重载零 RID/伪 RID 绑定持久化、包消费与导航统一、全部 9 ACC。PLAN-CHANGE-001 整体开放。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_ALL_UPDATE_KIND_INVERSE_FIDELITY_GREEN`。

## 45. 2026-09-11 墓碑机制扩展到四类成员中部行删除（轮次 41）

- 上轮全部更新种类保真闭环为 progress。本轮按 P03-CHANGE-002 v3 方案 §2.6 已登记的机制扩展，把操作期墓碑从 parameter 扩展到 **field/event/property/method 的中部行删除**；不改公开 schema/包格式（方案 §2.6 明文"后续轮次按同一机制扩展"）。
- **实现**：`EditDeletedRowsTombstone` 形状谓词泛化（保留 namespace+静态类外壳+无嵌套/泛型/特性/接口/布局/安全声明判别；成员不再限定为参数宿主——重挂行保持原名称与内容）；新增 `AcquireRow`/`ReleaseRow`（字段/事件/属性直接挂墓碑类型对应列表，方法挂 Methods）；四个 forward remove（field/event/property/method）对真实 RID 行重挂墓碑，Undo 先从墓碑释放再插回 owner（dnlib 列表监听器要求）；`HasReference` 跳过墓碑子树（被删成员的语义已消失，不得阻塞后续合法删除）。
- **本轮两个真实缺陷均被扩展后的门禁捕获并修复**（VM 日志 r4-r10 保留完整失败链）：
  1. **Undo 顺序**：先插回 owner 时行仍归属墓碑（dnlib `OnAdd` 抛 "already owned"）——先 `ReleaseRow` 再插回。
  2. **dnlib 惰性访问器解析陷阱**：`EventDefMD`/`PropertyDefMD` 的访问器（add/remove/get/set）只在 owner 为 `TypeDefMD` 时才从 MethodSemantics 表惰性解析；事件先移入墓碑（TypeDefUser）后任何读取都会把空访问器**永久缓存**，导致删除态与恢复镜像各丢 2 行语义（实证 `Sem8`→`Sem6`，sem-undo 显示 `a=-:r=-`）。修复：forward remove 在脱离 owner **之前**强制解析全部访问器。第一版修复放在 AcquireRow（仍在 Remove 之后）无效——二进制级反编译核对后定位时序，前移到 Remove 之前。
- **验证**（VM `p03-harness-20260911-r1`，生产 net10 DLL `7ecc9f86...`）：`--tombstone-gate` 扩展为四类成员检查（删除态镜像：无写者墓碑、TypeRef 行数不变、恰一个操作墓碑；Undo 恢复基线逐字节），双架构 exit 0（日志 SHA 均 `beec55eb...09eda`）；常规套件双架构 exit 0 且与轮次 40 逐字节一致（`3e30b166...848a`，零回归）；双 TFM 0 警告 0 错误；契约校验与 Python 客户端通过；diff-check 干净。
- 本轮源 SHA-256：`EditDeletedRowsTombstone.cs` `18563e7f9efafdf7b0332eb19fd0c51396ef107511ca92308450620b43fa7d8d`；`EditOperationRegistry.cs` `2b5fb169c915aa6ef9b7f76324d1b22d3ee7cdcb59e419db2165498281ad575b`。
- 边界与未完成：五类 definition_remove 的完整子树 inverse（含 type 子树）仍未实现（CompileInverse 对其仍硬拒绝）；type_remove 的 TypeDef 行空洞（类型删除不产生成员级空洞但类型表 RID 保持依赖已验证的表尾追加语义，随子树 inverse 一并处理）；无 Object 引用行模块与跨重载零 RID 持久化仍开放（PLAN-CHANGE-001）；全部 9 ACC 开放。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_TOMBSTONE_ALL_MEMBER_KINDS_GREEN`。

## 46. 2026-09-11 四类成员删除的 member_restore inverse（轮次 42）

- 上轮墓碑四类成员扩展为 progress。本轮为 field/method/property/event 四个 remove 种类实现完整编译 inverse `member_restore`，关闭"墓碑即序列化子树表示"的关键闭环：被删行连同全部语义（含访问器方法）由墓碑持有并随检查点镜像持久化，inverse 只需行/owner 身份与位置即可跨重载精确恢复，**无需独立的子树快照 codec**。
- **核心发现与修复（本轮三个真实缺陷全部由扩展门禁实证捕获，VM r12-r21 日志保留完整链）**：
  1. **dnlib 访问器解析的类型归属约束（结构性）**：`TypeDefMD.InitializeEvent/Property` 丢弃 `DeclaringType != 事件.DeclaringType` 的访问器（源码行级证据）。被删事件与其访问器分属墓碑/原 owner 时，删除态镜像写出虽含 8 行语义，重载后解析为空、永久缓存 null、再写丢 2 行（Sem8→Sem6）。修复：**事件/属性删除时其访问器方法一并移入墓碑**（语义上本随删除），恢复/Undo 按记录的原始索引精确回位；`member_restore` 状态携带 `accessors[{slot,row,owner_index}]`。
  2. **发射嵌套错误**：`accessors` 键误赋到外层字典（与 member_restore 平级），状态实际缺失（`inv-keys=kind,row,owner,owner_index` 实证）；反编译核对产物定位后修正为内层状态。
  3. **Restore 的 Undo 缺少先脱离 owner 步骤**（dnlib 归属监听器）——与轮次 41 forward Undo 同类，先 `detach` 再 `AcquireRow`。
- **实现**：`CompileInverse` 四类 remove 发射 `member_restore{kind,row,owner,owner_index[,accessors]}`；`RestoreMemberFromTombstone` 定位唯一墓碑→行/访问器校验归属→按升序索引回位→`RemoveIfEmpty`；其 Undo 逆序重挂（含重建墓碑）。`EventAccessors/PropertyAccessors/AccessorSlots/DetachAccessors/ReattachAccessors` 辅助族；forward `EventRemove/PropertyRemove` 强制解析后捕获访问器槽位。
- **验证**（VM `p03-harness-20260911-r1`，harness `107facc8...`、生产 net10 DLL `91fa5bb7...`）：`--tombstone-gate` 扩展**跨重载恢复循环**（删除→写镜像→重载→`ApplyCompiledInverse`→镜像==基线逐字节；墓碑清空；重复恢复 `EDIT_HISTORY_CONFLICT` 拒绝；恢复 Undo 重建墓碑），双架构 exit 0（日志 SHA 均 `0f0c76e4...b9499`），删除态 `canon-sem=8 deleted-sem=8` 语义完整；常规套件双架构 exit 0 且与轮次 40/41 逐字节一致（`3e30b166...848a`，三轮零回归）；双 TFM 0 警告 0 错误；契约/Python 客户端通过；diff-check 干净。
- 本轮源 SHA-256：`EditOperationRegistry.cs` `fb668b9bef31080dd1e0cc1aac8ad59a9ae6401723c3375ee4542baf...`；`EditOperationRegistry.Inverse.cs` `0d12a840ac85fef4adf39699ae8ecc7c4f3f7101503775e2aa412b91...`；`Program.cs` `f92abb163aca2df260ddd9ab86b5bf4dfc339c3c596390f74bf65624...`。
- 边界与未完成：`type_remove` 仍硬拒绝（整型子树归属处理待后续轮次）；无 Object 引用行模块、跨重载零 RID/伪 RID 持久化、包消费与导航统一（PLAN-CHANGE-001 核心）、全部 9 ACC 继续开放。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_MEMBER_REMOVE_INVERSES_GREEN`。

## 47. 2026-09-11 type_remove 子树 inverse（轮次 43）

- 上轮四类成员 remove inverse 为 progress。本轮完成最后一个删除种类：`type_remove` 整型子树经墓碑**嵌套类型**持有与恢复，`member_restore` 家族闭合。
- **实现**：墓碑谓词放宽允许 `NestedTypes`（承载被删类型子树，其内容为按原样保留的样本数据）；`AcquireRow/ReleaseRow` 增加 TypeDef 分支；`RemoveIfEmpty` 计入嵌套；forward `TypeRemove` 对真实 RID 行把整个类型（含其全部嵌套类型与成员，随对象图整体迁移）重挂墓碑，Undo 逆序；`CompileInverse` 发射 `type_restore` 状态（`row`、`top_level`/`parent`、`owner_index`——顶层用显式标记而非可空 owner，规避 WhenWritingNull 丢弃）；`RestoreMemberFromTombstone` 的 owner 改为可选（type 走 parent/top_level 路径），Move 族加 null 守卫。
- **门禁扩展与缺陷**：fixture 无满足守卫的可删类型（全部被引用或带接口），门禁改用专用 `GateRemovable` 标记类型并**先经一次镜像写出+重载赋予真实 RID**再执行删除/恢复，覆盖同会话 Undo 与跨重载恢复双循环。本轮修正两个实施缺陷：前次脚本断言中止导致墓碑谓词/TypeDef 分支未落盘（构建仍过因无引用点，运行时暴露）；恢复入口无条件读 `owner` 键对 type_restore 缺失抛 KeyNotFound（VM 日志 r22-r26 保留链）。
- **验证**（VM `p03-harness-20260911-r1`，harness `ca5fa378...`、生产 net10 DLL `b6066921...`）：`--tombstone-gate` 双架构 exit 0（日志 SHA 均 `c2396e99...c2ead`，输出含 `member-kinds=field+event+property+method+type reloaded-restore=dual-cycle`）；常规套件双架构 exit 0 且与轮次 40-42 逐字节一致（`3e30b166...848a`，四轮零回归）；双 TFM 0 警告 0 错误；契约/Python 通过；diff-check 干净。
- 本轮源 SHA-256：`EditDeletedRowsTombstone.cs` `e7e38f280d2075ea7adc707da005a459a4944a2614497f48dafb`；`EditOperationRegistry.cs` `7c379c1e3b2858c82caf922478921e0e7d24f3f2ec658b3db528`；`EditOperationRegistry.Inverse.cs` `0668ac259a526f37396d033c9de30f2eb6d6333c5e150e5d8f90`；`Program.cs` `6cd040ea67d96a5854e32b905d95b5d0f6afb1cdd7674f806383`。
- 边界与未完成：**全部 22 个公开 operation kind 现已具备可执行编译 inverse**（更新类经完整状态、删除类经墓碑 member_restore、新增类经 definition_tail_remove、重命名经 legacy 复合、body 经结构化 body）。PLAN-CHANGE-001 剩余：包消费与导航统一（`SerializeOperations` 仍存 replay_parent_prefix 摘要、`PlanNavigation` 从 forward 重编译，需把编译 inverse 持久化进操作信封并统一消费）、跨重载零 RID/伪 RID 绑定持久化、无 Object 引用行模块前提、`ValidateOperationEnvelope` 对新 inverse 形状的接纳、全部 9 ACC。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_ALL_KIND_INVERSE_COMPLETE`。

## 48. 2026-09-11 包消费与导航统一：compiled_state 信封（轮次 44）

- 上轮 22 kind inverse 闭环为 progress。本轮完成 PLAN-CHANGE-001 的核心子项"包实际承载且执行完整inverse；包、replay、live导航使用同一表示"。
- **表示**：操作信封的 inverse 从 `replay_parent_prefix` 摘要改为 **`compiled_state`**——`{format: dnspy.edit.inverse.v1, strategy: compiled_state, parent_checkpoint_id, prefix_operation_count, operation_kind, semantic_source: compiled_pre_state, state: <CompileInverse 原文>}`。
- **跨重载身份桥梁（关闭"跨重载零 RID 绑定持久化"子项的包路径）**：`SerializeOperations` 现在**重放编译**——从基线沿父链 `ApplyPersisted` 重建前缀状态（每检查点独立 map），对每行 forward 在应用前 `CompileInverse`（绑定 pre-state），应用后存信封。由此 state 内引用只有两种形态：元数据 token（跨重载可解析），或 `ApplyPersisted` 确定性分配的 `obj-NNN-NN` object_id（任何消费者重放同一前缀时同样分配）。重放期合成绑定（inverse-ref-N）不会出现：前缀内新建对象全部在 map 中、基线对象全部有真实 RID。零 RID 持久化自此在包路径闭合。
- **消费统一**：`ValidateOperationEnvelopes` 只接受 `compiled_state` 且强校验 state 恰含一个已知可执行形状（field/type/method/property/event/generic/parameter_state、member_restore、definition_tail_remove、absent_body、parameter/generic_tail_restore/remove、legacy，或 body_replace 的 forward 形状）；`PlanNavigation` 直接消费持久化 state（`GetRawText`），**无重编译回退**——缺失即 `EDIT_CHECKPOINT_INVALID`（PLAN-CHANGE-001 禁止独立摘要恢复路径）。`Replay` 保持 forward 重放作为三态分类 oracle。
- **同步更新**：`checkpoint-package.schema.json`（strategy→enum[compiled_state]、semantic_source→enum[compiled_pre_state]、新增必填 `state` 对象、additionalProperties 仍 false）；`validate_p03_contract.py`（断言 enum+state 必填）；store 测试新增断言：提交后的包内每个信封 `strategy==compiled_state` 且 state 非空——旧格式（或陈旧插件）在该断言下必然失败。
- **实施缺陷（VM 实证捕获）**：`ownerMethods` 提升到方法作用域后对 type_restore（owner 合法为 null）抛冲突——门禁 type 段失败（r27 日志）；改为惰性 `OwnerMethods()` 局部函数，仅访问器路径解析。另修包检查断言的 `Single`→遍历（多 operations 条目）。
- **验证**（VM `p03-harness-20260911-r1`，harness `2d0a8e98...`、生产 net10 DLL `42f98766...`）：常规套件双架构 exit 0（日志 SHA 均 `3e30b166...848a`）——store 测试经生产 commit→读回校验→`PlanNavigation.Apply` 全链路（持久化消费点）通过且 compiled_state 断言实际运行；`--tombstone-gate` 双架构 exit 0（SHA 均 `c2396e99...c2ead`，与轮次 43 一致零回归）；双 TFM 0 警告 0 错误；契约校验 PASS；Python 客户端 OK；diff-check 干净。
- 本轮源 SHA-256：`EditHistoryModule.cs` `aee6d8a8266f9ca03fc113888f41ff2ee1b0b9b7aabfb703daab`；`EditOperationRegistry.Inverse.cs` `97af004eae16a682cc8012197b04bdf4cb6e109396d79a9467e9`；`checkpoint-package.schema.json` `494370e23e4196e82e07de54c70bce7ae043897ab9b7f9db71bc`；`validate_p03_contract.py` `12a188608595d3f611aff4bfdd8e2ac939ac96c33421579456f0`；`Program.cs` `feaf50f2a09ea0ce7563f4028b2a36b2af03e311ec91f005edd2`。
- 边界与未完成：PLAN-CHANGE-001 剩余收窄为——无 System.Object 引用行模块前提（AUD-101）、非包路径的跨重载零 RID 绑定（直接 ApplyCompiledInverse 场景仍依赖会话 map，包路径已闭合）、编辑器内 legacy save/undo_live 对 compiled_state 的间接消费回归、全部 9 ACC 正式验收。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_PACKAGE_COMPILED_INVERSE_UNIFIED`。

## 49. 2026-09-11 PLAN-CHANGE-001 正式关闭（轮次 45）

- 本轮为作者闭环记录（无代码变更）：`PLAN/2026.09.11/2026.09.11-06-闭环-P03-PLAN-CHANGE-001-完整inverse表示.md` 按轮次 12 登记的六项"必须达到的新实现契约"与恢复条件逐条对照，证据指向轮次 37-44 的 VM 双架构日志与测试产物，**结论：恢复条件满足，PLAN-CHANGE-001 关闭**。
- 保留三项已登记边界（不阻塞）：无 System.Object 引用行模块的首次循环镜像不对称（AUD-101 前提，产物编码边界）；非包路径会话内合成绑定依赖同会话 map（包路径已由确定性 obj-id 闭合）；`Compute`/`ComputeRoundtrip` 双口径为设计语义。
- 后续工作回到 IMP/ACC 主线：九项 ACC 逐项取证 → IMP-002/004 收口 → IMP-008/009。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_PLAN_CHANGE_001_CLOSED`。

## 50. 2026-09-12 ACC 状态差距分析 + ACC-014 导出测试（轮次 46）

- 上轮闭环为 progress。本轮先按轮次 37-45 真实证据重写实施记录 §3 的九项 ACC 状态表（八项"部分证据"含精确缺口、ACC-014 标零覆盖），为逐项取证建立跑道；随后实现 ACC-014 的首批 harness 覆盖。
- **TestExport（生产 commit 路径端到端）**：提交 rename→exact 头评估→默认输出导出（路径=manifest 默认输出、字节==replay 镜像、SHA/长度身份、重载成功）；显式路径导出（字节一致）；**源保护**（显式请求覆盖源文件路径→`EDIT_EXPORT_BLOCKED`）；**阻断状态**（non-exact 分类评估→拒绝导出；测试 seam：同一 replay 事实构造 `Classification=validated_drift` 评估对象——首版试图变异已加载 manifest 被 `Load` 重读覆盖，VM r26 日志保留该失败）；**故障保留旧输出**（in-memory store `finalize` 阶段注入→IOException，默认输出字节不变）。in-memory store 增加 `OutputBytes` 测试读取器。
- **验证**（VM `p03-harness-20260911-r1`，harness `7d7f0a81...`、生产 net10 DLL `6afa9a0d...`）：常规套件双架构 exit 0（日志 SHA 均 `1b5c9cb4...da51`，新增 `PASS acc014-export default+explicit identity+reload source-protected blocked-state-refused fault-preserves-output`）；`--tombstone-gate` exit 0（SHA 与轮次 43-45 一致零回归）；双 TFM 0 警告 0 错误；契约/Python 通过；diff-check 干净。
- 本轮源 SHA-256：`EditCheckpointStore.cs` `a068c8eda96054bda7ba11560d51b903a9fe20a3ff56a7b74e2fcf56`；`Program.cs` `8067d638e4561095ac9d8397cc6dfd99c03438efdb50b38c565a65d5`。
- ACC-014 边界：已覆盖默认/显式/源保护/分类阻断/故障保留；仍缺：注入"临时文件重载失败"的产物级验证（Windows 真实 Adapter 路径）、部分提交/无效恢复状态的导出阻断（依赖 ACC-024 矩阵）、`EXPORT_BLOCKED` 经 MCP 工具形状的对外断言。其余八项 ACC 按差距表继续。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_ACC014_EXPORT_COVERAGE_STARTED`。

## 51. 2026-09-12 ACC-011 分支与 Ctrl+Z 隔离测试（轮次 47）

- 上轮 ACC-014 为 progress。本轮补齐 ACC-011 的两个核心缺口（多分支 Redo 显式目标、原生 Ctrl+Z 隔离），全部经生产 commit/导航路径。
- **TestBranching**：提交 A→Undo 回根→提交 B（root 两子分支 A/B 均保留、head=B）；**兄弟分支 Redo**（PrepareHeadMove+PlanNavigation 以显式 checkpoint 目标从 B 切换到 A，树形不变、live 恢复 A 名称）；**Ctrl+Z 隔离**（对 live 直接改名模拟原生撤销：MCP 历史包字节不变；随后导航按 pre-fingerprint 冲突拒绝；手工恢复改名后同一导航成功）。in-memory store 增加 `ListFinalIds` 读取器。
- **验证**（VM `p03-harness-20260911-r1`，harness `5091f1b1...`、生产 net10 DLL `3ace3844...`）：常规套件双架构 exit 0（日志 SHA 均 `09c3af2e...5b8f`，新增 `PASS acc011-branching undo-new-commit-sibling-redo-explicit-target ctrlz-isolation-conflict-retry`）；`--tombstone-gate` exit 0（SHA 不变零回归）；契约/Python 通过；diff-check 干净。ACC-011 剩余：validated 自动导航拒绝矩阵与 EDIT-ACC 案例目录取证。
- 本轮源 SHA-256：`Program.cs` `779f48436a48e1cc79b01b34bb2ac70b2cae9da9981d3e360bc87767`；`EditCheckpointStore.cs` `8e5339a9e268a278c085baf8dafe1ab500ae6284491918800c8c0c76`。未部署/启动 dnSpy、无提交、不推进 P04。检查点 `P03_ACC011_BRANCHING_COVERAGE`。

## 52. 2026-09-12 P03 插件部署与首个真实 dnSpy 编辑事务闭环（轮次 48）

- 上轮 ACC-011 为 progress。协调器级 ACC-024/019 矩阵需要已部署插件的真实 dnSpy 集成层（协调器依赖 dnSpy 宿主接口，headless harness 无法构造）——本轮正式进入该阶段。
- **部署**：当前 net48 插件 DLL（`becc3a42...dc3ae`，与专用构建逐字一致）部署到 `C:\Tools\dnSpyin\Extensions\dnSpy.Extension.MCP\`（该目录保留 2026-08-30 既有部署备份，环境部分延续）；三个 P01/P02 编排脚本与 ui_apply_settings 的 VM URL 从旧 `.149` 更新为 `.240`。宿主侧 `p03_deploy_smoke.py` 经管理 MCP 启动 dnSpy x64（`DNMCP_TEST=1 --dont-load-files`）、UIA 驱动 Options 启用 MCP 服务（回环），验证：health OK、tools/list 返回 73 个工具名——**P03 代码首次在真实 dnSpy 中运行**。
- **首个真实编辑事务闭环**（VM 内 `p03_vm_edit_smoke.py` 对回环 15378，隔离目录 `p03-integration-r1`）：`edit_begin`（capabilities/capacity/fingerprints/history/limits/source/transaction 全字段）→ `edit_apply`（type_update，返回 diffs/risks/created_object_ids/review_cleared）→ `edit_review`（structural+roundtrip+dynamic 三重验证）→ **`edit_commit`（checkpoint+history 建立）** → `edit_status` → `edit_history` → **`edit_undo`** → `edit_redo`。smoke-r5 **exit 0 全 PASS**（日志 SHA `ea716dc2...8bf6b`）。
- **ACC-011 直接产品证据**：多次运行使父节点积累兄弟分支后，redo 返回 `EDIT_BRANCH_SELECTION_REQUIRED` 并列候选检查点（r4 日志三候选，SHA `f81a9142...b683e`），显式 `child_checkpoint_id` 后成功——多分支 Redo 必须选定目标在真实产品中实证。
- 驱动修正链（r1-r5 日志保留）：事务字段嵌套于 `transaction`/`review` 子对象；redo 的 `expected_checkpoint_id` 语义为动作前 head（undo 后为父）；分支选择重试。`open_files` 对 `C:\Tools\mcp-repo` 路径失败（疑 allowedSampleRoot 策略），begin 仍解析到已加载模块完成全流程——按现有工具策略记录，不在 P03 范围内修。
- 证据目录 `C:\Tools\dnspy-mcp-edit-tests\p03-integration-r1`（smoke-r1..r5 日志 + 驱动 + 客户端包）；结束前 dnSpy 进程已停止。本轮源 SHA-256：`p03_deploy_smoke.py` `686220f049aaa11df34378bb1e8d193808ae0f26c56f8e5cb622`；`p03_vm_edit_smoke.py` `703e84adb554a439bd3b24f447da43724bc7463386ea9319271b`；`run_p01_vm_tests.py` `88e6b0f4d288e788f6505666085578130cfd585595936cc9de37`。
- 边界与未完成：协调器级 ACC-024/019 注入矩阵现在可在该集成层执行（部署已就绪）；open_files 策略待查（非 P03）；无 git 提交、不推进 P04。检查点 `P03_REAL_DNSPY_EDIT_CYCLE_GREEN`。

## 53. 2026-09-12 协调器级 ACC-024 注入矩阵全绿（轮次 49）

- 上轮真实 dnSpy 闭环为 progress。本轮在集成层执行 ACC-024 协调器级注入矩阵（`p03_vm_acc024.py`，对回环 15378 部署插件 343ede66），**31/31 PASS exit 0**（`acc024-r12.log` SHA `54cafe46...36581`）：
  - **A** finalize→`committed_without_checkpoint`（recovery 形状含 recovery_id/kind/allowed_actions[retry_checkpoint,undo_live]/temp 身份/双指纹）→`retry_checkpoint`→idle+head 推进；
  - **B** finalize→`undo_live`→idle（live 复位+temp 删除）；
  - **C** finalize→undo_live 撞 cleanup 故障→`EDIT_CHECKPOINT_CLEANUP_FAILED`+`aborted_temp_cleanup`（state=committing、allowed=[cleanup_temp]）→`cleanup_temp`→idle；
  - **C2** cleanup 故障二次注入→仍 cleanly 失败且保持 aborted→解除后 cleanup 成功→idle；
  - **D** prewrite（live 变更前）→无 partial、事务保留 reviewed→rollback→idle；
  - **E** readback→无 partial、事务整体关闭（state=idle，更彻底的合法终态）；
  - **F** undo 路径 finalize→partial（`operation_kind=undo`）→retry 完成导航→idle。
- **真实协调器缺陷（本轮发现并修复）**：commit 在 `state=committing` 置位后、`prepared` 赋值前的窗口（prewrite/readback 故障命中点）失败时，catch-all 的 `state != "committing"` 守卫使状态永久卡死在 committing 且无 partial；调度门又拒绝除 status/history/recover/test 外的一切工具（含 rollback），而 edit_recover 要求 partial 非空——会话不可恢复，违反 §6 线性化前两行。修复：守卫改为 `(state != "committing" || partial == null)`——仅真实 cleanup 恢复 partial 保持 committing，否则回滚事务状态（`EditTransactionCoordinator.cs` r5-r6 日志保留卡死证据链：`D idle after rollback state=committing` 后续 begin 全阻断）。
- **驱动修正链**（r1-r11 日志保留）：静态工具信封顶层无 result 键；错误信封以异常抛出需捕获解析；`run_transaction` 返回事务字典被当作 id 串（schema 拒绝暴露）；readback 终态口径（closed vs retained）。另实证 **§8.3 谱系偏离跨进程存活**（磁盘原始模块 vs Desktop ArtifactRoot 持久化谱系 → `EDIT_LINEAGE_DIVERGED` + family/lineage/basis 详情）与 accept_live 需 64hex live 指纹但偏离错误未携带该指纹（记录为产品观察，AI 无法自举接纳——后续轮次评估在偏离错误中补充指纹）。
- **零回归**：修复后隔离套件 x64 exit 0（`result-x64-r29.log` SHA `09c3af2e...5b8f`，与轮次 47 一致）、`--tombstone-gate` exit 0（SHA 不变）；契约校验 PASS；diff-check 干净。测试间清理 ArtifactRoot 编辑检查点/输出与 dnSpy 进程。
- 本轮源 SHA-256：`p03_vm_acc024.py` `9eedc2bef2eeee7a63db160f02826782af4491cbade162d32f7a`；`EditTransactionCoordinator.cs` `e5e7c4af1dff265ef7ce9fc61b5a7cd1215cff69ba67a703b69c`。证据目录 `p03-integration-r1`（acc024-r1..r12）。
- 边界与未完成：ACC-024 剩余：redo/restore/migration 路径的独立注入子集（Navigate 共享代码已覆盖 undo 路径，其余三路径同构）、与 session close 交错、forward/inverse 应用期故障（ACC-009 范畴）；ACC-019 会话关闭矩阵待做；其余按差距表。无 git 提交、不推进 P04。检查点 `P03_ACC024_COORDINATOR_MATRIX_GREEN`。

## 54. 2026-09-12 redo/restore 路径注入与 ACC-019 会话关闭矩阵（轮次 50）

- 上轮 ACC-024 commit/undo 路径为 progress。本轮在同一集成层补齐 **redo/restore 路径注入**（`p03_vm_acc024b.py`）并执行 **ACC-019 会话关闭矩阵**（`p03_vm_acc019.py`），全部对部署插件 343ede66 的回环 15378。
- **ACC-024B：17/17 PASS exit 0**（`acc024b-r3.log` SHA `c2d92981...4c6e`）：
  - **R1** redo 路径 finalize→partial（`operation_kind=redo`）→`retry_checkpoint` 完成导航→idle；
  - **R2** redo 路径 finalize→`undo_live` 恢复 pre-redo 状态→idle；
  - **R3/R4** redo 路径 prewrite/readback→无 partial、state 保持 idle、零副作用；
  - **S0** restore assess→exact（replay_id 取得）；**S1** restore(apply) 路径 finalize→partial（`operation_kind=restore`）→retry 完成→idle；**S2** restore 路径 prewrite→无 partial。
  - **migration 路径**：与 redo/restore 同走 `Navigate(context, lineage, from, to, kind)` 共享代码（`MigrateValidated` 以 existingAssessment 复用），按同构覆盖记录；真实 validated-drift fixture 需双工具链（ACC-013 范畴）留待其后。
  - 驱动补齐 restore 的 live 指纹自举：`edit_begin`→`fingerprints.current_live`→rollback（期间发现 `edit_restore(assess)` 的 ReplayResult 不含 live 指纹、AI 无法从 assess 自举 apply 所需 `expected_live_fingerprint`——与轮次 49 的 accept_live 缺口同类，登记为第二个指纹自举产品观察）。
- **ACC-019 集成子集：7/7 PASS exit 0**（`acc019-r1.log` SHA `cee7c507...0a39f`）：
  - **N1** idle 关闭→idle 保持；
  - **N2** 事务中途关闭（begin+apply 未提交）→会话删除触发回滚→新会话 state=idle 且可立即拥有新事务；
  - **N3** `committed_without_checkpoint` partial 期间关闭拥有者会话→新会话可查询 recovery（kind=temp_finalize、allowed 完整）→`edit_recover(retry_checkpoint)` 成功→idle。
- 本轮源 SHA-256：`p03_vm_acc024b.py` `d71fa370487eeec99c5551a7b9bc1df923cb391c6dfc486e2b4d`；`p03_vm_acc019.py` `fd8f0fc140d7725fa764d4170f36248c2bc79eb25ed2c09fdd82`。证据 `p03-integration-r1`（acc024b-r1..r3、acc019-r1）；结束清理 dnSpy 进程与 ArtifactRoot。无 git 提交、不推进 P04。
- 边界与未完成：ACC-019 剩余：listener Stop/Apply 重启交错、六 commit 相位 × 关闭的完整矩阵、16 会话名额释放验证（需多并发会话）；ACC-024 剩余 forward/inverse 应用期故障（ACC-009 范畴）。检查点 `P03_ACC024B_REDO_RESTORE_ACC019_SESSION_GREEN`。

## 55. 2026-09-12 六相位 barrier×会话关闭矩阵：驱动就绪、环境受阻（轮次 51）

- 上轮 ACC-024B/019 为 progress。本轮编写 `p03_vm_acc019b.py`：六 commit 相位 barrier（arm 必须由 commit 同会话执行——TestBarrierState.Owner 绑定 arm 调用会话，控制会话 snapshot/release、owner 会话 arm+commit、相位内关闭 owner、release 后观察唯一终态 + partial 可恢复 + 新会话可用）与 16 会话名额释放（含溢出拒绝与释放后再接纳）。
- **驱动迭代证据**（r1/r2 日志保留）：r1 暴露 arm-owner 语义与名额占用口径（control+observer 占 2 席→16 新会话仅 14 可入）；r2 暴露 commit 线程未按预期进入 barrier——已加 commit 信封诊断与 owner 侧 drain。r3/r4 运行期间确认并行残留 python 进程互相污染（前次 Start-Job 未收），清理命令 30 秒超时后 **VM 管理服务 28787 与 dnSpy 15378 均 Connection refused**（TCP 探测双端口拒绝、ICMP 正常）——按总纲 v2 §10.1 与环境交接约束，不自行恢复 VM 管理服务/网络，**保存错误并请求用户恢复**。
- 轮次结论：驱动与复跑步骤就绪（`tests/edit/p03_vm_acc019b.py` SHA-256 `92646c33fadc28ae82ac09732c6190c508c070a7f2bf074c9a57b7da8b715a6b`，VM 侧 `p03-integration-r1` 已暂存；恢复后：清 ArtifactRoot→启动 dnSpy→单实例运行 r5）；ACC-025 取证未开始。本轮无产品代码变更、无产品缺陷结论（barrier 未进入的根因在驱动并发残留，未经清洁复跑不得记为产品问题）。无 git 提交、不推进 P04。检查点 `P03_ACC019B_DRIVER_READY_ENV_BLOCKED`。

## 56. 2026-09-12 六相位矩阵全绿：三个产品缺陷发现修复 + ACC-025 端到端（轮次 52）

- 上轮环境受阻（管理服务已由用户恢复，ZCode 侧 MCP 会话经会话重启刷新后本会话工具恢复：28787 健康、15378 未运行属预期）。按恢复契约执行：清 ArtifactRoot→`p03_deploy_smoke.py` 启动 dnSpy x64（health OK、tools/list 73）→单实例运行 acc019b。**注意：VM 上仅存的两个 python 进程即 windows_mcp 管理服务本体（28787），不得清理。**
- **r5 暴露产品缺陷①（调度门遗漏测试缝合）**：P1 commit 在 barrier 停靠、state=committing 期间，控制会话的 `edit_test_barrier snapshot/release` 被 committing 门禁拒绝（豁免名单只有 status/history/recover/storage_fault/fault）→驱动永远看不到 entered、barrier 无法释放，P2-P6 全部 "already armed"。修复：门禁豁免加入 `edit_test_barrier`（`EditTransactionCoordinator.cs`，与既有 `edit_test_storage_fault`/`edit_test_fault` 同族；snapshot/release 不变事务状态）。
- **r6 暴露产品缺陷②（owner 会话中途关闭楔死）**：P1（线性化前取消）commit 被 OnSessionClosed 取消后，终态 `state=reviewed busy=True` 且事务保留——owner 已死无人能 rollback，新会话全部 `EDIT_TRANSACTION_BUSY`（违反子方案 §6"该点之前收到关闭事实时取消提交、删除本请求临时包并按未提交事务回滚"；轮次 50 N2 只覆盖了事务间关闭）。P2/P3（prepared≠null）经 `CleanupPreparedOrEnterRecovery`→`EndLocked`→idle 本就正确，仅 P1（prepared==null）走保留分支。修复：Transaction 增加 `OwnerClosed` 事实位（OnSessionClosed 取消在途操作时置位），commit 终局 catch 在 `OwnerClosed && !LiveLinearized` 时 `EndLocked(tx,"session_closed")`（→idle、事务结束、工作区释放）；apply/review 的 finally 同样处理（同类缺陷）。owner 存活的原语义（轮次 49 矩阵 D：保留 reviewed 供手动 rollback）不变。
- **r7 修复驱动缺陷（barrier 残留）**：会话关闭自动 release owner 的 barrier 但 barrier 对象保持 armed 直到显式 reset；驱动 happy path 从不 reset → P1 残留挡住 P2-P6 的 arm。修复：每个 case 开始时 control 侧 reset（not-entered 分支的 reset 也改走 control——owner 管线可能正被在途 commit 占用）。
- **r8 暴露产品缺陷③（最严重：UI 日志 marshal 冻结整个端点）**：P4 起 commit 停靠在 WPF UI dispatcher 上（live 变更经 `OnLive`→`dispatcher.Invoke`，`commit_after_live_first_mutation` 在回调内）时，裸 http.client（完全绕过 DnSpyClient）的 snapshot 也 40 秒超时——整个 MCP 端点停摆。根因：`McpServer.HandleRequest` 每请求执行 `settings.Log("MCP request: ...")`（McpServer.cs:922），而 `McpSettings.Log` 用同步 `Dispatcher.Invoke` 写 UI 日志环（纯展示的 100 条环形缓冲）→任何占用 UI 线程的工具（含合法停靠的 commit）冻结所有会话的所有请求。修复：改 `BeginInvoke` 异步投递（DEBUG 磁盘镜像保持同步；日志环更新仍在 UI 线程、WPF 绑定安全）。P4/P5 探针驱动与逐次快照日志保留（`p03_vm_p4_probe.py`/`p03_vm_p4_probe2.py`，`p4-probe-r1.log` SHA `5ba53211...5d29a`、`p4-probe2-r1.log` SHA `1f2d0238...2036`：t=0 快照即时、commit 启动后第一快照 40.01s 超时=冻结实证）。
- **r9 全绿**：清 r8 遗留谱系（dnSpy 重启后磁盘模块 vs 持久化谱系头 EDIT_LINEAGE_DIVERGED，§8.3 已知行为）→六相位×会话关闭 + 16 会话名额 **15/15 PASS**（`acc019b-r9.log` SHA `40ffca07...93b8a`，exit 0）：P1-P6 全部合法终态（pre-live 相位按未提交事务回滚 idle；post-live 相位关闭后 commit 继续完成 idle）+ 新会话即时可用 + 16 名额/溢出拒绝/关闭释放。
- **零回归**：三次产品修复后各重建（`tests/run-verify-local.sh` 契约+双 TFM 0warn/0err）重部署（harness+net10+net48 三件套）并在 VM 用 `C:\Tools\dotnet10-x64|x86\dotnet.exe` 重跑隔离套件+墓碑门禁：x64/x86 套件各 22 PASS、门禁 PASS，两次回归日志逐字一致（`result-*-r1-round52.log` SHA `a0ce1de7...554a`、`tombstone-gate-*-r1-round52.log` SHA `4296a7cb...1d71`）。最终部署 net48 SHA `4eac740edeac5dd2105b2dc84cefdc1af98074a07e71616e7988382489bfe384`。
- **ACC-025 端到端 16/16 PASS**（`p03_vm_acc025.py` r2，日志 SHA `ca976b8f...bc64`）：A0/A1 自建种子谱系（驱动自包含）；A2 seam 偏离+指纹自举（`edit_test_lineage_mutation` 返回 after_fingerprint——产品路径的自举缺口观察仍登记，不阻塞 seam 用例）；A3 begin 拒绝 diverged 且零副作用（错误带 family_id/lineage_id；进程内绑定路径合法无 match_basis——驱动断言已按双形状放宽）；A4 错误指纹 accept 拒绝且零新建谱系；A5 显式 accept 建新 lineage+root+superseded 链接；A6 旧谱系仍可读（4 checkpoint 含旧 head）；A7 新谱系单 root 无 parent（无伪造操作）；A8 superseded 链接可见；A9 接纳后 begin/rollback 可用。r1 日志（14/15，A3 断言过严）保留为迭代证据。
- 本轮源 SHA-256：`EditTransactionCoordinator.cs` `da6252a206a6915acd03962474ba3fbbe31f916abf7aedf3f0294552cf703e08`；`McpSettings.cs` `2178879924162b205ebf25f530f3bafd943e1043650fa4c7d439e55251fd6d1e`；`p03_vm_acc019b.py` `2dc8625c42617e090a0e0f5165a7753a197ffac0a331f731c84618b3211e406e`；`p03_vm_acc025.py` `6cf28d985eba6e7ecaca2c115a1f8e5c8cd72fa0b7cf474af450107f3561b203`。证据目录 `p03-integration-r1`（acc019b-r5..r9、p4-probe*、acc025-r1/r2）；结束清理 dnSpy 进程与 ArtifactRoot。无 git 提交、不推进 P04。
- 边界与未完成：ACC-019 剩余 listener Stop/Apply 重启交错（矩阵已覆盖六相位×关闭+名额）；ACC-024 剩余 forward/inverse 应用期故障（ACC-009 范畴）；轮次 49/50 两个指纹自举产品观察（diverged 错误、restore assess 不携带 live 指纹）仍登记待评估；P01 工具 open_files 路径策略待查（非 P03）。检查点 `P03_ACC019B_ACC025_GREEN`。

## 57. 2026-09-12 §10.1 取证包装落地：EDIT-ACC-019/025 双架构全绿（轮次 53）

- 入口簿记：GLM 平台迁移（快照 Codex→入口 GLM）按"用户显式修改"追加变更记录（审核者 `sub-plan-reviewer` 已验证在册、实施方 `主会话`、正文 SHA 演化 `40a6ff1f...0cfb7` 已记录、`PLAN/2026.09.11` 目录验证为 P03 变更管理记录无独立顺序文档、顺序仍为总纲 §8、付费外发维持不涉及、自动恢复 GLM Goal）；恢复事件已入账。
- **§10.1 最新稳定 Release 复核**（免费只读，GitHub API）：最新稳定 tag 仍 `v6.6.0`（2026-06-20，非 prerelease），VM 已装 6.6.0.0 一致，无需更新。
- **取证包装**（本轮新建）：案例文件 `tests/edit/cases/EDIT-ACC-019.json`/`EDIT-ACC-025.json`（suite=structured-edit-p03、架构、TFM、fixture 身份含 SHA、前置、动作序列、预期）+ 证据 runner `tests/edit/p03_vm_edit_acc_evidence.py`（monkeypatch 驱动 `call` 录制逐请求 actions.jsonl；`DnSpyClient` 跟踪子类在案例后统一关闭全部会话；driver.log 捕获；产物写入 `<ArtifactRoot>\edit-tests\<run-id>\EDIT-ACC-xxx\{case.json,driver.log,actions.jsonl,summary.json}`；summary 含 pass|fail|blocked、SHA、清理结果；退出码 0/1/2）。
- **迭代链**：r1-x64 暴露 runner 会话隔离缺陷（acc025 不关自身会话→名额泄漏→acc019b Q1 admitted=15——非产品缺陷）→runner 加 TrackingClient 统一清理；r2-x64 暴露编排协议违反（清 ArtifactRoot 未配对重启 dnSpy→进程内谱系绑定指向已删存储→begin `EDIT_INTERNAL_ERROR incident-4f8e...`——按既有协议清存储必须配对重启；**产品观察登记**：带外删存储后错误归类 INTERNAL 而非谱系缺失诊断）；r3-x64 修复后全绿。
- **取证结果（保留 run）**：`p03-evidence-20260912-r3-x64` 与 `p03-evidence-20260912-r1-x86` 各含两案例全 `pass`：EDIT-ACC-025 17/17、EDIT-ACC-019 16/16（15 PASS 行 + 终判行）。**EDIT-ACC-019 的 driver.log 双架构逐字节一致**（SHA `40ffca07...93b8a`，与轮次 52 r9 相同——跨架构确定性）。关键证据 SHA：019 actions x64 `786febd9...2aa5`/x86 `e214658a...5883`、summary x64 `9fc89018...8cb6`/x86 `dfdec403...9c86`；025 driver.log x64 `6cdc2730...568d`/x86 `e3cd8892...b7e6`、actions x64 `896e5ca2...2dc5`/x86 `c23086ae...753f`。失败迭代日志（evidence-r1/r2-x64.log）保留于 `p03-integration-r1`。
- ACC-019 的六相位×会话关闭 + 名额维度现具备双架构 §10.1 证据；ACC-025 端到端具备双架构 §10.1 证据。本轮源 SHA-256：`p03_vm_edit_acc_evidence.py` `8fe45b5e34bef170759ff29ea07ec555305399713df9c48fcf640adf481ddd05`；`cases/EDIT-ACC-019.json` `dfdbbc123f03a76595b9a189a257bb73cf8d61e706963d512fbc66e384f822f8`；`cases/EDIT-ACC-025.json` `3c8f6ad1e4dc80984e36316bab6ef6683d1513cd56568b05fd6a88ff204c5821`。结束清理 dnSpy-x86 进程与 ArtifactRoot 编辑目录（证据目录保留）。无 git 提交、不推进 P04。
- 边界与未完成：ACC-019 剩余 listener Stop/Apply 重启交错；ACC-011 validated 拒绝矩阵；ACC-014 Windows adapter 重载故障注入 + MCP 形状 EXPORT_BLOCKED；ACC-020 六入口 MCP 形状回归；ACC-012/013/029 fixtures/反例/容量曲线；ACC-024 forward/inverse 应用期故障；各 ACC 的 EDIT-ACC 案例文件将随各自取证轮补建。检查点 `P03_EDITACC_EVIDENCE_DUALARCH_GREEN`。

## 58. 2026-09-12 ACC-019 listener 重启交错矩阵双架构全绿（轮次 54）

- 上轮取证包装全绿。本轮实现 ACC-019 剩余的 listener Stop 维度：`p03_vm_acc019c.py`（六相位 barrier 停靠 → 经 P02 UI-signal 契约请求 AI 侧重启监听器 → 在途 commit 于拆除中落定 → 新会话验证唯一合法终态 + 新会话可用）+ 宿主编排器 `p03_round54_orchestrator.py`（轮询 ui-signals 请求 → UIA 重应用设置 → 健康确认 → 写 ack）。
- **迭代链**：r1 暴露重启机制误判——`apply_settings(False)` 只取消"物理机/未知环境"本地覆盖复选框，**不停止已运行服务器**（无停靠直接实验 33s 仍 up）；VM 侧探针（`p03_vm_restart_probe.py`，会话建立→重应用→旧会话 10054 强制断开）证明**重应用相同设置（enable+host+port）即重启监听器并拆除全部会话**——P02 同 URL 测试的机制。编排器改为单次重应用+健康等待后 r2 全绿。
- **结果（保留）**：`acc019c-r2.log`（x64）与 `acc019c-r1-x86.log`（x86）均 **ACC019C PASS 12/12**（六相位全部：commit 在拆除中落定 + 合法终态 + 新会话可用），**双架构日志逐字节一致**（SHA `cb7360043e1a4c5c6010c53bc6a083906648fc9eba4b82cb225a576f8c236264`）。所有终态均为 idle（无 partial 残留），与 §6 会话关闭取消/完成语义一致。
- ACC-019 维度覆盖现状：六相位×会话删除（acc019b，双架构，§10.1 证据）+ 16 名额（同）+ 六相位×监听器重启（acc019c，双架构）。`stream`（GET 长连接关闭）与 DELETE 同源产生 `McpTransportSessionClosed` 终结事实（P02 ACC-002 已验证传输层语义），按同源等价记录；如验收要求显式 stream×六相位再补。
- 本轮源 SHA-256：`p03_vm_acc019c.py` `d0e62e724403167f010d3e18c3fdce8e663e856d560a3aebc03854ce9c83f527`；`p03_round54_orchestrator.py` `81ace1429d1428d3ba806b3088d867343fd3aaa69f0e8942edc7221e5ecfd4e7`。结束清理 dnSpy-x86 与 ArtifactRoot。无 git 提交、不推进 P04。检查点 `P03_ACC019_LISTENER_RESTART_DUALARCH_GREEN`。

## 59. 2026-09-13 ACC-013 双工具分类矩阵 + §8 版本判定修复（轮次 55）

- 上轮 listener 矩阵全绿。本轮实现 ACC-013 的核心 fixture 与分类矩阵（harness 探针 `TestDualToolClassification`，注册入默认套件与 `--dual-tool-classification` 模式）：真实 commit 建 lineage 后，以**真实第二写入器**（同一 live 模块经裸 dnlib `module.Write()`，断言其图像 ≠ 检查点写入器图像）构造四类 fixture（经 ZIP manifest 改写 + InMemory CreateTemp/FinalizeTemp 注入）：validated（head 记录图像替换为裸写入器图像、语义不动）、unverified（语义指纹再改写）、unknown-format（manifest.format=v2）、unknown-envelope（操作信封 format=v2）+ malformed（v1 信封内 kind_version=2）。
- **矩阵断言全过**：exact 控制组；validated→`validated_drift`；unverified→`unverified_drift`；确认迁移经 `PrepareMigration`+`Finalize` 产生 **exact** 迁移子检查点且记录当前检查点图像；原 lineage 不受影响；live 指纹全程不变；存储 finals 恰为 1 真实+5 fixture。
- **发现并修复实现偏离 §8 的产品缺陷（错误分类）**：`ParsePackage` 中 manifest 先过冻结 v1 JSON schema（format const）再进 `ValidateManifest` 的格式检查——未知包格式（未来 producer 版本）被误分类为 `EDIT_CHECKPOINT_INVALID`（结构非法），而子方案 §8（152/253 行）规定未知包/信封版本 → `EDIT_OPERATION_VERSION_UNSUPPORTED`（换工具提示而非隔离）。修复：版本判定前置于 schema 校验（manifest 与操作信封两处；EditDomainException 直通不被 INVALID catch 吞掉）；v1 内部违规（如 kind_version=2）仍正确拒为 INVALID。迭代链 r1（DNMCP_TEST 未设+kind_version 预期错）→r2（实测拒绝码取证）→r3 全绿。
- **零回归**：契约+双 TFM 0warn/0err（`tests/run-verify-local.sh`）；VM 双架构探针 + 全套件（23 PASS，新增 dual-tool-classification 行）+ 墓碑门禁全部 exit 0；套件日志 x64/x86 逐字节一致（`result-*-r1-round55.log` SHA `56984e42...3ba9`）、探针日志双架构一致（`180ae7b9...019a`）、门禁 SHA 不变（`4296a7cb...1d71`）。新 net48 `11743e1879d7da9c063b496093fb6a72fbb5190a6ff207d12795643c19101149` 已部署 VM Extensions。
- 本轮源 SHA-256：`EditHistoryModule.cs` `b349cd7183606509c7b10d5e978d5331dfb3a7ca8c9b62965067e0621e795ab8`；`P03StoreHarness/Program.cs` `88406c7c852c4989993fd4e0a04f60420c0053098634c45444eac641690f2097`。无 git 提交、不推进 P04。检查点 `P03_DUAL_TOOL_CLASSIFICATION_GREEN`。
- 边界与未完成：ACC-013 剩余 MCP 工具形状矩阵（edit_restore assess/apply×确认/拒绝经真实回环）与 EDIT-ACC 案例文件；ACC-011 validated 自动导航拒绝矩阵复用本轮 fixture 构造法（同法可产出集成层 fixture 包）。

## 60. 2026-09-13 ACC-013/011 集成层确认/拒绝矩阵双架构全绿（轮次 56）

- 上轮双工具分类矩阵全绿。本轮实现集成层驱动 `p03_vm_acc013.py`：真实两次 commit 建 lineage（baseline+C1+C2）后，以 python 改写包 ZIP manifest 构造三个 fixture 直接落入 `<ArtifactRoot>\edit-checkpoints`（producer-B 图像 stand-in=真实包 SHA；真实双写入器分歧已由轮次 55 harness 探针证明）：V1（head 记录图像改写→validated head）、V2（祖先 C1 记录图像改写→validated ancestor + head C2 exact）、U（语义再改写→unverified）。经真实回环 15378 执行 MCP 形状矩阵。
- **16/16 PASS（x64 r7 与 x86 r1-x86，日志逐字节一致 SHA `578626f1...880c`）**：B1/B2 真实谱系+三 fixture 列出；B3 assess 分类（validated_drift/unverified_drift）；B4 validated apply 无 `confirm_validated_drift` → `EDIT_REPLAY_CONFIRMATION_REQUIRED`（**ACC-011 validated 自动导航拒绝**）；B5 unverified apply → `EDIT_REPLAY_UNVERIFIED`；B6 `edit_undo` 到 validated 祖先 → 同门拒绝（Navigate 共享路径）；B7 拒绝零副作用（4 lineage、真实 head 不变）；B9 普通 begin/commit 不绕过确认门（绑定真实谱系+回滚干净）；B8 祖先目标 assess validated → 确认迁移成功 → **迁移子 exact** 且 fixture head=迁移子（`history.head_checkpoint_id` 提取——迁移响应形状为 from/to/history/replay）；B10 迁移后 begin 可用+回滚干净。
- **驱动迭代链（r1-r7 保留）**：r1 缺 `request_id`（-32602）+基线计数（2 commit=3 checkpoint）；r2 `expected_live_fingerprint` 须取最后 commit 的 `fingerprints.after`（begin 时值已过期）；r3 暴露迁移契约方向——`MigrateValidated` 要求 **head exact + 目标 validated**（validated-head fixture 被正确拒为 `EDIT_LINEAGE_DIVERGED`），改用祖先 fixture；r5 迁移子检查点键名提取错误；r6 B10 预期修正——**进程绑定跟随被应用的历史**（迁移后绑定更新到迁移子 M，begin 合法成功而非偏离；与 undo/redo 后 begin 可用同机制，产品语义一致）。无产品缺陷结论（全部为驱动对既有契约的学习）。
- ACC-013 集成维度现具备双架构证据；ACC-011 的 validated 自动导航拒绝矩阵（restore+undo 双入口）闭合。本轮源 SHA-256：`p03_vm_acc013.py` `c9848c0706ce9039456b4e77f3827287c6d68d1106cdb7c57aabd10afa53fbc2`。结束清理 dnSpy-x86 与 ArtifactRoot（fixture 包一并清除）。无 git 提交、不推进 P04。检查点 `P03_ACC013_ACC011_RESTORE_MATRIX_DUALARCH_GREEN`。

## 61. 2026-09-13 ACC-020 六入口 legacy 矩阵 + Ldc_I4_S 缺陷修复（轮次 57）

- 上轮 restore 矩阵全绿。本轮实现 `p03_vm_acc020.py`：六个受门禁 legacy 写工具经真实回环的完整回归——`rename_symbol_by_token`（**泛型 MemberRef 引用完整性**：重命名 `TestIL.GenericMethodOwner\`1.Echo` 后 `GenericMethodCaller.Call` 的 IL 引用同步更新）、`patch_method_il`/`force_return`/`nop_method`（同路径结构化事务）、`edit_undo`/`edit_redo` 在 legacy 建立的单一谱系上导航、`revert_method_il`（受约束单方法回退，pending patch 须在当前 head）、`save_assembly`（ArtifactRoot 导出投影：saved_to/source_preserved/backup_path=null/兼容警告）；另闭合 **ACC-014 的 MCP 形状 EXPORT_BLOCKED 断言**（非精确检查点→`EDIT_EXPORT_BLOCKED` 阻断 + 精确检查点仍导出，fixture 构造复用轮次 56 方法）。
- **发现并修复真实产品缺陷（body 编解码操作数丢失）**：`force_return value=42` 经 `module_roundtrip` 验证失败——`expected [il|Ldc_I4_S|42], actual [il|Ldc_I4_S|0]`。根因：`ParseOperand` 对 `i32` 装箱 int，而 dnlib 写出侧要求 `Ldc_I4_S`（唯一 ShortInlineI 操作码）的规范 sbyte 操作数，装箱 int 写出静默降级为 0；读取侧 ValidateOperand 宽松接受 sbyte/byte/int 掩盖了该不对称。轮次 29 的 81 体语料无非零 Ldc_I4_S 操作数（覆盖盲区）。修复：body 应用赋值处增加 `CanonicalOperand`（Ldc_I4_S+int→sbyte）规范化。VM 复现探针（value=42 失败/value="default" 成功的对照）保留于 `fr_probe.py` 证据链。
- **零回归**：契约+双 TFM 0warn/0err；VM 双架构套件（23 PASS）+ 墓碑门禁全部 exit 0，套件日志与轮次 55 逐字一致（`result-*-r1-round57.log` SHA `56984e42...3ba9`）、门禁 SHA 不变（`4296a7cb...1d71`）。
- **取证结果**：ACC-020 矩阵 **x64 r6 与 x86 r1-x86 均 15/15 PASS，日志逐字节一致**（SHA `46bc16b9c0837b64c0005418335878ac37c21f010911ba36a4200dc73a0d7610`）。驱动迭代链 r1-r6（VM fixture 类型全名带命名空间、静态工具响应无 result 包装需直取对象、list_methods items 键、整数 token、revert 的 pending-at-head 契约、导出阻断条件为检查点非精确而非 live 偏离——均为驱动对契约学习，无产品缺陷结论除 Ldc_I4_S 外）。新 net48 `807a989976f176a1db76714e542a210cf2fa2bc91688930bfbeb7d1786820d71` 已部署 VM Extensions。
- 本轮源 SHA-256：`EditOperationRegistry.cs` `b449393e8d3a9a56af4e8585a9e0f55ae2b04b82e1a6efcf1dc1b69578038e69`；`p03_vm_acc020.py` `a5df03d1605ca3ca61e019ecf152d497bb5d0f3870833d9d5654f501bef0297a`。结束清理 dnSpy-x86 与 ArtifactRoot。无 git 提交、不推进 P04。检查点 `P03_ACC020_LEGACY_SIX_MATRIX_DUALARCH_GREEN`。

## 62. 2026-09-13 ACC-029 容量曲线/瞬态/residual + ACC-012 分页/唯一解析（轮次 58）

- 上轮 legacy 矩阵全绿。本轮新增 harness 探针 `TestCapacityResolution`（注册入默认套件与 `--capacity-resolution` 模式）：
  - **ACC-029**：22 次真实 commit 的容量曲线（存储字节单调不减、单谱系、23 检查点）；**瞬态双份占用**（第二次提交窗口 final+temp 并存实测）；**residual 计量**（窗口内 temp 计为 residual 存储对象、finalize 后全部 trusted 无残留）；确定性限额的容量拒绝零副作用已由 store 测试常驻。
  - **ACC-012**：`edit_history` 分页（首页 10 项携带 cursor、游标排空余量 13 项且无更多页）；**source-identity 反例**——注入 OriginMvid 改写的异源 fixture（内容相同）后，全新 history 模块对该模块的 `ResolveBegin` **跳过异源谱系并唯一解析绑定真实谱系**（mvid 过滤在候选循环中，路径别名冲突另行归类）。
- 迭代学习（r1-r3 保留）：首次提交无 final（双份窗口在第二次提交）；**payload 机制仅特定操作 kind 使用——纯 method_update 无 side-car payload（实测 0）**，断言改为确定性记账；去重场景由 payload-history 套件测试覆盖。
- **取证结果**：探针双架构 PASS（日志逐字节一致 SHA `a68d101a...40d6`）；套件扩至 **24 PASS**（x64/x86 日志一致 `0d8de4ff...1969`）；墓碑门禁 SHA 不变（`4296a7cb...1d71`）。无产品缺陷（两处断言均为驱动侧事实学习）。
- 本轮源 SHA-256：`P03StoreHarness/Program.cs` `148d23de6e1283094bfafc7669ebe3a50dd8ead72a72e38517198581571973f4`。无 git 提交、不推进 P04。检查点 `P03_CAPACITY_RESOLUTION_DUALARCH_GREEN`。
- 边界与未完成：ACC-014 剩余 Windows adapter 临时重载故障注入；ACC-024 剩余 forward/inverse 应用期故障；各 ACC 的 EDIT-ACC 案例文件与 §10.1 取证目录补齐（013/020/029 已具备驱动，待包装）。

## 63. 2026-09-13 ACC-024 导航 forward/inverse 应用期故障矩阵双架构全绿（轮次 59）

- 上轮容量/解析全绿。本轮补齐 ACC-024 的导航应用期故障维度：**测试缝扩展**（`edit_test_storage_fault` 新增 `navigate_forward`/`navigate_inverse` 两 stage：schema 枚举同步扩展 + `StorageFault` 注入点加入 Navigate/MigrateValidated 的 live 应用后与 catch 逆路径前；navigate_inverse 采用 arm 时置位的 `navigateInverseFailure` 专用标志实现"forward 失败 + 回滚逆也失败"双触发——单值 armed stage 无法表达两段故障）。契约依据：ACC-024 行明确要求"注入 forward/inverse 失败"，§6 规定导航语义（forward 失败→inverse 恢复 pre-head→temp 清理后 idle；inverse 失败→无产品恢复的 live_state_unknown + temp 降级 residual）。
- **驱动 `p03_vm_acc024c.py` 15/15 PASS（x64 r4 与 x86 r1-x86，日志逐字节一致 SHA `7bd0d76b...0528`）**：F1 undo×navigate_forward→`EDIT_CHECKPOINT_COMMIT_FAILED(stage=navigate_forward)`+idle+head 不变（pre-head 保持）+live 经 inverse 恢复（begin 干净绑定）；F3 redo×navigate_forward 同契约+故障清除后 redo 重试成功；F2 undo×navigate_inverse→`EDIT_LIVE_STATE_UNKNOWN` 终态+全部编辑阻断+**紧急清理不产生虚假恢复**（尝试后仍 unknown）+**temp 计入 residual 计量**+head 不变——§6"inverse 失败无产品恢复、只给 restart 指引"实证（导航路径的 live_state_unknown 与 commit 路径不同：无 emergencyLiveUndo 可回放，restart 是文档化恢复）。
- **迭代链（r1-r4 保留）**：r1 暴露 schema 冻结枚举拒绝新 stage（oneOf 0 分支，先于协调器）→schema 同步扩展；r2 暴露 F1 全过后 F2 的 navigate_inverse 不触发（单点注入只在 catch 内、forward 成功则 catch 不执行）→专用标志双触发设计；r3 暴露驱动对 §6 的误读（期望紧急清理恢复 idle——导航逆失败无 emergency undo 可用，恢复尝试正确地失败）→终态断言重组。均为驱动/缝设计学习，§6 产品语义自洽。
- **零回归**：契约+双 TFM 0warn/0err（含 schema 扩展后契约校验 PASS）；VM 双架构套件（24 PASS）+ 墓碑门禁 exit 0，套件日志与轮次 58 逐字一致（`0d8de4ff...1969`）、门禁 SHA 不变（`4296a7cb...1d71`）。
- 本轮源 SHA-256：`EditTransactionCoordinator.cs` `7ada58149f2bbf9afde337ec7f052fb0dd36eee72457e7eea601e58f94132df1`；`p03-tool-schemas.json` `a1f0c6b1f2d66f3add1c97d0b0e02327072d0f9638cac83d19bff99e1d4eb318`；`p03_vm_acc024c.py` `1d50ed9a303b1e36ae5716836ea082ae0894f32430fceddeea1d9b085284b0a9`。结束清理 dnSpy-x86 与 ArtifactRoot。无 git 提交、不推进 P04。检查点 `P03_ACC024_NAVIGATE_FAULTS_DUALARCH_GREEN`。
- 边界与未完成：ACC-014 剩余 Windows adapter 临时重载故障注入（产物级）；EDIT-ACC 案例文件与 §10.1 取证目录补齐；此后 P03 全 ACC 复核 → VERIFY_COMMIT。

## 64. 2026-09-13 ACC-014 Windows 真实 Adapter 文件锁注入双架构全绿（轮次 60）

- 上轮导航故障矩阵全绿。本轮闭合 ACC-014 最后缺口（"临时文件重载失败的产物级验证/Windows 真实 Adapter 路径"）：驱动 `p03_vm_acc014b.py` 以**真实文件锁**（无产品缝）驱动 `WindowsEditCheckpointStore.DeleteTemp` 的真实 IOException 路径——commit 停靠 `commit_after_live_complete`（finalize 故障已 arm）→ locker 子进程以**只读句柄**锁住磁盘上的 owned temp（`<lineage>.dnspy-mcp-checkpoints.tmp-<id>`；CPython 不传 FILE_SHARE_DELETE → 删除被阻而身份观察可过）→ 释放 barrier → finalize 故障 → partial(checkpoint_finalize, retry/undo_live) → `edit_recover(undo_live)`：inverse 恢复 pre-live 后真实 `File.Delete` 失败 → **`EDIT_CHECKPOINT_CLEANUP_FAILED` + `aborted_temp_cleanup`**（state=committing、allowed 仅 [cleanup_temp]）→ 解锁 → `cleanup_temp` 成功 → idle 且 temp 从磁盘消失。
- **10/10 PASS（x64 r3 与 x86 r1-x86，日志逐字节一致 SHA `b10e9636...4bf3`）**。迭代链 r1-r3 保留：r1 temp 命名 glob 错误（Windows 适配器为 `<finalPath>.tmp-<id>` 即 `.dnspy-mcp-checkpoints` 在中间）；r2 关键实证——`r+b`（含写访问）锁使产品 `TempMatches` 的身份观察（share=Read）共享冲突 → undo_live 被拒 `EDIT_HISTORY_CONFLICT`（锁释放后同 recovery_id 手工调用成功，实证归因），改只读锁后命中真实清理失败路径。产品语义自洽：不可观察的 temp 拒绝动作、可观察但不可删除的 temp 走 aborted_temp_cleanup 恢复。
- 至此 **P03 全部九项 ACC 的技术缺口全部闭合**（011 分支/validated 拒绝、012 分页/唯一解析、013 双工具分类+确认矩阵、014 导出+MCP 形状+真实适配器锁、019 六相位×关闭/重启/名额、020 六入口 legacy、024 五相位故障+导航故障、025 端到端、029 容量/瞬态/residual）。无产品缺陷本轮。无 git 提交、不推进 P04。检查点 `P03_ACC014B_REAL_ADAPTER_LOCK_DUALARCH_GREEN`。
本轮源 SHA-256：`p03_vm_acc014b.py` `627b5b274473ffc52af9d7294cff2d54641549f2e2c9a501e9e34c5c47d1d1ba`。
- 剩余（非技术缺口）：EDIT-ACC 案例文件与 §10.1 取证目录补齐（013/020/024c/024b/014b/029 驱动已就绪待包装）→ P03 全 ACC 复核 → VERIFY_COMMIT。

## 65. 2026-09-13 §10.1 取证包装补齐 + 全 ACC 终态复核（轮次 61）

- 上轮全部技术缺口闭合。本轮补建案例文件 `EDIT-ACC-013/014/020/024/029.json`（suite/架构/TFM/fixture 身份/前置/动作/预期）并扩展 `p03_vm_edit_acc_evidence.py`（新增四驱动案例映射 + `--arch` + 无头 harness 案例支持：`EDIT-ACC-029`→`--capacity-resolution`）。
- **取证编排 `p03_round61_orchestrator.py`：10/10 pass**——每驱动案例×架构均按配对协议（停 dnSpy→清存储→启动→单案例运行→读 summary）执行：`p03-evidence-20260913-r1-x64` 与 `-x86` 各含五案例的 case.json/driver.log/actions.jsonl/summary.json，全部 `status=pass`。**driver.log 双架构逐字节一致**且与各自独立运行复现：013 `578626f1...880c`、014 `b10e9636...4bf3`、020 `46bc16b9...7610`、024 `7bd0d76b...0528`、029（harness）`265382c5...`（x64/x86 一致）。actions.jsonl 逐请求转录（013=29 调用、014=19、020=16 等）。
- **§3 ACC 终态表重写**：九项 ACC 全部"通过"，各带轮次与 §10.1 证据锚点；产品观察四项登记（指纹自举×2、带外删存储归类、监听器重启机制/open_files 策略）不阻断验收。
- 本轮源 SHA-256：`p03_round61_orchestrator.py` `f1b4f19bb72cd2792059354fc50b5e9798f736d8289fab4821b25fd628596beb`；`p03_vm_edit_acc_evidence.py` `79646b155bdf613c9815c4c32032d0354d849368c5488d67025827c5713effce`。结束清理 dnSpy 与 ArtifactRoot（证据目录保留）。检查点 `P03_EDITACC_PACKAGING_COMPLETE`。下一步：VERIFY_COMMIT（全工作区归因审查→暂存可机械归属文件→提交）。

## 66. 2026-09-13 VERIFY_COMMIT：P03 完整落地（轮次 61 收尾）

- 最终验证门：`tests/run-verify-local.sh` PASS（契约+双 TFM 0 警告 0 错误）；`$HOME/.local/verify-venv/bin/python3 -m unittest discover -s tests/python` **33 测试 OK（7 项 live-URL 门控跳过；系统 python 缺 jsonschema 的 1 个导入错误为环境差异，非回归）**；VM 双架构套件（24 PASS）+ 墓碑门禁 exit 0（轮次 59 终态构建，轮次 60-61 仅驱动/案例变更）；§10.1 取证 10/10 pass（轮次 61）。
- 归因审查：全工作区 122 变更项中 119 项可机械归属 P03 并已暂存提交；**受保护三项按规则隔离未提交**——`PLAN/dnSpy-MCP.md`（用户既有修改 +40 行，非本工作流产物）、`tests/spikes/`、`PLAN/2026.08.26/独立核验/round-8/`。总纲 v2（2026-09-11 环境契约冻结）与长程状态/账本属工作流归因一并提交。
- **提交 `2142266` "Implement P03 checkpoint commit recovery and in-session history (v5)"**：119 files changed, 25834 insertions(+), 1874 deletions(-)。实施记录状态：**通过**（九项 ACC 全过，轮次 2-61 证据链完整）。子方案 P03 CHILD_DONE；workflow 推进至 P04。
