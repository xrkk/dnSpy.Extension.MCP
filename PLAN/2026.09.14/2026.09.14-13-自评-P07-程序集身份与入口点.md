# 自评 — P07 子方案 v0（程序集/模块身份、入口点与已加载模块影响）

- 自评对象：`PLAN/2026.09.14/2026.09.14-12-实施子方案-P07-程序集身份与入口点.md` v0
- 自评时间：2026-09-14
- 自评角色：作者（按主话术"自评清单"逐项）

## 准入检查

| 项 | 结论 | 说明 |
| --- | --- | --- |
| 版本与可实施边界 | 通过 | v0 未审核不得实施已标注；范围=OUT-007 行逐字对齐 |
| 前置条件 | 通过 | OUT-006/P06 CHILD_DONE（`2df8590`）已验收；无跳步 |
| 只新增种类 | 通过 | 4 新操作种类复用 P02 事务/P03 持久化；不复制提交/Undo/导出面 |
| 禁止项 | 通过 | 不引入 analyzer/扩展点/未加载扫描；NON-017 显式排除 |
| 冻结面 | 通过 | 既有 26 种语义零触碰；入口点指纹行新增（兼容策略已在审核重点声明） |
| 可验收性 | 通过 | ACC-006/015 精确预期+失败边界+证据形态齐备；harness+VM 双层 |

## 路由判定

首次提交审核 → **必须第三方审核**（sub-plan-reviewer 隔离上下文）。

## 已知薄弱点（供审核聚焦）

1. 入口点纳入指纹投影对既有检查点重放的兼容策略（旧检查点无该行）需要方案明示——已在 §6.3 声明为审核重点，但未给出既定答案。
2. ACC-006 元数据读回的只读面（是否新增 list_assemblies 类工具或以导出重载断言替代）未定——实现裁决项但影响验收命令形态。
3. edit_impact_scan 对"暂存后新名称"的扫描语义（旧名→新名过渡期两侧都扫）在 v0 文字中仅括号提示，需要更明确。
