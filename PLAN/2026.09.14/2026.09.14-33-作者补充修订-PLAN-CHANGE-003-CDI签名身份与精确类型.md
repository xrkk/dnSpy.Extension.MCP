# 作者补充修订-PLAN-CHANGE-003：CDI 签名身份与精确类型（R03 保真自评）

- 记录类型：作者补充修订（执行方按授权字段更新）；**不是独立审核、不是 AUD、不是核验结论**
- 日期：2026-09-14
- 关联：`PLAN/2026.09.13/2026.09.13-01-PLAN-CHANGE登记-002至004-独立核验整改规范同步.md` 之 PLAN-CHANGE-003（编号沿用）；`PLAN/2026.09.03/2026.09.03-01-实施子方案-P02-...md` v11；核验触发 `PLAN/2026.09.14/2026.09.14-31-独立核验...` §6 CHK-021；前次作者记录 `2026.09.14-32`（保留原样，未改写）
- 任务：T002-R03（RUN_ID `dnspy-chk021-026-opencode-20260914-01`）
- 背景：Codex R02 独立验收（`T002-R02-Codex独立验收.md`）在 x64/x86 发现两个真实遗漏，T002 未通过；本轮修正并在作者材料中如实承接。

## 1. 两个真实遗漏与修复

| 遗漏（Codex 反例） | 修复 | 证据 |
|---|---|---|
| **B1 签名身份不完整**：`PdbForwardMethodInfo.Method` 为 `MemberRef F(void, ClassSig(TypeRef N.T@A))`，只改参数 TypeRef.ResolutionScope A→B 时 guard 不变；modifier、嵌套 TypeRef、MemberRef 父项同样丢失，签名用未转义分隔符拼接 | `EditCdiGuard` 改为完整结构化编码：TypeRef 完整 scope 链（AssemblyRef 结构化身份/ModuleRef/嵌套 TypeRef 递归）、namespace/name；MemberRef 真实父（TypeDef/TypeRef/TypeSpec/ModuleRef/MethodDef）；TypeSig 全元素（modreq/modopt、array/sizes/lower_bounds、generic_inst、fnptr、sentinel、pinned、byref/ptr、module_sig）；MethodSig 以字段结构化编码；未知形状 `EDIT_CAPABILITY_UNAVAILABLE`，无 ToString fallback | harness 新增 B1 矩阵（scope A→B、嵌套外层 scope A→B、modreq/modopt、name/namespace 分离、含 `,()|` 名称、fnptr、array、generic、sentinel、ModuleRef/MethodDef 父、等价副本一致、未知形状拒绝）；本地真实 net10 产品 161 项全 PASS，`T002-R03-postfix-cdi-guard-content.log` |
| **B2 精确类型门禁缺失**：自定义 `ExtraDocs : PdbTypeDefinitionDocumentsDebugInfo` 携带 `Extra` 字段，被基类 switch 接纳，Extra 0→1 无 guard 变化也不报能力限制 | 编码前按绑定 dnlib 的**确切类型集合**门禁（23 个公共具体类型 + 精确 internal `PdbAsyncMethodSteppingInformationCustomDebugInfo`/`PdbTypeDefinitionDocumentsDebugInfoMD`）；已知类型的具体子类按未知拒绝；`PdbUnknownCustomDebugInfo` 仍合法；不用 FullName/IsAssignableFrom 宽松判定 | harness 新增 B2 正/负例（派生+Extra 拒绝、确切公共类型仍合法完整、真实 MD 正例保留） |

失败先行证据（B5 要求）：修前在 d476569 产品上运行同一 harness，B1/B2 共 12 项 FAIL（`T002-R03-prefix-failures.log`）；修后 161 项全 PASS。

## 2. B3 协调器路径覆盖与限制

- `EditTransactionCoordinator.TestPersistentExternalMutation` 的既有 `live-conflict:mutate-cdi` 分支扩展为**优先**选择基线中已存在的同种/同行内容变更：先找 `PdbDynamicLocalVariablesCustomDebugInfo`（≥65 flags）翻转第 65 位，再找 `PdbStateMachineHoistedLocalScopesCustomDebugInfo` 把绑定到方法体指令的 `End` 改为同方法另一指令；不存在时保留原 default-namespace 回退。Undo 精确恢复原值/原结构。测试 artifact JSON 增加 `cdi_shape` 字段（仅测试证据文件，不改工具 schema/响应契约）。
- **Schema 限制（如实报告）**：`edit_test_external_mutation.case_id` 在 `Editing/Contracts/p03-tool-schemas.json` 是封闭 enum；新增 `case_id` 需要 schema 变更，而本轮明确不授权，故未新增枚举值，而是通过既有 enum 内的 `live-conflict:mutate-cdi` 承载新形状（fixture 决定分支）。若 Codex 需要显式 case id，请在后续轮次授权 schema 变更。
- 新增 `tests/edit/cdi_guard_coordinator_driver.py`：VM 上对真实 loopback 执行 review 拒绝、review 后 commit 拒绝、已提交谱系的 edit_undo 保护、restore 后 review 复绿与 rollback；以 artifact `cdi_shape` 强制反例前提，缺少形状时明确 `FIXTURE_REQUIRED`，不伪造通过。**执行端未运行 Windows/VM**，源码断言不当作实际 PASS。
- **第二门禁限制（如实报告）**：Dispatcher 临界区暂停期间 commit 持有 edit 操作门，edit-family seam 无法在暂停窗口注入；该路径由既有 `chk_targeted_driver.case_chk013_gate`（debug 工具漂移）覆盖同一 guard 消费点，CDI 形状的二次门禁注入需要新的 debug-accessible seam，本轮未实现。

## 3. 需求保真自评（作者级，本轮范围）

| 项 | 数量 |
|---|---:|
| 应承接 | 8 |
| 完整 | 8 |
| 未落点 | 0 |
| 语义弱化 | 0 |
| 不可验收（执行端材料口径） | 0 |
| 失效或复核条件缺失 | 0 |

- IDs：REQ-008、REQ-009、REQ-025、CON-004、RACC-001、RACC-010、RACC-011、RACC-025。
- **与未运行证据分开**：A7 VM x64/x86（含新增 B1/B2 入口、协调器 driver、既有回归）与必要独立审核**未执行**；B3 driver 未在 Windows 运行；第二门禁 CDI 注入未实现。以上不计入“完整”。
- 前一记录 2026.09.14-32 的 8/8 自评保留为当时事实；本记录不以 8/8 代替实体验收。

## 4. 影响稳定边、验收与重新裁决条件

- 稳定边：OUT-002→OUT-003 及 OUT-002 供 P06/P07/P08；验收 ACC-001/010/011/025。
- 重新裁决条件：dnlib 升级/新增 CDI 或签名形状；确切类型集合变化；owner/引用身份规则变化；需要把 CDI 内容并入持久语义（T003/版本事件）。
- 审核状态：必要审核链未执行，标待审核；未伪造用户追认、未关闭旧 AUD、未宣告 CHK-021 最终闭环。
