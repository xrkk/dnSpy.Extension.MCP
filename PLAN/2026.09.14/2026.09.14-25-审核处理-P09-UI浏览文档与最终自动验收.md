# 审核处理记录 — P09 UI 浏览、文档与最终自动验收（首次全面审核）

- 工作流：dnspy-edit-master-01a050b5
- 审核对象：`PLAN/2026.09.14/2026.09.14-22-实施子方案-P09-UI浏览文档与最终自动验收.md` v0（blob `5f880e4df31eb1bb09efede9f3f47380e3814400`）
- 审核记录：`PLAN/2026.09.14/2026.09.14-24-审核-P09-UI浏览文档与最终自动验收.md`（blob `3c1db7cf39ef05046e5a3ad017204577e310d0b5`，结论 存在不同意见/含阻断级发现，门禁 10/9/0/0/1/2）
- 基线代码：`53507ca`（P08 实现）+ `b242b2f`（CHILD_DONE 记录）
- 处理时间：2026-09-14

## 1. 发现处置（AUD-001..009，全部同意）

| AUD | 级别 | 发现摘要 | 处置 | 落点（v1） |
| --- | --- | --- | --- | --- |
| AUD-001 | 高/阻断 | ACC-021/REQ-019 的 P09 终判切片整体缺席 | 同意。P09 拥有 ACC-021 终判：IMP-007 新增——严格 schema 校验器全景（tools/list 全部 25 工具内联 JSON Schema 逐工具校验；每错误类别实测触发；structuredContent 对象型契约与 debug 类型不回归归 ACC-028 分工）；案例 EDIT-ACC-021+runner 映射+acceptance 行 | §1/§3 IMP-007、§4 新 ACC-021 节 |
| AUD-002 | 高/阻断 | 命名基线 `53507ca` 不可独立构建（Debugger/DebugSessionService.cs 的 ReadEventsForEvidence 未提交） | 同意。执行方补提交 `8bdc1e7`（"Commit P08 debug session evidence accessor"）——工作区遗漏文件，语义与 P08 记录一致；基线更正为 `8bdc1e7`；P09 首步构建义务引用新基线 | 头部基线 |
| AUD-003 | 中 | 现状盘点计数双失实（全扩展工具 71=32 调试+22 静态+17 编辑族；提示词自述陈旧） | 同意。现状盘点更正：MCP 全扩展通告 71 工具（**编辑族 25**=17 产品+8 测试，调试族 32，静态/杂项 22……实测口径由 IMP-002 导出快照固定）；"16 工具时代"改为"初版提示词时代（自述已陈旧）"；文档数量标注一律以导出快照为准 | §2 |
| AUD-004 | 中 | CancelFromUi 属主语义未冻结且 §1 与 IMP-001 自相矛盾 | 同意。冻结语义：UI 取消**仅当无活动事务或活动事务属主会话已断开**（OwnerClosed/失效）时可用（守门员语义：清理悬置事务，不给 UI 一个凌驾活动 MCP 会话的撤销通道）；活动事务属主在场时按钮禁用并显示属主传输——不产生 EDIT_OWNER_MISMATCH 旁路；取消走 `EndLocked(tx,"ui_cancel")`（回滚释放路径） | §3 IMP-001 |
| AUD-005 | 中 | UI 线程/隔离契约缺位（gate×dispatcher 死锁、轮询×test_clock、UIA 可发现性） | 同意。冻结：面板读取走 `EditWorkspace.OnDispatcher`（协调器内部状态经新增只读快照方法 `BuildExplorerSnapshot()`——在协调器锁内组快照、锁外返回不可变行，UI 线程仅拿快照，不反向调用协调器加锁方法）；轮询间隔 1s 且在 DNMCP_TEST=1 时同样工作（不触碰 edit_test_clock——时间推进仅测试工具负责）；UIA 可发现性=面板标题固定 "MCP Edit Explorer"（NameProperty 可寻址） | §3 IMP-001 |
| AUD-006 | 中 | 导出脚本取数路径未闭合；描述硬编码计数将污染文档 | 同意。`tools/export_tool_registry.py` 取数=VM loopback `tools/list` 实测（Mcp-Session-Id 握手）+本地注册表源码扫描双源对照，不一致即失败；`EditToolProvider` 的 edit_apply 描述计数修正为 37（随 v1 落库） | §3 IMP-002 |
| AUD-007 | 中 | dnSpyEx Release 复核判据与重验范围弱于总纲 §10.1 | 同意。闭式定义：判据=dnSpyEx GitHub latest stable Release tag+commit vs VM 安装 exe 的 FileVersion/ProductVersion 与插件兼容冒烟；更新条件=tag 更新；更新后重验=双 TFM 构建+双架构部署冒烟+每架构一个代表性 ACC（x64: EDIT-ACC-005 全链；x86: EDIT-ACC-008）+tools/list 清单核对；不更新则记录证据即可 | §3 IMP-004 |
| AUD-008 | 低 | ACC-023 组装既有证据无可追溯清单；门禁拒绝态二选一 | 同意。列名：P08 EDIT-ACC-007/033 哨兵（run-id p08-resource-20260912-r1-*）、P01 ACC-017 拒绝态（计划记录 2026.09.04 既有证据）+定向补测（本驱动新探针：恶意 .resources 坏魔数/截断拒绝+编译激活器源静态查看零执行） | §3 IMP-003 |
| AUD-009 | 低 | §5 计数 8 与列出 9 不符；缺 REQ-019 | 同意。应承接 8→10（补 REQ-016/019 经 ACC-021 落点后重计：REQ-016；REQ-019；REQ-020；REQ-021；REQ-023；CON-001；CON-018；CON-026；CON-027；CON-028） | §5 |

## 2. 修复与复检

- 计划修订为 v1（blob `a0165227d22038c83397c69113b8626602b3801d`），内容仅限上表九项处置；范围/结论不变（UI 只读+取消、文档四类、汇总回归、最终报告）。
- 基线补提交 `8bdc1e7` 后双 TFM 构建义务可独立满足。
- 保真门禁复检（v1）：应承接 10 / 完整 10 / 削减 0 / 超范围 0 / 未覆盖 0 / 不可验收 0 / 复核条件缺失 0。
- 再审核触发条件评估：九项处置中 AUD-001 为**新增实施项/新 ACC 拥有权**（范围内补齐总纲既有映射，非范围变更——总纲 §9.1/§11 本就映射 P09）；其余为语义冻结/计数更正/基线修正 → **不触发再审核**，作者自评结项。

## 3. 结论

- 计划 v1 状态：**已审核， 可以实施**。
- 批次结论（-24 记录）：存在不同意见（阻断级发现经处置闭合）→ 处理后关闭。
- 下一步：P09 IMPLEMENT（IMP-001..007）。
