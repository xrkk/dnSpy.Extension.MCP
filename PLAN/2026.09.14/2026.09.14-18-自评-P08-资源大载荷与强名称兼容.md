# 自评 — P08 子方案 v0（资源、大载荷与强名称兼容）

- 自评对象：`PLAN/2026.09.14/2026.09.14-17-实施子方案-P08-资源大载荷与强名称兼容.md` v0
- 自评时间：2026-09-14
- 自评角色：作者（按主话术"自评清单"逐项）

## 准入检查

| 项 | 结论 | 说明 |
| --- | --- | --- |
| 版本与可实施边界 | 通过 | v0 未审核不得实施已标注；范围=OUT-008 行逐字对齐 |
| 前置条件 | 通过 | OUT-007/P07 CHILD_DONE（`0068254`）已验收 |
| 只新增种类 | 通过 | 6 新操作种类复用 P02 事务/P03 持久化；导入导出复用 checkpoint store 文件身份 |
| 禁止项 | 通过 | CON-019 反序列化硬边界以哨兵验收；无内联上限变更 |
| 冻结面 | 通过 | 既有 30 种语义零触碰；Win32 行指纹覆盖策略已声明审核重点 |
| 可验收性 | 通过 | 三个 ACC 精确预期+失败边界+证据形态齐备 |

## 路由判定

首次提交审核 → **必须第三方审核**（sub-plan-reviewer 隔离上下文）。

## 已知薄弱点（供审核聚焦）

1. file_id 引用载荷在检查点重放的确定性语义（字节快照入包 vs 取文件复制约束）未定——§6.2 已声明。
2. Win32 原生行不在现有指纹 resource 行（其只覆盖 EmbeddedResource）——三层覆盖先例是否适用需审核确认。
3. "动态失败证据"的格式与一次性绑定在 v0 只给了方向（P01 事件 id 文法实现裁决）。
