# 实施记录 P05：公开 C# 编译前端与安全限制

- 方案：`PLAN/2026.09.14/2026.09.14-01-实施子方案-P05-公开CSharp编译前端.md`（v1，终态 blob `af27b040bf74f40b980a4d02eeeb09e201787290`，状态 `已审核, 可以实施`）
- 总纲 v2 / 需求 v0；首次审核 `2026.09.14-03`（blob `933172af`）经 `2026.09.14-04` 全量引入
- workflow：`dnspy-edit-master-01a050b5`（长程实施授权进行中）
- 审核基线：HEAD `6081a4a`；分支 `feature/p01-public-contract-vm-gate`
- 实施状态：**进行中**（轮次 1 起）
- 受保护现场：`PLAN/dnSpy-MCP.md`（用户既有修改）、`tests/spikes/`、`PLAN/2026.08.26/独立核验/round-8/` 隔离不动

## 1. IMP/产出验收总表

| IMP | 内容 | 状态 |
|---|---|---|
| IMP-001 | 编译会话服务（EditCompileFrontend） | 通过 |
| IMP-002 | edit_compile 工具契约与安全限制 | 通过 |
| IMP-003 | 测试与验收资产 | 通过 |
| 产出验收 | OUT-005 产出（双 TFM/诊断/Portable PDB/禁止项） | 通过 |

## 2. 轮次记录

### 轮次 1-2（2026-09-14）：IMP-001..003 落地与产出验收双架构全绿

- **IMP-001/002**：`Editing/EditCompileFrontend.cs`（新 IMcpToolProvider 导出 `edit_compile`；MEF `IEnumerable<ILanguageCompilerProvider>` 注入 + Language Guid 优先/类型名后备的唯一提供器选择；`IAssemblyReferenceResolver` 生产移植——S02 spike 的 unsafe GCHandle 钉住辅助类整体移植（csproj `AllowUnsafeBlocks`，unsafe 边界限于该类）；引用闭包＝目标模块自身（EditClass 自引用）+ 已加载文档解析的 AssemblyRefs + corlib；**引用闭包与文档树枚举全部经 `EditWorkspace.OnDispatcher` marshal（dnSpy 文档树是 WPF UI 对象，跨线程访问实证抛 InvalidOperationException）**；**编译会话的 Create/Initialize/AddDocuments 在 dispatcher 上执行、CompileAsync 在请求线程 await（provider 是 UI 组件需要 STA，S02 事件处理器事实）**；有界产物登记缓存（8 项超限拒绝）；诊断行映射公开 `CompilerDiagnostic`（severity/id/description/filename/line/character）；**CON-022 契约面闭合**——输入键白名单 provider 侧强制（非 edit provider 不走协调器 schema 校验），未知字段（含 analyzer 探测）拒绝）。
- **IMP-003**：harness `--compile-frontend-matrix`（stub provider 注入：登记缓存/Lookup/容量/诊断映射/非编译工具转发——真实 Roslyn 路径在 VM 集成层）；集成 `p03_vm_acc005.py`（14 断言：G1 工具广告（协议级 tools/list 原始请求）、V1 有效编译（成功/诊断空/assembly+pdb 身份/compile_id/consumable）、E1 无效编译（success=false/诊断含 severity/位置/无 compile_id）、F1 未知扩展字段拒绝、S1 协调器 idle、S2 编译产物不可经 edit_apply 消费）。
- **取证结果**：集成 **x64 r6 与 x86 r1 均 14/14 PASS，日志逐字节一致**（SHA `213a0dbb...62866`）；harness 探针双架构 exit 0；§10.1 取证 `p05-compile-x64/x86-r1\EDIT-ACC-005`（summary pass 15 行，driver.log 跨架构一致 `213a0dbb...`）。**OUT-005 产出验收达成**（编译服务/引用解析/诊断/Portable PDB 内存载荷/不导入/双 TFM 0-0/禁止项不可启用）；ACC-005 终判按总纲归属 P06。
- **迭代链 r1-r6 保留**：DebugFileResult 构造形状、McpCallContext 会话初始化门、JSON 值形状（JsonElement 访问器）、WPF 跨线程（树枚举与编译会话的 STA）、引用闭包含目标自身、tools/list 协议级形状、字段白名单。均为契约学习。
- **零回归**：`run-verify-local` PASS（契约+双 TFM）；VM 双架构 harness 探针+默认套件+墓碑门禁全部 exit 0（`roundp5-summary`）。
- 本轮源 SHA-256：`EditCompileFrontend.cs` `c09528d5962f89d9f16afde8b0a415382e41b8563a4d5c247cbea68d13f454d4`；`dnSpy.Extension.MCP.csproj` `37f57029bc669d1b140a95f9d9f62cb0b90b28fbbae6ac3d646cd902ba1bb624`；harness `Program.cs` `2b1e973608d20b727229404aea29a20c88e57f372067337104fc188a196c96e8`；`p03_vm_acc005.py` `6f21d38fdbe5568746d9d20484ee45c0773d154a45c207e88e02e69ae261e653`；`EDIT-ACC-005.json` `b88b21c8d20864bb863de461d06cca317f03f0e5339385b23988d26640b5e0ed`；`p03_vm_edit_acc_evidence.py` `e1e89f0948e978fbd41eeb497350954abbf9fbb93f00e0acec6ecb6f0686c0d0`。
- 状态：IMP-001..003 与产出验收全部通过。待 VERIFY_COMMIT。
