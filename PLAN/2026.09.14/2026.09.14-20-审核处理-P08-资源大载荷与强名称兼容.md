# 审核处理记录 — P08 资源、大载荷与强名称兼容（首次全面审核）

- 工作流：dnspy-edit-master-01a050b5
- 审核对象：`PLAN/2026.09.14/2026.09.14-17-实施子方案-P08-资源大载荷与强名称兼容.md` v0（blob `a881a68b6ae38a9b200b5308e8b5dd3883823e5b`）
- 审核记录：`PLAN/2026.09.14/2026.09.14-19-审核-P08-资源大载荷与强名称兼容.md`（blob `6e9ac8ddaa72190ce6c357f659ea52bb7cf6c709`，结论 存在不同意见，门禁 11/10/0/0/1/3）
- 基线代码：`0068254`（P07 实现）+ `0cd84c3`（CHILD_DONE 记录）
- 处理时间：2026-09-14

## 1. 发现处置（AUD-001..008，全部同意）

| AUD | 级别 | 发现摘要 | 处置 | 落点（v1） |
| --- | --- | --- | --- | --- |
| AUD-001 | 高（阻断） | Win32 原生行不在指纹投影，"无需投影变更"对 Win32 失实；覆盖机制未闭合 | 同意。**沿用 P07 裁定先例（三层覆盖，零投影变更）**：(i) 检查点图像 SHA 字节级覆盖原生表；(ii) `edit_review` 在 Win32 类操作暂存时执行私有/live **原生资源行断言**（行集逐行：type/name/langId/字节 SHA）；(iii) harness 断言重放图与私有图导出图像字节一致。现状盘点更正（Win32 行不在投影） | §2、§3 IMP-002 |
| AUD-002 | 高（阻断） | file_id 载荷的检查点信封/重放取字节未闭合；总纲 §5.2 行 175 已冻结"资源载荷按 SHA-256 去重入包" | 同意。落定：**操作载荷始终内嵌字节（data_base64）**——`edit_resource_import` 在服务端读取 VM 路径字节后暂存内嵌载荷的操作（不进 MCP 请求正文，绕开 1 MiB 传输限制）；检查点包内按既有信封存储操作行（字节在载荷内），包级按 SHA-256 去重（总纲冻结语义）；重放自包内取字节，**零文件依赖**。载荷白名单/信封扩展点列全（method 两种类外新增资源/强名称种类行）；ACC-008 增含 file_id 检查点的恢复/重放断言 | §3 IMP-001/003、§4 ACC-008 |
| AUD-003 | 高（阻断） | ResourceWriter 名字哈希/排序跨进程不确定；"重载读回一致"低于字节级判据 | 同意。`EditResourceCodec` **自实现确定性 .resources 写出器**：固定 magic/版本头、文档化稳定名字哈希函数、按 (hash, ordinal 名) 排序、7 种 ResourceTypeCode 域固定编码——跨进程/跨面（私有/live/重放）字节级一致；harness 断言跨进程写出哈希相等 | §3 IMP-001 |
| AUD-004 | 中 | 强名称"动态失败证据"无既有持久机制、归因判据与防伪未冻结 | 同意。证据＝`{session_id, event_cursor, event_kind}`：coordinator 校验 P01 调试事件缓冲中该游标处事件为目标模块加载/验证失败类（归因＝事件携带目标程序集名）；一次性消费（已用证据 id 记录于事务+谱系，跨事务重用拒绝）；绑定事务模块 mvid；ACC-016 增证据重放拒绝断言 | §3 IMP-004、§4 ACC-016 |
| AUD-005 | 中 | Win32"替换/导入导出"子句未落点；import 硬编码 managed | 同意。新增第 7 种类 `win32_resource_update`（整行字节替换，复用 add 的载荷与图标组校验）；`edit_resource_import`/`export` 的 `resource_type` 值域冻结（embedded|linked|win32），win32 分派至 3 个 Win32 种类；`remove_mode` 语义与既有种类的 reject_if_referenced 统一 | §3 IMP-001/002/003 |
| AUD-006 | 中 | 标准值域不精确（"七种数值"vs ResourceTypeCode；流/Char/DateTime 取舍） | 同意。冻结值域：string、bool、byte/sbyte/short/ushort/int/uint/long/ulong/float/double（9 数值）、byte-array（含 stream 码——按字节数组读写）；Char/DateTime/TimeSpan/Decimal/自定义＝编辑域外（查看元数据+整体替换）；harness 矩阵按此 13 类逐项 | §3 IMP-001、§4 ACC-007 |
| AUD-007 | 低 | §5 计数漏 CON-012 与 NON-004/006/007/008/012 | 同意。应承接 7→11（补 CON-012（消费）；NON-004/006/007/012 为既有不变式经本方案消费不重构，NON-008 由强名称门禁落点） | §5 |
| AUD-008 | 低 | 容量定性不准；ACC-008/016 缺双架构条款 | 同意。§2 更正（MaxResourceBytes＝活模块内嵌资源总量门+单载荷上限，apply 路径重检）；ACC-008/016 增 x64/x86 独立产物条款 | §2、§4 |

## 2. 修复与复检

- 计划修订为 v1（blob `8851d8aebd8f2d4f31989902b82f5c74f2af52fd`），内容仅限上表八项处置；范围/结论不变。
- 保真门禁复检（v1）：应承接 11 / 完整 11 / 削减 0 / 超范围 0 / 未覆盖 0 / 不可验收 0 / 复核条件缺失 0。
- 再审核触发条件评估：八项处置为计划内修订（三项阻断均按既有裁定先例/总纲冻结语义闭合，无新承诺对象、无冻结语义变更）→ **不触发再审核**，作者自评结项。

## 3. 结论

- 计划 v1 状态：**已审核， 可以实施**。
- 批次结论（-19 记录）：存在不同意见 → 处理后关闭。
- 下一步：P08 IMPLEMENT（IMP-001..006）。
