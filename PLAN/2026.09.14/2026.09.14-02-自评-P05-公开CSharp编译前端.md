# 自评 P05：公开 C# 编译前端与安全限制

- 对象：`PLAN/2026.09.14/2026.09.14-01-实施子方案-P05-公开CSharp编译前端.md`（v0）
- 方案内容身份：git blob `731fb9f3e5c16043bd63fad3adcccfd4b757e75c` / SHA-256 `19318ea18ac3668f1c2f024937fb11144b7ee41e205842cc433fa4a3296d44ae`
- 总纲：v2（blob `bcc7b2c1...` 起点 + v2 环境契约）；需求 v0（blob `22574a90...`）
- Pxx：P05；前置 OUT-004/P04 已验收（提交 `6081a4a`）
- 本地 HEAD：`6081a4a`（分支 `feature/p01-public-contract-vm-gate`）
- 核心维护范围：`edit_compile` 工具 + `Editing/EditCompileFrontend.cs`（MEF 公开 Roslyn 提供器、产物登记缓存）+ 工具 schema 与安全限制
- 直接依赖范围：dnSpy 公开 `ILanguageCompilerProvider`/`CompilerProjectInfo`/`CompilerDocumentInfo`/`CompilationResult`/`PinnedReferenceSet`；P01 权威上下文/VM 门禁/工具注册
- 明确排除范围：结果导入与内嵌 PDB 转换（P06）；身份编辑（P07/P08）；事务内核改变（P02/P03 零依赖）
- 决策关键路径：S02/S02-R1 契约事实 → §2 盘点 → §3 IMP-001..003 → §3 子方案内验收（产出验收，ACC-005 终判归 P06）
- 上下文路径：总纲 OUT-005/ACC-005/P06 行、§12 分离规则、§13 审核重点 6；需求 REQ-004/RACC-005/CON-015/022/025；S02 结果.md（blob `41a47901df4fd406377c1c139227ef1b198d6b17`）；FeasibilityExtension.cs 调用序列；static-tools.baseline.json（32 工具无编译入口实测）
- 发现与修改（本次自评闭环）：
  1. ACC-005 判定权归属核对：总纲 §11 表明 ACC-005/P06——子方案因此以"产出验收"表述而非声称 ACC-005 通过（避免越权判定；P05 记录产出达成、ACC-005 留给 P06 联合判定）。
  2. 引用集默认目标模块闭包 vs 显式清单：定为默认闭包 + 缺失时显式清单（避免虚构 GAC 解析）。
  3. references_override 白名单策略：确认为 P01 工具策略复用而非新策略（超范围拒绝码沿用 EDIT_CAPABILITY_UNAVAILABLE）。
- 需求保真门禁（独立重做）：应承接数 4 / 完整数 4 / 未落点数 0 / 语义弱化数 0 / 不可验收数 0 / 失效或复核条件缺失数 0；异常 ID 无。
- 送审成熟度：来源切片（S02/S02-R1 + spike 代码 + 32 工具实测）、范围/非目标（§1）、关键决定（§2 事实 + §3 契约）、稳定依赖边（§1 清单）、IMP/验收双向追溯（§3↔§4）、异常/边界语义（三类无效样本 + 禁止项）、证据适用与失效条件（双架构+§10.1）全部落地；方向阻断、验收阻断、占位决定、无主依赖均为零 → **送审准入：通过**。
- 审核路线：本对象无任何既有第三方审计 → **必须首次第三方审核（全面）**，状态保持 `未审核, 不得实施`。
- 实施授权：workflow `dnspy-edit-master-01a050b5` 长程授权进行中。

## 需求保真确认

应承接 4 / 完整 4 / 未落点 0 / 语义弱化 0 / 不可验收 0 / 失效或复核条件缺失 0；异常 ID：无。
