# 自评 P06：MCP 自有稳定身份导入器、C# 方法/类型编辑与内嵌符号

- 对象：`PLAN/2026.09.14/2026.09.14-06-实施子方案-P06-稳定身份导入器与CSharp编辑.md`（v0）
- 方案内容身份：git blob `aa570832ddf4980ecbc67dbe000a4bbf159f3f4e` / SHA-256 `e47cf1f4cd48ac94256461121a13c72e41812f14ebee0bac5d14ca34ef72992c`
- 总纲：v2（blob `bcc7b2c1...` 起点 + v2 环境契约）；需求 v0（blob `22574a90...`）
- Pxx：P06；前置 OUT-005/P05（提交 `cf32659`）与 OUT-003/P03（提交 `2142266`）已验收
- 本地 HEAD：`cf32659`（分支 `feature/p01-public-contract-vm-gate`）
- 核心维护范围：`edit_import` 工具 + `EditImportMatcher`（稳定身份）+ `EditCSharpImporter`（产物→结构化操作 + sequence point/CDI 移植）+ body 载荷 sequence_points 扩展 + 内嵌 PDB 语义
- 直接依赖范围：P05 `EditCompileFrontend.Lookup`（产物唯一入口）；P02 `EditWorkspace`/`EditOperationRegistry.Apply`；P03 审查/提交/检查点链；dnlib `PdbState`/`PortablePdbReader`；`EditStructuredSignatureCodec`
- 明确排除范围：编译（P05）；身份/资源（P07/P08）；查询/UI/文档（P09）；AsmEditor 内部类型与反射代理（CON-025）
- 决策关键路径：S02 五边界+义务清单 → §2 盘点 → §3 IMP-001..005 → §4 ACC-005 终判
- 上下文路径：总纲 OUT-006/ACC-005/§4.1-4/§4.2 关键边界（"P05 编译结果只有 P06 导入器可以消费"）；需求 REQ-004/RACC-005/CON-015/022/025；S02+S02-R1 结果.md（blob `41a47901df7fd40637c1c139227ef1b198d6b17`）；P05 实施记录（cf32659，产物消费面 Lookup/consumable_by_import/S2 断言）；P03 实施记录（操作种类与检查点链）
- 发现与修改（本次自评闭环）：
  1. ACC-005 终判权核对：总纲 §11 归属 P06——方案以 P06 拥有终判、P05 编译子句已完成的复合表述（不重复已验收内容，终判矩阵包含编译步骤作为链路一环）。
  2. 产物跨进程缺失错误码未定（EDIT_CAPABILITY_NOT_FOUND vs 复用 EDIT_HISTORY_CONFLICT）标注为实现时定一（避免虚构未核实契约）——列为审核确认点而非占位（两个候选均为既有码族语义可承载）。
  3. 引用未映射＝失败：S02/ACC-005 硬性断言，导入器把未解析引用列为拒绝点而非降级映射（无 silent fallback）。
  4. 生成类型/状态机按整体子树处理（S02 义务"编译器生成类型及状态机"）——成员级部分匹配会破坏状态机结构，整体替换/新增为唯一安全语义。
- 需求保真门禁（独立重做）：应承接数 5 / 完整数 5 / 未落点数 0 / 语义弱化数 0 / 不可验收数 0 / 失效或复核条件缺失数 0；异常 ID 无。
- 送审成熟度：来源切片（S02+S02-R1 + P05 产物消费面 + P02/P03 操作族实测）、范围/非目标（§1）、关键决定（§2 五边界继承 + §3 契约）、稳定依赖边（§1 清单）、IMP/ACC 双向追溯（§4↔§5）、异常/边界语义（歧义/未解析/跨进程产物）、证据适用与失效条件（双架构+§10.1+断点链）全部落地；方向阻断、验收阻断、占位决定（错误码二选一已声明为既有码族候选，非虚构）、无主依赖均为零 → **送审准入：通过**。
- 审核路线：本对象无任何既有第三方审计 → **必须首次第三方审核（全面）**，状态保持 `未审核, 不得实施`。
- 实施授权：workflow `dnspy-edit-master-01a050b5` 长程授权进行中。

## 需求保真确认

应承接 5 / 完整 5 / 未落点 0 / 语义弱化 0 / 不可验收 0 / 失效或复核条件缺失 0；异常 ID：无。
