# 审核 P06：MCP 自有稳定身份导入器、C# 方法/类型编辑与内嵌符号

- 审核性质：**首次第三方审核**；模式：**全面审核**；轮次：**1**
- 历史链：任务包声明本对象无任何既有第三方审计记录（历史链：无）。**现场异常**：目录内已存在未跟踪文件 `2026.09.14-08-审核-P06-稳定身份导入器与CSharp编辑.md`（20842 字节，mtime 2026-09-12 05:25，晚于 07 自评的 05:18），内容为同一 blob（`aa57083…`）的完整"首次第三方审核/轮次 1/NN=08"记录，非本审核会话产物，来源无法在本上下文核实。按审计记录不可变规则未对其做任何改动；按流水号冲突递增规则本记录取 **NN=09**（写入前实测扫描 `2026.09.14-01..08` 全部占用）。本记录全部发现由本方独立取证得出（证据仅为下表列出的任务包指定对象与本地基线只读代码），未将该 08 文件用作证据输入；其存在本身作为工作区异常如实记录，不构成对本对象的审计阻断（被审对象身份、全部规范来源与代码事实均完整可验）。
- 审核批次身份：workflow `dnspy-edit-master-01a050b5` P06 首次送审；记录日期前缀按批次目录/任务包约定取 2026.09.14，实际创建日 **2026-09-12**（环境系统日期）如实注明。
- 被审对象：`PLAN/2026.09.14/2026.09.14-06-实施子方案-P06-稳定身份导入器与CSharp编辑.md`，文档版本 **v0**，状态 `未审核, 不得实施`
  - git blob：`aa570832ddf4980ecbc67dbe000a4bbf159f3f4e`（任务包声明一致，`git hash-object` 实测复核通过）
  - SHA-256：`e47cf1f4cd48ac94256461121a13c72e41812f14ebee0bac5d14ca34ef72992c`（与自评记录声明一致）
- Pxx：P06（OUT-006；ACC-005 终判归属 P06——总纲 §11 行 378）
- 本地基线：HEAD `cf326598121824afb6f76fde8920907fbf692f79`，分支 `feature/p01-public-contract-vm-gate`，仓库根 `/home/adminn/projects/dnSpy.Extension.MCP`
- 工作区边界：本工作流对象＝`2026.09.14-06/07` 两个未跟踪新文件＋本记录（09）；任务包声明范围外受保护项＝`PLAN/dnSpy-MCP.md`（用户修改）、`tests/spikes/`、`PLAN/2026.08.26/独立核验/round-8/`（均实测在位且未触动）。**另观察到两项未在任务包边界声明中的工作区差异**，经 diff 核阅不改变本审核依据：(a) P05 实施记录相对 HEAD 追加了"轮次 3 VERIFY_COMMIT"段（记录提交 `cf32659`，与基线声明互相印证）；(b) 总纲长程执行状态文件 `执行事件.jsonl`/`执行状态.json` 有修改（workflow 运行态簿记）。
- 输入材料与指纹（均本方实测）：
  - 总纲 v2：`PLAN/2026.08.31/2026.08.31-02-总纲-dnSpy-MCP结构化程序集编辑.md`（blob `6e2775d6d06c5e360e80d0ade2dc736ed16c89a1`；关键切片 §4.1-4/8、§4.2 关键边界、§8 OUT-006 行、§9.1/9.2/9.3、§11 ACC-005 行）
  - 需求 v0：`PLAN/2026.08.31/2026.08.31-01-需求提炼-dnSpy-MCP结构化程序集编辑.md`（blob `22574a9054a5016e8a2899f0508d0069e7742843`；子方案头部引用路径与实际文件名不符，见 AUD-006）
  - Spike S02+S02-R1：`PLAN/2026.08.31/技术Spike/S02-csharp-public-compiler-import/结果.md`（blob `41a47901df4fd40637c1c139227ef1b198d6b17`，实测一致）
  - P05 实施记录：`PLAN/2026.09.14/2026.09.14-05-实施记录-P05-公开CSharp编译前端.md`（工作区 blob `38f0ed702b0fb7eb94ba90d58ecd5ac2210d0f4a`，含 VERIFY_COMMIT 追加段）
  - 作者自评（输入上下文，非本方结论）：`PLAN/2026.09.14/2026.09.14-07-自评-P06-稳定身份导入器与CSharp编辑.md`（blob `1d0737f9414e8bde6feb2284b4470a98a593034b`）
  - 代码事实（只读，SHA-256）：`Editing/EditCompileFrontend.cs` `c09528d5…f454d4`、`Editing/EditOperationRegistry.cs` `20de0cf4…4fa84`、`Editing/EditWorkspace.cs` `2d555bbd…9317f`、`Editing/EditStructuredSignatureCodec.cs` `a127e5f0…83670`、`Editing/EditContracts.cs` `718a630a…97553`、`tests/edit/p03_vm_acc005.py` `6f21d38f…e653`、`tests/edit/cases/EDIT-ACC-005.json` `b88b21c8…e0ed`、`tests/edit/P03StoreHarness/Program.cs` `2b1e9736…c96e8`（`EditCompileFrontend.cs`/`p03_vm_acc005.py`/`EDIT-ACC-005.json`/`Program.cs` 四项与 P05 记录声明逐一吻合，证明本审核读码即 `cf32659` P05 落地态）
- 审核范围：按子方案 §1 声明——核心维护范围（`edit_import`、`EditImportMatcher`、`EditCSharpImporter`、body 载荷 `sequence_points` 可选字段扩展、内嵌 PDB 语义）；直接依赖范围（P05 `Lookup`、P02 `EditWorkspace`/`EditOperationRegistry.Apply`、P03 审查/提交/检查点链、dnlib `PdbState`/`PortablePdbReader`、`EditStructuredSignatureCodec`）；明确排除范围（P05 编译、P07/P08/P09、AsmEditor 内部与反射代理、既有种类语义修改）。

## 1. 机械检查结果

1. **对象身份与来源**：blob/SHA-256 双验一致；前置链成立——P05 于 `cf32659` 验收（实施记录轮次 3），P03 于 `2142266`、P04 于 `6081a4a` 提交（`git log` 实测存在），S02-R1 blob 一致。**通过**。
2. **稳定 ID 与双向追溯**：REQ-004 导入子句 ↔ OUT-006（§8 行 234）↔ ACC-005（§11 行 378，归属 P06）↔ IMP-001..005 双向可走通（§4 末行显式声明追溯行；IMP-005 前置 IMP-001..004）；RACC-005 导入侧子句、CON-015/022/025 均有正反向落点。**通过**（REQ-023 计数遗漏见 AUD-004；需求文档路径引用缺陷见 AUD-006）。
3. **占位符**：全文无 TBD/TODO 文本。唯一保留选择＝IMP-004 跨进程/过期 `compile_id` 错误码二选一（`EDIT_CAPABILITY_NOT_FOUND` 新增 vs 复用 `EDIT_HISTORY_CONFLICT`），已在正文披露并标注"实现时定一"。判定：失败语义（零副作用拒绝）已完整定义且 ACC 断言不依赖该选择，属**已披露的实现期选择而非未披露占位**；但候选集适当性存在问题（见 AUD-003）。IMP-003"扩展点待实现时定位"判定为**位置性延后而非语义占位**：行为要求完整（`PdbState.PdbFileKind=EmbeddedPortablePDB`、无 PdbState 则创建），`EditWorkspace.WriteCore`（165-189 行）已有同型机制，Spike 源码 238-241 行实证 `target.CreatePdbState(PdbFileKind.EmbeddedPortablePDB)` 公开 API。**通过（附 AUD-003）**。
4. **fallback 声明**：歧义＝拒绝、未解析引用＝拒绝、跨进程/过期产物＝拒绝，全部零副作用且无静默降级（§3 IMP-002"任何拒绝点在写私有副本之前"；§4 失败边界含"任一引用未映射"）。与总纲 §7 fallback 清单一致，未新增默认 fallback。**通过**。
5. **后序依赖**：全文（§1/§3/§4/§5/§6）无对 P07-P09 的任何依赖；ACC-005 仅用已验收的 P05 编译面与 P01 调试链；§6 明示"P07/P08 未写不影响本方案独立验收"。**通过**。
6. **ACC 可执行性**：命令（`p03_vm_acc005full.py` 退出 0、harness `--import-matrix`、取证 `--case EDIT-ACC-005`）、输入、精确预期、证据、通过/失败边界五要素齐全；断点链沿用 S02 实证路径（`breakpoint_hit` 于导入方法 IL 0，S02 结果.md 28-30 行）；x64/x86 独立产物同绿为硬边界；测试资产落点真实在位（`EDIT-ACC-005.json`、harness `Program.cs`、`p03_vm_edit_acc_evidence.py` 均实测存在）。**通过（覆盖缺口见 AUD-001）**。
7. **现状盘点核对**：§2 五条 S02 事实与 Spike 结果.md"实验中发现并固定的接口边界"（51-55 行）逐条对应；"22+4 种类"与 `EditOperationRegistry.Apply` switch（39-74 行）及 `EditWire.OperationKinds`（EditContracts.cs 36-42 行）实测 26 个结构化种类吻合；`Lookup`/`consumable_by_import` 在产（`EditCompileFrontend.cs:304/131/155`）。**通过**。

## 2. 覆盖清单（全面审核维度）

| # | 维度 | 结果 |
|---|---|---|
| 1 | 对象身份、来源、前置链 | 覆盖，通过 |
| 2 | 稳定 ID 与双向追溯 | 覆盖，通过（AUD-004 计数口径、AUD-006 路径引用） |
| 3 | 占位符与方向性决定闭合 | 覆盖，通过（AUD-003 候选集） |
| 4 | fallback 与零副作用语义 | 覆盖，通过 |
| 5 | 后序依赖（P07-P09）排除 | 覆盖，通过 |
| 6 | 总纲边界：编译产物仅 P06 消费（§4.2 行 144）、单一入口 `Lookup`、只写私有副本 | 覆盖，通过 |
| 7 | 总纲边界：新载荷语义归属与 P03 冻结规则（`sequence_points` 扩展判定） | 覆盖，判定合规（见 §3 判定 J1，AUD-002 附条件） |
| 8 | S02 五边界落实（边界 1-4） | 覆盖，通过（IMP-002/003 逐条对应） |
| 9 | S02 生产义务：结构化签名/访问器复合身份/歧义零副作用 | 覆盖，通过（IMP-001 + ACC 歧义样本与私有指纹） |
| 10 | S02 生产义务：生成子树/CDI/作用域/局部名/异步 | 部分覆盖——IMP 声明到位、ACC 验收缺位（**AUD-001**） |
| 11 | CON-015（仅内嵌符号、无 sidecar；NON-015 同向） | 覆盖，通过（保存/重载清点＋失败边界"独立 PDB 出现＝失败"） |
| 12 | CON-022（导入路径不得启用扩展点） | 覆盖，通过（IMP-004"纯内存变换/无扩展面"＋ACC 禁止字段探测与"禁止扩展点运行＝失败"） |
| 13 | CON-025（不引 AsmEditor 内部/反射代理） | 覆盖，通过（依赖面＝dnlib 公开 API＋同程序集 internal 类型；Spike 先例；构建级引用清点非主纲 ACC 要求，未列为通过条件） |
| 14 | 可实施性（公开/同程序集 API 面） | 覆盖，通过（见 §3 判定 J2-J6） |
| 15 | ACC-005 终判完整性 vs 总纲行（§11 行 378 五列） | 覆盖，逐条对应且强化（歧义零副作用＋双架构同绿）；验收强度缺口归 AUD-001 |
| 16 | 双架构独立产物 | 覆盖，通过 |
| 17 | 工作量与复用规则（总纲 §12"P06 只导入，复用 P02 事务和 P03 持久化"） | 覆盖，通过（新增面集中于两个新文件＋两处扩展＋测试资产；统一 apply/review/commit/checkpoint 链实测在 `EditTransactionCoordinator.cs:162/404/414`） |
| 18 | 需求保真门禁独立重做 | 覆盖，见 §5（不可验收数=1） |

## 3. 语义审查关键判定

- **J1（P03 冻结规则 vs `sequence_points` 可选字段）**：总纲 §4.2 行 145"后续 Pxx 新增操作种类时拥有各自载荷语义，不得重新解释已有种类"针对的是**重新解释**（改变既有种类/字段的含义），且 §4.1-4 行 128 明示"P02、P04、P06、P07、P08 分别拥有其操作语义"、操作信封携带"操作自身版本"即为载荷演进预留机制。直接先例：P04 已为 P02 创建的 `generic_parameter_add`/`generic_parameter_update` 增加可选 `constraints` 字段（`EditOperationRegistry.cs:304-305`，实现注释"P04 IMP-002"），经审核与实施接受。本方案对 `method_body_replace` body 载荷新增**缺省 absent 的可选字段**、明文"不改既有字段语义"；现状证据：`BuildBody`（363-373 行）不读序列点、构造的 `CilBody` 无序列点，故 absent 时旧行为恒等；`RejectUnknownRawFields` 为黑名单机制（768 行，仅拒 `raw_metadata/pe_bytes/heap/rva/hex_patch`）不阻断新字段；检查点重放经同一 `Apply`/`ApplyPersisted`→`BuildBody` 路径（`EditWorkspace.cs:217-220`）确定性重建。**判定：不构成"重新解释已有种类"，合规**；附加条件（并入 AUD-002）：缺省 absent 必须保持旧载荷恒等语义，且载荷进入持久化面的契约更新落点须完整声明（`edit_apply` 工具 schema 的 body 为 `additionalProperties:false` 闭集、`operation-lowering.json` 记载 `required:["target","body"]`——见 AUD-002）。
- **J2（Lookup 可达性）**：`EditCompileFrontend` 为 `internal sealed`（25 行），`Lookup` 与 `CompileArtifact` 均 internal（304/310 行）；新导入器同置本程序集 `Editing/`，同程序集 internal 可消费，不越权公开、不引第二产物通道（S02 边界 5 保持）。任务包提示的可达性问题不成立为阻断。
- **J3（结构化签名复用）**：`EditStructuredSignatureCodec.Capture/Restore`（41-42/87/111 行，TypeSig/CallingConventionSig＋binder 委托双向）正是 IMP-001"以目标模块为 binder 上下文重绑定 corlib/目标引用"所需的表示；harness 已有 `StructuredSignatureCodecProbe.cs` 在位。复用声明成立。
- **J4（内嵌写出与产物加载机制）**：`EditWorkspace.WriteCore`（165-189 行）已有"`PdbState != null` 时置 `EmbeddedPortablePDB`＋`PdbFileName="embedded.pdb"`＋写后还原"路径；IMP-003"目标原无 PdbState 则创建"是其自然扩展（Spike 源码 238-241 行 `CreatePdbState` 实证）。产物装配用 dnlib 公开 API 实证可行：Spike 以 `ModuleCreationOptions { TryToLoadPdbFromDisk = false, PdbFileOrData = <PDB 字节> }` 加载编译产物（FeasibilityExtension.cs 223-227 行）。S02 边界 3（禁手工插 `Documents`）与边界 4（writer 解析文档引用）在 IMP-003 明文继承。
- **J5（测试资产在位）**：`tests/edit/p03_vm_acc005.py`、`tests/edit/cases/EDIT-ACC-005.json`（P05 建）与 harness `Program.cs`（含 `--compile-frontend-matrix` 先例）均存在；harness 另已有序列点/CDI/PdbScope 探针族（`SequencePointSnapshotProbe.cs`、`PortablePdbCdiProbe.cs`、`PortablePdbGraphCdiProbe.cs`、`PdbScopeSnapshotCandidate.cs`、`PdbImportSnapshotCandidate.cs`，P03 期产物）——AUD-001 的闭合在既有设施上可直接落地。
- **J6（错误码族事实）**：`EditContracts.cs` 冻结表中 `EDIT_HISTORY_CONFLICT`（102 行）描述为"The expected history, head, live state, or replay identity changed"、恢复建议（127 行）为"Read edit_history and edit_status, then reassess"，不覆盖"compile_id 未登记/过期/跨进程"（正确恢复是重新 `edit_compile`）；表内已存在 `EDIT_CAPABILITY_UNAVAILABLE`（91 行）与 `EDIT_TRANSACTION_NOT_FOUND`/`EDIT_RECOVERY_NOT_FOUND`（83/106 行，NOT_FOUND 族，后者为 P01 之后新增先例，证明错误码表可扩展）。据此对自评"两个候选均为既有码族语义可承载"作部分否定（见 AUD-003）。
- **J7（ACC-005 vs 总纲行逐条）**：前置/动作/预期/证据/失败边界五列与 §11 行 378 逐条对应且额外强化（"私有指纹不变、事务可回滚"的歧义零副作用＋双架构同绿）；缺口不在对应关系而在验收强度（AUD-001）。

## 4. 审核意见（AUD）

### AUD-001（中）ACC-005 验收未覆盖 S02 生产义务中的生成子树与 CDI/作用域/局部/异步信息

- 来源层：Spike S02 结果.md"对正式方案的约束"（59-66 行："正式 P06 必须使用稳定签名和目标身份匹配，并逐项覆盖：……编译器生成类型及状态机；Custom Debug Information、作用域、局部变量名和异步信息；非法或无法唯一映射时无副作用失败"）＋总纲 §8 OUT-006 产出列（行 234："字段/方法/EH/泛型/CDI/作用域/局部/异步信息处理"）↔ 子方案 §3 IMP-005 输入族/断言集与 §4 三列。
- 证据：§4 ACC-005 输入族＝"字段/方法/EH/泛型类约束/新泛型方法/访问器"——**无 async/iterator（状态机）样本**；"符号正确"的具体化仅"IL 断言引用映射＋sequence points 重载后可读"；证据列"PdbState（内嵌格式＋点数）"仅断言序列点格式与数量；`--import-matrix` 断言集＝"匹配/操作编译/sequence point 移植/歧义与未解析引用拒绝零副作用"，无 CDI/作用域/局部名/生成子树断言。IMP-001（生成子树整体替换/新增、不做成员级部分匹配）与 IMP-003（CDI/作用域/局部名/异步移植）只有声明性落点，无终判级断言。
- 反例：导入一个 async 方法后，若导入器对状态机子树做了成员级部分匹配、或丢失 async CDI/局部名/作用域，方法体 IL、引用映射、序列点断言仍可全绿，ACC-005 照样通过——S02 义务清单两项（编译器生成类型及状态机；CDI/作用域/局部/异步）在终判层不可证伪。
- 风险/影响：RACC-005"保留内嵌调试信息"子句只能部分验收；OUT-006 产出列对应项无验收证据；调试体验劣化（异步单步/局部查看）不被发现。
- 受影响既有通过项：无（本对象首审）。
- 最小闭合条件：§4 输入族补 ≥1 个 async 或 iterator（必含编译器生成状态机）样本；预期/证据补三项断言——(a) 重载后局部变量名保留 ≥1；(b) 作用域或 CDI 存在性 ≥1 类（如 async 方法信息）；(c) 生成子树整体替换/新增、不做成员级部分匹配的行为断言（可置于 `--import-matrix`）；或在方案内书面给出降级验收的依据与证据。既有 `SequencePointSnapshotProbe`/`PortablePdbCdiProbe`/`PdbScopeSnapshotCandidate` 探针可直接复用。
- 复审证据期望：修订后 §3 IMP-005 与 §4 三列（输入/预期/证据）含上述样本与断言，或书面降级依据。

### AUD-002（低）`sequence_points` 载荷行模式、文档身份语义与契约面落点未闭合（附 J1 合规判定的闭合条件）

- 来源层：子方案 §3 IMP-002（"sequence_points 数组（文档名/起始/结束 IL 偏移）"）↔ dnlib `PdbDocument`/`Instruction.SequencePoint` 数据模型 ↔ S02 边界 3 与失败尝试 attempt 3（70-72 行）↔ 生成契约 `Editing/Contracts/p03-tool-schemas.json`（`edit_apply` → `method_body_replace` → body 为 `additionalProperties:false` 闭集，required 五字段）与 `PLAN/2026.09.03/P02-contract/generated/operation-lowering.json`（`method_body_replace` 条目）。
- 证据/反例：(1) 字段列表"文档名/起始/结束 IL 偏移"存在两种解析——(a) 文档＋起始/结束行列＋绑定 IL 偏移（正确模型；`EditFingerprint.cs:115` 的 sp 行即含 Url＋起止行列）；(b) 仅文档＋起始/结束 IL 偏移（丢失行列，序列点对断点无效，与 ACC"重载后可读/断点命中"自相矛盾）。(2) 文档标识若仅为显示名，两个同名不同目录源文档在载荷内碰撞（`PdbDocument` 身份含 URL/哈希/语言 GUID）；S02 attempt 3 证明自行构造 `PdbDocument` 会缺内部初始化状态，而载荷→`PdbDocument` 的重建/内联策略未声明。(3) 指纹层证据：`EditFingerprint.cs:115` 的 sp 行不含 IL 偏移，序列点与指令绑定的正确性不受私有指纹保护，须由 ACC/矩阵显式断言或以重载读取为准。(4) 契约面：IMP-002 声明落点仅 `EditCSharpImporter.cs`＋`EditOperationRegistry.cs`；若导入生成的操作需经 `edit_apply` schema 或持久化契约表示，body 闭集 schema 与 lowering 契约须同步更新（或书面说明导入路径不跨该 schema 的理由），该落点未声明。
- 风险/影响：符号保真（CON-015）与 ACC 序列点断言的可执行性依赖该模式；实现期返工或 schema 拒绝。
- 受影响既有通过项：无。
- 最小闭合条件：IMP-002 明确行模式为"文档 URL（身份）＋起始行列＋结束行列＋绑定 IL 偏移（或指令索引）"，声明按 URL 内联文档对象且不手工插入目标 `PdbState.Documents`；书面确认缺省 absent＝旧载荷恒等语义（J1 附加条件）；在落点清单补入 body 契约面（`Editing/Contracts/p03-tool-schemas.json` 及其生成源/`operation-lowering.json`）或书面豁免理由。
- 复审证据期望：修订后的载荷行模式一句＋缺省语义一句＋契约面落点/豁免一句。

### AUD-003（低）IMP-004 错误码二选一候选集不当：`EDIT_HISTORY_CONFLICT` 冻结语义不覆盖"产物未登记/过期/跨进程"

- 来源层：子方案 §3 IMP-004 ↔ `EditContracts.cs:102/127`（P01 冻结描述与恢复建议）↔ 总纲 §4.3（行 159："错误响应必须包含当前状态和恢复建议"）。
- 证据：见 §3 J6。`EDIT_HISTORY_CONFLICT` 冻结描述指向 history/head/live/replay 身份变化，恢复建议为读 `edit_history`/`edit_status`；对过期 `compile_id` 的正确恢复是"重新 `edit_compile`"。既有 `EDIT_CAPABILITY_UNAVAILABLE`、`EDIT_TRANSACTION_NOT_FOUND`、`EDIT_RECOVERY_NOT_FOUND` 提供更贴切族型，且 NOT_FOUND 族先例（P01 后新增）证明错误码表可扩展。任务包要求判定的"实现期选择 vs 未披露占位"已按前者判定（§1 机械检查 3），本条只针对候选集。
- 反例：跨进程客户端持旧 `compile_id` 调 `edit_import` 收到 `EDIT_HISTORY_CONFLICT`＋"Read edit_history and edit_status"，按恢复建议操作后仍失败——恢复建议失真，违反 §4.3 语义。
- 风险/影响：低（失败仍为零副作用，ACC 断言不依赖错误码值）；影响契约质量与自评判断准确性。
- 受影响既有通过项：无。
- 最小闭合条件：候选集收窄为"新增 `EDIT_CAPABILITY_NOT_FOUND` 或语义匹配的 NOT_FOUND 族码"；若保留 `EDIT_HISTORY_CONFLICT` 候选，须对照冻结描述书面说明理由。
- 复审证据期望：IMP-004 修订后的候选表述或书面理由。

### AUD-004（低）§5 应承接清单遗漏 REQ-023 的 P06 切片（计数口径）

- 来源层：总纲 §9.1 行 269（REQ-023 → OUT-005/P05、**OUT-006/P06**、OUT-009/P09｜ACC-005、ACC-023）↔ 子方案 §5（仅列 5 项）。
- 证据：P05 审核同型先例 `2026.09.14-03` AUD-003（REQ-023 计数遗漏被具名指出）。实质落地存在：IMP-004"导入器是纯内存变换"（导入路径不执行样本、无 analyzer 面）＋ACC"禁止字段探测/禁止项拒绝"；ACC 动态验证仅经 VM 门禁的 P01 调试链——不构成语义弱化。
- 风险/影响：低；追溯口径与总纲映射不一致，P09 汇总 ACC-005/ACC-023 时缺 P06 侧稳定 ID 锚点。
- 受影响既有通过项：无。
- 最小闭合条件：§5 补入 REQ-023 导入切片，或书面说明并入 CON-022 计数的口径。
- 复审证据期望：修订后 §5 清单或口径说明一句。

### AUD-005（低）IMP-004 契约字段语义未闭合：`compiled` 寻址形态与 action×目标存在性矩阵

- 来源层：子方案 §3 IMP-004 输入契约 ↔ S02 稳定身份义务（结果.md 59-66 行）。
- 证据/反例：(1) `compiled: "<产物内成员全名>"` 未定义形态：若为不含参数签名的裸名，同一次编译内的重载（`Foo(Int32)`/`Foo(String)`）不可寻址；须明确为含签名的唯一名（dnlib FullName 形态，含泛型 arity 与参数类型）或结构化签名。(2) `action: "replace_body"|"add"` 与目标存在性的四格矩阵只定义了两格（IMP-001：目标缺失＝新增路径；目标多候选＝歧义拒绝）——"add＋目标已存在"、"replace_body＋显式 target 形状不符"未定义；未定义格允许实现自行选择静默改道，与方案自身"歧义即拒绝"精神不一致。
- 风险/影响：低；影响 schema 冻结质量与 `EditJsonSchemaValidator` 校验规则，不影响既有 ACC 断言。
- 受影响既有通过项：无。
- 最小闭合条件：IMP-004 补两句——`compiled` 的唯一名形态；add＋目标已存在＝拒绝、replace_body＋显式 target 不匹配＝拒绝（或列为 confirmation 类风险，二择明示）。
- 复审证据期望：修订后 IMP-004 契约段。

### AUD-006（低）头部需求文档路径引用与实际文件名不符

- 来源层：子方案头部（第 5 行）"`PLAN/2026.08.31/2026.08.31-01-需求提炼-dnSpy-MCP结构化编辑需求.md`" ↔ 实际文件 `…-需求提炼-dnSpy-MCP结构化程序集编辑.md`（blob `22574a90…`，与总纲 §1.1 基线声明一致）。
- 证据/反例：按引用路径检索文件不存在；同目录仅一个 v0 需求文档，身份无歧义，可机械修复。
- 风险/影响：低；来源引用机械断裂，不影响内容身份判定（blob 已按实际文件复核）。
- 受影响既有通过项：无。
- 最小闭合条件：头部路径改为实际文件名。
- 复审证据期望：修订后头部一行。

## 5. 需求保真门禁（独立重做）

按总纲规范映射（§9.1 行 250/269、§9.2 行 284、§9.3 行 307、§11 行 378）独立枚举 P06 应承接集，不采信方案 §5 自查口径：

- 应承接数 **6**：REQ-004 导入子句（"把编译结果导入目标模块…嵌入式调试信息处理"）；REQ-023 导入路径切片（方案漏计，AUD-004）；RACC-005 导入侧子句（导入结果/保留内嵌调试信息/无新增独立 PDB/禁止项经导入路径同样不可启用）；CON-015（主责，§9.2 行 284）；CON-022 导入路径；CON-025 自有导入部分。（NON-015 为排除声明，与 CON-015 同向，不计入承接单元。）
- 完整数 **5**：REQ-004（IMP-001..004＋ACC-005）；REQ-023（IMP-004 纯内存变换＋ACC 禁止项，实质落地）；CON-015（IMP-003＋保存/重载清点＋失败边界"独立 PDB 出现＝失败"）；CON-022（IMP-004 无扩展面＋ACC 禁止字段探测）；CON-025（依赖面＝公开 dnlib API＋同程序集 internal；Spike 同型先例）。
- 未落点数 **0**；语义弱化数 **0**。
- 不可验收数 **1**：RACC-005"保留内嵌调试信息"子句的 CDI/作用域/局部名/异步与生成子树切片——IMP-001/003 有声明性落点，但 ACC-005 输入族、`--import-matrix` 断言集、证据列均无对应断言，按方案现状无法在终判层证伪（AUD-001）。
- 失效或复核条件缺失数 **0**（ACC 失败边界含"任一引用未映射/独立 PDB 出现/禁止扩展点运行/歧义产生副作用＝失败"＋x64/x86 同绿；歧义前后私有指纹为复核条件——指纹已含序列点行，`EditFingerprint.cs:115`）。
- 异常 ID：**AUD-001**（不可验收）、**AUD-004**（计数口径）。
- 与方案 §5 自查（5/5/0/0/0/0）的差异：应承接+1（REQ-023）、不可验收+1（AUD-001），均已具证据。后四项计数非全零，故本轮不予通过。

## 6. 结论

**存在不同意见**。

本结论仅报告本轮发现：AUD-001（中，ACC-005 验收覆盖缺口，致使独立门禁不可验收数=1）、AUD-002、AUD-003、AUD-004、AUD-005、AUD-006（低）。方案的边界合规性（编译产物单一入口消费、只写私有副本、P04 先例支持的可选字段扩展且 absent 缺省恒等）、S02 五边界落实、ACC-005 与总纲行的对应关系、公开 dnlib API 可实施性（`PdbFileOrData`/`CreatePdbState`/`WriteCore` 均实证）、后序依赖排除与工作量复用均审核通过。本记录交回方案作者或执行方引入处理；审核方不决定引入后的处理路线与后续审核模式。被审对象状态保持 `未审核, 不得实施` 直至处理流程完成。另请执行方注意本记录头部所载 NN=08 同对象未跟踪记录的现场异常，由工作流归属核查，不属于本审计意见范围。

— 第三方审核方（独立审核上下文；本轮全部证据来自任务包指定对象与本地基线 `cf32659` 只读取证；本记录创建后内容不变）
