# T004 Codex 接手修复自检

创建日期：2026.09.14。平台：Codex；角色：经用户后续授权的修复接手方。本文是实现自检，不是独立全面核验，不产生或关闭 AUD/CHK。

基线 HEAD：7d80945bd286b00947a028db7ac125923c62e835；分支 feature/p01-public-contract-vm-gate。RUN_ID=dnspy-glm-file-20260914-01，T004 ROUND=4/MAX=4；GLM 停写后由主会话接手，没有新增轮次。被测对象为该基线加本提交的 Editing 与 tests/edit 改动。

## 修复与反例

- Dispatcher 原先捕获业务 TypeLoadException/FileNotFoundException 后再次调用委托；现保持真实 WPF 调度，异常原样传播，双架构验证调用次数严格为 1。
- 合法 owner token/object_id 单选被 schema 拒绝；同步结构化签名、接口引用、泛型约束及 CDI 输入契约，验证真实 importer 每条输出。TypeNode 深层语义仍由 codec 验证，不宣称 JSON schema 穷尽递归形状。
- PDB 输入 snake_case 与既有 camelCase 不一致；增加仅输入别名，保持历史序列化字段不变；允许精确 hidden sequence-point 标记。property/event 生成记录消耗内部 __object_id，不泄漏到公共操作。
- 原模块无 PDB 时新增状态机后 Undo 遗留空 PdbState，导致 image 门禁失败；通过目标重放证明原始容器存在性，恢复无容器状态，同时保留原有空容器与补偿路径。dnlib 4.5.0 无公开清空 API，定点校验受保护 pdbState 字段后脱离容器；升级 dnlib 必须重新验证该布局和全部导航回归。
- 真实 dnSpy resolver 命中外部属性构造器后复用外部 MethodSig，System.Type 被写成无效本地 token；改用已验证、绑定当前模块的参数签名。新增启用 corlib resolver 的 CLR48 包重放反例，真实 MCP 导出运行通过。
- 保留语义、结构、image 指纹门禁；增加可定位错误详情，没有通过弱化指纹绕过失败。

## 主会话验证

Windows 指定 dnSpy VM、x64/x86：最终 12/12 既有分组通过（资源载荷、CDI 内容、归属版本、逆操作保真、导入矩阵、结构化签名），完整 T004 probe 2/2 通过；每架构遍历 63 个失败注入边界，验证补偿和引用映射清理，覆盖有/无原始 PDB 历史链。

真实 net48 MCP 双架构通过 compile/import/review/commit、Undo/Redo、分支恢复和 export，核对文件 SHA256；导出程序集在对应 CLR48 实际运行 async=1021、iterator=21,22。启用 corlib resolver 的 CLR48 package probe 通过头部 image 核对及返回基线。

net48/net10.0-windows 构建 0 errors（6 项既有 warnings）；公共契约校验通过；任务代码 diff --check 通过。保护文件 PLAN/dnSpy-MCP.md 的原有空行未改动。临时诊断代码已移除。

最终原始摘要：`.tmp/dnspy-glm-file-20260914-01/T004-Codex-final-verified-evidence.txt`，SHA256 `1c778895a8e70748a2136a6234ac578e702f4765598986e5adea9619f4986cb3`。
最终产物包：`T004-Codex-resolver-fix.zip`，SHA256 `de4e1ec4f481a88f497efaf03384b93f0cdcde6a6eee5093045501127e1c7b06`。VM 原始日志位于 `C:\Tools\dnspy-t004-codex-fix01\verified-*.log`、`resolver-package.log`，真实协调器记录位于 `C:\Tools\dnspy-t004-coordinator-{x64,x86}\evidence\state-resolver.json`。

## 边界与结论

本轮 T004 接手实现自检通过。未修改需求、总纲或 Pxx 规范；PLAN-CHANGE-003/005/006 必要审核及跨方案裁决仍归 T005，全部 103 条需求保真和新 HEAD 独立全面核验仍归 T006。本记录不替代这些验收，不关闭原 CHK-021—026，也不改写历史核查结论。受保护现场保持。

清理：已对本次 VM 两个实例按 PID 和路径核实后停止；临时 HTTP 服务通过宿主权限停止，18771 端口无监听。此前阶段记录称服务停止未被实际端口状态证明，本次以关闭端口的实测结果更正，旧记录不覆盖。
