# 整改收尾 — 独立核验 CHK-001—010 全回归与最终清单重建

- 日期：2026-09-13
- 会话角色：实施方（主会话）
- 前置：`2026.09.14-29-独立核验整改-CHK-001至010.md`（整改明细与定向证据）
- 本记录：CHK-006 要求的规定回归结果、诚实清单重建、以及收尾状态。不自行宣布核验通过；新 HEAD 由下一次独立核验复核。

## 1. 规定回归结果（整改构建，DLL SHA-256 前缀随部署核对）

| 组合 | 结果 | 说明 |
| --- | --- | --- |
| 双 TFM 构建（net48 + net10.0-windows，Release） | 0 error | 独立临时检出 `/tmp/dnspy-chk-build.8PGWfR` 构建；外部检出未被覆写 |
| 定向整改 7 案例（chk-remediation-20260912-223127） | 7/7 PASS | CHK-001/002/003/004/005(+BCL 读回)/007/008 正例闭合 |
| 13 案例 × 双架构（chk006-20260913-*） | 24 pass + 1 skipped-ui(x86) + ACC-018 以 chk001-ui 为证据 | x64/x86 全 pass；ACC-018 契约按整改后 REQ-016 重建 |
| P01 VM 套件复跑 | PASS | EDIT-ACC-017/027/030 + fixtures/debug 回归族双架构 |
| P02 VM 套件复跑（含 240 故障矩阵） | PASS | 含 x64+x86 全标签；见 §2 两项修复 |
| P02 监听器套件复跑 | **FAIL（环境）** | http.sys 400 Invalid Hostname（请求的 Host 不在监听绑定内）——设置重绑定 UI 自动化把监听器留在 localhost；未复现为产品缺陷，历史 PASS 保留；不以此 FAIL 为由修改旧预期 |
| Python 客户端单测（verify-venv） | 33 OK / 7 skipped | |
| resources 面 | 14/14 读回 | `resources/list`=14（bepinex://docs ×6 + dnspy://docs ×8） |
| 注册表 | 78 通告 + 8 不通告 | tools/list 实测 + 双源导出对照 |
| Release 身份 | v6.6.0 == v6.6.0 | 官方 latest（2026-06-20）与 VM 安装一致 |
| P02/P03 契约门禁 | 133/133、20/20、PASS | CHK-009 生成链迁移后 |

## 2. 全回归新发现并修复（非整改引入）

1. **屏障暂停期超时失效**（P03 提交 `2142266` 起）：`ExpireLocked` 的 `!OperationBusy` 保护进行中变更的同时，废除了 P02 冻结契约"屏障暂停事务在空闲边界超时"。修复：`parkedAtBarrier`（testBarrier.Entered）时保持可超时；EndLocked 原有语义（释放屏障、忙时不释放工作区）不变。
2. **open→edit_begin 水合竞态**：dnSpy 异步水合新文档 ModuleDef；open_files 成功后立即 edit_begin 可能 0 候选。修复：`EditWorkspace.Create` 每次派发器枚举候选、调用线程 ≤9×250ms 重试，恰好 1 候选才继续（0 候选重试、多候选立即拒绝）。两项均有 P02 全标签 PASS 作为回归证据。

## 3. 最终清单（诚实重建）

见 `docs/FINAL-TEST-MANIFEST.zh-CN.md`（重建于本记录同批提交）。要点：

- 修正 ACC 归属（ACC-001/009/010/026→P02、ACC-027→P01；旧清单误归 P01/P03）。
- pass(历史) 行明确标注（ACC-011/012/013/014/020/024/025/029）。
- 监听器套件 FAIL（环境）如实记录。
- 撤回 -28 最终汇报中"OUT-001..009 全部通过、29 RACC 通过+1 partial"的过强结论：以本清单为准。

## 4. 收尾状态

- CHK-001/002/004/005/007/008/009：已整改 + 定向证据 PASS + 回归无回归。
- CHK-003：版本安全守卫落地（冻结语义指纹与持久检查点值不变）；语义投影是否并入守卫行留作者裁决。
- CHK-006：规定回归完成、清单诚实重建；监听器套件环境 FAIL 留痕。
- CHK-010：只能由下一次具备 `Win10VM-dnSpy` 连接的独立核验在新 HEAD 复核。
- PLAN-CHANGE 追认位（3 项实施方决定）：见整改记录 §1，待作者裁决。
- 受保护对象未触碰；外部付费授权不涉及；VM 测试后无残留（dnSpy 进程 0、edit 目录清理）。
