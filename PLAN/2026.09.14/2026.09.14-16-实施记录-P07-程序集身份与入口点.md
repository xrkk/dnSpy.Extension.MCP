# 实施记录 — P07 程序集身份与入口点（v1）

- 工作流：dnspy-edit-master-01a050b5
- 子方案：`2026.09.14-12-实施子方案-P07-程序集身份与入口点.md` v1（blob `940f8b58…`，审核处理 `2026.09.14-15`）
- 基线：P06_CHILD_DONE（`2df8590` / 记录 `f9bdc1c`）
- 实施轮次：round 1–6（VM 迭代，2026-09-12）；结果：ACC-006 + ACC-015 + --identity-matrix 双架构 6/6 全绿

## 1. 落地清单（IMP-001..005）

| 项 | 落点 | 要点 |
| --- | --- | --- |
| IMP-001 四新操作种类 | `EditOperationRegistry.cs`（forward 4 case+逆）、`EditOperationRegistry.Inverse.cs`（4 编译逆状态+恢复）、`EditContracts.cs`（OperationKinds 30）、`EditHistoryModule.cs`（包络形状白名单 +4） | `assembly_update`（name/version/culture，至少一字段；版本文法非负整数≤4 段缺段补 0；culture null/空=清除）、`module_update`、`assembly_ref_update`（target=AssemblyRef 行 token/object_id；同字段集）、`entry_point_set`（本模块 MethodDef token 或 null 清除）。风险：identity/entry=confirmation_required、ref_change 普通。**冲突指纹投影零扩展**（裁定 AUD-001/002）：一致性三层承载——检查点图像 SHA（字节级）+ `edit_review` 身份断言（入口点 token、AssemblyRef 行集逐行，仅身份操作在暂存集时执行）+ harness 图像级断言。逆状态的 culture 用在场读取（中性空串合法） |
| IMP-002 影响扫描 | `EditTransactionCoordinator.ImpactScan` + 路由 + `EditToolProvider` 广告 | `edit_impact_scan {request_id, transaction_id, expected_revision}`；dispatcher 枚举已加载模块（排除被编辑模块）；**旧名∪暂存新名双集**匹配各模块 AssemblyRef；报告 `{scope:"loaded_modules", modules[], inbound_references[{module, assembly_ref_token, matched_name, sites, risk_id}], risk_ids, identity_operations}`；追加 `cross_assembly_inbound` 确认类风险（幂等去重、追加即失效 review、随事务销毁）；无全局完整性宣称文案（驱动断言） |
| IMP-003 入口样本启动 | 驱动内实现 | ACC-006：ImportHost 上 `entry_point_set`→commit→export→`debug_launch(net48-exe, entry)`→入口暂停帧＝入口方法 token |
| IMP-004 测试 | harness `IdentityMatrixProbe.cs --identity-matrix`、`p03_vm_acc006.py`、`p03_vm_acc015.py`、案例 EDIT-ACC-006/015/032、`InboundRef.cs` fixture | 见 §3 |
| IMP-005 契约同步 | schema（oneOf 30 分支+`edit_impact_scan`）、`PLAN/2026.09.03/P02-contract/generated/operation-lowering.json`（30 操作行）、`validate_p03_contract.py`（30 操作/15 产品工具/23 工具/ACCS）、`p03-acceptance.json`（ACC-006/015/032 行） | 契约校验 PASS |

## 2. 裁定落实与实现裁决

- AUD-001/002 三层覆盖按裁定实现（无投影变更——既有 P02–P06 谱系零退化）；review 身份断言只在身份操作暂存时执行。
- ACC-006 无效版本的拒绝可来自 schema pattern（-32602）或编码层 EDIT_VALIDATION_FAILED——两者均为零副作用拒绝（驱动断言指纹不变）。
- `edit_impact_scan` 在 revision 失配时 EDIT_REVISION_CONFLICT、事务错 ID 时 EDIT_TRANSACTION_NOT_FOUND（结构错误不可确认绕过，驱动断言）。
- 入口点清除在无入口样本上为图像级 no-op（harness 断言 reload 读回 null + 逆精确）。

## 3. 测试与证据（§10.1，run-id 前缀 p07-identity-）

- **EDIT-ACC-032（harness `--identity-matrix`）**：x64/x86 PASS 且日志字节相同。断言：assembly 名称/完整版本/部分版本（2.3→2.3.0.0）/区域性、模块名、AssemblyRef 版本、入口点设置/清除——应用→编译逆恢复精确前像→再应用→写出→重载文件级读回；live undo 精确恢复；6 组非法载荷零图像变化；组合序列一次写出重载全字段读回。
- **EDIT-ACC-006（`p03_vm_acc006.py` 真链）**：x64/x86 PASS（16/16）：G1 四种类广告；四操作暂存（含真实 exe 上 entry_point_set=Main）；无效版本零副作用；review+确认+commit；导出；重载；`debug_launch` 入口暂停帧＝入口方法 token。
- **EDIT-ACC-015（`p03_vm_acc015.py`）**：x64/x86 PASS（15/15）：双模块加载（ImportHost+InboundRef——VM 框架 csc /r: 生成入站引用）；改名事务；扫描报告 scope/实际列表/入站引用/风险 ID；无全局宣称（global/complete/gac/filesystem 关键词断言）；错事务/失配 revision 结构错误拒绝；未确认阻断提交、确认后提交回报回显确认集；无身份操作扫描仍显式 scope。
- 证据目录：`<ArtifactRoot>\edit-tests\p07-identity-20260912-r1-{x64,x86}\EDIT-ACC-{006,015,032}\…`（summary 均 pass）。
- 配套：`p07_round1_orchestrator.py`（部署+配对+取证；InboundRef fixture 构建）。

## 4. 契约与回归

- `validate_p03_contract.py` PASS：30 操作、15 产品工具（+edit_impact_scan）、23 工具、13 验收案例行。
- 本地单测 32 通过（1 例环境缺 jsonschema、5 例需依赖跳过——与 P06 基线一致）。
- 双 TFM 构建 0 警告 0 错误；dist net48/net10 刷新。

## 5. 需求保真门禁复跑

应承接 5 / 完整 5 / 削减 0 / 超范围 0 / 未覆盖 0 / 存疑 0（REQ-005；REQ-013；CON-013；CON-017；NON-017）。ACC-006 终判条件逐条达成：每项身份编辑保存重载文件级读回＝编辑值、文件 SHA 变化、入口真实运行至新入口（入口暂停帧）；ACC-015：scope/实际列表/引用/风险 ID/确认回报/结构错误不可绕过/无全局宣称；x64/x86 独立产物同绿。

## 6. 结论

P07（OUT-007）完整落地。提交清单见本记录对应 commit。
