# 审核处理记录 — P07 程序集身份与入口点（首次全面审核）

- 工作流：dnspy-edit-master-01a050b5
- 审核对象：`PLAN/2026.09.14/2026.09.14-12-实施子方案-P07-程序集身份与入口点.md` v0（blob `ce72c50a67b8b0e72b1f03f40e27a745fa035600`）
- 审核记录：`PLAN/2026.09.14/2026.09.14-14-审核-P07-程序集身份与入口点.md`（blob `33e0cc6eba013ab64a8ea5afda73da6a77fe59e9`，结论 存在不同意见，门禁 5/5/0/0/2/1）
- 基线代码：`2df8590`（P06 实现）+ `f9bdc1c`（CHILD_DONE 记录）
- 处理时间：2026-09-14

## 1. 发现处置（AUD-001..008，全部同意）

| AUD | 级别 | 发现摘要 | 处置 | 落点（v1） |
| --- | --- | --- | --- | --- |
| AUD-001 | 高（阻断） | 入口点纳入指纹投影将使既有 P02–P06 检查点谱系全量退化为 unverified_drift（投影变更无版本化/兼容策略与验收守门） | 同意。**撤回投影扩展**：入口点状态不进入冲突指纹投影。覆盖改由三层既有机制承载：(i) 检查点头图像 SHA（字节级，WriteCore 写出入口点）；(ii) `edit_review` 在事务含身份类操作时增加私有/live **身份断言**（入口点 token、AssemblyRef 行集逐行比对——直接对象比较，非投影）；(iii) harness 断言重放图与私有图导出图像一致（含入口点/AssemblyRef）。既有谱系投影零变更（审计确认的退化问题不存在化） | §3 IMP-001 目标行为改写 |
| AUD-002 | 高（阻断） | AssemblyRef 不在 EditFingerprint 投影内，v0 的"身份变更改变指纹 ✓"与"幂等重放/行级一致"对该操作失实 | 同意。§2 现状盘点更正（明确 AssemblyRef/入口点不在投影，覆盖由图像 SHA + review 身份断言承载，同 AUD-001 三层机制）；IMP-001 重放一致性断言以**图像级**（重放图 vs 私有图写出字节）为准 | §2、§3 IMP-001 |
| AUD-003 | 中 | edit_impact_scan 名称匹配语义未定（旧名/新名），改名事务按当前名称匹配会漏掉全部旧名入站引用 | 同意。明确：扫描按**旧名（基线）∪ 新名（暂存后）双集**匹配，报告行标注命中名称；任一命中即入 inbound_references | §3 IMP-002 |
| AUD-004 | 中 | edit_impact_scan 变更事务状态（追加确认风险）但输入缺 expected_revision，违背公共契约先例 | 同意。输入改为 `{request_id, transaction_id, expected_revision}`；追加风险使既有 review 失效（review_id/review_revision 清空，复用 apply 的暂存语义）；重复扫描按幂等（既有风险去重） | §3 IMP-002 |
| AUD-005 | 低 | ACC-006 证据列 list_assemblies 误导（内存态、无 Ref/入口点覆盖） | 同意。ACC-006 证据改为以**导出重载后的元数据读回**（文件级）为权威；内存只读面仅辅助 | §4 ACC-006 |
| AUD-006 | 低 | §5 应承接漏计 CON-017 | 同意。应承接 4→5（REQ-005、REQ-013、CON-013、CON-017、NON-017） | §5 |
| AUD-007 | 低 | 契约同步落点不全（lowering 实际路径、validator 三处硬编码、ACCS 清单） | 同意。IMP-005 列全同步点：`EditWire.OperationKinds`、schema oneOf、`PLAN/2026.09.03/P02-contract/generated/operation-lowering.json`（csproj 嵌入资源源文件）+ 既有降级/故障面按 P04 先例、`validate_p03_contract.py`（OPERATIONS 30、PRODUCT 15、schemas:tool-count 23、ACCS 增 ACC-006/ACC-015 行）、p03-acceptance.json 案例行 | §3 IMP-005 |
| AUD-008 | 低 | cross_assembly_inbound 风险与暂存操作的绑定/生命周期未声明 | 同意。风险行携带命中的身份操作索引与名称对；事务上任何后续 edit_apply/edit_import 使既有 review 失效并要求重扫（风险幂等去重保留，报告提示 stale）；回滚随事务销毁（workspace 风险集既有生命周期） | §3 IMP-002 |

## 2. 修复与复检

- 计划修订为 v1（blob `940f8b58cbdd6f883f5d5800a9025e01048e1515`），内容仅限上表八项处置；范围/结论不变（OUT-007 边界、只新增 4 种类、ACC-006/015 契约不变）。
- 保真门禁复检（v1）：应承接 5 / 完整 5 / 削减 0 / 超范围 0 / 未落点 0 / 不可验收 0 / 复核条件缺失 0。
- 再审核触发条件评估：八项处置均为计划内修订（撤回一处不可兼容的投影扩展、明确匹配/契约/生命周期语义、补计数与落点清单），无范围变更、无冻结语义变更、无新承诺对象 → **不触发再审核**，作者自评结项。

## 3. 结论

- 计划 v1 状态：**已审核， 可以实施**。
- 批次结论（-14 记录）：存在不同意见 → 处理后关闭。
- 下一步：P07 IMPLEMENT（IMP-001..005）。
