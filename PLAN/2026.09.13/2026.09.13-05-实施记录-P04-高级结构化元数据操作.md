# 实施记录 P04：高级结构化元数据操作

- 方案：`PLAN/2026.09.13/2026.09.13-01-实施子方案-P04-高级结构化元数据操作.md`（v1，终态 blob `61e3759b05a6ea4a140ceee49c06a12d72f6d578`，状态 `已审核, 可以实施`）
- 总纲 v2 / 需求 v0；首次审核 `2026.09.13-03`（blob `83cb4d03`）经 `2026.09.13-04` 全量引入
- workflow：`dnspy-edit-master-01a050b5`（长程实施授权进行中）
- 审核基线：HEAD `2142266`；分支 `feature/p01-public-contract-vm-gate`
- 实施状态：**进行中**（轮次 1 起）
- 受保护现场：`PLAN/dnSpy-MCP.md`（用户既有修改）、`tests/spikes/`、`PLAN/2026.08.26/独立核验/round-8/` 隔离不动

## 1. IMP/ACC 总表

| IMP | 内容 | 状态 |
|---|---|---|
| IMP-001 | attribute_add/remove（含位域所有权裁决） | 通过 |
| IMP-002 | generic constraints 列表 | 通过 |
| IMP-003 | overrides 映射 | 通过 |
| IMP-004 | type layout + field_offset | 通过 |
| IMP-005 | marshal（field/param，生产 codec 新增） | 通过 |
| IMP-006 | pinvoke | 通过 |
| IMP-007 | security_add/remove（XML→Blob Spike 前置） | 通过 |
| IMP-008 | initial_data | 通过 |
| IMP-009 | 契约冻结（22→26、风险表、双 TFM） | 通过 |
| IMP-010 | 测试矩阵（harness/集成/§10.1） | 通过 |
| ACC-004 | 八类成功/非法矩阵双架构 | 通过 |

## 2. 轮次记录

### 轮次 1（2026-09-13）：dnlib API 事实与 XML→Blob 最小 Spike

- **前置 Spike（IMP-007 硬前置，已闭合）**：本地 dnlib 4.5.0（`~/.nuget/packages/dnlib/4.5.0`，netstandard2.0 程序集）反射 + 实证探针（`/tmp/secspike`，net8.0 控制台引用同一 dnlib）：
  1. **XML→Blob 归一路径成立**：`SecurityAttribute.CreateFromXml(module, xml)` 将权限集 XML 解析为 `PermissionSetAttribute`（带 named 实参）→ `DeclSecurityUser(SecurityAction.Deny, attrs)` 挂 `TypeDef.DeclSecurities` → 模块写→重载：`DeclSecurities=1, action=Deny, SecurityAttributes=1, GetBlob()=432 字节`，`GetNet1xXmlString()` 返回与输入语义等价的 XML（往返成立）。security_add 的实现路径＝`CreateFromXml`+`DeclSecurityUser`；security_remove 的逆捕获＝记录 action+XML（`GetNet1xXmlString`）。
  2. `SecurityAction` 全域实测（Demand2/Assert3/Deny4/PermitOnly5/LinktimeCheck6/InheritDemand7/RequestMinimum8/RequestOptional9/RequestRefuse10…ActionMask31）；方案 9 值采用子集合法。
  3. `ImplMap` 成员实测：`Module(ModuleRef)/Name/CharSet/CallConv/SupportsLastError/IsNoMangle/BestFit/ThrowOnUnmappable*`——**无 exact_import**（AUD-003 修复正确）；`PInvokeAttributes` 含 CallConv{Winapi,Cdecl,Stdcall,Thiscall,Fastcall}、CharSet{NotSpec,Ansi,Unicode,Auto}、NoMangle、SupportsLastError、BestFit*/ThrowOnUnmappable*（后两组 use-assem 语义，方案维持排除）。
  4. `MethodDef.ImplMap`/`MethodDef.Overrides:IList<MethodOverride>`（ctor `MethodOverride(MethodBody, MethodDeclaration)`）、`TypeDef.ClassLayout.PackingSize/ClassSize`、`TypeDef.DeclSecurities`、`NativeType` 枚举全量（含 FixedArray/FixedSysString/CustomMarshaler/SafeArray/RawBlob 等）逐一在册。
- 探针源与输出保留于本记录附录路径（`/tmp/secspike` 为一次性探针；关键输出上文逐字摘录，dnlib 包身份 `~/.nuget/packages/dnlib/4.5.0` SHA 于提交时随锁文件不变）。
- 结论：IMP-007 前置闭合，进入 IMP 实现顺序（IMP-001..008 → IMP-009 冻结）。

### 轮次 2（2026-09-13）：切片一落地——IMP-002 约束 / IMP-004 布局+偏移 / IMP-008 初始数据

- **实现**（`EditOperationRegistry.cs` + `.Inverse.cs` + `EditContracts.cs`）：
  - IMP-002：`generic_parameter_add/update.constraints` 整组替换；裸泛型变量目标（`!n`/`!!n`）包装为 TypeSpec 行（ECMA II 23.2.5）；自约束/闭包循环/重复拒绝；forward Undo 与 compiled_state 逆（结构化 codec 逐约束捕获/恢复）。
  - IMP-004：`type_update.layout{kind,pack,size}`（auto 清行+两位；sequential/explicit 独占一位并实体化 ClassLayout 行）；pack 合法集 {0,1,2,4,8,16,32,64,128}（ECMA II 23.11）；**位域所有权裁决**——attributes 载荷改变 0x18 位即拒（AUD-008）；`field_update.field_offset` 仅显式布局 owner 接受；`layout_change` 风险；逆捕获布局行+完整属性。
  - IMP-008：`field_add/update.initial_data{bytes_base64}`；新常量 `MaxInitialDataBytes=1MiB`（AUD-009 推导）；static+非 literal 域；**两个 dnlib 4.5.0 文档级事实落地**——①写入器仅在 `HasFieldRVA` 置位时发射 FieldRVA 行（FieldDef.InitialValue remarks），②FieldRVA 无长度语义、重载按字段类型尺寸读取 → 长度必须等于基元类型尺寸（基元尺寸表；任意长度需同尺寸值类型，声明为域外）；清除时复位位；逆捕获属性位+旧数据；`data_section_change` 风险。
- **探针验证**（harness `--p04-slice1`，VM x64）：约束整组替换+自约束拒绝、sequential pack8+非法 pack3 拒绝、非显式布局偏移拒绝+显式布局偏移成功、初始数据（int32/4B）写入、literal 拒绝——**写→重载逐项精确**（布局行/偏移/约束/FieldRVA=0x2050+len=4+字节 0xAA✓）→ `PASS p04-slice1` exit 0。迭代链 r1-r14 保留（双 DLL 部署口径、泛型语法 `!n`、layout 对象内键名、新对象 token=0 须用 object_id、Simple 为静态类无实例字段、基元全名、HasFieldRVA、类型尺寸语义——均为契约学习，最终实现自洽）。
- **零回归**：`tests/run-verify-local.sh` PASS（契约+双 TFM）；VM 双架构套件（24 PASS）+ 墓碑门禁 exit 0（`result-*-r1-roundp4.log`/`tombstone-gate-*-r1-roundp4.log`）。
- 本轮源 SHA-256：`EditOperationRegistry.cs` `406d921f003c414e0d484f512d13289321aa483454cc7dfecc7ed70c2731e15f`；`EditOperationRegistry.Inverse.cs` `27b9c880316f4d5d13ab8181a378d6634a00b5deaa7285dde4f4971421de6ddc`；`EditContracts.cs` `77e30cce36ab2bb5c3d527d2116c30406be46ea9203f214691fd51e66b841b62`；harness `Program.cs` `935c429e27145b329bfd0b1ed08dce066a07cb14a6990868384237c4d484e40f`。
- 状态：IMP-002/004/008 实现+验证通过；IMP-001/003/005/006/007/009/010 未开始。无提交。

### 轮次 3（2026-09-13）：切片二落地——IMP-001 属性 / IMP-003 Override / IMP-005 Marshal

- **实现**：
  - IMP-005：新文件 `EditMarshalCodec.cs`（生产编解码，AUD-001 口径：8 类 13 形状，轮次 28 harness 探针为设计参照）；`field_add/update`、`parameter_add/update` 的 `marshal` 载荷（null 清除）；非法向量=codec 既有拒绝（custom 缺 guid/native_name 在载荷层拒绝）；forward Undo 与 compiled_state 逆（结构化签名 codec 捕获类型引用）；signature_change 风险。
  - IMP-003：`method_add/update.overrides` 整组替换；双方须 virtual（ECMA MethodImpl 行）；declaration 以 owner_type+name+parameter_types 解析到模块内唯一方法；自映射/非虚/重复拒绝；逆捕获旧行（InverseReference 双侧）；method_add 携带初始映射；signature_change 风险。
  - IMP-001：`attribute_add`/`attribute_remove` 新 kind（OperationKinds 22→24）；目标=七类定义 token 或 `scope:"assembly"`；构造解析策略：模块内 TypeDef 绑定真实 .ctor 行（形状必须匹配），可解析引用优先解析绑定，未解析引用合成 MemberRef（声明形状权威——写入器产匹配 blob；该边界记入 ACC 非法向量：参数个数不匹配为拒绝点）；fixed/named 实参域=基元/字符串/null（CaValue 强制）；AllowMultiple 检查（读取 AttributeUsageAttribute named 实参）；**构造身份比较**＝声明类型+参数形状（合成 MemberRef 为逐次实例，引用相等不适用——r1-r8 迭代发现）；attribute_change 风险；compiled_state 逆：add→按构造+序号移除，remove→逐字重挂（构造引用+实参捕获/恢复）。
- **探针 `--p04-slice2` 双架构 PASS**：marshal（字段 simple/I4 + 参数 array 形状 + custom 缺 guid 拒绝）、override（虚到虚映射 + 重载后保留 + 自映射拒绝）、attribute（fixed 实参挂载 + 参数个数不匹配/重复非 AllowMultiple 拒绝 + 移除恢复缺席）——写→重载 marshal/override 逐项保留。
- **零回归**：`run-verify-local` PASS（契约+双 TFM 0 警告 0 错误，含 CS9342/CS8603 清理）；VM 双架构 slice1+slice2+默认套件+墓碑门禁全部 exit 0（`roundp4s2-summary`）。
- 迭代链 r1-r8 保留（TypeRef 构造解析失败→合成策略；UTF8String 值返回 string；旧二进制滞留 VM→干净重建；作用域冲突；构造身份语义）。无产品缺陷结论（均为契约学习）。
- 本轮源 SHA-256：`EditOperationRegistry.cs` `d4567db11e57228731a4eb330cc91f00c11a2d96bf7f9d231c9d0c9986f3c22c`；`EditOperationRegistry.Inverse.cs` `b1724aac8563624a659fcdf9a4019f634eb9a0ceaf356a2f644472cc2004b08e`；`EditMarshalCodec.cs` `b8edbcbe7f75e19e0ba3dcb48efd66c7f3f2a1fe5a281e82a902696533c3bbff`；harness `Program.cs` `98b72b330f1bc64ffb0a93e36aec2cd4fa2a3262df1222bd746773ae93baabd7`。
- 状态：IMP-001/002/003/004/005/008 完成；IMP-006/007/009/010 未开始。无提交。

### 轮次 4（2026-09-13）：切片三落地——IMP-006 P/Invoke / IMP-007 Security

- **实现**：
  - IMP-006：`method_add/update.pinvoke{module_name, entry_name?, charset, no_mangle?, last_error?, calling_convention?}`（dnlib 4.5.0 实测旗标集，exact-import/best-fit 排除并在 schema 拒绝）；static+extern 域（有 body 即拒绝）；`PInvokeImpl` 位与 ImplMap 行联动（清除=移除行+清位，AUD-008 位域所有权：attributes 载荷路径既有掩码校验未含该位—dnlib 域 mask 65535 全位允许，写入器以 ImplMap 行为权威）；ModuleRef 按名惰性解析/创建；forward Undo 与 compiled_state 逆（module_name/entry_name/flags 捕获，PInvokeImpl 位经 attributes 捕获）；`signature_change`+`external_code_entry`（confirmation_required 类风险）风险。
  - IMP-007：`security_add{parent: token|scope:assembly, action: 9 值子集, xml}` / `security_remove{parent, action, index?}`；轮次 1 Spike 路径（`SecurityAttribute.CreateFromXml`→`DeclSecurityUser`）；parent=assembly/TypeDef/MethodDef；未知 action/越界 index 拒绝；逆：add→按 action+序号移除，remove→`GetNet1xXmlString()` 捕获逐字重挂；`security_change` 风险。OperationKinds 24→26。
- **探针 `--p04-slice3` 双架构 PASS**：pinvoke（method_add 携带→ImplMap 绑定 kernel32/Sleep/ansi/last_error/pinvokebit + 写→重载行保留 + 非 static-extern 拒绝）、security（deny 行挂载带解析属性 + 未知 action 拒绝 + 移除恢复）。迭代 r1-r5 保留：**`DeclSecurityUser.GetBlob()` 在解析态 fixture 模块上惰性序列化抛 NRE**——探针断言改为解析属性级（TypeFullName=PermissionSetAttribute），序列化正确性由写→重载步骤证明；UTF8String 比较/诊断清理。
- **全量回归**：`run-verify-local` PASS（契约+双 TFM）；VM 双架构 slice1/2/3 + 默认套件 + 墓碑门禁 **10/10 exit 0**（`roundp4s3-summary`）。
- 本轮源 SHA-256：`EditOperationRegistry.cs` `2e683187682a40a46370c19d83e46eab7263d7c843a401d7d7ee40c8eae1a529`；`EditOperationRegistry.Inverse.cs` `405607b4cf27f3afc01ab5e239cc57005ebecf726a35858b37eea0170ddb6058`；`EditContracts.cs` `718a630aab40f49f1314db2a5dabc27947081e113b1ed50ead34127c7a997553`；harness `Program.cs` `a1d119798052a513583aed344aea16f08da9c16b4a79fe0eabcbfb9c01709baa`。
- 状态：IMP-001..008 全部完成；IMP-009（契约冻结）与 IMP-010（矩阵+取证）待做。无提交。

### 轮次 5（2026-09-13）：IMP-009 契约冻结完成

- **契约产物**：`p03-tool-schemas.json` 的 edit_apply.operation oneOf **22→26 分支**（attribute_add/remove 含 target token/object_id/scope:assembly 三形 + constructor 形状 + fixed/named 实参域；security_add/remove 含 9 值 action 枚举 + xml 上限 64KiB）；既有 kind 可选字段扩展冻结——generic add/update.constraints、type add/update.layout（pack 合法域由编码层 ECMA 集校验）、field add/update.field_offset/initial_data（base64 1MiB 预算）/marshal（8 类形状 + NativeType/VariantType 枚举）、method add/update.overrides/pinvoke（charset/calling_convention 枚举）、parameter add/update.marshal。`operation-lowering.json` 同步 22→26 kind + 各新 optional 字段与 live_forward/live_reverse 语义行。`validate_p03_contract.py` 26-operation-order 断言更新。
- **风险分类表（CON-017 落地，硬编码处已实现）**：`pinvoke`→signature_change+`external_code_entry`（confirmation_required 类）、`security_add/remove`→`security_change`、`initial_data`→`data_section_change`（confirmation_required 类）、`layout`→`layout_change`、`constraints`/`overrides`→signature_change、attribute→`attribute_change`。确认内容与受影响引用走既有 confirmed_risks 通道。
- **验证**：契约校验 PASS；`run-verify-local` PASS（契约+双 TFM 0 警告 0 错误）；VM 双架构默认套件 + 墓碑门禁 exit 0（`roundp4s9-summary`）。
- 本轮源 SHA-256：`p03-tool-schemas.json` `e480d9bcdd19372b29fad7703145bc57819ba9c8c3efc39aa24c9d1482db13c9`；`operation-lowering.json` `e2eae0c611a0ae11da614e3254b2e6d61b2f4bf02b739f95e7612ff56cc12588`；`validate_p03_contract.py` `19b8bd614bcda35a7ef4c829332379f73e39f4a77db9a1af1e0b88d7de52f210`。
- 状态：IMP-001..009 完成；IMP-010（advanced-metadata-matrix 探针并入套件 + 集成驱动 + §10.1 取证）待做。无提交。

### 轮次 6-7（2026-09-13）：IMP-010 矩阵与集成验收迭代——四个真实产品缺陷发现修复，21/22

- **IMP-010 落地**：`--advanced-metadata-matrix`（三切片合并）注册入默认套件（套件 27 PASS 行）；集成驱动 `p03_vm_acc004.py`（八类×成功/非法经真实 MCP begin→apply→review→commit + 风险断言 + undo/redo）。
- **缺陷①（信封校验）**：attribute/security 提交 `EDIT_CHECKPOINT_INVALID`——`ValidateOperationEnvelopes` 的可执行逆形状白名单缺新 kind 状态；修复：白名单加入 attribute_remove_state/attribute_add_state/security_remove_state/security_add_state。
- **缺陷②（指纹投影不对称，最深）**：attribute 提交后 begin `roundtrip_fingerprint` 失败——`EditFingerprint` 对 CA 用 `RawData`（live=null/重载=原始字节）、ImplMap/MarshalType 用对象 ToString（ImplMapUser vs ImplMapMD）→ 修复：CA 投影改解析形状（构造器 FullName+实参+命名实参）、MarshalType 走 `EditMarshalCodec.Capture`、ImplMap 投影 Module/Name/Attributes。
- **缺陷③（外模块构造器绑定）**：dnSpy 模块上下文可把 corlib 属性类型解析为**外模块 MethodDef**，CA 构造器绑定后写出无效行重载为空（begin-compare 实证 `::`）；修复：resolved 构造器属外模块时改绑本模块 MemberRefUser。**根因取证链**：隔离 spike（PreserveAll/全 r4 提交序列复刻均无法复现——因 harness 无解析器上下文；`--ca-roundtrip-spike` 保留为证据）→ dnSpy 上下文差异推理 → 修复后 r5 全过。
- **缺陷④（构造器身份引用比较）**：attribute_remove 提交 `EDIT_HISTORY_CONFLICT`——逆编译用引用相等比较逐次合成的 MemberRef 实例；修复：共享 `SameAttributeConstructor`（声明类型+参数形状）用于 add/remove 与逆编译。**缺陷⑤**：method_add 的 pinvoke 风险未在出口报告（r7 修复）。
- **r7 结果：21/22 PASS**——唯一剩余：C2 attribute_remove 提交在 dnSpy（解析器启用）上下文 `EDIT_CHECKPOINT_INVALID`；harness（无解析器）同序列通过。已定位复现路径：给 harness 模块装 AssemblyResolver 复刻 dnSpy 上下文。
- 零回归保持：每轮 run-verify-local PASS；r5/r7 中间套件全绿。无提交。

### 轮次 8（2026-09-13）：attribute_remove 图像漂移终定位

- 诊断链完备化：信封校验器全部 34 个裸 `EDIT_CHECKPOINT_INVALID` 抛点编号标注（ck_invalid_N）；ck_invalid_2（StageValidated 的 post-head 重放门）加分类/双指纹/双图像详情。
- **终定位（r10）**：`classification=validated_drift`，`replayed_semantic == recorded_semantic`（a5ce22b6…完全一致），`replayed_image(62014d39…) != recorded_image(01441854…)`——语义等价、图像不等。机理：attribute_remove 包的重放 = baseline + 前缀 attribute_add（合成/重绑 ctor 引用行）+ remove（CA 行移除但前缀创建的 TypeRef/MemberRef **引用行保留**）；记录图像 = 私有模块写（其 ctor 引用行来自 live 往返的原始行）。两类行来源/序列不同 → 图像字节不同 → exact 门拒。与 P03 r24/P03-CHANGE-002 的"引用行一旦追加不再脱落"同类；处理路线属于该已注册机制的自然延伸（对 remove 类操作的图像归一/门语义），按方案偏离控制评估：门对 attribute_remove 应接受 validated_drift（§8 分类本义）或引用行归一——下轮以 PLAN-CHANGE 评估后实施。
- harness（无解析器+装解析器）同序列均通过（remove commit OK）——差异仅在 dnSpy 真实上下文的重绑路径行来源。
- r10 集成保持 21/22；零回归（run-verify-local PASS）。

### 轮次 9（2026-09-13）：image-drift 闭环（Option B）与 ACC-004 双架构 22/22 全绿

- **偏离评估**：三条路线实证——①门接受语义等价 validated_drift（r11 实施后被否证：提交通过但后续 begin 全部 EDIT_LINEAGE_DIVERGED，因 head 记录图像与重放图像跨来源类比较）；②ctor 引用行归一（CHANGE-002 SPIKE-B 已证引用行写时不可对称化）；③**记录身份改取重放图（选定）**：`PrepareCommit` 在 `SerializeOperations` 的确定性重放图上记录 head 图像/语义（与私有模块语义交叉校验，不等即 EDIT_VALIDATION_FAILED），暂存门与后续 begin/重放比较回到同一来源类。
- **r13（Option B + 所有权修复）**：集成矩阵 **22/22 PASS**——八类全部成功提交+非法拒绝+风险断言（C1 约束/C2 属性 add+remove/C3 MethodImpl/C4 布局三向/C5 marshal(harness)/C6 pinvoke/C7 security add+remove/C8 初始数据三向）+ F1-F3 终态/单谱系/undo-redo。r12 暴露 `using var` 释放过早（全部提交 NRE）→ 模块所有权移交调用方 + try/finally 释放。
- **清理与回归**：34 个 ck_invalid_N 诊断标注清理（仅保留 ck_invalid_2 的分类/双指纹详情作为该门的常驻错误细节）；`run-verify-local` PASS（契约+双 TFM）；干净构建复跑 22/22（r14）；VM 双架构套件+墓碑门禁 exit 0（`roundp4f-summary`）。
- **§10.1 取证**：`EDIT-ACC-004.json` 案例文件 + runner 映射；双架构取证 `p03-evidence-20260913-r1-x64/-x86\EDIT-ACC-004`（summary `status=pass` 23 PASS 行，driver.log 双架构逐字节一致 SHA `7dee9586...7a7f5`）；同 run-id 下 P03 五案例编排复跑 10/10 pass（含 ACC-004 后的完整回归证据）。部署 net48 `6a5768637b94489e6246d7e6f839139589a4a3e8f2c2701d8360f1f9b81f32ca`。
- 本轮源 SHA-256：`EditHistoryModule.cs` `31ff8437e5568e9e60f1d5813ce54e76e52480b89ff389589eeaf79248ff0509`；`p03_vm_acc004.py` `75a6b76d63a4396e17d605dda3ad663dd05f979220b662b47ed6d4ef171a127e`；`EDIT-ACC-004.json` `e76257547228f0fefefdecbe9a8eb6bc5df8d4a170bbcbf0c930bcfe27aedd62`；`p03_vm_edit_acc_evidence.py` `365336c0f6d24bc12478b505556dcd53fa043871a72dc92c49a2aad8acd343b8`。
- 状态：**ACC-004 验收达成**（子方案 §4 全部断言经真实 MCP 形状双架构通过 + harness 重载探针 + §10.1 证据）；全部 IMP/ACC 待 VERIFY_COMMIT。无提交。

### 轮次 10（2026-09-14）：VERIFY_COMMIT：P04 完整落地

- 最终门：`run-verify-local` PASS（契约+双 TFM）；python 33 OK（verify venv，7 live-URL 跳过）；VM 双架构套件+墓碑门禁 exit 0（`roundp4f-summary`）；集成 22/22 双架构逐字一致；§10.1 取证 EDIT-ACC-004 双架构 summary pass。
- 归因审查：17 项变更中 21 文件可机械归属 P04 已暂存提交（含 P03 实施记录的轮次 61-69 追加与长程状态）；三项受保护（`PLAN/dnSpy-MCP.md` 用户修改、`tests/spikes/`、`round-8/`）隔离未提交。
- **提交 `6081a4a` "Implement P04 advanced structured metadata operations (v1)"**：21 files changed, 5185 insertions(+), 64 deletions(-)。实施记录状态：**通过**（IMP-001..010/ACC-004 全过，轮次 1-10 证据链完整）。子方案 P04 CHILD_DONE；workflow 推进至 P05 LOCATE_OR_WRITE。
