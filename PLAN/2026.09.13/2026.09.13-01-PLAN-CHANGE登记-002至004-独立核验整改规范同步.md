# PLAN-CHANGE 登记 002—004：独立核验整改的规范同步

- 文档类型：偏离登记（作者级；待审核链与作者追认）
- 创建日期：2026-09-13
- 会话角色：作者/执行方（Pxx 方案作者；需求/总纲作者追认位保留）
- 来源：独立核验第二轮 CHK-011（`PLAN/2026.09.14/2026.09.13-01-独立核验-dnSpy-MCP结构化程序集编辑.md`，blob `cb8275a0`）；整改记录 `PLAN/2026.09.14/2026.09.14-29-独立核验整改-CHK-001至010.md`
- 编号承接：PLAN-CHANGE-001（P03 完整 inverse 表示，已闭环 `2026.09.11-06`）；本文登记 002/003/004。系列说明：全局 PLAN-CHANGE-xxx 编号与阶段内自命名文档（如 `P03-CHANGE-002`，属 P03 阶段内部变更记录）不同源；全局编号只用于本系列跨阶段偏离登记，不回收、不复用。
- 实现落点：初始整改提交 `5bbff88`；第二轮整改（CHK-011 审核修订、CHK-012 守卫补齐、CHK-013 二次门禁、CHK-014 UI 事实、CHK-017 partial 守卫）随本轮提交落 HEAD（见审核记录 §7 AUD-003 闭合说明）

## PLAN-CHANGE-002 — P09 IMP-001 本地取消触发条件恢复 REQ-016 原义

- **被偏离的冻结规范**：P09 v1 IMP-001（本地取消仅限 OwnerClosed 孤儿事务）及其在 `EditExplorerWindow`/coordinator 的落点。
- **上游依据**：REQ-016 v0 原文"提供本地取消**当前事务**的操作"未含属主断连前置；总纲 §6.3 同义。RACC-018 同源。
- **变更内容**：本地取消面向当前活动事务，属主在线即可，仅以"操作执行中/提交已开始"为禁用条件（忙时按钮禁用且协调器返回 busy）；孤儿事务保持无条件可取消。回滚路径复用既有 rollback 释放链，无第二实现。
- **采纳理由**：原 IMP-001 的收窄没有上游需求依据（首轮 CHK-001 判定）；收窄使 REQ-016 的"当前事务"语义不可达。
- **历史兼容**：取消仅作用于未提交事务的私有副本，不影响任何已提交检查点/谱系/导出；UIA AutomationId 不变（McpEditCancelButton 等），既有 UI 测试锚点不变；ACC-018 驱动与案例 JSON 已同步为该语义。
- **需求保真自评**：REQ-016 保真度由"弱化"恢复为"完整承接"；无其它 REQ/CON 受影响。CON-008（所有权）不受影响——取消不改变属主判定，只结束事务（与 edit_rollback 同权）。
- **机器套件范围**：不改变任何既有套件基数；ACC-018 驱动（p03_vm_acc018.py）与案例 JSON（cases/EDIT-ACC-018.json）同步为该语义，定向证据 chk001-ui 覆盖 W3/W5（属主在线取消→回滚）。
- **边界**：忙态（操作/提交执行中）与 `live_state_unknown` 期间不可取消，UI 显示原因。

## PLAN-CHANGE-003 — P02/P07/P08 冲突检测口径：并行全量外漂移守卫（冻结语义指纹不变）

- **被偏离的冻结规范**：P02 v9 §3.4/§4（完整 module/PDB 外部变化检测以单一指纹表达）；P07/P08 v1"不扩语义投影、以 review 局部断言替代"的既有裁决。
- **上游依据**：CON-004/REQ-008——"提交必须通过实时指纹检测这种外部修改"；语义投影无法覆盖入口点/AssemblyRef/Win32/ClassLayout/DeclSecurity/CDI 内容（两轮核验 V06 反例独立证实）。
- **变更内容**：新增 `EditFingerprint.ComputeExternalGuard`（冻结五通道 + 入口点 + AssemblyRef + 程序集标志/哈希算法 + ClassLayout + DeclSecurity（含 SecurityAttributes 内容反射渲染）+ Win32 资源树 + 全实体 CDI **内容完整编码**（T002-R02 回流：绑定 dnlib 4.5.0 全部具体类型；字段/复合值/完整序列/owner 绑定引用；见下）），**仅**用于实时漂移比较（begin→review/commit、实时应用临界区二次门禁、逆恢复校验、partial 前后态绑定、cleanup 前置）。冻结语义指纹 `Compute/ComputeRoundtrip` 与全部持久检查点值（ResultImageSha256/ResultSemanticFingerprint）**不变**；旧历史三态/迁移边界不受影响。
- **采纳理由**：在不动冻结持久格式与历史兼容的前提下闭合 CON-004 的可观测性缺口；若把守卫行并入语义指纹将使全部旧历史三态迁移，代价不成比例。
- **机器套件范围**：不改变任何既有套件基数；新增漂移证据钩子（`live-conflict:mutate-entrypoint/-layout/-cdi`）走 edit_test 契约扩展（schema 同步，P03 契约门禁 PASS）。
- **需求保真自评**：CON-004/REQ-008 由"弱化"恢复为"承接"（以并行守卫实现外部修改检测语义）；RACC-001/010 的冲突族由守卫覆盖正负例。
- **边界**：守卫不进入检查点包、不参与重放分类（重放仍以正向重放为 oracle）。
- **CDI 内容完整性（T002-R02 回流，替换原"截断/深度/概率极低"边界）**：对绑定 dnlib 4.5.0 的**确切类型集合**（公共具体类型 + 精确 internal MD/stepping 类型）做显式编码，不使用任意对象反射或 `ToString`；已知类型的具体子类（携带额外状态）不属于受支持类型，按未知拒绝，`PdbUnknownCustomDebugInfo` 仍合法。public 内容字段与复合值（hoisted/async/state struct、dynamic local、tuple、document、metadata reference、compilation options 等）全部编码；序列编码完整长度/顺序/重复次数，无 64 项截断、无深度常量；raw bytes 以 SHA-256+长度编码。引用身份采用确定性的模块内位置：type=层级路径、method/field=所属类型槽位、Instruction=所属方法+`Body.Instructions` 索引（实际引用查找，区分 RID0 新增对象）、Local=所属方法+`Variables` 索引；方法外引用按**完整结构化签名身份**编码：TypeRef 的完整 scope 链（AssemblyRef 结构化身份/ModuleRef/嵌套 TypeRef 递归）、namespace/name，MemberRef 的真实父（TypeDef/TypeRef/TypeSpec/ModuleRef/MethodDef），TypeSig 全元素（modreq/modopt、数组/尺寸、泛型实例、fnptr、sentinel、pinned、byref/ptr）与 MethodSig 字段以结构化字段编码，不使用未转义分隔符拼接；未知签名形状显式失败。不递归整个元数据图。CDI 所属实体（module/type/method/field/property/event；property/event 含所属类型与槽位）与列表行序/重复次数保留；嵌套 `PdbDocument.CustomDebugInfos` 用同规则；有限环与共享引用按确定遍历 ID+回引用编码，不无限递归、不静默跳过。真正未知的 CLR CDI 子类、不可读成员或悬空引用在修改前以 `EDIT_CAPABILITY_UNAVAILABLE` 失败，不生成"看似成功"的哈希；合法已知 CDI 不被拒绝。dnlib 升级、新增 CDI 形状或身份规则变化时必须重新裁决本边界。
- **003 独立状态（T002-R02 作者级回流）**：正文已按上述完整性要求更新（实现、枚举测试、双 TFM/harness 构建与本地探针证据见 `PLAN/2026.09.14/2026.09.14-32-作者回流-PLAN-CHANGE-003-CDI完整守卫与需求保真自评.md`）。必要的审核链尚未执行，本项标记为待审核；未获独立 AUD/核验背书，不宣告 CHK-021 或本登记最终闭环。

## PLAN-CHANGE-004 — P02 §3 生成链唯一权威迁移至 37 操作（机器套件冻结为前 22）

- **被偏离的冻结规范**：P02 v9 §3（单一源生成 22 操作契约表，产物不得手改）；P04/P07/P08 各自"直接扩展 generated/operation-lowering.json"的指示。
- **上游依据**：RACC-001/003/009（操作全集可再生、可复核）；后续阶段规范自身已要求扩展同一表。
- **变更内容**：`contract_source.py` 成为 37 操作唯一权威源；`SCHEMA_OPERATIONS`/`MACHINE_SUITE_OPERATION_COUNT=22` 冻结 P02 机器故障套件与工具模式 oneOf/枚举/RACC-003 案例表为前 22 种；`validate_contract.py` 迁移为 37 顺序 + `operation-set:p02-prefix` + `fault-contract:machine-suite-scope`（133/133 PASS；篡改检测 20/20 PASS）。新增 15 种的 lowering 行含诚实 `delete_policy`（其中 5 个新增 `_remove` 种类补齐；验证器对全部 12 个 `_remove` 种类强制 hard_fail_if）。
- **采纳理由**：恢复再生一致性（旧链手改产物与生成源冲突，CHK-009）；同时不改变任何冻结基数（240 故障行、schema 基数、产品 oracle）。
- **历史兼容**：expanded-tool-schemas/capacity/mutation-corpus/requirements-map 再生字节与 P02 完成归档一致；旧 22 种的机器门禁以"前缀+范围"检查保留；新 15 种由 P03StoreHarness 族门禁覆盖。
- **需求保真自评**：RACC-003（常用结构化编辑）机器注册表保持 P02 纯集；RACC-009 故障契约基数不变；RACC-001 可再生性对全部 37 种成立。

## 审核与追认状态

- 审核链：本文已交 sub-plan-reviewer 审核（记录见后续 `审核-PLAN-CHANGE-002至004` 文档）。
- 需求/总纲作者（用户）追认位：保留。若作者否决任一项，实施方按否决回退实现并登记新 HEAD。
- 生效范围：登记即约束实施；三方独立核验在新 HEAD 复核本登记与实现一致性。
