# 功能定向复验 — 第2轮

- 子核验：gpt-5.6-terra / xhigh，同一无主会话上下文fork的子智能体；最终报告自动通知主会话，未轮询。
- 固定提交：`76753f8b557da9b7e7b08f18eca57acb1e840612`。
- 只读快照：`/tmp/dnspy-fv-76753f8-bCtIgo`；独立宿主：`/tmp/dnspy-host-fv-76753f8-SRZXfW`。
- 子会话未修改工作区代码、规范或旧记录。

## 子核验最终结论

在可执行的Linux范围内未发现新的产品功能缺陷；真实Windows与精确VM连接依赖项仍阻断，不宣布总体功能验收通过。

## 已验证修复

1. 资源导入调用ReadAllowedResource：允许根、绝对路径、本地盘/ADS、逐组件非reparse句柄、同句柄长度/字节/SHA/卷序列号/file ID；生产入口返回实际观察身份。Windows句柄分支尚未实测。
2. linked规范化内嵌操作、Win32文字/数字type和name及language导入/导出，缺失身份拒绝。
3. ExportResource经公共WriteValidatedOutput进入Store.WriteOutputAtomic；容量、源路径与同文件身份保护未绕开。临时friend-assembly探针调用固定编译产物并以故障存储适配器证明委托给WriteOutputAtomic，故障后旧输出字节保留并返回存储身份。此探针不等于真实Windows文件系统原子性测试。
4. 外漂移守卫覆盖自定义属性固定/命名/数组参数及声明式安全参数；属性还做实际程序集写出/重载。
5. Dispatcher二次门禁在实时应用恢复try之外：外部编辑、调试占用和取消均forward=0、inverse=0。
6. 完整逆恢复后清除LiveLinearized；前缀失败恢复，逆失败保持unknown；已线性化屏障不再被超时释放。

## 子核验执行结果

- FunctionalGuardHarness：24项PASS，Windows句柄项明确SKIP。
- resource contract：5/5 PASS。
- 全Python：38项完成，7 live条件跳过。
- P03契约：25 tools/17产品/37操作/20验收条目，PASS。
- 调试契约：36 fixtures、189 cases，PASS。
- 新/tmp宿主上的net10.0-windows和net48 Release构建均成功；nullable与NuGet漏洞元数据网络警告保留。

## 环境阻断

当前工具面无总纲指定Win10VM-dnSpy精确连接；未使用其他VM或手写管理HTTP。真实Windows句柄/file-ID、目录/reparse/容量拒绝、输出故障后临时残留、x86/x64，以及p03_vm_acc007/008实际dnSpy/MCP端到端仍未实测。

恢复精确连接后需要按P08驱动核对根外/目录/reparse/超限拒绝、真实稳定身份、linked、Win32文字/数字/语言、旧输出SHA及临时清理。本记录不把这些项提升为通过。

## 后续

主会话在第2轮运行期间发现并修复了5个工具的响应契约不匹配（见07记录）。这些未进入本轮固定快照；提交后另发第3轮定向复验。用户循环上限仍为5，没有消耗方案第三方审核批次。
