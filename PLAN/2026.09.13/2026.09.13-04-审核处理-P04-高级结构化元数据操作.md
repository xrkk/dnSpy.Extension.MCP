# 审核处理 P04：高级结构化元数据操作（首次审核引入）

- 原审计：`PLAN/2026.09.13/2026.09.13-03-审核-P04-高级结构化元数据操作.md`（blob `83cb4d03e9a4322f05b204815a89444720fcf374`，首次/全面/轮次 1，结论 `存在不同意见`，AUD-001..010）
- 引入批次一致性：本批次已用全面额度 1、定向额度 0；唯一有效结论＝上述记录；无同批次冲突结论；输入无漂移（对象 blob `a47490c7…` 复核一致）。
- 处理前方案身份：v0 blob `a47490c719c6702fd6b3d1dd7d8e38bc66ddfe67`
- 处理后方案身份：**v1**——全部 AUD 修改落点完成、复评通过时的内容身份 blob `83526ef40c2925a1c2af8c2533e74eae26f1813b`；随后按本记录结论将文档状态行置为 `已审核, 可以实施`，终态身份 blob `61e3759b05a6ea4a140ceee49c06a12d72f6d578`（仅状态行差异）。无未受影响切片需要显式继承——本轮意见均为方案内修正，不改变范围、Pxx 边界、依赖图或验收目标。
- 引入时基线：HEAD `2142266`（未漂移）

## 逐项处理

| AUD | 处理 | 理由与证据 | 修改位置 |
|---|---|---|---|
| AUD-001（中） | 同意 | 审核方实证 `EditStructuredSignatureCodec` 无 marshal 路径；8 形状在 harness 测试类 | IMP-005：生产 codec 明确为本方案新增代码；轮次 28 证据降级为设计参照与非法向量来源 |
| AUD-002（中） | 同意 | 风险发射确为逐 kind 硬编码（`EditOperationRegistry` risks 发射路径），"自动成立"不成立 | IMP-009 新增数据驱动风险分类表（pinvoke/security/initial_data=confirmation_required；layout/constraints/overrides=受影响清点）；ACC-004 新增 risk_ids/confirmed_risks 断言；§5 计数 3→4 |
| AUD-003（中） | 同意 | dnlib 4.5.0 无 exact_import 成员 | IMP-006：旗标集改为 charset/no_mangle/last_error/calling_convention，exact_import/best-fit 显式排除并在 schema 拒绝 |
| AUD-004（中低） | 同意 | 9 值为 ECMA 子集；Spike 为唯一未闭合事实 | IMP-007：标注"本方案采用子集"+排除项；XML→Blob 最小 Spike 升级为硬前置（IMP-009 冻结前留痕） |
| AUD-005（低） | 同意 | pack 集出处应为 ECMA-335 II 23.11；成员名修正 | IMP-004/007：出处改标；`ClassLayout.PackingSize`；DeclSecurity 父级形状修正 |
| AUD-006（中低） | 同意 | "在册证据"不可机械判定，与总纲"每类执行一正一反"两解 | ACC-004 第 3 类改为**本轮实际执行**的 impl_attributes 成功/非法样本对 |
| AUD-007（低） | 同意 | 形状缺字段 | IMP-001：`match.index=0` 越界拒绝；`allow_multiple` 缺省 false 语义定义 |
| AUD-008（中低） | 同意 | 双写歧义真实（掩码含布局位/PInvokeImpl 位） | IMP-001：位域所有权裁决——0x18 与 0x2000 由结构化字段独占，attributes 载荷携带即结构性拒绝 |
| AUD-009（低） | 同意 | 8 MiB 预算 × base64 4/3 膨胀不相容 | IMP-008：新常量 `MaxInitialDataBytes = 1 MiB` 附推导 |
| AUD-010（低） | 同意 | 总纲 §3.2 义务未重申 | IMP-009：双 TFM 0 警告 0 错误构建义务明文（`tests/run-verify-local.sh` 门） |

## 受影响既有通过项与继承

无（对象为 v0 首审，无既有通过切片）。全局不变量保持：只增不改既有 22 类字段语义；无第二提交/Undo/导出路径；不依赖 P05-P09。

## 处理后复评（作者从头复评）

- 六计数门禁重跑：应承接数 4 / 完整数 4 / 未落点数 0 / 语义弱化数 0 / 不可验收数 0 / 失效或复核条件缺失数 0；异常 ID 无（AUD-002/006 已闭环）。
- 机械复查：稳定 ID 未变；双向追溯完整（REQ-003↔OUT-004↔ACC-004↔IMP-001..010）；无占位符（XML→Blob 已声明为带留痕义务的硬前置 Spike，非占位）；无后序依赖；无未声明 fallback。
- 再次审核触发条件判定（阶段 2 清单逐项）：改版均为方案内契约细化与出处修正——不改跨项目契约/共享契约（仅本仓库 edit 操作集）、无迁移/权限/安全隐私/外部关键契约变化、不改总体架构/核心数据流/并发一致性/恢复策略（复用 P03 全部机制）、不影响核心主且回滚困难（实现未开始）、无多方向证据分歧（每项 AUD 单一闭合）、无用户/项目约定额外要求 → **零命中，作者自评闭环**，不重新派遣审核。
- 结论：v1 由首次审核祖先（记录 03）+ 本处理记录支持，状态置为 **已审核, 可以实施**。实施授权：workflow `dnspy-edit-master-01a050b5` 长程授权进行中，完整输出本引入记录后直接进入实施。
