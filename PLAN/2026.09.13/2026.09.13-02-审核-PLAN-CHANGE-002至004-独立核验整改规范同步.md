# 审核记录 — PLAN-CHANGE 登记 002—004：独立核验整改的规范同步

- 文档类型：第三方审核记录（不可变；创建后内容不变）
- 创建时间：2026-09-13 07:27:32 CST
- 审核方角色：隔离上下文第三方审核（sub-plan-reviewer；只审核，不修改方案、登记或业务实现）
- 审核性质：首次第三方审核（针对偏离登记文档本身）
- 审核模式：全面审核（无历史审核轮次；本记录为该登记的首份审核记录）
- 编号说明：文件名按审核任务指定为 `2026.09.13-02-审核-…`（PLAN-CHANGE 系列内第 2 份：01=登记、02=审核）。同目录另有 P04 系列 `-01..-05` 文档，分属不同系列且无文件名冲突（目录内 `-01-` 已存在双系列先例）。写入前已复查：目录内不存在名为 `2026.09.13-02-审核-PLAN-CHANGE-002至004-独立核验整改规范同步.md` 的文件。

## 1. 对象身份

| 项 | 值 |
|---|---|
| 审核对象 | `/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.09.13/2026.09.13-01-PLAN-CHANGE登记-002至004-独立核验整改规范同步.md` |
| 对象 sha256 | `7f501eb2ec88dad08c91ffc4560c02e414109e5a2f011f2869660f97f91bf725` |
| 对象 git blob | `205193ab85e64bb57a651bb725e53aecd184b05c` |
| 对象状态 | 偏离登记（作者级；待审核链与作者追认；未提交，git 工作区 untracked） |
| 登记内容 | PLAN-CHANGE-002（P09 IMP-001 本地取消语义）/ 003（P02/P07/P08 并行外漂移守卫）/ 004（P02 生成链 37 操作权威源） |
| 来源引用核验 | 登记引用来源核验记录 blob `cb8275a0` —— 与 `PLAN/2026.09.14/2026.09.13-01-独立核验-…md` 实测 git blob `cb8275a0606d5c149211d5bfaed28221e91f34b3` 一致 |
| 涉及 Pxx | P02、P03（间接）、P04、P07、P08、P09 |

## 2. 历史链与本地基线

- 历史链：需求 v0 → 总纲 v2 → P01…P09 实施 → 独立核验第一轮（CHK-001…010）→ 整改记录 `2026.09.14-29`（git blob `14279f16`，对象引用其记录内容经抽查一致）→ 提交 `5bbff88`（CHK-001…010 整改）→ 独立核验第二轮（CHK-011…017，blob `cb8275a0`，结论"核查不通过"）→ 本登记文档（2026-09-13）→ 本审核。
- 本地基线 HEAD：`dbe19c3648ddd9a593371e0eb9d0ce2853fbe5fd`。
- 工作区状态（审核时实测）：8 个文件已修改未提交（`.gitignore`、`Editing/Contracts/p03-tool-schemas.json`、`Editing/EditCheckpointStore.cs`、`Editing/EditExplorerWindow.cs`、`Editing/EditFingerprint.cs`、`Editing/EditHistoryModule.cs`、`Editing/EditTransactionCoordinator.cs`、`PLAN/dnSpy-MCP.md`）。**登记所述 CHK-012/013/014/017 对应的守卫扩面、二次门禁等实现全部位于该未提交工作区，不在 `5bbff88` 或 HEAD 中**（见 AUD-003）。
- 审核证据边界：本轮在 Linux 审核环境执行源码级核对与 Python 验证器实跑；未重跑 Windows VM UI/回归驱动，VM 证据（chk001-ui 等）以 `-29`/`-30` 记录为文档证据采信。

## 3. 路径指纹（审核时点，sha256）

| 路径 | sha256 |
|---|---|
| `Editing/EditTransactionCoordinator.cs` | `9cea4a0f2a2b59b19d26d4e4313e33ac18cd51c9a37b138f1ecebeaf7b1bc421` |
| `Editing/EditFingerprint.cs` | `b1f1803f4bd2dc5b8b7980c152705c2365c736cef519b651c27272ae643e78a8` |
| `Editing/EditExplorerWindow.cs` | `3ab43114b854bd1b3c62c39f8302607511ed876511e99683db3456579cd10cbf` |
| `Editing/EditWorkspace.cs` | `d25d033423329d1ad2d302cc7cf708033d8109424372425ff024b4dc754e0304` |
| `PLAN/2026.09.03/P02-contract/contract_source.py` | `ce6b7050f5f1969b1a39c3fa071d213b572058b67fcf55ab7210a7bd581bb94b` |
| `PLAN/2026.09.03/P02-contract/validate_contract.py` | `0862e2d8e93fd1d53681097446386c8008aec3d73324069ed5d1af76549d4ab0` |
| `Editing/Contracts/p03-tool-schemas.json` | `ef97b0cefa03df5a210f326e8fad883ebdca766ff10130cc164f4b24176b62d9` |
| `PLAN/2026.09.14/2026.09.14-29-独立核验整改-CHK-001至010.md` | `994c6f49e3d728ca150103171b30ca741e208a1177af3bec49f5fd6e6c2b8581` |
| `PLAN/2026.09.14/2026.09.13-01-独立核验-dnSpy-MCP结构化程序集编辑.md` | `61f70107d37bf717ea3e775a3fce1d5555128dbf645ba5564cc3f400085603ae`（git blob `cb8275a0`） |

## 4. 覆盖清单（逐项核对结果）

### 4.1 PLAN-CHANGE-002（本地取消语义）——通过（附 AUD-006 建议）

| 核对项 | 结果 | 证据 |
|---|---|---|
| 取消语义与 REQ-016 原文一致 | 通过 | 需求第 77 行："…并提供本地取消当前事务的操作"，无属主断连前置；总纲 §6.3："…以及本地取消当前未提交事务"；RACC-018："…并可取消当前事务"。登记引述逐字一致 |
| 忙态/提交态禁用边界明确 | 通过 | `EditTransactionCoordinator.cs:289-302` `CancelTransactionFromUi`：`!tx.OwnerClosed && (tx.OperationBusy \|\| tx.CommitStarted)` 返回 `busy`；孤儿（OwnerClosed）无条件可取消；快照 `CanCancel = !OperationBusy && !CommitStarted`（:195）；`EditExplorerWindow.cs:107-116` 按钮门控 + 禁用原因文案（"Commit is executing…" / "An edit operation is executing…"）。`live_state_unknown` 期间 `active=null`（coordinator :1202），返回 `no_transaction`，与登记边界一致 |
| 与 CON-008 属主语义不冲突 | 通过 | 取消不读取/不伪造属主身份（CON-008 关注属主来源与自报 ID 不可信，:94）；总纲 §6.3 明确授予 UI 本地取消权；取消走 `EndLocked(tx,"ui_cancel")` → `Workspace.Dispose()`，与 `edit_rollback`（:1090-1097，`EndLocked(tx,"client_rollback")`）同一释放链，登记"与 edit_rollback 同权、无第二实现"成立；属主下次调用收到 NOT_FOUND（代码注释与 chk001-ui W5 证据一致） |
| 实现与登记一致（含 UIA 锚点） | 通过 | `McpEditCancelButton` 等 AutomationId 不变（`EditExplorerWindow.cs:55-67`）；ACC-018 驱动同步已核实：`tests/edit/chk_ui_driver.py` W3（属主在线取消可用）/W5（属主在线取消回滚）按新语义断言 |

### 4.2 PLAN-CHANGE-003（并行外漂移守卫）——部分不通过（AUD-001 前置；AUD-002/003 建议）

| 核对项 | 结果 | 证据 |
|---|---|---|
| "冻结语义指纹与持久检查点值不变"诚实声明 | 通过 | `Compute/ComputeRoundtrip/Projection/Channels` 未改（工作区 diff 仅在 `ComputeExternalGuard` 区块内新增行与新增辅助函数）；`EditCheckpointStore.cs`/`EditHistoryModule.cs` 工作区 diff 仅新增 CHK-014 只读目录探针，持久格式零改动 |
| 守卫覆盖面描述与代码一致（入口点/AssemblyRef/程序集标志/ClassLayout/DeclSecurity/Win32） | 通过 | `EditFingerprint.cs:49-98`：entrypoint 行（:51-53）、asmref 行（:54-55）、asmflags=Attributes+HashAlgorithm（:58-60）、classlayout=PackingSize+ClassSize（:63-66）、declsec 含 SecurityAttributes 反射渲染（:69-75、:100-115）、Win32 资源树递归哈希（:76、:159-177）。逐项与登记列举一致 |
| CDI 内容覆盖面 | **不通过（模块级）** | 实体级（type/method/field/property/event）CDI 已用 `CdiContent` 全属性反射（:81-96、:121-137）；但 **module 级 CDI 行仍为类型名渲染**：`:80 rows.Add("module-cdi\|" + CdiRows(module.CustomDebugInfos))`，`CdiRows`（:179-180）仅取 `x.GetType().Name`。而登记自证钩子 `live-conflict:mutate-cdi`（coordinator :1559-1571）恰作用于 `module.CustomDebugInfos` 的 `PdbDefaultNamespaceCustomDebugInfo.Namespace`——同种类异值变更（CHK-012 反例原样形态）在"已存在该 CDI"分支下守卫不变。详见 AUD-001 |
| 接入点清单与代码一致 | 通过 | review 入口 :1080、commit 入口 :1123、实时应用临界区二次门禁 :1745-1760（dispatcher 临界区内、首个 live 写入前，语义指纹+守卫+调试态三查）、逆恢复校验 :1296/:1309/:1314/:1333（retry/undo/cleanup_temp）与 :1796-1798（逆执行后双校验）、partial 前后态绑定 :60-61/:1190-1191/:1628-1629/:1705-1706/:1856/:1871-1876/:1888-1893、cleanup 前置 :1411/:1888、测试导航入口 :1423。登记清单全部落位，无虚列 |
| 是否夸大 | 有 | 登记需求保真自评称 CON-004/REQ-008 "恢复为'完整承接'（以并行守卫实现'完整检测'语义）"——被模块级 CDI 表面（AUD-001）与未列出残余表面（AUD-002：程序集级自定义属性在语义投影与守卫中均不可见；assembly 行仅 FullName :230，asmflags 仅 Attributes+HashAlgorithm）证伪为过度声明 |
| 机器套件范围声明 | 通过 | 240 故障行基数不变（validate_contract `fault-contract:rows` 240 PASS）；新增钩子走 edit_test 契约扩展，`p03-tool-schemas.json` case 枚举已加 `mutate-layout`/`mutate-cdi`（工作区 diff），`tests/edit/validate_p03_contract.py` 实跑 PASS |

### 4.3 PLAN-CHANGE-004（生成链 37 操作权威源）——通过（附 AUD-004 建议）

| 核对项 | 结果 | 证据 |
|---|---|---|
| "机器套件冻结为前 22"与代码一致 | 通过 | `contract_source.py:339-340`：`MACHINE_SUITE_OPERATION_COUNT=22`、`SCHEMA_OPERATIONS=OPERATIONS[:22]`；工具 oneOf/DIFF/FAULT_COUNT（:461/:623/:662/:726-727）均取 SCHEMA_OPERATIONS；实测 `OPERATIONS=37`、`SCHEMA_OPERATIONS=22`、前 22 序列等于 `validate_contract.py:14` 的 `P02_OPS` |
| 133/133 可由仓库现状复核 | 通过 | 本轮实跑 `python3 PLAN/2026.09.03/P02-contract/validate_contract.py`：`checks_total=133, checks_passed=133, result=PASS`（含全部 `regeneration:*` 字节一致检查与 6 项 RACC 保真） |
| 20/20 可由仓库现状复核 | 通过 | 本轮实跑 `python3 test_validator_mutations.py`：`passed=20, total=20, result=PASS` |
| P02 前缀检查存在 | 通过 | `validate_contract.py:194` `operation-set:p02-prefix`（含于 133/133 PASS）；`:231` `fault-contract:machine-suite-scope`（240 行全部属 P02_OPS） |
| 历史兼容声明 | 通过 | 再生字节一致（regeneration 检查 PASS 证实 expanded-tool-schemas/capacity/mutation-corpus/requirements-map 等与生成源一致）；整改前 `8e725db` 的 generated lowering 已是 37 操作（CHK-009 所指手改产物），迁移后由单一源再生 |
| 变更内容事实精度 | 有偏差 | 登记称"新增 15 种的 lowering 行含诚实 delete_policy（8 个 `_remove` 种类补齐）"——实测带 delete_policy 的 `_remove` 种类共 **12** 个（前 22 中 7 个 + 新增 15 种中 **5** 个：attribute/security/managed_resource/win32_resource/strong_name_remove）；整改前 generated 仅 7 个有 delete_policy。任何口径均得不出 8。见 AUD-004 |

### 4.4 三项登记要素完备性——基本齐备（附 AUD-006 建议）

- 采纳理由：002/003/004 均具备且均锚定首轮核验判定或上游条文。
- 历史兼容：三项均具备；003 的"冻结不变"经代码 diff 证实为真；004 经再生检查证实为真。
- 机器套件范围：003（显式）、004（显式）具备；**002 无显式字段**，仅在历史兼容段以"ACC-018 驱动与案例 JSON 已同步"实质覆盖（CHK-011 最小闭合条件将"机器套件范围"列为登记要件）。
- 需求保真自评：三项均具备；002（REQ-016 完整承接、CON-008 不受影响）核实成立；003 的"完整承接"表述不成立（AUD-001/002）；004（RACC-001/003/009）核实成立。
- 作者追认位：以文档级"审核与追认状态"段保留（否决即回退并登记新 HEAD），覆盖三项，可接受。
- 上游依据引用精度备注：003 段引号句"提交必须通过实时指纹检测这种外部修改"为 CON-004 原文；REQ-008 经 RACC-010（"实时模块变化时旧审查不能提交"）实质相关，联合引用成立，非错引。

### 4.5 编号承接与 PLAN-CHANGE-001 边界——通过（附 AUD-005 建议）

- PLAN-CHANGE-001 确已闭环：`PLAN/2026.09.11/2026.09.11-06-闭环-P03-PLAN-CHANGE-001-完整inverse表示.md` 存在且为闭环记录；登记承接表述正确。
- 与 001 边界无冲突：001 主题为"完整 inverse 表示统一"（包/replay/live 导航同一表示，不留独立摘要恢复路径）；003 的 Pre/PostExternalGuard 仅为比较值、不构成第二恢复表示，恢复仍消费编译逆（`ApplyCompiledInverse`）；002 复用释放链；004 扩展同一信封格式并由 P03StoreHarness 族门禁覆盖。无重复、无悬空。
- 命名歧义：`2026.09.11-02`《方案变更 P03-CHANGE-002》文档类型自declare"方案变更登记（PLAN-CHANGE，作者自评 + 第三方审核）"，且 001 闭环记录以"P03-CHANGE-002 v1→v3"为审核链。全局 `PLAN-CHANGE-NNN` 与作用域 `P03-CHANGE-NNN` 编号字符串不冲突，但同一 PLAN 树并存两个"CHANGE-002"语义系列，追溯存在误读风险（AUD-005）。

## 5. AUD 意见清单

分级含义：前置＝追认前必须闭合；建议＝随追认或下一轮整改闭合。

### AUD-001【前置】PLAN-CHANGE-003 的"CDI 内容反射渲染"未覆盖模块级表面，"完整检测语义"声明被登记自身证据路径证伪

- 证据：`Editing/EditFingerprint.cs:80` `module-cdi` 行使用 `CdiRows`（:179-180，仅 `x.GetType().Name`）；工作区 diff 证实该行在本次"CDI CONTENT"整改中未被改动，而其上方新注释（:77-79）恰以"默认命名空间变更"为例。`EditTransactionCoordinator.cs:1559-1571` 的 `live-conflict:mutate-cdi` 钩子变更 `module.CustomDebugInfos` 中 `PdbDefaultNamespaceCustomDebugInfo.Namespace`：模块已有该 CDI 时（mutate-existing 分支）类型名集合不变 → 守卫不变 → 漂移不可检出；仅"新增 CDI 行"分支（existing==null）因行增加而触发。CHK-012 反例（"PdbDefaultNamespaceCustomDebugInfo.Namespace 从 Before 改 After 后 guard_equal=True"）在该表面原样复存。
- 反例：UI 在事务期间修改模块级默认命名空间 CDI 的值 → review/commit/二次门禁守卫均不报 `external_drift_conflict` → CON-004"不能覆盖用户操作"在该表面失守。
- 风险与影响：登记需求保真自评"CON-004/REQ-008 恢复为'完整承接'（完整检测语义）"与代码不符；作者按当前文本追认将固化一个已被反例证伪的覆盖声明。受影响通过项：003 的需求保真自评、`live-conflict:mutate-cdi` 钩子的证据效力、RACC-001/010 冲突族"由守卫覆盖正负例"的声明（模块级 CDI 子集）。
- 最小闭合条件（二选一，作者裁决）：(a) `module-cdi` 行改用 `CdiContent` 同构内容渲染，并使 `mutate-cdi` 钩子（或新增 case）覆盖 mutate-existing 分支后重放漂移正负例；(b) 保持实现不变，但把登记文本降格为如实声明（"模块级 CDI 仅行类型可见，内容为已知残余"），并将"完整承接/完整检测"措辞收敛至实际覆盖面。闭合后本 AUD 方可随新 HEAD 复核解除。

### AUD-002【建议】守卫仍有登记未列出的残余表面，"完整检测"汇总性措辞宜收敛或补面

- 证据：程序集级自定义属性（`module.Assembly.CustomAttributes`）在冻结语义投影中不可见（assembly 行仅 FullName，`EditFingerprint.cs:230`），守卫的 `asmflags` 仅含 `Attributes`+`HashAlgorithm`（:58-60）；dnSpy UI 程序集信息编辑可改此类属性而双指纹均不变。另 `CdiContent/ReflectValue` 存在有界渲染：序列截断 `Take(64)`（:152）、深度上限 4（:141）、不可反射回退仅类型名（:135）、非枚举嵌套对象走默认 `ToString`（:156）——代码注释"any value change alters the guard"在这些边界上不严格成立。
- 风险与影响：属 AUD-001 同族的覆盖面诚实性问题，但当前无已登记反例。受影响通过项：003 需求保真自评的措辞（同 AUD-001）。
- 最小闭合条件：在登记"边界"段显式列出残余表面（程序集级自定义属性、渲染界），或补齐 asmflags 相邻的程序集级行；与 AUD-001 一并裁决。

### AUD-003【建议】登记的实装锚点当前不可复核：所述"本轮后续整改提交"实为未提交工作区改动

- 证据：登记"实现落点：提交 `5bbff88`（及本轮后续整改提交）"；实测 HEAD=`dbe19c3`（其后无整改提交），CHK-012/013/014/017 对应改动（`EditFingerprint.cs` +94、`EditTransactionCoordinator.cs` +179、`EditExplorerWindow.cs`、`EditCheckpointStore.cs`、`EditHistoryModule.cs`、`p03-tool-schemas.json` 等共 8 文件）全部处于未提交状态；checkout `5bbff88`/HEAD 所见守卫仍是 CHK-012 指认的类型名 CDI/无 ClassLayout 版本。
- 风险与影响：CHK-011 最小闭合条件要求"再对新 HEAD 发起独立核验"；若在提交前追认或核验，对象与实现将错位。受影响通过项：003/004 的实现一致性核对结论均以工作区状态为准（本记录 §3 指纹即工作区指纹）。
- 最小闭合条件：将工作区整改提交为新 HEAD，并把登记"实现落点"补写为确切提交号（或追认前注明待定锚点）；后续独立核验以该 HEAD 为准。

### AUD-004【建议】PLAN-CHANGE-004"8 个 `_remove` 种类补齐"与代码事实不符

- 证据：`contract_source.py` 实测：带 `delete_policy` 的 `_remove` 种类共 12 个（P02 前 22 中 7 个：type/method/field/property/event/parameter/generic_parameter_remove；新增 15 种中 5 个：attribute/security/managed_resource/win32_resource/strong_name_remove）；整改前 `8e725db` 的 generated lowering 仅 7 个带 delete_policy。任何计数口径（总量 12、新增 5、存量 7）均无 8。
- 风险与影响：数字性事实错误；不影响规范不变量（`operation-set:delete` 对全部 12 个 `_remove` 种类强制 `hard_fail_if` 且 PASS）。受影响通过项：004"变更内容"段的事实精度。
- 最小闭合条件：追认前将"8 个"更正为实际口径（例如"新增 15 种中 5 个 `_remove` 种类补齐 delete_policy，全量 12 个 `_remove` 均受验证器强制"）。

### AUD-005【建议】PLAN-CHANGE 编号系列与既有"P03-CHANGE-002"存在追溯歧义

- 证据：`PLAN/2026.09.11/2026.09.11-02-方案变更-P03-检查点镜像哨兵编码.md` 标题为"P03-CHANGE-002"且文档类型自declare"方案变更登记（PLAN-CHANGE，作者自评 + 第三方审核）"；`2026.09.11-06` 闭环记录审核链写"P03-CHANGE-002 v1→v3"。本文登记全局系列 002/003/004，编号字符串不冲突（前缀不同），但同一 PLAN 树并存两个"CHANGE-002"语义系列。
- 风险与影响：轻微；后续检索"CHANGE-002"可得两义。受影响通过项：无。
- 最小闭合条件：在登记"编号承接"或闭环索引中加一句系列区分说明（全局 `PLAN-CHANGE-NNN` 与作用域 `P03-CHANGE-NNN` 为不同系列）。

### AUD-006【建议】PLAN-CHANGE-002 缺显式"机器套件范围"字段

- 证据：CHK-011 最小闭合条件将"采纳理由、历史兼容和机器套件范围"并为登记要件；002 段内机器套件相关内容仅以"ACC-018 驱动与案例 JSON 已同步为该语义"出现于历史兼容段（经 `tests/edit/chk_ui_driver.py` W3/W5 核实为真），无显式字段。
- 风险与影响：形式要件缺口；实质影响已覆盖。受影响通过项：无。
- 最小闭合条件：追认前在 002 增加一行显式声明（机器套件基数不变；机器可见变化仅 ACC-018 驱动/案例 JSON 的语义同步）。

## 6. 总体判定与结论

- 总体判定：**需修订后追认**。PLAN-CHANGE-002、004 的方向、依据与实现一致性核实成立，可分别在落实 AUD-006/AUD-004（建议级）后进入作者追认；PLAN-CHANGE-003 因 AUD-001（前置）须先在"补面"或"如实降格声明"之间作出裁决并闭合，方可在新 HEAD 进入追认与独立核验（同时落实 AUD-003 的提交锚点）。
- 结论：**存在不同意见**（本轮发现 1 项前置、5 项建议；本结论只表示本轮发现，登记交回方案作者/执行方引入，审核方不决定引入后的处理路线）。
- 后续：登记作者/执行方引入本清单并在新 HEAD 复核后，由新的独立核验轮对本记录全部 AUD 做闭合核验；本记录内容创建后保持不变。

---

## 7. 复核补记（第一轮定向复验；追加段）

- 追加时间：2026-09-13 07:32:39 CST
- 审核方角色：同本记录（隔离上下文第三方审核；只审核，不修改方案与实现）
- 审核性质：定向复验（针对首轮 AUD-001…006 修订项；不重开全量审核）
- 追加声明：本节为追加段；§1—§6 原文与原 AUD 清单保持逐字不变；追加后本记录整体不再改动。
- 修订后对象身份：登记文档 sha256 `fd937f84997b51fc2f8d1ef0614cdfc7d2693f257aecff6d214cc2f7cb940d57`、git blob `fe45d2f083bddb5e10b1a95303d04cb3a7d9beea`（原版 `7f501eb2…91bf725` 已被修订替换，本补记针对新版）。
- 本轮定向复审范围：AUD-001（EditFingerprint module-cdi 内容渲染 + mutate-cdi 双分支）、AUD-002（asmattr/modattr 行 + 登记边界如实化与措辞降格）、AUD-003（锚点声明核对）、AUD-004（_remove 数字更正）、AUD-005（系列区分说明）、AUD-006（002 机器套件范围字段）；继承复核对象身份/冻结不变量/登记越界改动检查；排除全量语义重审。
- 修订 diff 归因（相对首轮审核时点）：仅 `Editing/EditFingerprint.cs`（`b1f1803f…` → `c9949e87f2d37752d4344745ba4c4d693f685b679d7ea110fad33ef0b3696687`）与登记文档本体变更；`EditTransactionCoordinator.cs`（`9cea4a0f…`）、`EditExplorerWindow.cs`（`3ab43114…`）、`p03-tool-schemas.json`（`ef97b0ce…`）、`contract_source.py`（`ce6b7050…`）、`validate_contract.py`（`0862e2d8…`）均未变；`EditCheckpointStore.cs`/`EditHistoryModule.cs` diff 仍仅含 CHK-014 只读 LineageDirectory 探针（冻结持久格式不变量保持；`Compute/ComputeRoundtrip/Projection/Channels` 未被本轮补面触及）。登记文档逐段对照仅 §7 编号承接、§18 机器套件范围、§28-29 保真自评/边界、§35 变更内容五处修订，无越界改动。

### 7.1 逐项处置判定

| AUD | 处置 | 依据与证据 | 残留/附注 |
|---|---|---|---|
| AUD-001 | **闭合（代码级）** | `EditFingerprint.cs:86` `module-cdi` 行改用 `CdiContentRows`；`:187-188` `CdiContentRows = infos.Select(CdiContent).OrderBy(…)`（逐 CDI 全属性内容反射）。`live-conflict:mutate-cdi` 钩子（coordinator `:1562-1573`）双分支保持：mutate-existing 分支改 `existing.Namespace`，经 `CdiContent` 对 `Namespace` 属性的反射现必然改变守卫；create 分支仍以行新增改变守卫。AUD-001 反例（同种类异值模块级 CDI 变更不可见）在该表面消除 | VM 正负例重放按登记"生效范围"随新 HEAD 复核；钩子注释 "is invisible to type-name-only CDI rows" 现描述的是修复前缺陷，属陈旧文案；旧 `CdiRows` 辅助函数仍在但守卫不再引用（死代码）——均非阻断 |
| AUD-002 | **闭合（附残留备注）** | 代码侧：`EditFingerprint.cs:63-66` 新增 `asmattr|`（`module.Assembly?.CustomAttributes`，TypeFullName+ctor MDToken）与 `modattr|`（`module.CustomAttributes`）行。文本侧：登记 §28 "完整承接"降格为"承接"；§29 边界如实声明序列截断（≤64）/深度（≤4）/反射回退三项工程边界并以"漏检概率极低而非数学完备"表述 | 残留（随新 HEAD 复核，非阻断）：(i) `asmattr/modattr` 为存在性/类型级渲染，未复用实体级 CA 的全参数渲染 `CustomAttributeRow`（`:306-310`）——同型属性参数值变更仍不可见，§29 "已核实覆盖面含…程序集与模块级自定义属性"宜按此精度理解或补一句限定；(ii) §25 变更内容的守卫成分枚举尚未列入 asmattr/modattr，与 §29 不对称 |
| AUD-003 | **待提交锚点（未闭合，按作者声明顺延）** | 登记第 8 行仍为"提交 `5bbff88`（及本轮后续整改提交）"；实测 HEAD 仍为 `dbe19c3`，8 个文件（含本轮 EditFingerprint 修订）仍未提交 | 闭合条件不变：统一提交形成新 HEAD 后，登记"实现落点"补写确切 commit 号；作者追认与独立核验以该 HEAD 为准。此项为唯一未闭合项 |
| AUD-004 | **闭合** | 登记 §35 更正为"其中 5 个新增 `_remove` 种类补齐；验证器对全部 12 个 `_remove` 种类强制 hard_fail_if"，与本记录 §4.3 实测（新增 5、全量 12、`operation-set:delete` PASS）一致 | 无 |
| AUD-005 | **闭合** | 登记 §7 增加系列说明：全局 `PLAN-CHANGE-xxx` 与阶段内自命名文档（如 `P03-CHANGE-002`）不同源、不回收、不复用 | 无 |
| AUD-006 | **闭合** | 登记 §18 增加显式"机器套件范围"字段；所引 `tests/edit/p03_vm_acc018.py` 与 `tests/edit/cases/EDIT-ACC-018.json` 实存，案例 JSON 语义为"属主会话在线且无操作/提交执行时取消按钮启用"（与登记 002 语义一致） | 无 |

### 7.2 继承项复核

- 契约链指纹未变（contract_source `ce6b7050…`、validate_contract `0862e2d8…`、p03-tool-schemas `ef97b0ce…`），首轮 133/133、20/20、P03 契约门禁 PASS 的复核结论继承有效，无需重跑。
- `EditTransactionCoordinator.cs`（`9cea4a0f…`）与 `EditExplorerWindow.cs`（`3ab43114…`）未变：首轮 4.1（PLAN-CHANGE-002 全项）与 4.2 接入点清单的通过结论继承有效。
- 冻结不变量：持久层 diff 仅 CHK-014 只读探针；守卫补面改动全部位于 `ComputeExternalGuard` 区块与新增辅助函数，语义指纹与检查点持久值不受影响——登记 §25 冻结声明在修订版上继续成立。

### 7.3 处置汇总与总体判定

- 处置汇总：闭合 5（AUD-001、AUD-002、AUD-004、AUD-005、AUD-006；其中 AUD-002 附两条残留备注）；待提交锚点 1（AUD-003）。
- 总体判定：**可进入追认**。前提：(1) AUD-003 按作者声明在统一整改提交形成新 HEAD 后补写锚点闭合；(2) AUD-002 的残留备注（asmattr/modattr 参数级渲染、§25 枚举不对称）与 AUD-001 的 VM 正负例重放，随新 HEAD 的三方独立核验一并复核。
- 本轮结论：**审计通过**（定向复验；结论仅覆盖本轮范围，登记引入与追认路线由方案作者/执行方决定）。
