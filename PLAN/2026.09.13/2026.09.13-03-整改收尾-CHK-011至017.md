# 第二轮整改收尾 — CHK-011 至 017

- 日期：2026-09-13
- 会话角色：实施方（主会话）
- 对象核验记录：`PLAN/2026.09.14/2026.09.13-01-独立核验-dnSpy-MCP结构化程序集编辑.md`（blob `cb8275a0`，不可变）
- 逐条处置与证据如下；环境阻断如实登记。

## 1. 逐条处置

| CHK | 处置 | 状态 |
| --- | --- | --- |
| CHK-011 规范变更未闭环 | 登记 PLAN-CHANGE-002/003/004（`PLAN/2026.09.13/2026.09.13-01-登记`）并经 sub-plan-reviewer 审核（`-02`，6 AUD：1 前置已闭合、5 建议中 4 闭合 1 为提交锚点）+ 定向复验"可进入追认"；登记含采纳理由/历史兼容/机器套件范围/需求保真自评/作者追认位 | 已登记+审核通过（提交锚点随本轮提交补写；作者追认仍开放） |
| CHK-012 守卫漏检 | 守卫补齐：ClassLayout、程序集 flags/HashAlgorithm、程序集+模块级自定义属性、DeclSecurity（含 SecurityAttributes 内容反射）、CDI **内容**反射（模块级亦然）；新增 layout/cdi 漂移钩子（schema 同步） | 代码完成+构建 0 error；VM 正负例重放随环境恢复（钩子与 chk003-drift 同机制，该机制已证有效） |
| CHK-013 Dispatcher 二次门禁 | ApplyTransactionToLive 临界区内、首个 live 写入前：语义指纹+外漂移守卫+debug idle 三重二次核对（失败零写入） | 代码完成；屏障竞态用例随环境恢复 |
| CHK-014 UI 事实 | 操作行 kind\|target；验证摘要通道（review 绑定/操作数/风险与确认计数）；检查点行全量事实（完整 image/semantic、sequence、review 绑定、structural/roundtrip、确认集、尽力时间戳）；选中详情面板（McpEditDetailLine） | 代码完成；UIA 证据随环境恢复 |
| CHK-015 验收记录错误 | 监听器套件根因查明并修复（第二轮 PASS）；P03 受影响族当前 HEAD 重跑（新发现并修复清理路径前置缺陷）；ACC-019 归属修正为 P03；资源/注册表证据保留 | 大部分完成；P03 族重跑被环境阻断 |
| CHK-016 VM 工具阻断 | 环境属性，本轮同样阻断实施方所有 VM 证据（见 §3）；新 HEAD 复核 | 阻断登记 |
| CHK-017 部分提交撤销冲突 | PartialCommit 携带 Pre/PostExternalGuard；retry_checkpoint/undo_live/cleanup_temp 双指纹前置与事后校验；导航/迁移 partial 捕获真前置守卫 | 代码完成+构建 0 error；ACC-024 重跑（edit 家族）待环境恢复 |

## 2. 本轮新发现并修复（非 CHK 直接项）

1. **监听器套件根因链**（第一轮 FAIL(环境) 的实际成因）：整改轮 `apply_settings(True, localhost)` 把持久远程配置（RemoteAllowedCidrs/Host-Only ack）规范化清空；其后 LAN 重绑定的坐标输入漂移使 host 静默不落地，产品校验对无效组合静默回退 → 监听器留 loopback → http.sys 400。修复：apply_settings 改 UIA 定位+坐标输入（含 loopback/远程组合规范化）；部署侧恢复有效远程配置种子。第二轮全套 PASS。
2. **清理路径前置缺陷**：配置 ArtifactRoot（`C:\dnspy-mcp-artifacts`）与回归清理路径（`%USERPROFILE%\Desktop\dnspy-mcp-artifacts`）为两个目录；陈旧谱系使 TestIL begin 报 `EDIT_LINEAGE_DIVERGED`（产品行为正确）。修复：重跑器清理配置根。
3. AUD-001（审核前置）：module 级 CDI 内容行此前仍为类型名渲染——已改 `CdiContentRows` 并复核闭合。

## 3. 环境阻断（如实登记，不自恢复）

2026-09-13 本地时间约 09:5x，P03 族重跑进行中，VM 管理服务 `http://192.168.204.240:28787/mcp` 挂起：ICMP 通、TCP 28787 三次握手成功、HTTP 请求 60s 零字节（多次复测、多客户端一致）。按受保护约束（执行方绝不自行恢复 VM 管理服务/网络），停止所有 VM 操作并保存现场。**待用户恢复该服务后**，剩余动作：P03 受影响族双架构重跑（清理修复已就位）+ CHK-012/013/014/017 的 VM 定向证据 + 新 HEAD 独立核验。

## 4. 构建与门禁（本地可复核）

- 双 TFM Release 0 error（独立临时检出）。
- P02 契约 133/133、篡改检测 20/20、P03 契约 PASS（审核记录复核继承）。
- 监听器套件 PASS 证据：`tests/edit/p02-listener-summary.json`。
