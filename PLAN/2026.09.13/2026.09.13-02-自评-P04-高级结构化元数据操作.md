# 自评 P04：高级结构化元数据操作

- 对象：`PLAN/2026.09.13/2026.09.13-01-实施子方案-P04-高级结构化元数据操作.md`（v0）
- 方案内容身份：git blob `a47490c719c6702fd6b3d1dd7d8e38bc66ddfe67` / SHA-256 `ac12069a257d1705fd391994c51b8fb6328e25a5715c5fe9e4a1d0809d637eb8`
- 总纲：v2（blob `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a` 起点 + v2 环境契约变更）；需求文档 v0（blob `22574a9054a5016e8a2899f0508d0069e7742843`）
- Pxx：P04；前置 OUT-003/P03 已验收（提交 `2142266`）
- 本地 HEAD：`2142266`（分支 `feature/p01-public-contract-vm-gate`）
- 核心维护范围：`dnspy.edit.op.v1` 注册表的八类高级元数据载荷/新 kind（attribute_add/remove、security_add/remove 及六类字段扩展），复用 P02 事务与 P03 持久化
- 直接依赖范围：EditOperationRegistry(.Inverse)、EditWorkspace、EditStructuralValidator、EditHistoryModule、EditDeletedRowsTombstone、EditStructuredSignatureCodec、EditSchemaCatalog/EditJsonSchemaValidator、P03StoreHarness、契约生成物 operation-lowering.json
- 明确排除范围：C# 编译导入（P05/P06）、资源/身份/入口点（P07/P08）、UI/文档汇总（P09）、MethodImpl 重复实现（已存在复用）
- 决策关键路径：REQ-003 八类 → §2 盘点（22 kind 载荷实测 + 删除守卫现状）→ §3 IMP-001..010 → §4 ACC-004（成功/非法×双架构×§10.1）
- 上下文路径：总纲 OUT-004/ACC-004 行、需求 REQ-003/RACC-004/CON-017/CON-020、总纲 §12 复用规则、operation-lowering.json、EditOperationRegistry.cs、EditTransactionCoordinator.cs、实施记录 P03 §3/§66
- 发现与修改（本次自评闭环）：
  1. IMP-001 程序集级 attribute 与 IMP-007 assembly 父级不一致 → 已修正为两者一致（assembly 目标纳入，身份编辑仍属 P08）。
  2. ACC-004 泛型约束成功样本措辞含混 → 已改为可机械执行的整组替换描述。
  3. MethodImpl 复用归属核对：`method_add/update.impl_attributes` 存在且 `attribute_domains.MethodImplAttributes`（掩码 6143/非法向量 2048）已冻结 → 方案正确复用并在 §2/§4.3 标注。
  4. 逆操作完备性对齐 PLAN-CHANGE-001 关闭标准：新字段全部要求 compiled_state 逆 + 墓碑子行捕获（IMP-009 显式承接）。
- 需求保真门禁（独立重做）：应承接数 3 / 完整数 3 / 未落点数 0 / 语义弱化数 0 / 不可验收数 0 / 失效或复核条件缺失数 0；异常 ID 无。
- 送审成熟度：来源切片（§2 实测表）、范围/非目标（§1）、关键决定（§2/§3 各 IMP 的 dnlib 事实源）、稳定依赖边（§1 只读复用清单）、IMP/ACC 双向追溯（§4 末）、异常/边界语义（各 IMP 非法向量与 ACC 边界）、证据适用与失效/复核条件（ACC-004 双架构+§10.1）全部落地；方向阻断、验收阻断、占位决定、无主依赖均为零 → **送审准入：通过**。审核批次身份＝方案内容身份 `a47490c7…` + 规范性来源切片（REQ-003/RACC-004/CON-017/CON-020/总纲 v2 OUT-004/ACC-004/§12）+ 直接依赖身份（HEAD `2142266` 与 §1 清单）+ 决策证据（§2 盘表 + dnlib API 清单）机械派生。
- 审核路线：本对象无任何既有第三方审计 → **必须首次第三方审核（全面）**，状态保持 `未审核, 不得实施`。
- 实施授权：workflow `dnspy-edit-master-01a050b5` 长程授权进行中（覆盖本子方案审核引入后的实施与回流）。

## 需求保真确认

应承接 3 / 完整 3 / 未落点 0 / 语义弱化 0 / 不可验收 0 / 失效或复核条件缺失 0；异常 ID：无。
