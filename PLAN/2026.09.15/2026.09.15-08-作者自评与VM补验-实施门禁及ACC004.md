# 作者自评与 VM 补验：实施门禁及 ACC-004

日期：2026.09.15；平台Codex；角色作者接手执行/自检，不冒称独立核验方。基线HEAD f79ac1c；本提交只修改总纲门禁、测试驱动和本记录，产品DLL源码不变。

## 用户授权与自评

用户本轮明确“自评通过即可实施”，并授权验收包传输到指定VM。总纲升至v6 §14.2作为当前实施门禁，历史待审核描述不再阻止实施；未追认任何独立审核或关闭AUD/CHK。需求v1、总纲v6、P03v9的v1 exact/漂移显式新v2谱系边界一致；本轮未改变业务需求或删减验收。自评通过，允许继续实施和补验；这不是总体验收通过。

## 实际传输与环境

指定Win10VM-dnSpy 192.168.204.240；试验根 C:\Tools\dnspy-t006-main-6b0c65a。初始9文件包24774字节，SHA256 3d2c1f3866857dd2e8fea7ef7d67dba959488d64f104335d0315597d6891bfa4，传后核对通过；Windows命令长度限制后分段写入同包，无新增分块上传产品功能。后续只更新两个验收脚本，原始及修正证据分别保留。

专属x64/x86 dnSpy v6.6.0实例，端口15563/15564，专属settings/fixtures/artifacts；产品DLL SHA256 a7cc6286649131047030971f964319082577f99b19bef2675458aea1c7f14439，与本地已验证构建一致。dae1c55以来产品Editing/csproj无diff，可复用该实现产物而不把新文档HEAD冒充重新构建。此次未重新查询官方Release，不宣称完成ACC022最新Release前置。

## 驱动缺陷及修正

旧ACC004查get_type_fields漏pattern且期望不存在的Token字段，导致marshal/initial-data被跳过；错误generic目标还能被当成功；非法结构任意失败均被接受，汇总行重复计PASS。原始pilot为实际22个断言（runner误报23），不能覆盖完整ACC004。

改用get_type_info发现真实字段和泛型参数Token；必须找到目标，约束更新实际成功；非法输入精确区分EDIT_VALIDATION_FAILED与JSON-RPC -32602，连接异常不能算拒绝通过；逐次检查拒绝后work_revision不变；统计仅计行首PASS。修正过程中三项RPC前置拒绝被旧包装误标DRIVER_TRANSPORT，已按真实异常code识别，不修改产品预期或弱化断言。

## 验证证据

最终x64/x86真实MCP各39个断言通过，0失败，退出0；含约束、属性、impl、布局、marshal、pinvoke/security、initial-data、非法输入无暂存、idle、单谱系、Undo/Redo。另行双架构advanced-metadata-matrix通过：constraints/layout/offset/initial-data、attributes/overrides/marshal、pinvoke/security均重载保持且非法结构拒绝。这两组互补，不把integration单独说成八类全覆盖。

原始摘要：`.tmp/dnspy-t006-main-6b0c65a/ACC004-final-dual-VM-evidence.txt`，SHA256 `671dfa42352f1d55d19f10569476797e03a06039060ddc7a20401d3f333b1419`；包含每架构summary/driver日志/fixture及DLL和脚本hash。完整VM证据在专属各架构artifacts-final/edit-tests/pilot-final/EDIT-ACC-004目录，advanced-metadata.log在架构根。输入fixture副本与原fixture SHA一致。

本地隔离测试12/12通过，限定diffcheck通过；受保护.gitignore、PLAN/dnSpy-MCP.md和历史untracked现场不纳入提交。专属实例按PID和exe路径核对后停止，证据保留。其余ACC、最新Release核对与新HEAD全量结论仍待完成；本记录只作为ACC004补验证据，不产生独立核查结论。
