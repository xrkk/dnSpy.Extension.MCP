# 作者补充自评：PLAN-CHANGE-002 至 006 正式需求映射（T005-R02）

- 创建日期：2026-09-15
- 基线：`b2a6b3bde9e7679da689707d36c55058c3292c72`
- 性质：替代 R01 的 5/5 粗粒度计数；不改 R01 或任何历史登记、AUD、CHK。

## 去重正式 ID 双向映射

| ID | 源文完整语义 | 总纲 / 负责 Pxx / 当前落点 | 变更、证据与方向条件 |
|---|---|---|---|
| REQ-016 | UI 浏览事务、操作、差异、验证/风险、树/详情，并提供本地取消当前事务。 | 总纲 §14-002；P09 v2 IMP-001/ACC-018。 | 002；`EditExplorerWindow`/coordinator，在线属主不禁用，忙/提交/unknown 禁用。 |
| RACC-018 | UI 可查看上述事实并取消当前事务；不得提供绕过 MCP 的提交或恢复。 | 总纲 ACC-018；P09 v2 ACC-018。 | 002；`p03_vm_acc018.py`、案例 `EDIT-ACC-018.json`。 |
| CON-004 | UI 可编辑实时模块；提交须用实时指纹检出外改并返回冲突，不能覆盖用户操作。 | 总纲 §14-003；P02 v14 §3.4；P07/P08 v3。 | 003；`ComputeExternalGuard` 的实时门与 `live-conflict` 证据。 |
| REQ-008 | 统一审查返回差异、结构/静态/可执行时动态验证、风险和确认；提交绑定未过期审查。 | 总纲 OUT-002/ACC-010；P02 §3.4/§4。 | 003；guard 使外改审查失效，不改变审查/持久语义。 |
| RACC-001 | 私有连续编辑不改实时，第二事务拒绝；UI 外改后提交冲突且不覆盖。 | 总纲 ACC-001；P02 ACC-001。 | 003/004；guard 与单源再生。当前 validator `operation-set:order` 失败，故当前机器契约证明待整改。 |
| RACC-010 | 审查给固定修订/差异/验证/风险；继续编辑、实时变化或调试活动令旧审查不能提交。 | 总纲 ACC-010；P02 ACC-010。 | 003；guard 接入 review/commit 与实时临界门。 |
| RACC-003 | 类型、成员、IL、局部、EH 分别修改、审查、提交、保存、重载、核验。 | 总纲 ACC-003；P02 ACC-003，P04/P06 消费。 | 004；39 全集单源但 P02 machine schema 前 22 冻结。当前 validator order 失败，不能把历史 PASS 当新 HEAD 证据。 |
| RACC-009 | 每个实时应用步骤故障后，要么恢复原指纹，要么 `LIVE_STATE_UNKNOWN` 阻断编辑/导出。 | 总纲 ACC-009；P02 ACC-009。 | 004；240 fault 行与前 22 范围不变。 |
| REQ-011 | 当前工具链重放历史检查点须区分 exact、validated_drift、unverified_drift 并采用不同允许行为。 | 总纲 §14-005；P03 v8。 | 005；format 选择语义、禁止跨版本混比。 |
| REQ-022 | 检查点语义操作有稳定显式格式版本；未来版本不得静默重释旧操作。 | 总纲 §14-005/006；P03 v8。 | 005/006；unknown format/kind/version hard-stop。 |
| CON-010 | 首版操作格式为 `dnspy.edit.op.v1`；未知版本/操作必须停止而非猜测。 | 总纲 §14-005/006；P03 v8。 | 005/006；`kind_version` 能力表为同一信封的受限值域，不静默升级。 |
| CON-023 | exact 可提交；validated 经确认可迁移；unverified 只诊断，不能改实时、成检查点或导出。 | 总纲 §14-005；P03 v8。 | 005；v1 image 漂移一律 unverified 后显式 accept-live 新建 v2，是有意能力边界。 |
| RACC-013 | 跨工具版本构造三态并验证；未知操作版本失败，unverified 无实时/磁盘副作用。 | 总纲 ACC-013；P03 v8。 | 005；`P03StoreHarness --owner-version` 与固定包入口；VM/第三方复核仍待执行。 |
| REQ-004 | 支持 C# 编辑方法/类型并导入目标，含编译诊断和嵌入式调试信息。 | 总纲 OUT-006/ACC-005；P06 v3 §1。 | 006；首次 async/iterator、接口/显式 override 与 PDB 同域导入。 |
| REQ-023 | C# 编辑只走普通编译，禁止 analyzer/generator/脚本/自定义构建；目标引用仅作编译元数据读取。 | 总纲 OUT-006；P06 v3 IMP-001。 | 006；引用合成是导入期私有结构变换，不开编译扩展面。 |
| RACC-005 | C# 编辑返回诊断/导入结果/内嵌调试信息，无新增 sidecar PDB，并证明扩展点不可启用。 | 总纲 ACC-005；P06 v3 ACC-005。 | 006；`T004ImportProbe` 与 VM `--import-matrix` 为入口，后者尚待本轮外部复核。 |

CON-008 未计入“受影响”：002 不改变权威属主来源，只结束当前事务；NON-001—NON-018 无本轮实际扩张，故计数为零。

## 计数、异常与诚实边界

去重正式 ID：应承接 **16**、完整落点 **16**、未落点 **0**。累计语义能力边界 **1**：`WEAK-005-V1`（v1 image 漂移不走 validated/migration，只能 explicit `edit_accept_live` 建 v2，见 005 补充记录）；该边界是已裁决，不得写成 v1 全量语义检测。当前不可独立验收 **3**：`PENDING-003-VALIDATOR`（RACC-001/003 的当前 39-kind validator order 失败）、`PENDING-005-VM`（RACC-013 VM/独立审核）、`PENDING-006-VM`（RACC-005 VM/独立审核）。失效/复核条件缺失 **0**；条件本身已在登记 003（dnlib/CDI/身份规则）、005（owner/writer/固定包）、006（Roslyn 形态/跨运行时引用/留白）列明。

本表是受影响正式需求的作者自评，非 T006 的全 103 项独立核验，也不宣告 AUD/CHK 闭合。
