# dnSpy MCP 托管动态调试增强方案实施记录

## 头部(实施前门禁快照)

- 实施日期起始:2026.08.27
- 会话角色:作者/执行方
- 方案路径:`PLAN/2026.08.26/2026.08.26-01-dnSpy-MCP托管动态调试增强方案.md`
- 方案版本/状态:v16;`已审核, 可以实施`
- 方案 blob:`e35e59ad6de9ea167307d94b5936ebf7f52b44fc`;SHA-256 `35d85c3d88b8dfb91cf6822de3dd131b11ab6f4614b0999276304e5a72a78357`
- 首次第三方审计:第 1 轮(`2026.08.26-02-审计-...md`,当前 main blob `8776fa48b8250fb007f1d0beb78422845fef4a32`,SRC-AUD-001),已引入为 v2
- 当前版本闭环方式:首次审核祖先 + 后续改版自评闭环记录 `2026.08.27-02-自评-dnSpy-MCP托管动态调试增强方案.md`(blob `1685b0907cc31106dfbb2d0f2834ebbd731c7009`),路线 `AI 自评闭环`,绑定 v16 最终身份
- 实施授权来源:用户于 2026.08.27 在本作者会话粘贴 `一般功能-04-实施` 提示词;状态 `进行中`
- 规范性来源切片:SRC-BASE-001(v0 基线)、SRC-PLAN-001(`PLAN/dnSpy-MCP.md` HEAD blob `0a1c3761...`)、SRC-PROJ-001(`CLAUDE.md` blob `c23b655f...`)、SRC-API-001(dnSpyEx `v6.6.0` commit `e2bada24b1c63683f41bdc0ae2b0119c686ce88a`,已核实 tag 指向)、SRC-DEC-001(DEC-DYN-001)
- 本地审核基线 HEAD:`51ceaa37c2aa952ae509ec1363201e2011995532`;决策关键路径指纹核对:方案 v16 blob、自评记录 blob、`CLAUDE.md` blob 均与闭环记录一致;漂移结论 `无变化, 继续`
- 分支:`main`(未创建/切换分支,遵守默认策略)
- 实施基线 HEAD:`51ceaa37c2aa952ae509ec1363201e2011995532`
- 实施前工作区状态:
  - `PLAN/2026.08.26/2026.08.26-01-dnSpy-MCP托管动态调试增强方案.md`:v16 修改未提交(方案状态行已审核)
  - `PLAN/2026.08.26/2026.08.27-02-自评-dnSpy-MCP托管动态调试增强方案.md`:未跟踪(自评闭环记录)
  - `PLAN/dnSpy-MCP.md`:用户自有未提交改动,**受保护,不属于本实施任务,不得触碰**(用户 2026.08.27 明示与本流程无关)
  - 其余文件与 HEAD 一致;无 staged 内容
- 任务边界:本仓库内 `McpServer.cs`、`McpTools*.cs`、`McpProtocol.cs`、`McpSettings*.cs`、`McpSettingsControl.xaml*`、`TheExtension.cs`、`BepInExResources.cs`、`dnSpy.Extension.MCP.csproj`、`tests/`、`.github/workflows/`、`README*.md`、`CLAUDE.md` 及方案允许的新增 `Tools/`、`Debugger/` 目录文件;`PLAN/` 下仅本实施记录。仓库外构建工具链(dnSpyEx v6.6.0 浅克隆于 `/home/adminn/tmp/dnSpyEx`,只读使用,不修改)不属于交付边界。
- 实施轮次:第 1 轮(2026.08.27 起)

## 实施环境勘察结论(第 1 轮)

- 构建前置:本仓库设计为克隆进 dnSpyEx 检出的 `Extensions/dnSpy.Extension.MCP/`(csproj 引用 `../../DnSpyCommon.props` 与两个 Contracts 工程)。本机(Linux x64)无任何既有 dnSpyEx 检出(已搜索 `/home/adminn`、`/mnt` 常见深度)。
- 工具链:dotnet SDK 8.0.424 / 10.0.400 可用;GitHub 访问正常(dnSpyEx tag `v6.6.0` → `e2bada24...`,与 SRC-API-001 一致);磁盘余量 303 GiB。
- Windows 运行时:本机为 Linux;实验环境 Windows VM `192.168.204.149` 当前不可达(ping 100% 丢包,VM 未运行)。`/home/adminn/vmware-machines/` 存在若干 VMware 虚机目录,但本轮无凭据、无远程执行通道、无已运行实例。
- 推论:
  1. 代码级实施与编译检查:在 dnSpyEx v6.6.0 父级检出就位后,可在本机对 `net10.0-windows` TFM 尝试编译(`EnableWindowsTargeting`);`net48` TFM 依赖 Windows 语义,能否在 Linux 编译待验证。
  2. 全部需要 Windows 运行时/dnSpy OS 进程的 ACC 验证(含六项 launch E2E 矩阵、CorDebug 调试、Windows 文件身份租约运行时行为)在本环境**无法执行**,属于"缺少必要资源/外部依赖不可用"的真实阻碍,解除条件:在 Windows VM 内以同位数 dnSpy 实例运行 `tests/debug/run-debug-tests.ps1` 全量 ACC。
  3. 实施循环的最小直接检查以"双 TFM 中至少 net10.0-windows 编译通过 + 契约 fixture 静态验证"为下界;凡超出该下界的验证在本环境记为被阻断,不伪称通过。

## IMP 状态表

| ID | 类型 | 方案位置 | 状态 | 实际落点 | 验证动作 | 退出码/等价结果 | 精确摘要 | 持久证据路径/哈希 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| IMP-001 | 实施 | 4/IMP-001 | 进行中 | `Tools/IMcpToolProvider.cs`、`Tools/McpToolRegistry.cs`、`Tools/StaticToolProvider.cs`、`Debugger/DebuggerDispatcherBridge.cs`(新增);`McpServer.cs`(改经 registry 分派);`McpTools.cs` 零修改 | net10.0-windows 编译(Debug,`EnableWindowsTargeting`,经 `/home/adminn/tmp/build-check.sh` 同步构建) | 退出码 0,0 警告 0 错误 | 32 工具 schema/分派经 registry+provider 隔离;静态 provider 保持 WPF Dispatcher;`McpTools` 原样保留保证零语义漂移;bridge 为 CON-DYN-003 串行 Start 槽位骨架,DbgManager 接入在 IMP-005 | 编译日志:`已成功生成,0 警告 0 错误`;schema 快照 `tests/snapshots/static-tools.baseline.json`(32 工具,SHA-256 `34d3f4c6a40caadfd185f6d84d9a7f8950da9f046dc4548f8f58ab46d2972324`) | 成功响应快照与 ACC-001 需 Windows E2E,本环境被阻断 |
| IMP-002 | 实施 | 4/IMP-002 | 进行中 | `tests/debug/contracts/dnspy.debug.utf8-limits.json`、`dnspy.debug.v1.schema.json`、`generate_fixtures.py`、`fixtures/`(36 文件 189 例,新增) | ①jsonschema Draft 2020-12 `check_schema` 通过;②10 项字节清单 pointer 全部可解析;③def 族:25 args/22 results/21 events;④envelope/条件正反例全过;⑤fixtures 不变量机器校验:确定性(两次生成字节一致)、结构期望(invalid-fields 必无效、valid/invalid-state/disabled-gate/fixed-capability-unavailable 必有效、byte 阶段用例必结构有效)、-32602 仅出现于 invalid-fields/byte-input、2025-06-18 text 与 structuredContent 深等且通过 envelope schema、旧版本无 structuredContent、invalid-state 的 current_state∉required_states 且固定顺序、EVT-021 通过 event_envelope schema | 全部通过 | 覆盖:21 API × {valid,invalid-state,invalid-fields,disabled-gate} + capabilities 3 例 + 3 禁用 API × 3 版本 × 2 + launch 五模式/四 break/六互斥反例 + 3×3 控制失败及 EVT-021 + 10 pointer × 3 编码 × 2 边界(60);status/read/wait 无 3.6 禁止状态,invalid-state 槽位以 NOT_FOUND 呈现并注记;pattern 化句柄指针仅 ASCII at-limit 到达字节阶段,其余为 structure 阶段 | 生成器与产物同库,`ALL FIXTURE INVARIANTS PASSED`;sha256 随提交 | 剩余:C# 契约层(DTO/错误分类/序列化)、三版本 wire shape 实现、debug_capabilities handler |
| IMP-003 | 实施 | 4/IMP-003 | 未开始 | - | - | - | 传输安全、专用实例门禁、固定限额与设置 | - | 依赖 IMP-001/002 |
| IMP-004 | 实施 | 4/IMP-004 | 未开始 | - | - | - | 会话所有权、状态、句柄与事件 | - | 依赖 IMP-001..003 |
| IMP-005 | 实施 | 4/IMP-005 | 未开始 | - | - | - | 启动与生命周期 | - | 依赖 IMP-004 |
| IMP-006 | 实施 | 4/IMP-006 | 未开始 | - | - | - | 强身份断点与异常策略 | - | 依赖 IMP-004/005 |
| IMP-007 | 实施 | 4/IMP-007 | 未开始 | - | - | - | 线程、调用栈与步进 | - | 依赖 IMP-004/005 |
| IMP-008 | 实施 | 4/IMP-008 | 未开始 | - | - | - | 无目标代码执行的 locals 与字段 | - | 依赖 IMP-007 |
| IMP-009 | 实施 | 4/IMP-009 | 未开始 | - | - | - | 模块、只读内存与临时产物 | - | 依赖 IMP-004/005 |
| IMP-010 | 实施 | 4/IMP-010 | 未开始 | - | - | - | 全局 mutation 门控与不可信数据 | - | 依赖 IMP-002/004 |
| IMP-011 | 实施 | 4/IMP-011 | 未开始 | - | - | - | fixture、可复用验证、CI、发布与部署文档 | - | 依赖 IMP-001..010,发布门禁 |

## ACC 状态表

| ID | 类型 | 方案位置 | 状态 | 实际落点 | 验证动作 | 退出码/等价结果 | 精确摘要 | 持久证据路径/哈希 | 备注 |
|---|---|---|---|---|---|---|---|---|---|
| ACC-001 | 验收 | 6/ACC-001 | 未开始 | - | - | - | 静态兼容与线程域 | - | |
| ACC-002 | 验收 | 6/ACC-002 | 未开始 | - | - | - | 能力、专用实例门禁、响应和错误分层 | - | |
| ACC-003 | 验收 | 6/ACC-003 | 未开始 | - | - | - | 全端点认证、来源和固定绑定 | - | |
| ACC-004 | 验收 | 6/ACC-004 | 未开始 | - | - | - | 传输资源限额恢复 | - | |
| ACC-005 | 验收 | 6/ACC-005 | 未开始 | - | - | - | 事件游标、容量和等待 | - | |
| ACC-006 | 验收 | 6/ACC-006 | 未开始 | - | - | - | 句柄失效 | - | |
| ACC-007 | 验收 | 6/ACC-007 | 未开始 | - | - | - | net48 生命周期 | - | Windows E2E |
| ACC-008 | 验收 | 6/ACC-008 | 未开始 | - | - | - | CoreCLR 两种启动 | - | Windows E2E |
| ACC-009 | 验收 | 6/ACC-009 | 未开始 | - | - | - | attach 能力固定禁用 | - | |
| ACC-010 | 验收 | 6/ACC-010 | 未开始 | - | - | - | 强身份断点 | - | Windows E2E |
| ACC-011 | 验收 | 6/ACC-011 | 未开始 | - | - | - | 非法断点拒绝 | - | |
| ACC-012 | 验收 | 6/ACC-012 | 未开始 | - | - | - | 异常策略不污染全局 | - | Windows E2E |
| ACC-013 | 验收 | 6/ACC-013 | 未开始 | - | - | - | 线程与栈 | - | Windows E2E |
| ACC-014 | 验收 | 6/ACC-014 | 未开始 | - | - | - | 三种步进 | - | Windows E2E |
| ACC-015 | 验收 | 6/ACC-015 | 未开始 | - | - | - | locals 不执行目标代码 | - | Windows E2E |
| ACC-016 | 验收 | 6/ACC-016 | 未开始 | - | - | - | 取值预算与旧 value | - | |
| ACC-017 | 验收 | 6/ACC-017 | 未开始 | - | - | - | 模块身份 | - | Windows E2E |
| ACC-018 | 验收 | 6/ACC-018 | 未开始 | - | - | - | 只读内存范围和零填充语义 | - | Windows E2E |
| ACC-019 | 验收 | 6/ACC-019 | 未开始 | - | - | - | dump、目录、配额和零自动删除 | - | Windows E2E |
| ACC-020 | 验收 | 6/ACC-020 | 未开始 | - | - | - | 全局调试阻断六个静态写工具 | - | |
| ACC-021 | 验收 | 6/ACC-021 | 未开始 | - | - | - | 不可信样本数据 | - | |
| ACC-022 | 验收 | 6/ACC-022 | 未开始 | - | - | - | 可复用验证成为 build/release 门禁 | - | CI 层 |
| ACC-023 | 验收 | 6/ACC-023 | 未开始 | - | - | - | 当前 VM 可达且撤销后安全 | - | 需实验 VM |
| ACC-024 | 验收 | 6/ACC-024 | 未开始 | - | - | - | 不支持范围交接 | - | |
| ACC-025 | 验收 | 6/ACC-025 | 未开始 | - | - | - | 人工调试会话与所有权丢失 | - | Windows E2E |
| ACC-026 | 验收 | 6/ACC-026 | 未开始 | - | - | - | launch 路径、哈希与 argv | - | Windows E2E |
| ACC-027 | 验收 | 6/ACC-027 | 未开始 | - | - | - | transport 重连与异常状态回收 | - | |
| ACC-028 | 验收 | 6/ACC-028 | 未开始 | - | - | - | 公共 schema、状态矩阵与幂等 | - | 可静态验证 |
| ACC-029 | 验收 | 6/ACC-029 | 未开始 | - | - | - | 六项 runtime/architecture 功能矩阵 | - | Windows E2E |
| ACC-030 | 验收 | 6/ACC-030 | 未开始 | - | - | - | 正向 harness 建权与生命周期 | - | Windows E2E |
| ACC-031 | 验收 | 6/ACC-031 | 未开始 | - | - | - | 断点启用与禁用 | - | Windows E2E |
| ACC-032 | 验收 | 6/ACC-032 | 未开始 | - | - | - | FB-001 三分支确定性验证 | - | Windows E2E |
| ACC-033 | 验收 | 6/ACC-033 | 未开始 | - | - | - | Windows 文件身份租约关闭 TOCTOU | - | Windows E2E |
| ACC-034 | 验收 | 6/ACC-034 | 未开始 | - | - | - | 专用实例边界、可检测歧义与局部 restart | - | Windows E2E |
| ACC-035 | 验收 | 6/ACC-035 | 未开始 | - | - | - | 动态模块弱身份断点消歧 | - | Windows E2E |
| ACC-036 | 验收 | 6/ACC-036 | 未开始 | - | - | - | 专用实例部署确认与进程隔离 | - | Windows E2E |

## FB 状态表

| ID | 类型 | 方案位置 | 状态 | 备注 |
|---|---|---|---|---|
| FB-001 | fallback | 7/FB-001 | 未开始 | raw bytes 不可用时 ForceMemory 重构 |
| FB-002 | fallback | 7/FB-002 | 未开始 | 不支持 runtime 工作流交接 |

## 轮次日志

### 第 1 轮(2026.08.27)

- 门禁:闭环身份/漂移核对通过(见头部);`实施授权: 进行中`。
- 环境:建立 dnSpyEx v6.6.0 浅克隆于 `/home/adminn/tmp/dnSpyEx`(tag → `e2bada24b1c63683f41bdc0ae2b0119c686ce88a`,与 SRC-API-001 一致);建立 `/home/adminn/tmp/build-check.sh`(rsync 同步 + net10.0-windows 编译);基线(实施前 HEAD 内容)编译通过。
- IMP-001:未开始 → 进行中。新增 `Tools/IMcpToolProvider.cs`、`Tools/McpToolRegistry.cs`、`Tools/StaticToolProvider.cs`、`Debugger/DebuggerDispatcherBridge.cs`;`McpServer.cs` 五处最小改动(using、字段、构造参数、两个调用点);`McpTools*.cs` 零修改。编译通过(0 警告 0 错误)。
- IMP-001 前置条件之 schema 快照:以反射(`RuntimeHelpers.GetUninitializedObject` + `GetAvailableTools`)从构建产物提取 32 工具完整 schema,写入 `tests/snapshots/static-tools.baseline.json`(SHA-256 `34d3f4c6a40caadfd185f6d84d9a7f8950da9f046dc4548f8f58ab46d2972324`);生成器源码在 `/home/adminn/tmp/snapgen`(仓库外工具,IMP-011 测试基建时收编)。成功响应快照需 Windows 运行时,未完成。
- 阻碍记录(ACC 层,持续有效):本机 Linux、实验 VM `192.168.204.149` 不可达(未运行)。所有需要 Windows 运行时/dnSpy OS 进程的 ACC(ACC-001..036 中的 E2E 项,含六项 launch 矩阵、CorDebug、文件身份租约运行时行为、ACC-023 的 VM 可达性)无法在本环境执行;解除条件:Windows VM 内同位数 dnSpy 实例运行 `tests/debug/run-debug-tests.ps1` 全量验证。net48 TFM 在 Linux 的可编译性未验证(基线仅验证 net10.0-windows)。
- 本轮止点:IMP-002..011 未开始。IMP-002 需先产出 3.4—3.6 的 contract fixtures(25 API + 19 TYPE + 21 EVT schema),工作量大,留待下一轮次继续;本轮不提交(未完整通过,遵守"未完整通过时不得提交不完整实现")。

### 第 2 轮(2026.08.27,用户指令"先提交, 再继续推进")

- 提交:`a5f5ded`(docs: v16 方案 + 自评闭环记录)、`376b1e6`(feat: IMP-001 代码 + 快照 + 本记录);用户 `PLAN/dnSpy-MCP.md` 保持未提交。
- IMP-002:未开始 → 进行中。产出并冻结 3.4 规定的两份契约工件:
  - `tests/debug/contracts/dnspy.debug.utf8-limits.json`:`dnspy.debug.utf8-limits.v1`,10 项 pointer/方向/上限,按 ordinal 排序,与 3.4 逐字一致。
  - `tests/debug/contracts/dnspy.debug.v1.schema.json`:Draft 2020-12,110 个 `$defs`(19 TYPE + 25 API args + 22 result + 21 event payload + envelope + 标量),全部对象 `additionalProperties=false`;Unicode 字段不设字符上限以保证"结构校验先于字节校验"判定顺序(EVD-SCHEMA-001);base64url 句柄用 pattern+长度(字符=字节等同);launch 五模式/harness 与 dotnet 互斥字段、break_kind 模式收窄、value_node 句柄条件、domain_error retry/required_states 映射、file_identity sha256 条件、capabilities 全常量(29 limits)、event_breakpoint_bound/session_end 条件均以 if/then 编码。
  - 机器验证:`uv run --with jsonschema`(仓库外工具)完成 Draft 校验、pointer 解析、def 计数与全部正反例,输出 `ALL functional spot checks passed`。验证中发现并修复两处:payload 基础定义误加 additionalProperties=false(会排斥非空 payload)、unsupported_target_evidence 需独立为 $def(字节清单 pointer 要求)。
- 编译检查:本轮无 C# 改动,构建环路未使用。
- 工程修正:`.gitignore` 的 VS 构建产物模式 `[Dd]ebug/` 误伤源码目录 `tests/debug/`;新增定向反排除 `!tests/debug/`(bin/obj 下的构建目录仍被既有规则忽略)。
- 本轮止点:IMP-002 的 canonical fixtures(约 200 例)与 handler 层未开始;IMP-003..011 未开始。下一轮从 fixtures 生成器开始。

### 第 3 轮(2026.08.27,用户指令"推进")

- IMP-002 fixtures:完成。新增 `tests/debug/contracts/generate_fixtures.py`(确定性生成器,入库)与 `tests/debug/contracts/fixtures/`(36 文件、189 例 + MANIFEST)。
- 覆盖清单:21 个状态机 API × {valid, invalid-state, invalid-fields, disabled-gate};capabilities 的 valid-enabled/valid-disabled-gate/invalid-fields;3 个禁用 API × 3 protocolVersion × {fixed-capability-unavailable, invalid-fields};launch 五模式 valid + 四 break_kind + 六个互斥字段反例;pause/terminate/restart 各 3 例控制失败(scheduled-deadline/issued-deadline/issued-explicit-failure)及配套 EVT-DYN-021;10 个字节受限 pointer × {ASCII,BMP,非BMP} × {恰好上限,超过上限}。
- 两个规范化决定(记录在案):
  1. `debug_status`/`debug_read_events`/`debug_wait_event` 在 3.6 无禁止状态(全状态 Y/R),其 invalid-state 槽位以各自实际的 NOT_FOUND 错误呈现,不虚构 INVALID_STATE;
  2. pattern 化句柄指针(opaque_handle/session_id/page_cursor)因 charset+`{1,1024}` 只有 ASCII at-limit 值真正到达字节阶段,其余超限/非 ASCII 值在结构阶段即拒绝,fixture 以 `stage` 字段精确标注。
- 机器校验通过:确定性(重复生成零 diff)、全部结构期望、-32602 出现范围、三版本 wire shape(text/structuredContent 深等、旧版本省略)、invalid-state 不变量、EVT schema;输出 `ALL FIXTURE INVARIANTS PASSED`。
- 本轮止点:IMP-002 剩余 C# 契约层(DTO/固定错误映射/envelope 序列化)与 `debug_capabilities` handler(依赖 IMP-003 的 gate snapshot);IMP-003..011 未开始。

### 第 4 轮(2026.08.27,恢复中断后继续)

- IMP-002 C# 契约层:完成。新增 `Debugger/DebugProtocol.cs`:
  - wire 常量(DebugStates/LaunchModes/BreakKinds/EventKinds 等 21 事件 kind、12 错误码)以 const string 表达,规避 enum 命名策略无法导出 `net48-exe`、`module_cctor_or_entry` 的陷阱;
  - `DomainErrorDto.Create` 固定 §3.4 code/message/recovery 一一映射与 TYPE-DYN-011 retry 规则(LIMIT_EXCEEDED→1000、TIMEOUT→0、其余省略);
  - 成功/失败 envelope、DebugContext、UnsupportedTargetDetails、pause result、RuntimeMatrixEntry 与完整 capabilities result DTO(29 项 limits、artifact_policy、security、unsupported 常量),`[JsonPropertyName]` 声明顺序即 canonical 字段顺序,nullable 可选字段经 WhenWritingNull 省略;
  - `ToolsFor(gate)`(22/1 工具清单)与 `MatrixFor(host)`(六项固定序、同位数 launch/restart=true、异位数唯一 unavailable_reason)。
- 验证:net10.0-windows 编译 0 警告 0 错误;独立 harness(`/home/adminn/tmp/contractcheck`,仓库外)对冻结 fixtures 做字节级 text 比对——`debug_pause/valid`(成功 envelope+pause result)、`debug_pause/invalid-state`(INVALID_STATE+required_states)、`debug_pause/disabled-gate`(DEBUG_DISABLED)、`debug_capabilities/valid-enabled`(全部 capabilities 常量与六项矩阵)四例全部逐字节相等(`pass=4 fail=0`)。
- 本轮止点:IMP-002 剩余 `McpServer`/协议版本 wire shape 接线与 `debug_capabilities` handler 广告(依赖 IMP-003 的 `McpSettingsSnapshot`/冻结 gate);IMP-003..011 未开始。

### 第 5 轮(2026.08.27,继续推进 IMP-003)

- IMP-003 设置核心(纯逻辑层):未开始 → 进行中。新增 `McpSettingsSnapshot.cs`:
  - 不可变 11 字段 `McpSettingsSnapshot` + `SafeDefaults()`(逐字段等于 CON-DYN-014);结构验证覆盖 Port 范围、Host 分类(localhost/非回环 unicast literal,拒绝主机名、通配、回环 literal、多播、IPv4-mapped)、remote 三字段与 loopback/remote 组合矩阵、verifier 64 位小写 hex;
  - `CanonicalizeCidrs`:≤16 项、拒绝 IPv4-mapped、host bits 必须为零、文本必须已是 canonical 形态(点分十进制/RFC 5952 小写,拒绝前导零与大写)、去重 + ordinal 有序;
  - `Jcs`:领域受限 RFC 8785 writer(对象/字符串/整数/布尔/null/数组;拒绝浮点并注明理由);
  - `McpSettingsPersistence`:两键 `SettingsSnapshotJson`/`SettingsPendingJson`;`TryParseEffective` 要求恰好 11 字段、类型精确、全部组合规则通过且原始字符串恰为 canonical 形态;`Recover` 实现 CON-DYN-014 启动矩阵(双缺失→legacy 一次候选,同验证;pending==committed 加载并清标记;pending≠/无效→committed 权威并尽力清;committed 缺失/无效→安全默认,invalid 固定警告);固定文案常量与 CON-DYN-014 逐字一致。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/snapcheck`)33/33 通过——SafeDefaults 逐字段、remote 有效组合、无序/重复/前导零/大写 IPv6/host-bits/mapped/17 项 CIDR 拒绝、端口边界、主机名/通配/多播拒绝、verifier 大写拒绝、组合矩阵正反、canonical round-trip、键序/空格/缺字段/类型错位的非 canonical 拒绝、恢复矩阵 8 分支;C# JCS 输出与 Python 参考实现(`json.dumps(sort_keys, compact, ensure_ascii=False)`,本域等价 RFC 8785)字节级一致。
- 本轮止点:IMP-003 剩余 `ApplySnapshot` 事务接线(McpSettings/McpServer/设置页)、AcceptLoop 认证/CIDR/429/413/有界读取、gate snapshot 与 `effective_debug_launch` 冻结、`debug_capabilities` handler 广告;IMP-004..011 未开始。

### 第 6 轮(2026.08.27,继续推进 IMP-003 传输安全)

- IMP-003 传输安全纯逻辑组件:新增 `Transport/TransportSecurity.cs`、`Transport/TransportLimits.cs`:
  - `RemoteTokenAuth`(CON-DYN-006):严格无填充 base64url 解码(拒绝 `=`、非字母表、%4==1 长度与非零尾位)、恰好 32 字节、SHA-256、与 64 位小写 hex verifier 解析值的固定时间比较(net48 无 CryptographicOperations,手写常时比较);
  - `CidrFilter`:IPv4-mapped IPv6 peer 规范化为 IPv4 后按前缀位匹配,转发头从不读取;null/不可规范化 fail closed;
  - `HttpRejectShapes`:401(WWW-Authenticate `Bearer realm="dnspy-mcp"`)/403/429(Retry-After: 1)/413 的固定形状常量;
  - `BoundedBodyReader`(CON-DYN-009):ContentLength 预检>1048576 先拒(证明不触流)、原始读取上限 1048577 字节、严格 UTF-8 解码(拒绝非法序列与未配对 surrogate)、严格 JSON 解析;
  - `AdmissionGate`:非阻塞槽位(短请求 16/长连接 8/wait 8 由接线处实例化);`ResponseBudget`:完整渲染消息 8388608 严格 UTF-8 字节预算。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/transcheck`)46/46 通过——认证向量由 Python 生成权威值(bytes 0..31 的 base64url token 与 SHA-256 verifier),覆盖 round-trip/填充/非字母表/非法长度/非零尾位/错误 token/错误 verifier/大小写/短 token;CIDR 匹配与 mapped 规范化正反例;请求体预检不读流(ThrowingStream)、恰限/超限/小包;严格 UTF-8 三类拒绝;JSON malformed/trailing;准入闸容量与释放;响应预算 ASCII/非 ASCII 恰限与超限、字节计数。
- 本轮止点:IMP-003 剩余:上述组件接入 `McpServer` AcceptLoop/各传输路径、`ApplySnapshot` 事务与设置页接线、gate 冻结与 `debug_capabilities` 广告;IMP-004..011 未开始。

### 第 7 轮(2026.08.27,目标:推进至完整落地或真实阻碍)

- IMP-003 事务核心:新增 `McpSettingsStore.cs`:
  - `ISettingsSnapshotIO` 接缝 + `SettingsSectionSnapshotIO`(ISettingsSection 适配;已核实 dnSpy SettingsService 为内存字典、退出时统一 Save——两键崩溃语义由此成立:崩溃时盘上只有上次完整成功状态,未 committed 的 candidate 永不落盘;恢复矩阵防御分支仍覆盖手改文件);
  - `McpSettingsStore`:启动经 `Recover` 加载(含 legacy 一次候选)、`Apply` 严格执行 ①写 pending→②server transition→③写 committed→④内存 swap+恰一次 SnapshotChanged→⑤尽力清 pending;②/③失败恢复 old server 或强制 Stop、保持 committed/memory old、尽力清 pending、绝不回写 old JSON;活动会话门(冻结开关/root 变化 + coordinator 非 idle → 固定 modal 文案拒绝、零写入);
  - `DebugFeatureGate`:冻结 `effective_debug_launch = DebugToolsEnabled && DedicatedInstanceAcknowledged && StartupDbgWasIdle`;`StartupDbgWasIdle` 采样需调试契约(IMP-005 接入),不可采样时 gate 恒 false——不伪造采样。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/storecheck`)26/26——五步顺序与写入序列、①/②(返回 false 与抛异常)/③/⑤ 逐边界故障注入、恢复 old 与 forceStop、committed 不落未提交值、活动门三类触发与仅 Host/Port 豁免、启动 pending==committed 加载清除、gate 四冻结分支。harness 两次失败均为测试设计缺陷(Snap 的 ArtifactRoot 与 SafeDefaults 不同使"仅 Host"用例实为 root 变化,产品正确拒绝),修正后全绿。
- 本轮止点:IMP-003 剩余接线条:`McpServer` 快照驱动(Host/Port/远程绑定、AcceptLoop 认证/CIDR/准入/请求体/响应预算)、`McpSettings`/`TheExtension`/设置页改造、`debug_capabilities` 广告;IMP-004..011 未开始。Windows E2E 阻碍不变。

### 第 8 轮(2026.08.27,IMP-003 接线条)

- `McpServer.cs` 快照驱动与安全集成:
  - `ApplySnapshot(candidate)` 过渡(CON-DYN-014 步骤②):先 Stop,再按 candidate 绑定;远程模式拒绝端口漂移,失败恢复 old 监听或强制 Stop;`StartListener` 同步绑定使调用方可观测成败;IPv6 字面量前缀加方括号;
  - AcceptLoop 管道(CON-DYN-006/009):每端点认证→CIDR(401 固定 WWW-Authenticate/403,空体、无 CORS)→ 按 method/path/headers 分类长/短连接→非阻塞闸预留成功才创建 worker(finally 释放)→ 第 17 短/第 9 长 → 429+Retry-After+空体;accept 循环 break 路径补日志;
  - 三条传输路径:`ReadToEnd` 全部替换为 `TryReadRequestBody`(ContentLength 预检 413 + 1048577 有界读取 + 严格 UTF-8/-32700 固定对象);malformed JSON 同为 -32700;响应统一经 `RenderBoundedResponse`(8388608 预算,超限重建固定 -32603);Legacy SSE 与 Streamable HTTP 各自 16 会话上限在分配前原子判定(超限 429);CORS 通配仅在回环模式发送。
- `McpSettings.cs`/`McpSettingsPage.cs`:删除逐 setter 持久化与 PropertyChanged 副作用;`McpSettingsImpl` 以 `McpSettingsStore` 为权威(启动恢复 + legacy 一次候选 + 固定警告日志),`ApplyEdited` 单入口执行五步事务(候选=编辑的三字段+快照其余八字段,失败/门拒绝同步 UI 回权威值);设置页 `OnApply` 一行改经 `ApplyEdited`;`McpSettings.Log` 改 virtual(可测性)。
- 验证:编译 0 警告 0 错误;**Linux 真实 HTTP 端到端冒烟 harness(`/home/adminn/tmp/smoke`,30/30)**——回环:ApplySnapshot 启动、/health、initialize、tools/list 恰 32 工具、CORS 保留、1MiB+1 请求体 413 空体、malformed JSON/非法 UTF-8 → 字节精确 -32700、8 SSE 打开+存活探针、第 9 长 → 429;远程(192.168.1.14):过渡后回环停止、无认证 401(固定 WWW-Authenticate、无 CORS)、短 token 401、认证过+CIDR 拒绝 403、正确 token+Cidr 200 无 CORS、远程端口被占 Apply 失败且旧监听恢复。harness 曾有的失败均为测试自身缺陷(正则误计 schema 内 "name" 属性、Linux Stop 后连接拒绝 vs 非 2xx、占用已监听端口的测试逻辑),修正后全绿。
- 本轮止点:IMP-003 剩余:设置页 11 字段 UI 与一次性 token 显示、`StartupDbgWasIdle` 采样接线(IMP-005 契约)、`debug_capabilities` 工具广告(依赖 gate);IMP-004..011 未开始。Windows E2E 阻碍不变。

### 第 9 轮(2026.08.27,IMP-003 收尾:capabilities 广告与 gate 接线点)

- 新增 `Debugger/DebugToolProvider.cs`(MEF IMcpToolProvider):
  - `Gate` 属性:首次使用冻结 `DebugFeatureGate.Freeze(snapshot, startupDbgWasIdle: null)`——采样点留给 IMP-005,不可采样时 gate 恒 false;
  - `GetTools()`:恒含 `debug_capabilities`;`effective_debug_launch=true` 时追加 21 个会话工具(其 handler 随 IMP-004..009 落地,在此之前不广告未实现能力);21 个工具的 inputSchema 以**程序集嵌入资源**逐字复用冻结的 `dnspy.debug.v1.schema.json`(`$defs/<tool>_args`),单一事实来源、不可漂移;csproj 增加显式 EmbeddedResource(覆盖 tests/** 默认排除);
  - `ExecuteTool("debug_capabilities")`:按 API-DYN-001 构建完整 capabilities envelope(debug_enabled/gate 冻结值/host_architecture 按进程位数/22 或 1 工具清单/六项矩阵/security/artifact_policy/29 项 limits/unsupported)。
- **契约缺陷修正(规范性来源唯一确定)**:第 3 轮 fixture 生成器给无 session 的 envelope 套用了 1/2/10 模板计数,违反 TYPE-DYN-001"无 session 时三个计数为 0"。修正 `generate_fixtures.py`(`ctx_no_session`,覆盖 capabilities 两 valid、全部 disabled-gate、fixed-capability-unavailable 与 name_filter at-limit),重生成 189 例并重验全部不变量(含新增无 session 零计数断言);第 4 轮字节级契约验证同步刷新(4/4)。
- 验证:编译 0 警告 0 错误;Linux 真实 HTTP 冒烟扩至 **35/35**——gate=false 时 tools/list 恰 33(32 静态 + debug_capabilities,无其他 debug_*)、tools/call debug_capabilities 的 envelope 与修正后冻结 fixture **逐字段深相等**(仅 extension_version 豁免)、debug_context 无 session 零计数、六项矩阵/29 limits/3 unsupported 常量全对。
- 本轮止点:IMP-003 剩余:设置页 11 字段 UI 与一次性 token、`StartupDbgWasIdle` 采样(IMP-005)、tools/call 外层三版本 wire shape(IMP-002 遗留接线);IMP-004..011 未开始。Windows E2E 阻碍不变。

### 第 10 轮(2026.08.27,IMP-004 启动:事件缓冲)

- IMP-004:未开始 → 进行中。新增 `Debugger/DebugEventBuffer.cs`(REQ-DYN-005/CON-DYN-009):
  - 每 session 有界单调游标日志:游标从 1 起、session 内单调;条数 4096 与内容 8 MiB 双上限,任一触发的驱逐递增累计 `events_lost`;生产上限即契约值,构造参数可注入供测试;
  - 单个超总预算事件改写为 EVT-DYN-019 `payload_omitted`(original_kind/original_utf8_bytes/64 位小写 sha256/payload_omitted=true,timestamp 可注入);原始事件不可解析时 debug_context 兜底为无 session idle context,保证改写事件始终满足 event_envelope 的 required 字段;
  - `session_end` 写入即冻结,后续 Append 一律拒绝;`Read(afterCursor, limit, kinds)` 返回 events/next_cursor/earliest_cursor/events_lost,支持 kind 过滤。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/evtcheck`)23/23——游标单调与 after/limit/kinds 语义、条数上限驱逐(8→留 3、lost 5、earliest 6)、字节上限驱逐、超预算单事件改写(kind/cursor/timestamp/original 字段/sha256/上下文保留)、冻结后拒绝、冻结后可读;改写事件经 jsonschema 对 `event_envelope` 与 `event_payload_omitted` 双重验证通过。期间修正一个 harness 缺陷(超长测试事件的 payload 未加引号导致非法 JSON)与一个产品健壮性缺口(不可解析原始事件的 context 兜底)。
- 本轮止点:IMP-004 剩余:DebugSessionCoordinator(状态机/所有权/control record/观察对账)、DebugHandleStore、双 lane 队列、request_id 缓存、pause cause 仲裁;IMP-005..011 未开始;IMP-003 尾项(设置页 UI/gate 采样/wire shape)未做。Windows E2E 阻碍不变。

### 第 11 轮(2026.08.27,IMP-004:控制记录与暂停原因仲裁)

- IMP-004 继续推进。新增 `Debugger/ControlOperation.cs`、`Debugger/PauseCauseArbiter.cs`:
  - `ControlOperationRecord`(§3.2):session/generation/control_epoch/request_id/operation/prior_state/phase∈{scheduled,issued,settled};30 秒 monotonic deadline 以 `Stopwatch.GetTimestamp()` 原始时间戳比较(默认 30s,可注入短限期供测试);`TryMarkIssued`/`TrySettle` 以 CAS 保证相位唯一前进——settled 记录拒绝 issue(迟到 callback 不得调用上游),并发结算恰一次(响应/EVT-021/cache/lane 各释放一次);每个 operation 固定 EVT-DYN-021 late_completion_policy(pause→reconcile_owned_pause、terminate→finish_owned_termination_only、restart→finish_restart_as_failed);记录不持有任何 dnSpy 对象。
  - `PauseCauseArbiter`(§3.2/AUD-027):封闭优先级 exception>breakpoint>step>entry>process>manual>unknown,同优先级取最小 original ordinal、与到达顺序无关;候选条件——exception 需会话策略要求暂停、step 需匹配当前未完成 step_id、Break 仅在唯一 pause record 处于 issued 未 settled 时候选 manual;其余(空集/未知类型/未匹配 step/无 issued 的 Break)一律 unknown;`DetailOrder` 固定 exception,breakpoint,step 顺序(均按 ordinal)。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/ctlcheck`)35/35——字段与初相、deadline 29s/31s 边界与短限期、相位 CAS 全转移、settled 后拒绝 issue、8 线程并发结算恰一次、三个 policy 映射、仲裁器逐类候选/全优先级/最小 ordinal 平局/AUD-027 issued+Break→manual(state_satisfied 语义不因果升级)、detail 顺序。**测试发现并修复一个真实产品缺陷:构造器 `deadlineTimestamp = deadlineTimestamp` 自赋值使字段恒 0(deadline 恒过期)**。
- 本轮止点:IMP-004 剩余:把这些组件组装进 `DebugSessionCoordinator`(七态状态机、session/generation/pause_epoch 计量、权威观察对账、EVT 序列、fault/recovery)、DebugHandleStore、双 lane 队列、request_id 缓存;IMP-005..011 未开始;IMP-003 尾项未做。Windows E2E 阻碍不变。

### 第 12 轮(2026.08.27,IMP-004:协调器状态机组装)

- IMP-004 继续。新增 `Debugger/DebugSessionCoordinator.cs`(纯逻辑,零 dnSpy 对象,上游交互留 IMP-005 接缝):
  - 七态公共状态机与 §3.6 固定转移:idle+launch→starting;claim→running|paused(初始 paused 按固定 break_kind reason 写 EVT-010,不经 BreakInfos 仲裁);running+pause 观察→paused;paused+resume→running;restart→restarting→(移除)stopping→(再发射)starting→running/paused(全程不经过 idle、保留 session);terminate→stopping→session_end→idle;非重启意外退出→target_exited;歧义→faulted(ownership_lost);recovery 仅两观察理由→EVT-018+EVT-020(ownership_recovered)→idle;
  - 计量:generation 于取得 start reservation 时递增且失败不回收;pause_epoch 进入与离开暂停各递增;session_id 单活跃 + lastSessionId;终局日志保留 10 分钟(注入时钟)或下一次 launch reservation,过期 read → NOT_FOUND;
  - 控制准入(§3.6 矩阵):pause@running;terminate@running/paused/faulted(仅 ControlFault,ownership_lost 拒绝);restart@running/paused;同 session 至多一个未 settled 记录;control_epoch 单调;deadline 自 admission;
  - 控制失败结算:scheduled 恢复 prior_state(restart 另释放 reservation);issued 进 confirmed-owned ControlFault(restart 置 abandoned 并释放 reservation);EVT-021 恰一次(TrySettle 先赢先写),重复结算无第二事件;restart 控制记录在新 Start 发射时结算(API-DYN-008 响应即新 generation+claim deadline),reservation 释放推迟到 running/paused;
  - 权威观察对账:paused/removal 均先按 session/generation/owned identity 验证,旧值零副作用;paused 观察按封闭仲裁选唯一 primary、EVT-010 恰一次、兼容 pause record 以 state_satisfied 结算、重复观察去重、restarting/stopping/faulted 只更新 observed 状态;removal 为权威终局——pending restart 保留 session 进入 post-exit stopping;terminate(任一相位)结算成功;意外退出先以 INTERNAL_ERROR/current idle 结算不兼容 pause 再 session_end(target_exited);
  - 事件经 session 的 DebugEventBuffer 写入(envelope 字段顺序即 wire 顺序,debug_context 携带当前计量)。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/coorchek`)36/36——完整生命周期、busy 拒绝、pause 观察对账(含旧 generation 拒绝/去重/EVT 计数)、resume、scheduled 与 issued 两类失败、EVT-021 恰一次、Y* 例外边界、移除三结局、终局保留窗口(9 分钟/过期/新 launch)、重启全路径(含 abandoned 后移除→restart_failed 边界的 unexpected-exit 落点)、ownership_lost/recovery 与 EVT 顺序。**测试捕获并修复一个真实设计缺口:pending restart 记录未结算会永久阻塞后续控制准入——按方案改为新 Start 发射即结算、reservation 保持至 running/paused。**
- 本轮止点:IMP-004 剩余:DebugHandleStore、双 lane 队列、request_id 统一缓存(RFC 8785 参数规范化/容量/envelope 预检)、观察与 launch 流的 dnSpy 接缝(归 IMP-005 适配器);IMP-005..011 未开始;IMP-003 尾项未做。Windows E2E 阻碍不变。

### 第 13 轮(2026.08.27,IMP-004:HandleStore/双 lane/request_id 缓存)

- IMP-004 组件层收齐。新增 `Debugger/DebugHandleStore.cs`、`Debugger/DualLaneQueue.cs`、`Debugger/SideEffectRequestCache.cs`:
  - `DebugHandleStore`(REQ-DYN-006):句柄绑定 session/generation/pause_epoch(可为空表示 generation 绑定);resolve 要求整条绑定链精确匹配;epoch/generation/session 三级失效各关闭仍存活句柄**恰一次**(close 回调一次、重复失效零效果);失效句柄永久死亡。
  - `DualLaneQueue`(CON-DYN-013):8-control/56-general/64-total 固定容量,第 9/第 57 个在入队前拒绝;lane 容量按**在飞计数**而非队列长度(已释放未出队的 id 立即让位);DequeueNext 控制优先、lane 内 FIFO、跳过已释放 id;Ticket 释放恰一次,二次释放被拒。
  - `SideEffectRequestCache`(CON-DYN-013):key=request_id,身份=method+RFC 8785 规范化参数(剔除 request_id);4096 条/268435456 字节双容量,admission 预留 method+args+65536、settle 结算精确字节和;65536 字节 envelope 模板预检——预计超限即缓存小型 LIMIT_EXCEEDED 且不执行副作用;同 ID 同身份 join in-flight 或命中 settled,不同 method/参数 → REQUEST_ID_REUSE;保留期为完成(或更晚的 session terminal)后 10 分钟,**在飞条目不过期**(其时钟始于完成);容量不足在队列/预留/副作用前拒绝,不逐出未过期项。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/imp4check`)44/44——句柄绑定链正反/三级失效恰一次/相邻句柄不受影响;双 lane 容量边界/释放立即让位/控制优先与 lane FIFO/双重释放;缓存 canonical 化/预留与结算字节/in-flight join/两种 REUSE/envelope 预检缓存/10 分钟保留窗口/常量。**测试捕获并修复两个真实产品缺陷:①在飞条目 TTL 时钟误用 default(DateTime) 导致第二次访问即被过期清除;②lane 容量误用队列长度而非在飞计数,释放后槽位不释放。**
- 本轮止点:IMP-004 剩余:观察与 launch 流的 dnSpy 接缝(IDbgProcessControlAdapter 等,归 IMP-005)、value snapshot 句柄记账与协调器/缓存/队列的端到端组装;IMP-005..011 未开始;IMP-003 尾项未做。Windows E2E 阻碍不变。

### 第 14 轮(2026.08.27,IMP-005 启动:契约引用/gate 采样/控制适配器接缝)

- csproj:新增三个调试契约工程引用(`dnSpy.Contracts.Debugger`/`.DotNet`/`.DotNet.CorDebug`,主树 dnSpy/ 子模块;已核实 `DbgManager.Dispatcher/IsDebugging`、`DbgProcess.Break/Terminate/Run/Detach`、契约 `DbgDispatcher` 仅暴露 `BeginInvoke`);**契约工程链在 Linux net10.0-windows 编译通过**(0 警告 0 错误)。
- `Debugger/DebugGateService.cs`(CON-DYN-014):启动快照首次捕获即固定;恰一次经 `DbgManager.Dispatcher.BeginInvoke` 投递 `!IsDebugging` 采样(契约仅异步,采样值回调落地时冻结且不可变;落地前 Current 为不可采样 gate 恒 false——不可采样的 gate 永不启用调试);无调试器宿主不抛;委托对测试接缝。`TheExtension.OnEvent(Loaded)` 在 `server.Start()` 前 `CaptureStartup`;`DebugToolProvider.Gate` 改由 gate 服务提供。
- `Debugger/DbgProcessControlAdapter.cs`(IMP-005 接缝):`IDbgProcessControlAdapter`(PostBreak/PostTerminate → Delivered|ExplicitFailure,void 上游返回绝不当作完成、同步异常明确归属本 record;`Observation` 只带身份与已分类 BreakInfos,**不携带任何 request/control token**);production 封装 owned `DbgProcess` 投递与观察转发;`FakeDbgProcessControlAdapter` 可脚本化 accepted/explicit_failure/never_complete/late_complete。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/imp5check`)18/18——gate 采样恰一次/二次捕获零投递/落地前 false/落地后冻结不受后续变化影响/busy 采样 false-but-sampled/无宿主不抛/禁用快照 false;适配器-协调器端到端:pause admitted→issued→Delivered(void≠完成)→无 token 观察→state_satisfied 结算、ExplicitFailure→issued 失败→ControlFault、Y* 重试→Delivered→removal settled-terminate→idle。
- 本轮止点:IMP-005 剩余:launch 生命周期本体(五模式映射/文件身份租约/argv 编码/claim 匹配)、三个禁用 API stub;IMP-006..011 未开始;IMP-003 尾项未做。Windows E2E 阻碍不变。

### 第 15 轮(2026.08.27,IMP-005:argv 编码器与五模式映射器)

- 新增 `Debugger/LaunchPlanner.cs`:
  - `WindowsArgumentEncoder`(§3.5 WindowsEncode):空串→`""`;含空白/引号→整体加引号;引号前的 n 连续反斜杠→2n+1 个再接引号;**闭合引号前(即参数末尾且加引号时)的 run→2n;其余位置反斜杠原样**;不经 shell。
  - `LaunchPlanner`:五模式到 dnSpy Start options 的逐字段纯映射(net48-exe/coreclr-apphost:Filename=target+CommandLine=encode(target_argv)+WD=已验证目录;coreclr-dotnet:UseHost=true、Host=host_path、HostArguments=encode(host_argv,默认 [`exec`])、Filename=target;auto:按 PE/CLR 检测族唯一解析到 net48-exe/coreclr-apphost,不可唯一→失败不 Start;harness:按 harness 自身 PE 族选择 exe 映射、Filename=harness_path、CommandLine=encode([target_path]+harness_argv)、首参数合同固定);`UpstreamBreakKind` 四值→四个 PredefinedBreakKinds 常量名(拒绝字面量透传);工作目录用 Windows 语义父目录(不依赖宿主 OS 的 Path 规则)。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/launchcheck`)12/12 固定向量与映射表(含 dotnet 默认 exec、harness 首参数、auto/harness 不可解析失败、break-kind 全映射);**300 例随机参数经 Python 参考 CommandLineToArgvW 解析器(MSDN 规则 + 引号构成参数的 seen 语义)往返全通过(ok=300 bad=0)**。性质测试捕获并修复两个真实缺陷:①编码器把所有中间位置的反斜杠 run 都按 2n 加倍(仅闭合引号前的末尾 run 才加倍,其余原样);②纯映射器误用宿主 `Path.GetDirectoryName`(Linux 下按 POSIX 语义处理反斜杠),改为 Windows 语义父目录提取。
- 本轮止点:IMP-005 剩余:文件身份租约(WindowsFileIdentityLease,CON-DYN-011)、claim 匹配与 WPF Start bridge 接线、三个禁用 API stub、LaunchPlan→真实 dnSpy options 对象转换;IMP-006..011 未开始;IMP-003 尾项未做。Windows E2E 阻碍不变。

### 第 16 轮(2026.08.27,IMP-005:禁用 stub 与文件身份模型)

- `DebugToolProvider`:接入三个固定禁用 API(`debug_list_attachable_processes`/`debug_attach`/`debug_detach`)——永不广告,但 schema-valid 直接调用返回固定无副作用 CAPABILITY_UNAVAILABLE envelope(无 session idle 上下文、零计数、§3.4 固定 message/recovery、无 details、`IsError=true`),参数被完全忽略,未知工具仍走 Unknown-tool 路径;类改 public。
- 新增 `Debugger/FileIdentityModel.cs`:
  - `FileIdentityDto`(TYPE-DYN-012):role 四值/object_kind 二值/final_path/volume_serial(`0x`+16 小写 hex)/file_id(32 小写 hex)/sha256(file 必有且 64 小写 hex、directory 必省略);`SameObjectAs` 按 volume serial+file id(+sha256)判同一对象;
  - `WindowsPathRelation`:Windows 语义(双分隔符、忽略大小写、去尾分隔符)的相等/包含(严格组件边界,`C:\sample` 不含 `C:\samples\x`)、三根两两不等且互不包含(CON-DYN-008/011);
  - `FileIdentityLeaseBook`:纯租约账本——角色首租约生效、重复角色拒绝;`Observe` 按 role 对比观察身份(身份相同且 final path 相同→Match;身份或路径变化→Mismatch 即 TARGET_MISMATCH 语义;无租约→NoLease)。Windows 句柄获取(CreateFileW/FILE_ID_INFO/final path)为 production 侧,不在纯模型内。
- 验证:编译 0 警告 0 错误;harness(`/home/adminn/tmp/fidcheck`)27/27——三 stub 不广告、三个名字直接调用均返回**与冻结 fixture 字节级相等**的固定 envelope(参数忽略)、未知工具仍 null;FileIdentity 正反例(file 无 sha/directory 带 sha/大写 hex/坏 role 拒绝);路径关系(大小写/包含边界/盘根/三根互斥正反);LeaseBook(重复角色/大小写路径 Match/换 file_id Mismatch/换路径 Mismatch/无租约)。harness 一处失败为自身提取缺陷(fixture case id 带 @version 后缀使 EndsWith 不匹配),修正后全绿。
- 本轮止点:IMP-005 剩余:WindowsFileIdentityLease 的 Windows 句柄侧(production)、claim 匹配与 WPF Start bridge 接线、LaunchPlan→真实 dnSpy options 转换;IMP-006..011 未开始;IMP-003 尾项未做。Windows E2E 阻碍不变。
