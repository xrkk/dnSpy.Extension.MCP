# 审计：dnSpy MCP 托管动态调试增强方案（第 3 轮）

- 审计日期：2026.08.26
- 审计轮次：第 3 轮
- 审计角色：第三方独立审计师
- 审计环境：云端审核；仅使用 GitHub 不可变对象和公开规范性页面，不使用方案作者对话、自评结论或未持久化本地运行态作为通过依据
- 结论：存在不同意见, 需处理审计结果
- 方案路径：`PLAN/2026.08.26/2026.08.26-01-dnSpy-MCP托管动态调试增强方案.md`
- 方案版本：v6
- 方案状态核验：未审核，不得实施
- 方案 blob：`a268baf74e655f64d07e1c068969d0432a9fad41`
- 方案 SHA-256：`5bd553c5e235782b806b54b812514ffef9f2ff4937820db1df164cdc7701877c`
- repository：`xrkk/dnSpy.Extension.MCP`
- 目标主分支：`main`
- audit_commit_sha：`42bea3d8af2551177d32bb884fbfdf204ac2d368`
- audit_commit_sha 之后的主分支提交：不影响本轮结论
- 上一轮审计记录：`PLAN/2026.08.26/2026.08.26-03-审计-dnSpy-MCP托管动态调试增强方案.md`；本快照 blob `6d22fb06cce8094e48605360c671dd09255db1e3`；SHA-256 `1e40d75066292ece100af76103ef8a25df10c121215065c511dc5f175debaf74`

## 1. 规范性来源切片

| 来源 | 不可变身份或稳定定位 | 本轮独立核验 |
|---|---|---|
| SRC-BASE-001 | v0 blob `9769919e093374b530f7d66302f84c851141b7d6`；SHA-256 `cebbdec964a9cbc22b14dddc48150377481369b1956f5ba6ed24c1491d7c44f3` | 完整读取，核对原始 REQ-DYN-001..014、IMP-001..011、ACC-001..024、FB-001..002；确认 v0 原含 launch/attach |
| SRC-PLAN-001 | `PLAN/dnSpy-MCP.md`；blob `0a1c37614d5344781daabd5c26e9928e62232aa3`；SHA-256 `bdc5a3416f614ecbf44aa44a4a1a38bcba064255b717181eeb1f0f3e4513f95a` | 完整读取；只确认“调试增强”意图 |
| SRC-PROJ-001 | `CLAUDE.md`；blob `c23b655f65bd9d4e3386094183f4dbee9a0cabda`；SHA-256 `ccab877dd1bca66bb7f45dc8279a8be660ee73b9d0b45494a8d7fa90e761fd99` | 完整读取 Build、Architecture、Conventions、Test fixture；核对双 TFM、MEF、HttpListener、设置即时生效和测试约定 |
| SRC-AUD-002 / SRC-DEC-001 | 上一轮记录本快照 blob `6d22fb06cce8094e48605360c671dd09255db1e3`；其第 8—9 节；方案 v6 的 DEC-DYN-001 及所列 REQ/CON/NON/RACC | 只核对已持久化的 B1+B2 需求决定，不沿用作者对 AUD-006..009 的处理结论；确认决定把 v1 收缩为专用实例、launch-only、条件性所有权 |
| SRC-API-001 | dnSpyEx/dnSpy `v6.6.0`；commit `e2bada24b1c63683f41bdc0ae2b0119c686ce88a` | 复核 DbgManager 异步 Start、首次 listener、start options、逐进程控制、break kind、locals/value、内存和模块契约 |
| EVD-OS-001 | Microsoft Learn 的 CreateFile、FILE_ID_INFO、GetFinalPathNameByHandleW 稳定页面；2026.08.26 读取 | 复核 reparse、share mode、最终路径与文件身份的作用边界 |
| EVD-MCP-001 | MCP 官方 `2024-11-05`、`2025-03-26`、`2025-06-18` tools 规范和 `2025-06-18` changelog；2026.08.26 读取 | 复核旧两版 content/isError 与 2025-06-18 outputSchema/structuredContent 分界 |

稳定页面：

- https://modelcontextprotocol.io/specification/2024-11-05/server/tools
- https://modelcontextprotocol.io/specification/2025-03-26/server/tools
- https://modelcontextprotocol.io/specification/2025-06-18/server/tools
- https://modelcontextprotocol.io/specification/2025-06-18/changelog
- https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-createfilea
- https://learn.microsoft.com/en-us/windows/win32/api/winbase/ns-winbase-file_id_info
- https://learn.microsoft.com/en-us/windows/win32/api/fileapi/nf-fileapi-getfinalpathnamebyhandlew

比较上一轮审计交付 commit `09706a896f500a12cd2eb5d6d0e55f061eb12868` 与本轮 `audit_commit_sha`，只有本方案和上一轮审计记录发生变化；实施代码、项目约定、workflow 与现有测试 blob 均未变化。方案身份、来源切片和代码快照均可从不可变对象重建，不触发环境阻断。

## 2. 审核范围与证据

### 2.1 核心维护范围

- v6 方案、v0 需求基线、SRC-DEC-001、上一轮审计记录和生效项目约定。
- `McpServer.cs`、`McpTools.cs`、`McpSettings.cs`、`McpSettingsPage.cs`、`McpProtocol.cs`、`TheExtension.cs`、`dnSpy.Extension.MCP.csproj`。
- 方案明确新增的 registry、debugger provider、coordinator、Windows lease、artifact store、contracts、fixtures、verify workflow、部署脚本和文档。

### 2.2 直接依赖范围

- dnSpyEx v6.6.0 的 Debugger、Debugger.DotNet、Debugger.DotNet.CorDebug 公共契约。
- DbgManager/DbgManagerImpl、IDbgManagerStartListener、StartDebuggingOptions/PredefinedBreakKinds、CorDebug/DotNet start options、CoreCLRHelper、UI Start/Attach。
- 断点、线程、栈、locals/value、内存、模块与 Windows 文件身份 API。
- 三种 MCP protocolVersion 的 tools/list、tools/call 和结构化输出契约。

### 2.3 明确排除范围

- Unity/Mono、混合模式、纯原生、GUI 自动化和 v1 已固定禁用的 attach/detach 实现。
- 与本功能无直接依赖的静态分析算法、BepInEx 文档和其他 dnSpy 模块。
- audit_commit_sha 之后的提交。
- Windows VM、真实 dnSpy 组合、网络和六项动态 E2E 的未实施运行结果；本轮不声称测试已经执行。

### 2.4 决策关键路径和上下文路径

| 决策关键路径/对象 | blob/commit | 核验用途 |
|---|---|---|
| v6 方案 | `a268baf74e655f64d07e1c068969d0432a9fad41` | 15 REQ、14 CON、7 NON、12 RACC、1 DEC、25 API、18 TYPE、20 EVT、11 IMP、36 ACC、2 FB |
| v0 需求基线 | `9769919e093374b530f7d66302f84c851141b7d6` | 原始范围和稳定 ID 含义 |
| 上一轮记录 / DEC 来源 | `6d22fb06cce8094e48605360c671dd09255db1e3` | AUD-006..009 原意见和 B1+B2 持久化决定 |
| `CLAUDE.md` | `c23b655f65bd9d4e3386094183f4dbee9a0cabda` | 构建、架构、设置和测试约定 |
| `McpSettingsPage.cs` / `McpSettings.cs` / `TheExtension.cs` | `3eebeb40eaba3884ad5d9fbef0cbaa9351dfcc69` / `fdc31d531e9abb21800c3a456e3a9f53032a42f1` / `9255e98698bc857c170f5fb89a218560fe9fcd8d` | 设置 clone/CopyTo、逐属性持久化、服务器启动顺序 |
| `McpServer.cs` / `McpTools.cs` / `McpProtocol.cs` | `8bb061aee61492cc2a080829d2f7b0ec2c54785c` / `f6b9e69ef34198f4900c35e9d8ccbad58327511b` / `fb64241d46b1cbad41272fb9b76c767b5b015b55` | 三协议、传输、32 工具、Dispatcher 和结果 DTO |
| csproj / build / release / tests | `926b94d8fb687cf524de7b7599521294432e471a` / `13fd59fc34498da62b0ae8e55ba4be2e4f98be4d` / `682f2dbe375624158edf7e1a3b060ee5c548845c` / `d61c3278e704dd5e86e9d151cb6e84cf475391e4` | 依赖、双 TFM 和当前发布门禁 |
| dnSpy `StartDebuggingOptions.cs` | `f275fad1fef4c0a7aad1ad29b87f62e938ddba51` | `BreakKind` 的四个上游常量 |
| dnSpy `DbgManagerImpl.cs` / `IDbgManagerStartListener.cs` | `841ba23fa194df7156878542b167b01edb825f55` / `1cd84d8eff43028c660fbe039e677a33c093def7` | 异步 Start 和无调用方 correlation 事实 |
| dnSpy v6.6.0 | commit `e2bada24b1c63683f41bdc0ae2b0119c686ce88a` | 其余公共调试直接依赖 |

上述排序 path=blob/commit 清单的 SHA-256 为 `12a29dd29ffdc97e7afef5a700db35e0d189f09c0d648df5741918fd4bf8477a`。

上下文路径只辅助理解：上一轮审计的作者处理段、UI Attach、DotNet options page、CoreCLR 命令组装和版本变更记录；不以其自评措辞替代本轮证据判断。

## 3. 上一轮意见独立复核

| 原意见 | 本轮判断 | 独立证据 |
|---|---|---|
| AUD-006 debug_context/cursor/ACC-018 矛盾 | 原问题已闭合 | 3.4 示例含 state；3.5 分离 page cursor 与 event cursor；API-DYN-023、ACC-018 逐项固定四类结果和零 ReadMemory 副作用 |
| AUD-007 无法证明 Start/Attach 调用因果 | 按持久化需求收缩闭合，不是技术消除 | DEC-DYN-001/REQ-DYN-015/CON-DYN-014 将保证限定为专用实例前提下的 launch-only 条件保证；禁用三项 attach API；capabilities、ACC-034/036 保留同身份单一幸存残余风险 |
| AUD-008 restart 公开经过 idle | 原问题已闭合 | 3.2/3.6 固定 restarting→stopping→starting，全程保留 session/reservation；ACC-020/028/034 覆盖 post-exit/pre-Start barrier |
| AUD-009 三 MCP 版本结果兼容缺口 | 原问题已闭合 | 3.4、IMP-002/011、ACC-002/022/028 固定三版本 tools/list 与 tools/call wire 矩阵 |

以上只说明原意见的具体事实变化；v6 仍须接受下面的新意见，不能由作者“已闭合”表述升级为通过。

## 4. 审计意见

### AUD-010：公共 schema 与 canonical fixture 仍存在两个不可同时满足的定义

- 严重度：阻断性。
- 来源或方案 ID：REQ-DYN-003、REQ-DYN-006；CON-DYN-013、CON-DYN-014；RACC-DYN-002、RACC-DYN-011；方案 3.3—3.5；API-DYN-001；TYPE-DYN-011；IMP-002；ACC-002、ACC-028。
- 证据：
  1. 第 3.3 节的 22 个活动工具包含 `debug_capabilities`（API-DYN-001）。第 3.4 节、CON-DYN-014、API-DYN-001 和 ACC-028 均规定它在 gate=false 时仍成功。
  2. 第 3.5 节 contract 段却要求“22 个广告 API 各自”提供 `disabled-gate` fixture，且该 fixture 固定期望 `DEBUG_DISABLED`。因此 API-DYN-001 的 fixture 同时必须成功和失败。
  3. TYPE-DYN-011 把 `required_states` 定义为 0..8 项；同节封闭数组清单又规定 `DomainError.required_states` 最多 7 项。单一 JSON Schema 不能同时以 maxItems=8 和 maxItems=7 逐字表达。
- 风险：schema、fixture、handler 和 ACC-028 可各自选择不同解释，两个独立实现无法得到同一协议。
- 影响：公共协议、条目双向追溯、可交接实施性和验收边界未闭合。
- 建议：把 API-DYN-001 从 `disabled-gate=DEBUG_DISABLED` fixture 集合中明确排除并为其建立 gate=false success fixture；把 `required_states` 的唯一最大项数固定为 7，并同步 3.5、schema、fixture 和 ACC-028。

### AUD-011：四种 break_kind 没有到 dnSpy 常量、状态和事件的完整映射

- 严重度：阻断性。
- 来源或方案 ID：REQ-DYN-002、REQ-DYN-004；RACC-DYN-004、RACC-DYN-009；方案 3.5 API-DYN-003、launch_mode 表、EVT-DYN-010；IMP-005、IMP-011；ACC-007/008/022/029/030；SRC-API-001。
- 代码证据：dnSpy v6.6.0 `StartDebuggingOptions.cs` blob `f275fad1fef4c0a7aad1ad29b87f62e938ddba51` 的 `PredefinedBreakKinds` 只有 `DontBreak`、`CreateProcess`、`ModuleCctorOrEntryPoint`、`EntryPoint`；`StartDebuggingOptions.BreakKind` 是传给 manager/engine 的 string。
- 已证实问题：方案公开 `none`、`process`、`module_cctor_or_entry`、`entry`，但 launch_mode 的“Start 映射”没有固定四值到上述上游常量的映射；也没有固定 `process` 和模块 cctor 暂停在 EVT-DYN-010 中使用哪个 `reason`。现有 ACC 主要验证默认 `entry`，没有逐 runtime/mode 覆盖四值及 `none` 不暂停边界。
- 风险：实施者可传 API 字面量、上游常量、null 或自行转换，并可把 process/cctor 暂停报告为不同事件；部分组合可能完全不暂停或在错误位置暂停。
- 影响：launch 生命周期、事件协议和六项发布矩阵不能直接编码或机械验收。
- 建议：在 API-DYN-003/launch 表逐值固定 `BreakKind` 映射，补齐 process/cctor 的状态和 EVT reason；在可支持的每种 runtime/launch_mode 上为四值给出正/负验收矩阵，明确 `none` 的无 paused 事件边界。

### AUD-012：v6 所称“整页事务”与现有逐属性持久化和即时启动机制不相容

- 严重度：阻断性。
- 来源或方案 ID：REQ-DYN-003、REQ-DYN-011、REQ-DYN-015；CON-DYN-006、CON-DYN-014；RACC-DYN-003、RACC-DYN-012；EVD-CODE-003；方案 IMP-003；ACC-003、ACC-036；SRC-PROJ-001。
- 代码证据：`McpSettings.cs` blob `fdc31d531e9abb21800c3a456e3a9f53032a42f1` 的 `CopyTo` 依次设置 EnableServer、Host、Port；每个 setter 触发 `PropertyChanged`。`McpSettingsImpl_PropertyChanged` 每次都 `RecreateSection` 并写当前字段，且 EnableServer 变化会立即 `Start/Stop` server。`McpSettingsPage.cs` blob `3eebeb40eaba3884ad5d9fbef0cbaa9351dfcc69` 的 OnApply 只调用一次 CopyTo。
- 已证实问题：CON-DYN-014、IMP-003 和 ACC-036 把“一次 CopyTo”当作“本页各值一次提交”，却没有要求批量抑制 PropertyChanged/persistence/server side effect。按当前机制，idle 时同时把 EnableServer 从 false 改为 true并修改 Host/Port，server 会先按旧 Host/Port 启动，随后新 Host/Port 只被持久化；进程中断还可留下部分字段。
- 风险：远程绑定、认证相关设置和冻结门禁可出现持久化值与实际监听状态不一致；v6 新增的 active 拒绝不能修复 idle 提交路径。
- 影响：设置数据一致性、用户可见保存结果、远程安全状态和直接实施顺序未闭合。
- 建议：明确新增批量 ApplySnapshot/commit 边界：先完整验证，抑制逐属性持久化和 server 动作，一次写入全部字段并原子替换内存快照，最后只按新完整快照执行一次 Start/Stop/rebind；ACC-036 增加 persistence 调用次数、server 启停参数/顺序和中途故障注入。

### AUD-013：FB-002 要求返回的识别证据和工作流没有公共 wire 落点

- 严重度：阻断性。
- 来源或方案 ID：REQ-DYN-002、REQ-DYN-003；NON-DYN-001；RACC-DYN-010；方案 3.4、TYPE-DYN-011、API-DYN-003、ACC-024、FB-002。
- 证据：FB-002 和 ACC-024 要求 `CAPABILITY_UNAVAILABLE` 同时返回识别到的目标种类、证据及 Mono 或 PE/x64dbg 交接；失败 envelope 只能包含 TYPE-DYN-011 error。TYPE-DYN-011 没有 target kind、evidence 或 recommended workflow 字段，`recovery` 只有泛化的 `choose_supported_workflow`，且明确 `message` 不作为机器判断依据。所有对象又固定 `additionalProperties=false`。
- 风险：实现者只能把关键交接信息塞入不可机械判断的 message，或添加违反 schema 的字段；ACC-024 无法按共同 schema 判定“正确工作流”。
- 影响：不支持范围的正常失败闭环和 FB-002 验收未成立。
- 建议：为 TYPE-DYN-011 增加受限、稳定的结构化 details variant，或为 API-DYN-003 的该错误固定 `detected_target_kind`、`evidence`、`recommended_workflow` schema；同步 ACC-024 对每类 fixture 逐字段断言。

### AUD-014：ArtifactRoot 自身及其父链未纳入 reparse/final-path lease

- 严重度：阻断性安全问题。
- 来源或方案 ID：REQ-DYN-010、REQ-DYN-011；CON-DYN-008；NON-DYN-002；RACC-DYN-007；方案 TYPE-DYN-008/018、IMP-009、ACC-019；EVD-OS-001。
- 证据：CON-DYN-008 只要求 session 目录是 ArtifactRoot 的非 reparse 直接子目录，并把当前 root 的 file ID 写入 marker；IMP-009 只要求用户路径规范化且字面上不位于 sample allowed root/扩展目录。ACC-019 覆盖 session 目录和内部 file reparse，但没有覆盖 ArtifactRoot 本身为 junction/symlink、父组件 reparse 或验证后替换。CON-DYN-011 的逐组件 handle lease 仅适用于 launch 输入，不适用于 ArtifactRoot。
- 风险：一个指向样本、扩展或其他目录的 root reparse 可使 artifact CreateNew 和 7 天清理落到字面路径之外；marker 记录被重定向目标的身份仍可能自洽，不能阻止越界写入/删除。
- 影响：产物隔离、保守清理和 NON-DYN-002 的任意路径写入边界未闭合。
- 建议：像 CON-DYN-011 一样对 ArtifactRoot 及父链使用 OPEN_REPARSE_POINT/final path/file ID 进行拒绝和 lease，验证最终 root 不在样本/扩展目录并在创建、写入、清理期间保持身份；ACC-019 增加 root/父链 reparse 与验证后替换 barrier。

### AUD-015：ACC-016 的命令使用 API-DYN-020/021 不存在的预算参数

- 严重度：高。
- 来源或方案 ID：REQ-DYN-006、REQ-DYN-009；CON-DYN-007、CON-DYN-009；RACC-DYN-006；方案 TYPE-DYN-017、API-DYN-020/021、IMP-008、ACC-016。
- 证据：API-DYN-020 只接受公共 paused 字段、frame_handle 和 PageRequest；API-DYN-021 另只增加 `depth` 1..4。node/string/response budget 是 TYPE-DYN-017 的服务端固定结果字段，不是请求参数。ACC-016 却要求“以最大允许和超限 depth/node/string/response 参数调用展开”。
- 风险：按 schema 提交 node/string/response 参数应为 `-32602`，按 ACC 文字又应触发截断或 `LIMIT_EXCEEDED`；测试作者必须自行改写操作。
- 影响：无目标代码执行的资源边界没有可执行的验收命令。
- 建议：depth 仅测试 4 与 5 的 schema 边界；node/string/response 通过确定性 fixture 的节点数、字符串字节数和总响应体大小触发，并分别固定 truncated 与 LIMIT_EXCEEDED 预期及 provider 调用计数。

### AUD-016：认证与 CIDR 失败的 HTTP 状态没有逐输入固定

- 严重度：高。
- 来源或方案 ID：REQ-DYN-011、REQ-DYN-013；CON-DYN-006；RACC-DYN-003；方案 3.4、IMP-003、ACC-003。
- 证据：3.4 只写“认证/CIDR 失败返回 401/403”；ACC-003 对无 token、错误 token、轮换后的旧 token统一写“401/403”，也未逐项固定 CIDR 不匹配的唯一状态、响应头和 body 边界。
- 风险：两个实现可对同一输入返回不同状态；验收脚本把 401 或 403 任一值都当通过，无法证明错误优先级和端点一致性。
- 影响：每个 ACC 的精确预期和通过/失败边界不满足审核门禁。
- 建议：为缺失/格式非法/错误/旧 token、CIDR 拒绝和同时失败逐项固定唯一状态、必要响应头/body及认证/CIDR判定优先级，并在七类端点分别保存断言。

## 5. 检查结果汇总

| 检查项 | 结果 | 说明 |
|---|---|---|
| 需求覆盖 | 未通过 | break kind 全分支、ArtifactRoot 根边界、FB-002 结构化交接未形成可执行落点 |
| 双向追溯 | 形式通过、语义未通过 | REQ/CON/NON/RACC 均能指向 IMP/ACC，但 AUD-010..016 使若干落点不能证明来源 |
| 功能闭环 | 未通过 | 设置应用、启动暂停语义、unsupported 交接和 artifact 根级状态未闭环 |
| 可交接实施性 | 未通过 | 实施者仍须自行决定 schema、break mapping、设置批量提交和错误 wire |
| 决策闭合 | 未通过 | API-DYN-001 gate fixture、required_states 上限、break/event 映射和 HTTP 状态存在并列方向 |
| 验收契约 | 未通过 | ACC-003、016、019、024、028、036 存在不精确或不可执行边界 |
| 条目 ID 完整性 | 通过 | REQ-DYN-001..015、CON-DYN-001..014、NON-DYN-001..007、RACC-DYN-001..012、DEC-DYN-001、IMP-001..011、ACC-001..036、API-DYN-001..025、TYPE-DYN-001..018、EVT-DYN-001..020、FB-001..002 均连续唯一；正文无实施进度状态 |
| 正确性/兼容性/性能/数据一致性/资源/测试/现实阻碍 | 未通过 | 三协议和 restart 已改善；设置数据一致性、artifact 路径安全和测试可执行性仍有当前证据支持的缺口 |
| fallback | 未通过 | FB-001 通过；FB-002 必要且可退出，但其结构化结果与 ACC-024 未落入公共 schema，见 AUD-013 |

## 6. Fallback 逐项评定

| fallback | 必要性 | 退出与错误边界 | 验收 | 评定 |
|---|---|---|---|---|
| FB-001 raw 不可用时重构 PE | EVD-API-005 支持，必要 | raw 可用即退出；ForceMemory 也失败则 CAPABILITY_UNAVAILABLE、无产物 | ACC-032 固定 raw/reconstructed/双失败三分支和 byte_equivalence | 通过 |
| FB-002 不支持 runtime 交接 | REQ-DYN-002/003、NON-DYN-001 支持，必要 | 不建 session/进程；切换 Mono 或 PE/x64dbg 工作流，退出路径成立 | ACC-024 需要的种类、证据和工作流没有 TYPE-DYN-011/API-DYN-003 wire 字段 | 未通过，见 AUD-013 |

除 FB-001、FB-002 外未发现其他显式或隐式 fallback。DEC-DYN-001 的 launch-only 范围收缩是需求决定，不是 fallback；三个固定禁用 API 直接失败，不掩盖主流程错误。

## 7. 最终结论

存在不同意见, 需处理审计结果。

本结论只针对 `audit_commit_sha=42bea3d8af2551177d32bb884fbfdf204ac2d368` 的 v6 方案。AUD-006、AUD-008、AUD-009 的原问题已闭合；AUD-007 仅按已持久化的 DEC-DYN-001 需求收缩为条件性 launch-only 保证，底层无 correlation 的事实继续保留。AUD-010..016 在公共协议、生命周期、设置一致性、产物安全和验收契约中形成新的不同意见。方案应保持“未审核，不得实施”，处理意见并重新审核前不得实施。
