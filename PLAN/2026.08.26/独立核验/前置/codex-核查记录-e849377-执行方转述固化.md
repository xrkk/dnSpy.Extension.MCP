# dnSpy MCP 托管动态调试增强方案 — 第三方核查记录（Codex，2026-08-29，基准 e849377）

> **固化说明（执行方，2026-08-29）**：以下为 Codex 第三方核查方于对话中给出的核查结果**原文转述固化**（CHK-008 整改）。核查方原话未以文件形式留存于仓库，本文件由执行方逐字转录；五项发现的整改证据见实施记录第 61 轮与提交链（d78c807..c83bdd1）。

---

核查结论：**不通过**。

依据指引第 4 节，只要发现非环境性断言空转，即应判定不通过。当前仓库存在多处可确定复现的空转/门禁缺陷，因此即使历史记录声称 36/36 `exit 0`，也不能据此认定验收成立。

### 已确认通过的部分

- 当前 HEAD：`e849377`，基准 `8599fae` 是其直接祖先；基准后仅新增核查指引。
- 工作区干净。
- 本次实际执行 Linux 门禁成功：189 项契约 fixture 全部通过；`net48 dependency guard PASSED`；net48/net10 双 TFM Release 均为 0 警告、0 错误；`LOCAL VERIFY PASSED`。
- 本次 net48 DLL SHA-256：`0e513f2485edd227d59c38c5ddcf12604dc2a74f016ff6aaee30ed3029bd4b5f`
- 36 个 case manifest 和 36 个 handler 均存在。
- 六个 `debug_test_*` 工具的代码级环境变量门控基本成立。

### 导致“不通过”的关键发现

1. **ACC-001 dispatcher 断言确定性空转**：assert-dispatchers.ps1 固定输出 `WPF=unknown DbgManager=unknown` 并 `exit 2`，主驱动只匹配文本仍登记 pass。
2. **result.json 的证据/schema 门禁没有实现**：退出码只检查断言状态，未校验 evidence_paths 存在、required evidence 与 `dnspy.debug.test.v1` schema。
3. **ACC-004 的关键边界断言可错误放行**：恰 1 MiB 同样返回 413 也放行；并发断言未验证“恰好 8+9”。
4. **ACC-022/IMP-011 的 CI E2E 门禁未真正落地**：hashFiles 可跳过 E2E job；调用驱动未传 `-Case`；verify-status 接受 `skipped` 且使用不存在的 `needs.jobs[job]` 动态引用。
5. **“11/11 IMP 落地”与仓库记录矛盾**：IMP-001/002/010/011 状态表仍“进行中”；TEST-PLAN 仍称 L4 未执行。

### Windows 复跑情况

VM `192.168.204.149` ICMP 可达但 3000/15100 未监听，无远程执行凭据或通道；工作区无 `tests/debug/results/**/result.json`，本次无法独立执行 36 案或调阅历史结果。

裁定：**不通过**（核查方：Codex；核查记录模板略，全文见对话）。
