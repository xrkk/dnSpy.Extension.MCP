# 一般功能方案验收结果核查 — 第 6 轮（定向复核）

- 核查对象：R5 裁定 CHK-014/015/016 的整改提交 `70f8b7a` 及批 11 重立证据
- 核验方：general-plan-acceptance-verifier（再恢复实例，接续 R1..R5 记录链）
- 核查模式与轮次：定向复核，本轮 R6（轮次延伸依据：用户 2026-08-29 指令"执行整改，整改后让子智能体审核，通过后才算任务结束"，已入闭环状态 round_cap_note；5 轮计数不重置）
- 核查时间：2026-08-29T13:35:03Z（取证窗口 13:29Z–13:35Z）
- 结论：**核查通过**（CHK-014/015/016 全部关闭；无新发现；既有项零回归）

## 1. 对象身份 / HEAD / 增量自证

| 项 | 声明 | 实测 | 裁定 |
|---|---|---|---|
| 分支/HEAD | main / `8a788a2` | `8a788a256d1f55aed54ab920216d8d84a37fc09e`，工作树干净 | 一致 |
| 整改提交 | `70f8b7a`（单提交） | `70f8b7a1372f9cf84381d1f00cad48713550e93f`；VM `C:\Tools\mcp-repo` HEAD 同完整 SHA、工作树干净 | 一致 |
| 增量 `70f8b7a..HEAD` | 仅批证据/第 66 轮记录/闭环状态三文件 | `git diff --stat` 恰 3 文件（batch11-summary.json +231、实施记录 +3、闭环 JSON +11/-2），无代码 | 一致 |
| 产品 DLL 未变 | `e322f19b…` | 本地 `run-verify-local.sh` 构建 `dist/dnSpy.Extension.MCP-net48.x.dll` SHA-256 逐字节一致；`git diff e9bff3b..70f8b7a -- Debugger/` 为空 | 证实 |
| R5 记录不可变性 | blob `dee82237` | `git ls-tree HEAD` 与工作区 hash-object 均为 `dee82237023f45ade90795e51365648528bf4309`（70f8b7a 入库后未改动） | 证实 |
| 闭环簿记 | READY_TO_REVERIFY/rectification_r6/round_cap_note | 闭环 JSON diff 实读：status、rectification_r6（trigger CHK-014/015/016、commits [70f8b7a]、batch11）、轮次延伸授权注均在册 | 一致 |

## 2. CHK-014/015/016 逐项关闭裁定

### CHK-014（schema 门禁 string-null 放行＋证据门禁 null 短路，2/36 产物违例）— **关闭**

整改 diff（`git show 70f8b7a -- tests/debug/run-debug-tests.ps1`，25 行）逐 hunk 复核：

1. **根因修复（大小写不敏感变量碰撞）**：PowerShell 变量名大小写不敏感，`$p1 = $P1.domain.result` 覆盖 `$P1` 本身→其后 `@($P1.rpc.resp)` 在域对象上取 `.rpc` 得 null。四 site 全改：`$p1→$pol1`、`$p2→$pol2`（Run-ACC012 :1741-1746 区）、`$f→$fl`、`$f2→$fl2`（Run-ACC005 :2258-2268 区），引用点同步（插值/条件/游标计算）。**残留检查**：全文件 grep `$p1\b|$p2\b` 零命中；Run-ACC005 内 `$F/$fl/$F2/$fl2` 配对正确；ACC-028 :958 局部 `$f` 为独立文件路径变量、无 `$F` 碰撞对——无残留、无误改。
2. **string 分支拒 null**（:3891）：`if ($null -eq $Value -or $Value -isnot [string])` ——条件正确反转。
3. **证据门禁 null 计 missing**（:3831-3833）：`if (("$ep" -ne 'result.json') -and ("" -eq "$ep".Trim() -or -not (Test-Path …)))`——去真值短路，null/空白一律 missing（`"$ep".Trim()` 对 null 插值结果为空串，无 NRE）。

**独立验证**：
- VM 上从 70f8b7a 驱动原样提取 Test-SchemaNode（纯内存）负面测试：baseline 符合 0 错误（无误伤）；`evidence_paths=[null,'x']`（CHK-014 原案）→拒；`expected=null`（string-null 泛化）→拒；证据门禁逻辑等价测试 null/空白 → missing 计 2。
- **独立 jsonschema 全量对拍（R5 同款：Draft202012Validator.iter_errors、VM Python 3.11.7/jsonschema 4.26.0、utf-8-sig、全 36 文档）：checked=36 valid=36 invalid=0**（R5 时 34/36）。
- 四碰撞 site 证据直读全部真实 wire 路径、无 null，与执行方声明逐一吻合：`a5-eviction=[wire/0030,wire/0031]`（原 `[null,wire/0031]`）、`a5-payload-omitted=[wire/0032,wire/0033]`（原 `[null,wire/0033]`）、`a12-policy-switch=[wire/0010]`（原 `[null]`）、`a12-policy-roundtrip=[wire/0011]`（原 `[null]`）——ACC-012 两断言从"零实际证据"恢复为实证据。
- 批 11 VM 产物直读：36 案齐、exit_code 全 0、status 全 pass、断言合计 **490 失败 0**、`result-schema-shape` 全 pass、顶层 evidence 全在/evidence_paths 全无；时间戳严格顺序无缝（2026-08-29 13:15:37Z–13:28:41Z UTC＝本地 21:15–21:28，与 batch11-summary "21:15–21:27" 吻合，out-of-order=0、>30s 间隙=0——单次连续成立）；batch11-summary 算术对账 36/490/0/@70f8b7a/DLL e322f19b 一致。

### CHK-015（minLength 未实现而注释宣称 every keyword）— **关闭**

:3897 新增 `if ($Schema.PSObject.Properties['minLength'] -and "$Value".Length -lt [int]$Schema.minLength)`。负面测试：空串 assertion_id → errors=1 拒止。schema 关键字全覆盖复核：type/const/enum/required/additionalProperties/properties/items/pattern/minItems/**minLength**——驱动 :3841 "every keyword the schema uses" 注释现真。

### CHK-016（verify.yml job/step 名称仍标 Dynamic＋实施记录 :813 声称部分失实）— **关闭**

- verify.yml:89 `name: Dynamic launch E2E matrix (six fixtures)` → `E2E harness gate (static) (six fixtures)`；:122 step `Run dynamic verification (ACC cases)` → `Run E2E harness gate (-VerifyHarness, static)`——diff 实读确认，UI 可见名称误导消除。
- 实施记录第 66 轮勘误实写："第 65 轮'job 名称/注释改'声称中 job/step 显示名未改部分至此属实修（勘误:487c36b 仅改了注释行）"——满足 R5 最小闭合条件的两项（改名＋勘误）。
- 残余注记（不立案）：verify.yml:91-92 hashFiles 时代阶段注释（"until the file exists this leg is skipped…"）仍过时；其不含 Dynamic 误导且非 R5 立案点，记为文档清理建议。

## 3. 整改 diff 审查（70f8b7a）

5 文件全部可机械归属：run-debug-tests.ps1 25 行（CHK-014 根因+双侧加固、CHK-015 minLength）、verify.yml 4 行（CHK-016 两处显示名）、闭环 JSON（rectification_r6/状态）、实施记录（第 66 轮）、R5 核查记录入库（116 行，blob 未变）。**无越界修改、无方案外改动**；门禁改动方向全部为收紧（null→missing、null→type 错误），**无新增 fallback/放宽路径**；schema 加载失败 fail-closed 路径未触碰。

## 4. 既有通过项回归

- CHK-001..006/008/010（R2）、CHK-007/009/011（R3）、CHK-012（R4）、CHK-013（R5）：依赖证据锚定 e9bff3b/39193f8 代码状态——本轮确认 Debugger/ 与 csproj 自 e9bff3b 零改动、DLL 哈希三批一致（e322f19b）、批 8/10 证据文件未在 70f8b7a 及其后触碰（diff 范围核对）——**全部维持关闭**。
- Linux 门禁：`uv run --with jsonschema python tests/debug/contracts/validate.py` → ALL CONTRACT CHECKS PASSED exit 0（result schema 三重检查含 EF-1 突变拒止仍绿）；`DNSPY_DIR=/home/adminn/tmp/dnSpyEx bash tests/run-verify-local.sh` → LOCAL VERIFY PASSED exit 0，构建 0 警告 0 错误。
- 批 11 本身即 36 案全量回归（490 断言 0 失败）。

## 5. 新发现

无（CHK-017 起为零）。

## 6. 结论

**核查通过**。CHK-014（根因实证+双侧拒止+36/36 独立对拍）、CHK-015（minLength 实现+拒止验证）、CHK-016（两处显示名实改+勘误入册）全部以代码级 diff 与独立复测关闭；定向复核条件满足：全部历史 CHK（001..016）已关闭、无新发现、当前证据（VM 直读+独立 jsonschema 对拍+Linux 双门禁+DLL 哈希复现+时间戳连续性）支持完整落地。整改链 487c36b→bf2fa9e→39193f8→70f8b7a 与六轮核查记录链（blob 67cf2059/4b929190/5e85e9b1/22ff1a67/dee82237/本记录）构成完整闭合证据链。

---
核验方：general-plan-acceptance-verifier（再恢复实例，接续 R1..R5 记录链）
记录时间：2026-08-29T13:35:03Z
本记录为不可变核查记录，创建后内容保持不变。
