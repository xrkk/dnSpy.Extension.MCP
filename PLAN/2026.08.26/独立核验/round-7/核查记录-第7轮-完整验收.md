# dnSpy MCP 托管动态调试增强方案 — 第 7 轮完整验收

- 复核依据：`2026.08.26-11-第三方复核指引-v2.md`
- 复核日期：2026-08-30（Asia/Shanghai）
- 实际代码锚：`a4b6af07b2d9e6728e72df91ff4a17fdf71481c4`
- 运行环境：Win10 x64 VM `192.168.204.149`
- 裁定：**通过（严格按 v2 第 4 节；第 5 节已披露挂账继续保留）**

## 1. 结论与锚点替代

v2 文档中的历史主张锚 `e9bff3b / 490 assertions` 已被本轮实际整改取代，不能继续用于当前代码裁定。本轮先后发现并实修 net48 数值规范化、工件身份复核共享模式、验收驱动竞态及真实断点命中判定等问题，最终重立代码锚 `a4b6af0` 与一轮全新的、不拼接的 36 案证据。

最终结果满足 v2 第 4 节全部通过条件：36/36 连续批跑 exit 0、同一 commit、D 抽检一致、E 无空转/拼图/测试工具泄露发现。

## 2. A/B：本地门禁、部署与身份

| 项 | 实测 |
|---|---|
| 工作树（代码锚时） | clean；`git diff --check` 通过 |
| 契约门禁 | 189 cases，`ALL CONTRACT CHECKS PASSED` |
| 静态工具快照 | 32 tools，完整 |
| net48 依赖守卫 | PASS（System.Text.Json 9.0.0 与 dnSpy 匹配） |
| 双 TFM Release | net10 / net48 均 0 warning、0 error |
| 本地总门禁 | `LOCAL VERIFY PASSED` |
| git bundle SHA-256 | `af86427f6d17d233c280939f97791c3de5a90c10016d1406f559d9f5932c40dd` |
| net48 DLL SHA-256（本地=VM） | `2b1c1a6ecdfe3aee1b3ed02d184839299ff099d6aa8cec002723a4873cfa1aa8` |
| VM repo | `C:\Tools\mcp-repo-a4b6af0-full`，HEAD 与代码锚一致，工作树 clean |
| VM harness | 36 manifests、36 handlers、脚本解析、result schema 均 PASS |
| runner SHA-256 | `bcfabcd7d2180f868156b9d79b798f452753be193e9055265d38bea57255b329` |

## 3. C：单次连续 36 案

最终批次位于 `C:\Tools\dnspy-full-a4b6af0`：

| 指标 | 实测 |
|---|---|
| 时间窗（UTC） | `2026-08-29T17:36:16.7442991Z` – `2026-08-29T17:52:46.1659282Z` |
| 总耗时 | 989.422 秒 |
| 案例 | **36/36 pass** |
| 断言 | **500/500 pass** |
| 失败清单 | 空 |
| result.json | 36 个，status 全 pass |
| commit_sha | 36 个全部为 `a4b6af0…81c4` |
| 时间戳重叠 | false |
| result 哈希缺失 | 0 |
| `result-evidence-complete` / `result-schema-shape` | 0 failure |
| runner stderr | 空 |

原始 `summary.json` SHA-256 为 `d79aa7e9ea7392ceda0f6341be3ed0e853310c1af334e820a4c5df7066a108a8`。按 case_id 排序后的 36 个 `case=result_sha256` 行聚合 SHA-256 为 `84087473e5fb9df17ceadf035834047aa8054762a33a6358a2716c7a19410d6d`。

本轮没有采用历史结果拼图。用于定位问题的 `19c46b1` 观察批次明确记为 35/36（仅 ACC-010），随后在新锚点、新克隆、新控制目录中从 ACC-001 重新开始，最终裁定只取 `a4b6af0` 这一轮。

## 4. D：关键点抽检

| Case / assertion | 最终实测 |
|---|---|
| ACC-004 / `a4-body-limit-413` | pass；`at=200 over=413` |
| ACC-008 / `a08-apphost-launch` | pass；`ok=True fam=coreclr mode=coreclr-apphost` |
| ACC-016 / `a16-stale-handle` | pass；`code=STALE_HANDLE` |
| ACC-024 / `a24-unity-over-limit` | pass；`INTERNAL_ERROR noDetails=True start=+0` |
| ACC-025 / `a25-ctl-terminate` | pass；`OWNERSHIP_LOST` |
| ACC-025 / `a25-no-unowned-operation` | pass；`break=+0 terminate=+0` |
| ACC-033 / `a33-lease-share-conflicts` | pass；overwrite/delete/rename/reparse/root-rename 全部受阻 |

上述值均直接从最终批次自产的 `result.json` 读取，未引用旧批结果。

## 5. E：反空转、反拼图与测试工具隔离

1. **虚假闭合检查**：没有直接接受“已落地”声明。本轮实际复跑先后抓到并修复非整数参数形状、工件复核共享冲突、ACC-010/011/019/026/029/032/033/035 等真实问题；每次改动后均重跑相关案例，最终另起完整批次。
2. **连续性**：36 个结果 commit 全同、时间戳无重叠、结果 SHA 全存在；最终证据没有跨轮拼接。
3. **提交事实交叉**：`639411a..a4b6af0` 的 12 个整改提交逐项查看 diff；产品、契约、驱动修改与提交主题对应。旧 `e9bff3b` 主张被显式作废，不以 PLAN 文字覆盖代码事实。
4. **测试工具隔离动态检查**：停止测试模式实例；确认 MCP 执行环境原本 `DNMCP_TEST` 为 null，并在启动前显式移除；新 dnSpy `/health=200`。`tools/list` 实测 54 个工具、22 个正式 debug 工具、`debug_test_*` **0 个**，而 `debug_status` 等正式工具仍在，排除“关闭全部 debug 工具”的伪通过。
5. **历史记录不可变性**：round-1..6 blob 依次为 `67cf2059`、`4b929190`、`5e85e9b1`、`22ff1a67`、`dee82237`、`28286cea`，前四项与 v2 索引一致。

## 6. 已披露边界（不虚报为完成）

本次“通过”是 v2 第 4 节定义下的验收通过，不改变 v2 第 5 节披露：

- 子集案尾差仍包括 003、004、019、020、027、029、036 的指定未覆盖注入/传输/UI/双实例细项，以及 ACC-022 的 CI 动态维度；
- 过程性挂账仍包括缓存跨协议按版本重渲染、il_offset 方法体边界、静态工具 outputSchema、模块 MVID 真值；
- IMP-003 Remote 三字段 UI 与一次性 token 展示仍未落地（committed JSON 可配）；
- IMP-011 / ACC-022 的 CI 动态 E2E 编排仍未接入，CI 仍是 `-VerifyHarness` 静态门禁，动态验证依赖 Windows VM runbook。

因此，不把“v2 验收通过”扩张解释为上述挂账已实现。

## 7. 证据索引

- 结构化摘要：`round-7/batch-evidence/final-a4b6af0-summary.json`
- VM 最终摘要：`C:\Tools\dnspy-full-a4b6af0\summary.json`
- VM 每案证据：`C:\Tools\mcp-repo-a4b6af0-full\tests\debug\results\a4b6af07b2d9e6728e72df91ff4a17fdf71481c4\ACC-xxx\`
- VM 部署 DLL：`C:\Tools\dnSpy\bin\Extensions\dnSpy.Extension.MCP\dnSpy.Extension.MCP.x.dll`

验收结束后已停止验收用 dnSpy 进程；VM 保留最终部署 DLL、独立克隆、控制摘要与每案 wire 证据供复查。
