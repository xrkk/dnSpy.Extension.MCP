# 一般功能方案验收结果核查（第 2 轮·定向复核）

- 核查记录路径：`PLAN/2026.08.26/独立核验/round-2/核查记录-第2轮-定向复核.md`
- 创建时间：2026-08-29T10:45:47Z（UTC）
- 核查模式：定向复核（第 2 轮；第 1 轮全面核查已完成）
- 核验方：general-plan-acceptance-verifier——恢复实例（接续 agent_ae530c8a 第 1 轮记录；宿主会话中断后按闭环协议以相同名称恢复同一逻辑核验方）
- 不可变性声明：本记录创建后内容保持不变；后续轮次以新记录引用本记录。
- 被核验对象：仓库 `/home/adminn/projects/dnSpy.Extension.MCP`，分支 `main`

## 1. 历史链

| 轮次 | 模式 | 结论 | 记录 | 核查基线 |
|---|---|---|---|---|
| 1 | 全面核查 | 核查不通过（CHK-001..010） | `PLAN/2026.08.26/独立核验/round-1/核查记录-第1轮-全面核查.md`（blob `67cf20597f9bcb9dc4d5bbd0d8d4d00fd174b390`，本核验重新 hash-object 复核一致） | HEAD `dced7bb`；锚定 `c83bdd1` |
| 2（本轮） | 定向复核 | 见 §8 | 本文件 | HEAD `99f1cc5`；整改锚定 `e9bff3b` |

执行方逐项回应与整改记录：闭环状态 JSON `rectification.chk_response`（全部"接受"）+ 实施记录第 62 轮两段（检查点/完结）+ 批 8 证据 `PLAN/2026.08.26/独立核验/round-2/batch-evidence/batch8-summary.json`。

## 2. 对象身份 / HEAD / 增量自证

| 项 | 结果 | 证据 |
|---|---|---|
| 当前 HEAD | `99f1cc52131400f495ac191b545afd3c30fc0b50`（main），工作区干净 | `git rev-parse HEAD` / `git status --porcelain`（空） |
| 整改锚定 | `e9bff3ba5659baa86c6b7b9f5f44502a139d82d8`（2026-08-29 18:05:24 +0800） | `git show -s` |
| 增量 `e9bff3b..HEAD` | **恰 3 个文件**：闭环状态 JSON（M，+32/−5）、实施记录 md（M，+7）、`round-2/batch-evidence/batch8-summary.json`（A，+229）；无任何代码/驱动/测试增量 | `git diff --name-status e9bff3b..HEAD` |
| 整改区间 `c83bdd1..e9bff3b` | 19 提交，改动面仅：`Debugger/DebugSessionService.cs`、`tests/debug/run-debug-tests.ps1`、`tests/debug/fixtures-src/AccFixture.cs`（区间内净零：+19/−19）及 PLAN 簿记文件（第 1 轮记录固化、状态 JSON、指引勘误、Codex 固化、round-1 证据）。**无越界修改** | `git diff --name-status c83bdd1..e9bff3b` |
| 方案身份 | v16 blob `e35e59ad…` 未变（整改区间未触碰方案文件） | `git diff c83bdd1..HEAD -- PLAN/2026.08.26/2026.08.26-01-…方案.md` 为空 |

自证通过：增量与调用方声明一致（仅批证据/实施记录/核验状态三文件）。

## 3. 独立验证（本环境实际执行）

1. **本地全量门禁独立复跑**：`DNSPY_DIR=/home/adminn/tmp/dnSpyEx bash tests/run-verify-local.sh` → `LOCAL VERIFY PASSED`，exit 0；net48 依赖守卫 PASSED；双 TFM Release 0 警告 0 错误。
2. **构建可复现性交叉印证（关键）**：本核验在 HEAD 自建产物 `dist/dnSpy.Extension.MCP-net48.x.dll` SHA-256 = `e322f19b9224809512673606579dbf3e53e6805b5c28c8691d11e58b9a8873ac`——与批 8 证据 JSON 声明的 DLL 哈希**逐字节一致**（HEAD 与 e9bff3b 仅差 PLAN 文件，代码等价）。批 8 运行的二进制即本仓库该源码状态所构建。
3. **VM 原始产物独立读取（超出调用方最低要求，只读）**：经 Win10VM 只读访问 `C:\Tools\mcp-repo`（`git log -1` = `e9bff3b`，与锚定一致）：
   - 结果目录唯一：`results\e9bff3ba…\`（36 个 case 子目录）。
   - 逐案 `result.json`：36/36 `exit_code=0`、`status=pass`、断言合计 **490、失败 0**，每案含 `result-evidence-complete`/`result-schema-shape` 两门禁且均 pass——与 `batch8-summary.json` **逐案逐数完全一致**（含 007=21、008=17、034=14 等全部 36 行）。
   - 单次连续性：36 案 `commit_sha` 全为 `e9bff3ba…`；按 `started_utc` 排序无重叠、无 >120s 间隔；首案起 10:15:11Z、末案止 10:28:54Z（=本地 18:15–18:28），与"2026-08-29 18:15 起"声明相符。
   - 关键断言实际值抽读：006 `running-state-rejected actual=stack=STALE_HANDLE locals=STALE_HANDLE`；018 `running-invalid-state actual=code=STALE_HANDLE`；008 `a08-apphost-launch actual=ok=True fam=coreclr mode=coreclr-apphost`；034 `a34-launch3 actual=ok=True tries=2`、`a34-claim-spy candidates=6 windows=6`；010 `a10-entered-hot actual=hot=0x06000001`；031 `entered-hot actual=hot_token=0x06000001`；016 `a16-locals-ok actual=ok=True depth_used=1 tries=2`。
   - 证据落点：`dt-*` 分离请求/响应文件 22 个 + `p1b-*/a34-*` 6 个均位于各案结果目录内。
   - 反空转：ACC-007 断言全集完整（21 项含双门禁），`git log -L` 证实 ACC-007 自 c83bdd1 起仅 05f884f 触碰且无断言删减。

核验边界声明：VM 侧仅执行**只读**核验（读取既有产物），未做任何复跑或写入；上述裁定不单独依赖 VM 声明，而以"执行方产物 + 本地构建哈希复现 + 代码级整改机制复核"三方交叉。

## 4. 逐 CHK 关闭裁定

| CHK | 第 1 轮要点 | 整改证据（本核验独立复核） | 裁定 |
|---|---|---|---|
| CHK-001（严重）36/36 主张不成立 | 跨轮拼图，c83bdd1 批跑 25/36 | 修复链（12 个 fix 提交）逐一下述；批 8 于 e9bff3b 单次连续 36/36 exit-0、490/0——**本核验直接读取 VM 原始 result.json 逐案核实**（§3.3），DLL 哈希经本地构建复现一致；实施记录第 62 轮以批 8 重立主张并如实记载前值系拼图 | **关闭** |
| CHK-002（严重）apphost 误拒 pure_native | 检测器无条件运行 | `DebugSessionService.cs:712-714`：`launchMode == LaunchModes.CoreClrAppHost ? null : Detect(...)`（05f884f）；仅按声明的 launch_mode 旁路，检测器本体与 ACC-024 四类拒绝断言（driver:3256-3352）原样保留，批 8 ACC-024=11/11；VM 实测 `a08-apphost-launch ok=True mode=coreclr-apphost` | **关闭** |
| CHK-003（一般）006/018 驱动断言违契约 | 期望 INVALID_STATE | driver:937（006）与 :1137（018）均改为期望 STALE_HANDLE 并注明方案 §6 依据（05f884f）；VM 实际值 `STALE_HANDLE`；与产品 `PausedGateFailure` 纪元优先语义一致（第 1 轮已代码级确认） | **关闭** |
| CHK-004（一般）C:\Tools 挥发证据 | Invoke-Detached/p1b/a34 写结果目录外 | `Invoke-Detached` 改写 `$script:OutDir` 并返回相对路径、`Read-DetachedResp` 相对解析（05f884f）；p1b（1590-1607）、a34 T1（7bb4c54）同迁；VM 实测 28 个分离证据文件均在结果目录内且 36 案双门禁全过 | **关闭** |
| CHK-005（一般）034 转移窗口 launch 被拒 | — | 三层闭环：①真根因修正——T2/T2b fake 结算泄漏活孤儿，驱动改真杀 `Get-Process ArgvFixture`（9c36b97/843a353，ArgvFixture 即 034 启动目标，已核对 driver Case 头）；②产品侧有界拆场等待（2211831+843a353：`owned==null` 时 ≤3s/50ms 轮询，spy 计数 `manager_teardown_waits`，有 owned/测试 seam 时仍立即 INVALID_STATE，契约保持）；③驱动有界重试+唯一 request_id（05f884f/2211831）。VM 实测 `a34-launch3 ok=True tries=2`、claim-spy 6/6。第 1 轮最小闭合条件（"驱动等待 idle 或产品收敛转移窗口"）满足 | **关闭** |
| CHK-006（一般）顺序批跑跨案污染 | Ensure-CanonicalDnSpy 复用残留 | 派发处无条件 `Stop-DnSpyAndTargets`（driver:3781-3784，杀 dnSpy/全 fixture+1.5s），批==冷启动（05f884f）；批 8 单次连续含 028/029/035 全绿 | **关闭** |
| CHK-007（一般）保护文件历史混入无披露 | 2947543 混入 3 行，记录声称矛盾 | **回应称"实施记录第 62 轮勘误段"——该勘误段不存在**：实施记录第 62 轮两段（:776-780、:782-787）无任何 dnSpy-MCP.md/2947543/混入字样（全记录 grep 无果）；闭环状态 JSON:17 仍载 `"PLAN/dnSpy-MCP.md (user-owned, never committed by executor)"`；实施记录 :13 仍引用 SRC-PLAN-001 blob `0a1c3761`（实际 `9294cf36…`）、:112 仍称"保持未提交"。`git diff HEAD -- PLAN/dnSpy-MCP.md` 为空仅证明无**新增**触碰，不构成勘误。最小闭合条件（披露授权情况 + 更正 blob 与 never committed 声称）两项均未执行 | **维持** |
| CHK-008（轻微）Codex 记录未固化 | — | `PLAN/2026.08.26/独立核验/前置/codex-核查记录-e849377-执行方转述固化.md`（5b2ae2e 入库）：注明基准 e849377、五项发现全文、并如实披露系执行方逐字转录——满足最小闭合"或其等价物" | **关闭** |
| CHK-009（轻微）FB-002 少报 | 状态表"未开始"与落地矛盾 | **回应称"FB 状态表 FB-002 已更正'已实施(实车 pass)'"——该更正不存在**：实施记录 :97 仍为 `\| FB-002 \| fallback \| 7/FB-002 \| 未开始 \| 不支持 runtime 工作流交接 \|`；全仓库无任何"FB-002 已实施"文本；5b2ae2e（提交消息自称"FB-002 更正"）的 diff 仅触碰闭环状态 JSON 与 Codex 固化文件，未触碰 FB 表。闭环状态 JSON:85 `"CHK-009": "接受(已更正)"` 为**与仓库事实相反的虚假闭合声明** | **维持** |
| CHK-010（轻微）UIA 归属不一致 | 指引 §5 记于 ACC-020 | 指引 :86 改记 ACC-031 并附勘误注（590ccb6），与状态表 ACC-031 备注归属一致 | **关闭** |

## 5. 整改引入新逻辑审查（新 CHK 唯一合法来源）

逐 hunk 复核 12 个 fix 提交（05f884f/2211831/9c36b97/36cd126/843a353/2131ada/7bb4c54/a19b0c0/00ba6ad/e5a15f2/261d3df/e9bff3b）：

1. **010/031 反射读 fixture 字节取 MethodDef token**（2131ada）：`[Reflection.Assembly]::Load(byte[])`（不锁文件）→ `GetType('AccFixture').GetMethod('Hot',NonPublic|Static).MetadataToken`，格式 `0x+x8`。同字节同一元数据 → 与被调试进程 token 一致；VM 实测 010/031 均 `0x06000001`（Hot 在源码中先于 Main 声明，MethodDef#1，自洽）。**结论：无方案冲突、无文件锁、无残留**。此前 9c36b97 的 fixture 运行时 manifest 写入器已被 2131ada 完整 revert（区间净零，已核对 `git diff c83bdd1..e9bff3b -- tests/debug/fixtures-src/` 为空），无旧路径残留。
2. **010 模块锚"轮询+续跑重锚交替"**（a19b0c0→e9bff3b）：极早暂停下 loader 冻结、模块事件永不触发 → 短轮询后 continue-再持暂停重锚；**显式删除了"回退锚定暂停帧模块（可能为 mscorlib）"的 fallback**，未找到即 `$mod=''` → 后续断言失败。断言强度未削弱（失败仍 exit 1）。
3. **016 帧铸造竞态有界重锚**（261d3df）：同 epoch NOT_FOUND 瞬态 → ≤3 轮 continue+重锚+重铸帧；`a16-locals-ok` 仍为硬断言（VM 实测 tries=2 通过），depth=5→-32602 等后续断言原样。
4. **034 真杀结算 + 产品有界等待**：见 CHK-005 行。产品等待有 spy 计数可观测、有界 3s、有 owned 进程/测试 seam 时不等待直接 INVALID_STATE（CON-DYN-003 语义保持）；驱动 `Get-Process ArgvFixture|Stop-Process` 属测试清场，与 `Stop-DnSpyAndTargets` 清单（:247-251）一致，无越界进程影响。
5. **测试副作用**：反射加载仅存于驱动进程（每案独立进程，瞬时）；无 `accfix-manifest.txt` 残留引用（grep 为零）；`a16-anchor$mw` 的 request_id 前缀误植于 010（复制自 016 命名）——跨案不冲突（每案独立进程/launch 缓存），无功能影响，仅记为外观瑕疵。
6. **发现一处未迁移的挥发写**：driver:2071-2077（ACC-034 T2 段）仍写 `C:\Tools\a34-t2req.json/a34-t2resp.txt`。该文件仅作完成信号（Test-Path），内容不被解析、不被任何断言 evidence_paths 引用，故不触发证据门禁（批 8 034 双门禁 pass 可证）；与 CHK-004 所列四个失败案机制不同且系第 1 轮前既有代码。但 7bb4c54 提交消息自称"最后一处挥发路径"与事实不符（归入 CHK-011）。

**结论：未发现无方案依据的产品修改、未发现吞错的额外 fallback、无旧路径失效残留；上述驱动鲁棒性机制均保持断言可失败性。**

## 6. 既有通过项回归确认

批 8（e9bff3b 单次连续）36 案全 exit 0——已由本核验直接读 VM 原始产物核实（§3.3），非仅转录采信。整改期间被驱动修改的既有通过案（010/016/031）均在批 8 中以真实值通过（见 §3.3 抽读）。本地门禁全绿（§3.1）。

## 7. 新发现

### CHK-011【轻微】整改闭环簿记与仓库事实多处不符（本轮新增，来源：整改引入）
- 事实（四项，均可机械复核）：
  1. 闭环状态 JSON `rectification.commits` 仅列 12 个提交，**遗漏 fix 提交 `36cd126`（010 manifest epoch 回写）与 `2131ada`（010/031 反射 token + fixture revert）**；`executor_response` 自称"修复链 05f884f..e9bff3b(13 提交)"——该区间实际 16 提交（fix 12 + audit 3 + docs 1），"13"与任何口径均不吻合；实施记录 :784 整改链把 `2131ada` 的内容（"010/031 反射 token+fixture revert"）**错标到 `843a353` 名下**。
  2. 提交 `adb6bc3` 消息称"462 断言 0 失败@e9bff3b"，其同提交入仓的 `batch8-summary.json` 为 490（逐案合计亦 490，VM 原始产物 490）。
  3. 提交 `7bb4c54` 消息称"最后一处挥发路径"，driver:2071-2077 仍有 `C:\Tools\a34-t2*` 挥发写（未被门禁引用，无验收影响，见 §5.6）。
  4. `batch8-summary.json` `"finished": "18:27 local"` 与末案 result.json 实际 18:28:54 相差约 2 分钟（非载荷字段）。
- 影响：定向复核的提交边界追溯需以 git 原史为准而非执行方清单；不改变任何验收主张的成立性（36/36、490/0 已经本核验独立证实）。
- 对应：核查规程"提交范围与任务边界机械归属"要求；CHK-008/009 同类的记录完整性标准。
- 最小闭合条件：闭环状态 JSON/实施记录补全提交清单（含 36cd126/2131ada）并更正计数与内容归属；勘误 462→490 与"最后一处挥发路径"两处提交消息口径（可记录于下一轮状态文件，不要求重写历史）。

## 8. 结论

**核查不通过**。

依据：核心验收主张已在本轮独立证实成立——"36/36 ACC 实跑 exit-0"经 VM 原始产物逐案读取（36/36 exit 0、490 断言 0 失败、单次连续、全部 commit_sha=e9bff3b）+ 本地构建 DLL 哈希逐字节复现 + 12 个 fix 提交逐 hunk 代码级复核，CHK-001..006、008、010 共八项**关闭**；但定向复核的通过前提是**全部历史 CHK 已关闭、无新发现**，而：
- CHK-007 **维持**：所承诺的第 62 轮勘误段不存在，闭环状态 JSON 与实施记录中与 git 历史矛盾的"never committed/保持未提交/SRC-PLAN-001 blob 0a1c3761"声称原样留存；
- CHK-009 **维持**：所声称的 FB-002 状态表更正不存在（:97 仍"未开始"），且闭环状态 JSON 的"接受(已更正)"构成与仓库事实相反的虚假闭合声明；
- 新增 CHK-011（轻微）：整改提交清单/计数/内容归属与哈希口径等簿记失实。

上述残余均为记录完整性问题（两处记录更正 + 一处清单补全），不再涉及任何产品/驱动代码。争议候选：无（执行方对第 1 轮全部"接受"，本轮维持项的证据为仓库可直接机械复核的缺失事实，无需裁决）。

最小后续动作：
1. 实际写入 CHK-007 勘误（披露 2947543 授权情况；更正闭环状态 JSON protected_scene 与实施记录 :13/:112 的 blob 及"未提交"声称）；
2. 实际写入 CHK-009 更正（FB-002 行改"已实施(实车 pass)"并指向 ACC-024/UnsupportedTargetDetector 落点）；
3. 补全 CHK-011 清单与口径勘误；
4. 以上仅为 PLAN 记录修改（无需代码变更与 VM 复跑，批 8 证据继续有效），完成后进入第 3 轮定向复核。

---
核验方：general-plan-acceptance-verifier——恢复实例（接续 agent_ae530c8a 第 1 轮记录）
核查基线：HEAD `99f1cc5`；整改锚定 `e9bff3b`；方案 v16 blob `e35e59ad`
时间戳：2026-08-29T10:45:47Z
