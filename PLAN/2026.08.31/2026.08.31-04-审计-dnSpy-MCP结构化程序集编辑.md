# dnSpy MCP 结构化程序集编辑总纲方案 审计记录

- 审计性质：**首次审核**（同目录及全仓库范围内不存在与当前总纲精确匹配的任何历史第三方审计或审计处理记录）
- 会话角色：第三方审核方（独立于作者/执行方会话）
- 审计对象：`PLAN/2026.08.31/2026.08.31-02-总纲-dnSpy-MCP结构化程序集编辑.md`
  - 总纲版本：**v0**；文档状态：未审核, 不得实施（满足审核门禁）
  - 总纲 blob：`1024bde949e165ff6f7f83db38f025710ae227f6`
- 来源切片：
  - 需求提炼：`PLAN/2026.08.31/2026.08.31-01-需求提炼-dnSpy-MCP结构化程序集编辑.md`，v0，blob `22574a9054a5016e8a2899f0508d0069e7742843`，切片为第3节正式需求、第4节固定约束、第5节阶段边界、第6节非目标、第7节取舍、第8节 RACC、第9节已知未知项——已逐项核对
  - Spike S01：`PLAN/2026.08.31/技术Spike/S01-private-copy-live-commit/结果.md`，blob `b0bb7f3676d7fdd102daba546c7a9d406de72a9f`
  - Spike S02/S02-R1：`PLAN/2026.08.31/技术Spike/S02-csharp-public-compiler-import/结果.md`，blob `41a47901df4fd406377c1c139227ef1b198d6b17`
  - Spike raw 证据五个 SHA-256 全部与两份结果文档引用一致；`x64/x86-evidence.json` 为含 `s01`、`s02` 分段的合并证据文件，两份结果文档引用同文件同 SHA 属实非错误
  - Spike 分支 `spike/structured-edit-feasibility-20260831`，提交 `63058f6`→`f83f3d9`→`3113942` 顺序存在，基于主线 `96a8078`
- 适用历史审计/处理记录：无。`PLAN/2026.08.26/` 下 15 份审计及处理记录全部属于方案 `dnSpy-MCP托管动态调试增强方案`，与当前总纲不匹配，不构成本总纲的前轮审计祖先
- 本地审核基线 HEAD：`96a8078e7128560d19eb63725aa2f09279d9b464`（与总纲 §1.4 声明的当前主线基准一致）
  - 边界内工作区状态：`PLAN/dnSpy-MCP.md` 已修改未提交；`PLAN/2026.08.26/独立核验/round-8/`、`PLAN/2026.08.31/`、`tests/spikes/` 未跟踪。代码文件均在 HEAD 未修改状态，工作区差异不影响任何代码事实核对
- 审核模式：**全面审核**（首次审核；完成机械扫描、全部评审项语义审查与反例检查）
- 三类范围：
  - 决策关键路径：总纲、需求提炼、两份 Spike 结果与 raw 证据、`Tools/McpToolRegistry.cs`、`Tools/IMcpToolProvider.cs`、`Debugger/StaticWriteGate.cs`、`McpTools.IL.cs`、`McpServer.cs`、`Transport/TransportLimits.cs`、`McpDocumentationResources.cs`、`BepInExResources.cs`、`Debugger/SettingsRootLease.cs`（路径策略存在性）、`dnSpy.Extension.MCP.csproj` 与 `/home/adminn/tmp/dnSpyEx/DnSpyCommon.props`（TFM）、`tests/run-verify-local.sh`、`tests/python/`、`tests/fixtures/run-tests.ps1`、`tests/debug/run-debug-tests.ps1`
  - 上下文路径（只辅助理解）：`McpTools.cs`、`Debugger/DebugSessionService.cs`、`dnspy_mcp/` Python 客户端与 stdio 桥、`PLAN/2026.08.26/`（仅用于确认审计对象不匹配）、`PLAN/dnSpy-MCP.md`
  - 排除范围：dnSpy 上游与 `dnSpy.AsmEditor` 源码细节、其他扩展及其数据、外部系统、作者 GrillMe 会话记录与自评文件（首次审核不索取）
- 内容指纹（决策关键文件 SHA-256 前16位）：`2010a5be` McpToolRegistry.cs、`0135a79f` IMcpToolProvider.cs、`4277d527` StaticWriteGate.cs、`4d414088` McpTools.IL.cs、`abcdbdc1` McpServer.cs、`3021891f` TransportLimits.cs、`41e59dd3` McpDocumentationResources.cs、`fa81c40b` BepInExResources.cs、`99df105f` run-verify-local.sh、`73989f47` run-debug-tests.ps1
- 轮次：第 1 轮
- 结论：**存在不同意见**（AUD-001，低严重度；无阻断、无熔断）

## 1. 逐项覆盖清单（全面审核）

### 1.1 身份与门禁

| 项 | 结果 |
|---|---|
| 总纲路径/版本/blob 核对 | 一致；状态为"未审核, 不得实施"，满足审核前提 |
| 来源身份（需求 v0 blob） | 一致 `22574a90…` |
| Spike 结果身份与分支提交 | 一致；分支三提交顺序核对通过 |
| Spike 证据 SHA | 五个全部一致 |
| 首次审核判定 | 同目录无本总纲匹配的历史审计；2026.08.26 审计属另一方案 |
| 稳定 ID 纪律 | 总纲声明 OUT/ACC 不得重编号复用；v0 内 OUT-001..009、ACC-001..030、P01..P09 无重复 |

### 1.2 机械扫描

| 项 | 结果 |
|---|---|
| 需求计数 27 REQ / 28 CON / 18 NON / 30 RACC | 与需求文档逐条清点一致；总纲 §1.1 声明相符 |
| 每个 REQ 至少一个 RACC | 27/27 通过（RACC 引用列逐条归并） |
| ACC↔RACC 一一对应 | ACC-001..030 分别源引 RACC-001..030，30/30，唯一归属成立 |
| §9.2 CON 唯一主责 | 28 个 CON 全部唯一归属（CON-025 为复合约束，被显式拆为"公开编译前端部分"归 P05 与"自有导入部分"归 P06，语义无重叠，可接受） |
| §9.3 NON 覆盖 | 18/18 有主责 Pxx 与验收/排除证据 ACC，引用的 ACC 全部存在 |
| §9.1 反向索引 | **发现 AUD-001**：REQ-021 行漏 ACC-007；REQ-023 行 OUT 列漏 OUT-006/P06、OUT-009/P09（详见意见） |

### 1.3 需求覆盖与追溯语义

| 项 | 结果 |
|---|---|
| 30 个 ACC 对 30 个 RACC 的语义充分性 | 逐条比对 RACC 可观察预期与 ACC 实际动作/预期/判定边界，全部覆盖；含 ACC-001 对 NON-018 的"能力失败不算通过"、ACC-003 对 P02 不发布无检查点产品路径的隔离、ACC-016 对严格不可恢复样本的诚实边界、ACC-024 重试/撤销/导出阻断 |
| ACC-028 无 REQ 行 | RACC-028 本身仅引 CON-001、CON-026，无 REQ 引用，§9.1（REQ 索引）不含 ACC-028 属结构性正确；其追溯经 §9.2 P09 行与 §9.3 NON-001 行闭合 |
| REQ-007 额外映射 ACC-024 | 超集映射，由 REQ-007"提交成功后建立对应检查点"条款支撑，合理 |
| 阶段边界与 Pxx 顺序 | 需求 §5 五阶段与 P01-P09 内容对应：阶段1=P01+P02+P03（UI 浏览移至 P09）、阶段2=P04、阶段3=P05+P06、阶段4=P07、阶段5=P08+P09 汇总。实质能力顺序未颠倒；UI 浏览后移不破坏任何 RACC（RACC-018 整体在 P09/ACC-018，P04-P08 无 ACC 依赖 UI）。组织形式源自总纲 §1.2 所引阶段7最终输出，该输出仅由总纲自身持久化，属可接受的首版沉淀方式 |

### 1.4 代码事实一致性（总纲 §1.4 逐条）

| 总纲声明 | 核对结果 |
|---|---|
| 当前主线基准 `96a8078e…` | 与基线 HEAD 一致 |
| `McpToolRegistry.ExecuteTool` 只传工具名和参数 | 属实：`ExecuteTool(string toolName, Dictionary<string, object>? arguments)` |
| 六个旧写工具由 `StaticWriteGate` 守卫 | 属实：`GatedTools` 恰为 patch_method_il、force_return、nop_method、revert_method_il、rename_symbol_by_token、save_assembly |
| IL 修改维护独立 `ilSnapshots` | 属实：`McpTools.IL.cs` 独立字典，属 P03 必迁的第二套状态 |
| `IMcpToolProvider` 聚合已实施 | 属实 |
| dump 缺陷已在主线修复 | 属实：HEAD 提交即 `fix debug dumps across dnSpy restarts`；P01 只建回归门禁（ACC-027）与 REQ-027 一致 |
| 已实施并验证的既有机制不替换 | 与 CON-001 一致；调试状态机/ArtifactStoreLedger/双通道/Python 客户端等均在代码中存在 |

### 1.5 常量、架构、契约与安全边界

| 项 | 结果 |
|---|---|
| §3.2 常量来源 | 逐项核对：1 MiB=CON-012（`TransportLimits.MaxBodyBytes=1048576` 属实）、16 会话=现有 `MaxTransportSessions=16` 属实、600 秒=RACC-002、`dnspy.edit.op.v1`=CON-010、三态=REQ-011/CON-023、`LIVE_STATE_UNKNOWN`=CON-020、`committed_without_checkpoint`=REQ-024、scope=loaded_modules=CON-013、VMware/VirtualBox=REQ-015/CON-014、Embedded Portable PDB=CON-015、net48/net10.0-windows=CON-018（`DnSpyCommon.props` 属实） |
| §4 架构与代码事实 | 权威上下文缺口、协调器单事务、私有副本、短临界区、检查点原子替换均与代码现状和 S01/S02 结论一致，无方向性冲突 |
| §4.3 状态与错误类别 | 覆盖 REQ-019 要求区分的冲突/验证失败/部分提交/能力不可用/导出阻断；枚举名与错误码字符串正确下放 P01/P02 |
| §5 路径与安全 | `ArtifactRoot`/`AllowedSampleRoot` 现有策略存在（`SettingsRootLease.cs`）；服务端生成 ID、原子替换、不可信输入不实例化、VM 门禁共享判定均与基线一致 |
| §6 MCP/Python/UI 契约 | 与 CON-007/CON-008/CON-024/RACC-018/RACC-002 一致；不虚构工具名，名称冻结下放各 Pxx |
| 14 个 resources | 属实：8 个 `dnspy://docs/*` + 6 个 `bepinex://docs/*`，CON-027 与 §7 fallback 表述准确 |

### 1.6 Pxx、验收协议与交付

| 项 | 结果 |
|---|---|
| OUT 表职责/顺序/前置/门禁 | P01-P09 无职责重叠；OUT-006 前置含 S02-R1 已完成属实；每个 OUT 的"为后续解除的门禁"构成闭合链 |
| 逐篇推进约束 | §8 明确不得提前编写全部子方案；与 CON-028 一致 |
| §10.1 既有测试命令 | 四个脚本存在；`EXT_DIR/DNSPY_DIR` 环境变量契约与脚本一致；`tests/python` 可被 unittest discover；现有 debug 套件独立使用 ACC-001..036 编号，总纲以 `EDIT-ACC-xxx` 文件名、`suite=structured-edit` 字段消歧，处理正确 |
| 新测试工具契约 | 冻结位置/命名/退出码/证据字段而不虚构命令，实际命令由首建 Pxx 补入，边界合理 |
| §10.2 通过/失败/被阻断 | "未实现"不得记为被阻断、不得以旁路改写为通过、允许检查范围明确 |
| §7 Fallback | 8 项全部有基线依据（CON-014、CON-007、REQ-026/CON-012、REQ-014、CON-023、CON-003+NON-001..003、CON-020+REQ-024、CON-027），无凭空新增；`unverified_drift` 仅诊断的限定与 CON-023 一致 |
| 需求 §9 已知风险 11 项 | 1实时提交→S01+P02 反向操作；2导入复杂度→S02+P05/P06 分离；3大程序集内存→ACC-001 判定边界+NON-018；4外部修改识别→指纹+ACC-001/ACC-025；5工具链漂移→ACC-013；6强名称→ACC-016；7VM检测局限→ACC-017+NON-013；8ZCode resources→CON-027+ACC-022；9大资源→ACC-008；10dump→ACC-027；11进程崩溃窗口→NON-018/NON-010 已接受代价+CON-009 原子替换+ACC-024/ACC-029 验证，基线未要求崩溃注入 ACC。全部有落点 |
| 范围外错误阻断 | §2.2"只记被阻断并保存证据，不扩大实施范围"规则明确；排除范围完整包含 NON-001..018 |
| 无依据过度设计 | 未发现超出基线的机制；检查点防篡改（HMAC/哈希链/DPAPI）、数据库、分块上传等均被显式排除 |
| 可交接性 | §12 交接包清单完整（方案身份、评审、提交、注册清单、ACC 证据、双 TFM、架构测试、清理、文档、门禁结论） |
| 迁移/清理策略 | 旧六工具在 P03 单点迁移、`ilSnapshots` 与保存旁路在 P03 验收前清除、P09 不兜底前序缺口，链路明确 |

## 2. 审计意见

### AUD-001（低严重度）：§9.1 REQ→OUT/ACC 反向索引两行精度缺陷

- 首次轮次：第 1 轮
- 来源层级：总纲 §9.1（双向追溯表），对照需求文档 §8 RACC 引用列
- 反例与证据：
  1. RACC-007 引用 `REQ-006、REQ-021`，对应 ACC-007（P08）。总纲 §9.1 的 REQ-021 行仅列 `OUT-001/P01、OUT-009/P09 | ACC-023`，未列 ACC-007/OUT-008/P08。仅按 §9.1 审计 REQ-021（不可信输入）覆盖时会漏掉 P08 的资源对象不实例化验收。
  2. RACC-005 引用 `REQ-004、REQ-023`，对应 ACC-005（负责人 P06）；RACC-023 引用 `REQ-021、REQ-023`，对应 ACC-023（负责人 P09）。总纲 §9.1 的 REQ-023 行 OUT 列仅写 `OUT-005/P05`，未含其两个 ACC 的实际负责方 OUT-006/P06 与 OUT-009/P09。
- 影响：纯索引精度问题。ACC-005、ACC-007、ACC-023 在 §11 中的定义、归属与内容均正确且完整，不存在需求覆盖缺口；缺陷只影响追溯表的机器可读完整性与按表审计的可靠性。
- 最小闭合条件：作者升版时修正 §9.1 两行——REQ-021 行补 `ACC-007`（OUT-008/P08），REQ-023 行 OUT 列补 `OUT-006/P06、OUT-009/P09`；不改变任何 ACC 定义、归属或需求语义。
- 发现来源分类：机械扫描（§9.1 与需求文档 §8 RACC 引用列逐行交叉核对）发现的文档一致性缺陷，非语义审查缺陷。

## 3. 审计边界说明

- 本次审核为本地执行，未访问 dnSpy 上游仓库内容（仅在 `/home/adminn/tmp/dnSpyEx/DnSpyCommon.props` 核对 TFM 一行），未执行任何测试，未修改总纲、需求或代码。
- 阶段7最终输出（"结束评估"与"一个总纲加九个子方案"组织形式、RACC 唯一归属、已消除门禁）无独立本地记录，本审计按首次审核规则不索取作者会话记录，仅核对总纲对该输出的沉淀与需求基线之间无矛盾。
- 本记录创建后不可修改；后续复审须新建记录。
