# P01 公共契约、权威调用上下文、VM 执行门禁与调试基线审计处理记录

- 记录性质：不可变审计处理记录；首次审核后的作者自评闭环
- 处理轮次：第 1 轮
- 会话角色：作者/执行方
- 原审计记录：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.08.31/2026.08.31-08-审计-P01-公共契约权威调用上下文VM执行门禁与调试基线.md`
- 原审计记录 blob：`96ccaad99c2a1ae0faaedf308999c8302006addb`
- 处理前方案：`2026.08.31-06-P01-公共契约权威调用上下文VM执行门禁与调试基线.md` v1，blob `8de0a6add57b0a5d867b5dedbcf49c470140ff21`，状态“未审核，不得实施”
- 处理后方案：同路径 v2，blob `03123bb5be7394651215571b10f929a17f519ad0`，状态“已审核，可以实施”
- 所属总纲：`2026.08.31-02-总纲-dnSpy-MCP结构化程序集编辑.md` v1，blob `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a`
- 需求基线：`2026.08.31-01-需求提炼-dnSpy-MCP结构化程序集编辑.md` v0，blob `22574a9054a5016e8a2899f0508d0069e7742843`
- 所属产出：`OUT-001 / P01`
- 本地审核基线 HEAD：`96a8078e7128560d19eb63725aa2f09279d9b464`
- 处理日期：2026-08-31

## 1. 定位与身份门禁

| 门禁 | 核验 | 结论 |
|---|---|---|
| 审计对象 | 审计绑定 P01 v1/blob `8de0a6...`，与处理前文件身份一致 | 通过 |
| 审计性质 | 首次第三方全面审核，第 1 轮；记录已持久化且结论不是“审计被阻断”或“结果持久化被阻断” | 通过 |
| 总纲与需求 | 总纲 v1/blob `bcc7b2...`、需求 v0/blob `22574a...` 与审计和方案一致；引用 ID 未改变、删除、复用或产生歧义 | 通过 |
| Pxx 顺序 | OUT-001 唯一归属 P01，P01 无前序子方案；同目录仍无 P02-P09 子方案或实施记录 | 通过 |
| 总纲审核链 | 总纲首次审计/处理记录及 S01、S02/S02-R1 身份均与审计核验一致 | 通过 |
| 角色 | 当前会话一直是作者/执行方，没有执行本方案的第三方审核或独立核验 | 通过 |
| 实施授权 | 当前会话只有 P01 子方案写入/自评和本次审核引入授权；没有 P01 实施提示词或进行中实施记录 | 未授予 |

原审计记录保持不可变；本轮只修改 P01 子方案并新增本处理记录，没有修改需求、总纲、作者自评、审计记录或业务代码。

## 2. 本地基线与决策关键路径漂移

审计记录绑定的 HEAD 仍为 `96a8078e7128560d19eb63725aa2f09279d9b464`。边界内工作区状态仍为既有 `PLAN/dnSpy-MCP.md` 修改，以及未跟踪 `PLAN/2026.08.26/独立核验/round-8/`、`PLAN/2026.08.31/`、`tests/spikes/`；这些变化没有进入 P01 产品实现范围。

以下审计决策关键路径的当前 SHA-256 前 16 位与审计记录逐项相同：

| 路径组 | 当前指纹 | 漂移结论 |
|---|---|---|
| `McpServer.cs` | `abcdbdc123fba6f9` | 无漂移 |
| `IMcpToolProvider` / registry / static provider | `0135a79f37f9d144` / `2010a5be122a6fb1` / `61b357a010f4a7a2` | 无漂移 |
| debug session / protocol / provider | `3eb5a8fe3f9ce10a` / `c5013c4f13945e38` / `14ce221319c3e8b0` | 无漂移 |
| settings page / XAML | `1d121db6bea51422` / `d7a782b22421c324` | 无漂移 |
| transport limits | `3021891fe72a0f4a` | 无漂移 |
| debug/local/fixture 测试驱动 | `73989f475a535ea7` / `99df105fb841f629` / `ddd1d66349b5b53d` | 无漂移 |
| `CLAUDE.md` / csproj | `9eba301553fed677` / `da391d5841340fc7` | 无漂移 |

上下文和排除范围变化不影响方案依据；不存在需要使审计结论失效的接口、数据、行为、约束或验收漂移。

## 3. AUD 逐项处理

### AUD-001：OUT-001-H01/H02 缺少钉死的执行载体与结果记录方式

- 处理结论：同意。
- 严重度复核：低；问题属于当前子方案测试契约精度，不是需求、总纲、架构或产品权限变化。
- 理由：P01 v1 的 §8.1 只有三个 `EDIT-ACC-*` 案例，§4.5 未列上下文/生命周期观察面，而 §8.4 又要求 H01/H02 独立通过。实施者确实需要自行决定承载位置、证据文件和 summary 聚合方式，不能据此形成稳定交接证据。
- 规范性证据：总纲 §10.1 固定以 `EDIT-ACC-xxx` 案例、案例证据目录、`summary.json` 和退出码产证；总纲没有为 H01/H02 分配新的全局 ACC，因此最小闭合方式应复用 `EDIT-ACC-030` 而不是增设第四个案例/ACC。
- 代码证据：当前 Python client 已提供 `open_request`、`raw_request`、initialize 和 DELETE 能力，可由 P01 测试驱动发出真实传输动作；现有 DNMCP_TEST 未广告测试入口可扩充只读观察面，不需要产品 MCP 工具。
- 实际修改：
  - P01 v2 §4.5：冻结 DNMCP_TEST probe 的上下文观察、close reason/通知次数/活动会话数观察和一次性观察者异常注入；明确生产限制和 probe 不得伪造关闭。
  - P01 v2 IMP-005：明确 `EDIT-ACC-030` 唯一承载 H01/H02 两个独立命名子检查。
  - P01 v2 §8.1：冻结 `outcome_checks` 输入、`out-001-h01.json`/`out-001-h02.json` 路径、summary 子检查 shape，以及 case/subcheck 的退出码聚合。
  - P01 v2 §8.4：明确真实客户端触发、probe 只观察、各自证据文件和独立状态。
  - P01 头部和版本记录：升为 v2；完整复评通过后状态改为“已审核，可以实施”。
- 不修改：`IMP-001..007`、全局 `ACC-017/027/030`、`OUT-001-H01/H02` 身份、三个案例文件数量、产品工具数量、P01-P09 顺序和职责。
- 最小闭合条件：已满足。执行载体、证据路径、summary 独立记录和整体退出码均唯一确定。
- 复审请求：无。修改后由作者完成全方案复查，且未命中再次第三方审核条件。

## 4. 实际 diff 与影响分析

### 4.1 实际 diff 清单

| 位置 | 变更 | 分类 |
|---|---|---|
| 头部/版本记录 | v1→v2；记录 AUD-001；状态在闭环后改为可实施 | 审核链和状态 |
| §4.5 | 增加测试模式 context/lifecycle 观察面及生产隔离规则 | 测试契约细化 |
| IMP-005 | `EDIT-ACC-030` 固定承载 H01/H02 | 实施边界细化 |
| §8.1 | 新增 outcome_checks 输入、两个证据文件、summary shape、聚合退出码 | 验收证据契约细化 |
| §8.4 | 明确真实动作与 probe 分工、独立证据/状态 | 验收动作细化 |

方案由 v1 的 404 行变为 v2 的 422 行。没有新增/删除 IMP、全局 ACC、Pxx、产品 MCP 工具或持久数据。

### 4.2 受影响的既有通过项

原审计清单第 14、15、18 项中涉及 H01/H02 执行载体和结果记录的保留意见已经闭合：

- 后序依赖与独立闭环：H01/H02 现在可随 `EDIT-ACC-030` 在 P01 内独立执行；
- ACC 契约质量：两个交接检查有精确证据文件、状态和退出码影响；
- 可交接实施性：实施者不再需要发明 probe 观察面或 summary 表达。

三项由“随 AUD-001 闭合后通过”变为通过。

### 4.3 未受影响并继承的通过项

原审计其余通过结论全部继承，包括对象身份、代码事实、公共调用上下文、生命周期语义、VM 分类、共享 gate、capabilities、数据/迁移、兼容性、Pxx 边界、追溯、fallback、工作量、双架构和拒绝路径副作用。

### 4.4 全局不变量

- 需求 v0 和总纲 v1 的规范含义不变。
- `OUT-001/P01` 目标、入口、产出和后序解锁条件不变。
- `IMP-001..007` 数量、顺序和职责不变。
- 全局 `ACC-017/027/030` 定义及唯一归属不变；H01/H02 仍不是全局 ACC。
- 权威会话来源、VM 许可、进程期本地覆盖、unknown fail-closed 和三入口共享 gate 不变。
- 无新产品工具、持久化、迁移、fallback、网络/逃逸检测或后序 Pxx 实现。
- P01 仍可独立实施和验收，不依赖 P02-P09。

## 5. 作者引入后的完整复评

| 维度 | 复评结论 |
|---|---|
| 来源和范围 | 全部变更落在 OUT-001/P01、总纲 §10.1 和 AUD-001 最小闭合范围，没有把建议升级为上层需求 |
| ID 和追溯 | IMP、ACC、OUT、REQ/CON/NON/RACC 均稳定唯一；H01/H02 明示非全局 ACC |
| 子方案职责 | 不进入事务、检查点、编辑、导出或 P09 UI；测试 probe 仅服务 P01 证据 |
| 前后序 | P01 无前序；H01/H02/三 ACC 均不依赖后序；P02 只消费 OUT-001 通过结果 |
| 接口和数据 | 产品接口、capabilities 契约、持久数据和迁移均未因 AUD-001 改变；新增 shape 仅属于测试 summary |
| 实施可行性 | Python client 已有原始 HTTP/SSE 请求、initialize、DELETE 基础；新 observer/probe 明确归属 IMP-002/005 |
| 验收 | H01/H02 与 ACC-030 同一次命令但独立记录；case status 不冒充 H 状态；总退出码确保 H 失败不能被隐藏 |
| 兼容性 | 总纲既有 `summary.json` 必有字段保留，只增加 `outcome_checks`；仍只有三个稳定案例 |
| 异常边界 | probe 不能伪造真实关闭；关闭后由新会话读取快照；未实现仍不能记 blocked |
| 资源和工作量 | 只增加两个小型 JSON 证据和内存测试快照，不新增持久产品状态或未归属设施 |
| fallback | 4 个既有 fallback 原样保留；没有以测试 probe 形成生产 fallback |
| 可交接性 | 输入、命令、动作、文件名、summary 字段、状态聚合和证据读取方全部确定 |

机械复核结果：7 个 IMP、3 个 P01 全局 ACC、2 个 OUT-001 交接检查保持唯一；没有 `TODO`、`TBD`、占位路径、`ACC-030-A/B/C` 残留、后序依赖或未声明 fallback。修改后的方案 blob 为 `03123bb5be7394651215571b10f929a17f519ad0`。

## 6. 再次第三方审核触发判定

| 触发条件 | 判定 | 证据和影响 |
|---|---|---|
| 跨两个以上独立项目或改变多业务共享契约 | 未命中 | 只修改本仓库 P01 测试证据契约；产品/MCP/Python 公共契约不变 |
| 不可逆或高风险迁移、权限、安全、隐私、外部关键契约 | 未命中 | 无迁移/持久化；权威所有权和 VM 安全语义不变；测试 snapshot 可删除 |
| 总体架构、核心数据流、并发/一致性、恢复或切换策略改变 | 未命中 | 只钉死既定 H01/H02 的测试承载和记录；生命周期并发设计未改 |
| 核心路径难回滚或影响无法可靠限定 | 未命中 | diff 仅 5 个文档位置；可回滚到 v1，影响完全限定为 EDIT-ACC-030 证据 |
| 修改后仍有多个有证据的可行方向 | 未命中 | 审计给出的最小闭合方向与总纲案例协议唯一相容，无剩余取舍 |
| 用户或生效项目约定要求再次审核 | 未命中 | 要求默认作者自评闭环；没有强制二次审核指令 |

因此，本轮不需要再次第三方审核，不输出定向或全面复审模式。此记录同时构成“首次审核后的作者自评闭环”。

## 7. 最终状态与下一动作

- 审核性质：首次第三方全面审核结果已引入；一条低风险意见已接受并闭合。
- 处理后方案路径：`/home/adminn/projects/dnSpy.Extension.MCP/PLAN/2026.08.31/2026.08.31-06-P01-公共契约权威调用上下文VM执行门禁与调试基线.md`
- 处理后版本/blob：v2 / `03123bb5be7394651215571b10f929a17f519ad0`
- 处理后状态：已审核，可以实施。
- 审核循环：第 1 轮即闭合；没有同根因复开，不触发熔断。
- 实施授权：未授予。
- 下一动作：已取得实施资格，等待用户粘贴 P01 实施提示词；不得因本记录自动实施。

结论：方案已修改并由作者自评闭环，无需再次第三方审核，状态为已审核，可以实施。
