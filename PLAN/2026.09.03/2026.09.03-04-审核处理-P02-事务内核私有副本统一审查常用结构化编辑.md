# P02 事务内核、私有副本、统一审查、常用结构化编辑审核处理记录

- 会话角色：作者/执行方
- 处理日期：2026-09-03
- 处理轮次：第 1 轮首次审核引入
- 原审计：`2026.09.03-03-审核-P02-事务内核私有副本统一审查常用结构化编辑.md`，blob `48a16ad42fef7900043a561ac9bfb13a18842d4e`
- 处理前方案：`2026.09.03-01-实施子方案-P02-事务内核私有副本统一审查常用结构化编辑.md` v0，blob `4c82a626f7f8b0c4b67ef4bf071090d1e7bd3599`
- 处理后方案：同路径 v1，blob `5c2714c7785215b755f885ec3286290809ca9117`
- 总纲/需求：v1 `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a` / v0 `22574a9054a5016e8a2899f0508d0069e7742843`
- 本地基线：`feature/p01-public-contract-vm-gate` @ `236d023c15990bd8240f0f8af98c8b8f8cad3c04`
- 处理结论：全部意见闭合到方案 v1；因修改触及公开契约、并发一致性和实时恢复语义，进入再次第三方全面审核

## 1. 身份与漂移

审计记录、被审方案、总纲、需求、P01 交付和 S01 身份均与审计绑定一致。审计后只有审计者新增不可变 03 记录、作者修改 01 方案并新增本文；决策关键代码和 HEAD 未变化，三个受保护现场未触碰。审计记录保持不可变。

## 2. 逐项处理

| AUD | 结论 | 理由、修改位置与闭合证据 |
|---|---|---|
| AUD-001 | 同意 | v1 §4.1 冻结五工具字段/required/输出和严格 schema 通则；§4.2 冻结 TypeSig、attribute mask/default、patch/null/remove；§4.3 逐项冻结 22 kind payload、owner/target、索引、accessor、删除和 success；§7 增加 golden/负例矩阵。 |
| AUD-002 | 同意 | v1 §4.4 增加 IL CFG/stack/ret/call/branch/leave/MaxStack/prefix/locals/args、EH、signature/generic/semantics/owner/入站引用硬验证；明确硬错误不可 risk 确认；§7 列固定失败 fixture。 |
| AUD-003 | 部分同意 | 同意身份/指纹缺口，不采用“每次验证后替换工作对象图”的假设。v1 §3.4 冻结单一私有对象图；验证 reload 为一次性图；基线 token 与新 obj ID 分治、tombstone/失败恢复；规范 length-prefix 指纹与 metadata-row 投影，不靠名称重绑定。连续 add→update/link/remove、重复名称和指纹敏感性进入 ACC-003。 |
| AUD-004 | 同意 | v1 §3.2 冻结唯一 monitor+operation lease、generation/cancelled、所有工具/close/timer/test apply 的线性化点、UI close 不等待、in-flight 取消发布、600000ms 等号和旧 close generation 隔离；同 ID pending 合并/异载荷拒绝；§7 固定六类屏障并发测试。 |
| AUD-005 | 同意 | v1 §3.3a 冻结 operation/object/bytes/diff/cache/body/live plan/response 上限和 `EDIT_CAPACITY_EXCEEDED`；Dispatcher 外预编译、内二次指纹和至多 256 个常量步骤；§7 固定每个上限边界/超一与最大 plan 证据。 |
| AUD-006 | 同意 | v1 §7 固定 22-kind CRUD 矩阵、硬错误集合、fault manifest 集合等价、容量矩阵和 dynamic passed/failed/blocked/not_applicable/清理矩阵；ACC-003/009/010/026 逐项收紧，NON-001 明确查无接口和原始字段 `-32602`。 |
| AUD-007 | 同意 | 修正歧义句。v1 §5 冻结成功 `ok=true,state=reviewed,live=pre-apply` 且 review 保留；正向失败恢复成功 `ok=false,state=reviewed`；反向失败 `ok=false,state=live_state_unknown`；明确不产生 committed/checkpoint/export/Undo。 |

AUD-003 的部分同意满足其最小闭合目标：问题要求稳定连续寻址和规范指纹，不强制替换私有图；v1 选择更小且可验收的单对象图方案，无未解决争议。

## 3. 实际 diff、继承项和全局不变量

实际契约 diff：v0→v1 增加逐工具/逐操作 schema、TypeSig/attributes/update/remove、硬验证器、单一线性化协议、资源上限、规范身份/指纹、动态结果可用性、apply/restore 三路径及固定测试矩阵；IMP 数和六个 ACC ID 均未改变。

受影响既有项：方案 §3.1-3.4、§4、§5、IMP-001/002/004/005/006/007/008、ACC-001/002/003/009/010/026、审核重点。

继承未变：总纲/需求、OUT-002/P02 目标与 P01 前置、五产品工具名称、P02/P03 顺序、VM/idle 门禁、双 TFM/双架构、旧工具迁移唯一归 P03、所有 NON 和允许 fallback。

全局不变量：实时模块在产品 P02 工具下不变；全进程单事务；owner 只来自 P01；结构错误硬拒绝；测试实时应用同调用恢复；unknown 唯一阻断；不覆盖源样本；不引用 AsmEditor；无产品 commit/checkpoint/export/Undo。

## 4. 修改后作者复评与需求保真

范围、Pxx/IMP/ACC 双向追溯、前后序、数据/接口、状态、所有权、并发、资源、结构验证、动态验证、恢复、错误、兼容、测试和 fallback 已从头复核。实现者不再需要裁决审计列出的方向问题。

- 应承接数：6
- 完整数：6
- 未落点数：0
- 语义弱化数：0
- 不可验收数：0
- 异常 ID：无

RACC-003 由完整 22-kind/硬验证矩阵恢复；RACC-009 由稳定 fault manifest 和集合等价恢复可验收性；RACC-010 由真实四态动态矩阵恢复。其余三项保持完整。

## 5. 再次审核判定与请求

命中再次审核条件：v1 新增并冻结公开工具/操作 schema，改变审核基线下未闭合的并发线性化、资源限制和实时失败恢复契约；这些是 P02 核心数据流与一致性边界，错误会影响所有后续 P03-P08，不能仅以局部文字修正自行闭环。回滚能力是回退到 v0，但 v0 已被审计判定不可实施。

下一审核模式：全面复审。理由：七项修改横跨全部六个 ACC、五工具契约、全部 22 kind、协调器并发、结构验证、动态验证和实时恢复，无法可靠限定为少量章节/不变量的定向范围。

全面复审请求：

- 基准：首次审计 03 及 v0→v1 实际 diff；逐项确认 AUD-001..007 的最小闭合条件。
- 纳入：方案全文，特别是 §3.1-3.4、§4、§5、§6/7；全部六个 ACC、九个 IMP、五工具、22 kind、并发/容量/动态/fault 矩阵。
- 继承但仍核对：OUT-001 前置、P02/P03 边界、总纲/需求保真、NON/fallback、双架构与回归。
- 待回答：公开契约是否唯一可实现；硬验证/身份指纹是否充分；线性化是否无 UI deadlock/结束后发布；容量/短临界区是否可证明；六个 ACC 是否独立可验收。
- 通过条件：需求保真 6/6，后三项为 0；AUD-001..007 均闭合且无新中高风险意见；方案可直接实施、无后序依赖或未声明 fallback。

方案状态保持“未审核, 不得实施”；实施授权继续有效但在复审通过并由作者引入前不使用。
