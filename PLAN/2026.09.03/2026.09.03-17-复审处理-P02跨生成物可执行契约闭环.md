# P02 v5 跨生成物可执行契约闭环复审结果处理

- 记录版本：v0（创建后不可修改）
- 日期：2026-09-03
- 会话角色：作者/执行方
- 原复审：`2026.09.03-16-复审-P02跨生成物可执行契约闭环.md`，blob `44343ede89aaafa3cb3839ab7237301e8ae9119d`
- 被审核方案：P02 v5，blob `38e50065065c5505e5c6561a5ef99aabd7bce3ab`
- 审核性质：上游契约 Spike 后的独立全面复审结果引入
- 结论：接受可复现反例；保持“未审核, 不得实施”，继续执行同一解除任务但不得进入业务实现

## 逐项处理

| 项 | 处理 | 依据与后续唯一落点 |
|---|---|---|
| AUD-001 | 同意，未闭合 | TypeSig 补 generic arity/context；attributes 增加非法稀疏位向量和服务器 bitmask 规则；u8 修为 UInt64 全域；生成完整 dnlib opcode→OperandType 依赖事实。 |
| AUD-002/004/007/008 | 同意继承闭合 | 不修改其既有契约。 |
| AUD-003 | 同意，未闭合 | recipe 改为 exact container/table-or-stream ID/region selector/byte mutation/independent slice readback/restore；reorder 明确作用于规范投影枚举器，不能是回显标签或 no-op。 |
| AUD-005 | 同意，未闭合 | capacity 必须消费 P01 `ResponseBudget.MaxResponseBytes=8388608`，同时计算 payload、JSON text 再转义、structuredContent 和 JSON-RPC 外层的完整上界；不能只看内部 slot。 |
| AUD-006 | 同意，未闭合 | 单调用改为 manifest/oracle 全集相等、actual 为有序前缀、covered 仅当次 armed row；跨 240 次的 union 由无服务器隐藏状态的 runner 聚合契约负责。动态 success 只允许 not_requested/not_applicable/passed，error 只允许 blocked/failed，并按 phase 约束 artifact/cleanup。 |
| fidelity/validator | 同意，未闭合 | 建立逐 RACC 完整 machine acceptance cases；每个 evidence category 非空且逐 case 校验，覆盖 RACC-002 和 RACC-026 全语义；增加稀疏 mask、u8、dynamic envelope、8 MiB 和删关键 case 变异。 |
| 报告身份 | 同意 | contract-index 只绑定生成契约，不再声称绑定运行报告；报告和三个验证脚本由独立 evidence index 绑定，并保证有序输出。 |

需求保真暂接受第三方的 `6/3/0/1/2`，异常为 `RACC-003/009/010`。解除证据重新通过且新一轮独立全面复审闭合前，不恢复实施资格。

本记录没有修改业务代码、总纲、需求或 Pxx 边界；实施授权保持进行中，`IMPLEMENT` 未进入。
