# P02 第 2 次全面复审处理记录

- 记录版本：v0（创建后不可修改）
- 日期：2026-09-03
- 会话角色：作者/执行方
- 原复审：`2026.09.03-07-复审-P02-事务内核私有副本统一审查常用结构化编辑.md`，blob `878300eadfd54009de2f2df2f72fa5a47af7f2be`
- 处理前方案：`2026.09.03-01-实施子方案-P02-事务内核私有副本统一审查常用结构化编辑.md` v2，blob `affc4f7346395fda064390366431cdd6f8fabeb3`（原文已写入 Git 对象库，可由 `git cat-file blob` 恢复）
- 处理后方案：同路径 v3，blob `c18dc7e2cd6b73f29a3045a8fbddf5760347528b`
- 总纲/需求：v1 `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a` / v0 `22574a9054a5016e8a2899f0508d0069e7742843`
- 分支/HEAD：`feature/p01-public-contract-vm-gate` / `236d023c15990bd8240f0f8af98c8b8f8cad3c04`
- 审核性质：首次审核链第 2 次全面复审引入

## 1. 漂移与范围

复审对象和历史链身份复算一致，关键代码无实质漂移。仅原地升版 P02 子方案；需求、总纲、Pxx、业务代码和受保护现场均未改。v2 在升版前已用 `git hash-object -w` 持久化，解决审核记录指出的历史字节不可恢复问题。

## 2. AUD 处理

| AUD | 接受度 | v3 修改 | 闭合判据 |
|---|---|---|---|
| AUD-001 | 同意 | §3.4 固定 error.details oneOf；§4.1 展开 source/transaction/limits/capacity/fingerprints/risk/diff/validation/review 以及五 result；§4.3 使用受 opcode 约束的 IL operand discriminated union并拒绝旧 FullName tag。 | 注册表 schema 可由独立 Draft 2020-12 validator 验 success/所有 error/22 branch，方法/字段/类型/Ref/Spec 均只按 token/object ID 往返。 |
| AUD-002 | 同意 | parameter target 增加 owner method+index；Sequence=0 固定拒绝，return type 只走 method_update；无 ParamDef 槽在 update 时物化、在尾部 remove 时可直接移除。 | return、无/部分/完整 ParamDef fixture 全覆盖，失败四组事实不变。 |
| AUD-003 | 已闭合 | 不改 v2 完整 module/PDB image 指纹。 | 继承第 2 次复审通过项。 |
| AUD-004 | 同意 | request ID 拆成 session begin、transaction apply/review、session terminal rollback 生命周期；异 ID apply/review 立即 busy；rollback 取消并锁外等待；锁内清理后锁外异步 SetResult。 | 跨事务旧 ID、异 ID、rollback、continuation 反入屏障矩阵全部有唯一响应/顺序。 |
| AUD-005 | 同意 | begin/apply/review/rollback 使用独立有界缓存；apply 满载不阻止 review/rollback；计时固定预热 3 次+连续 10 次且任一超 1000ms 失败。 | 项数/字节精确满载后新 review、重放、rollback、close、timeout 及双架构原始计时可判定。 |
| AUD-006 | 同意 | 四个测试 seam 展开封闭 input/result 和 fault 生命周期；动态五态 oneOf 固定未创建 artifact 为 false/null/null；primitive 规定新对象预构造边界并逐 kind 给精确 live step 顺序。 | 五态 golden、四 seam schema、oracle/manifest/trace/coverage 四集合和多边 fault 逐点通过。 |
| AUD-007 | 已闭合 | 不改三条 apply/restore 路径。 | 继承。 |
| AUD-008 | 已闭合 | 不改端口变化案例。 | 继承。 |

全部意见同意，无部分同意/驳回。另完成低风险机械修正：横切约束显式枚举六个 ACC、修复 TypeSig Markdown、章节顺序改为 §3.3/3.4/3.5。

## 3. 影响与不变量

实际 diff 影响公共 schema、参数操作、并发/缓存、测试 seam 与验收 oracle；不改变工具数量（五产品工具）、22 operation kind、P02/P03 顺序和职责。继承不变量：全进程单事务、owner 只来自 P01、私有编辑不改 live、结构错误不可确认、测试实时应用同调用恢复、unknown 阻断、无产品 commit/checkpoint/export/Undo/原始编辑。

需求保真重做：应承接 6、完整 6、未落点 0、语义弱化 0、不可验收 0；异常 ID 无。IMP-001..009 与 ACC-001/002/003/009/010/026 唯一、未重编号、均有上游和独立动作/精确预期/失败边界。无后序依赖或未声明 fallback。

## 4. 再次审核判定

v3 再次改变公共 schema、并发线性化和 fault/恢复测试契约，命中“核心数据流/并发一致性/恢复策略”条件；虽影响限定且可回滚，仍需全面复审，不能由作者直接放行。

- 复审基准：v3 blob `c18dc7e2cd6b73f29a3045a8fbddf5760347528b`
- 模式：全面复审
- 范围：AUD-001..008、六 RACC 保真、五产品/四测试 schema、22 kind、参数/泛型、完整指纹、异 ID/rollback/cache、动态五态、primitive oracle、容量/时限、端口和 P02/P03 不变量。
- 通过：AUD 全闭合，无新增中高风险不同意见，保真 6/6/0/0/0。

当前状态：`未审核, 不得实施`。实施授权保持进行中；下一阶段为再次全面第三方审核。
