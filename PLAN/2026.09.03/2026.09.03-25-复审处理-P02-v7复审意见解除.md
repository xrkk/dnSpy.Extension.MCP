# P02 v7 复审意见解除处理记录

- 记录类型：复审处理（创建后不可修改）
- 记录版本：v0
- 日期：2026-09-03
- 会话角色：作者/执行方
- 原审计：`2026.09.03-24-复审-P02-v7复审意见闭环.md`，blob `0a7e6cc39e3ee1bd7c73694ee399ed4aed3a9f58`
- 处理前方案：P02 v7，blob `295f3ea3cfd8f29c6edfa141e04eb4113c22a9cd`
- 处理后方案：P02 v8，blob `e1ff1f118c0f985df99b20738d68badad17ce448`
- 总纲：v1，blob `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a`
- 需求：v0，blob `22574a9054a5016e8a2899f0508d0069e7742843`
- 本地基线：分支 `feature/p01-public-contract-vm-gate`，HEAD `236d023c15990bd8240f0f8af98c8b8f8cad3c04`

## 1. 处理结论

| AUD | 接受度 | 实际处理与证据 | 最小闭合条件 |
|---|---|---|---|
| AUD-001 | 同意 | `operation-lowering.json` 新增 14 个带稳定 ID、精确 context/type arity/method arity/预期结果的必需 TypeSig 向量；独立 validator 固定同一完整集合并执行独立 parser。删除必需向量的变异 `typesig-required-vector-deleted` 命中 `type-sig:required-vectors-exact`。 | 已满足；删除、改写或遗漏必需向量不能通过。 |
| AUD-003 | 同意 | EmbeddedPdb live target 改为 `MethodDef.Body.Instructions[first SequencePoint != null].SequencePoint.Document.Url`，selector 固定选择实际带 sequence point 的 instruction。`tools/DnlibFacts/Program.cs` 以直接成员访问针对固定 dnlib 4.5.0 编译成功，提取的 `public_api_paths` 被 dependency facts、contract index、evidence index 和 validator 共同绑定；二次提取逐字节相等。 | 已满足；不再使用不存在的 `PdbMethod.SequencePoints`。 |
| AUD-005 | 同意 | review lifecycle 改为带 transition ID、数值 guard、entry limit 和清理结果的结构化行；固定 tombstone schema，单条独立上界 3,168 bytes，64 条 202,752 bytes，槽为 262,144 bytes。独立计算发现 rollback 完整响应上界 517,179 bytes，故原 64 KiB 不可行，最小上调为 1 MiB。将 tombstone entries/bytes 或 rollback slot 改为 1 的变异分别命中稳定容量错误。 | 已满足；常量、序列化上界和生命周期共用同一机器闭包。 |
| AUD-006 | 同意 | fault suite 从宽松字符串改为结构化 `per_call` 与 `runner` 契约，精确固定 actual trace 的整调用有序前缀/armed 终点、covered 关系，以及 240 个新事务、240 份按 fault ID 唯一产物、covered 并集恰好一次。独立 validator 精确比较完整结构；弱化终点和复用产物的变异均失败。 | 已满足；不再依赖关键词字符串。 |
| AUD-002/004/007/008 | 继承闭合 | parameter、并发/waiter/generation、测试 apply/restore 三终态和显式端口重连契约均未改动；122 项检查及完整 diff 复核未发现回退。 | 原结论继承。 |

## 2. 实际 diff 与影响分类

修改仅位于 P02 方案和 `PLAN/2026.09.03/P02-contract/`：

- 规范源增加 14 个必需 TypeSig 向量、结构化 review lifecycle/fault suite、tombstone 与 rollback 独立容量上界；
- dnlib facts 提取器与固定事实增加已编译的 PDB API 路径；
- validator 增加独立集合、API、容量、生命周期和 fault suite 检查；
- mutation validator 从 14 项增至 19 项；
- 六个规范生成物、两个运行报告和两个 index 依其单一源重生成；
- P02 方案升为 v8 并更新身份、数值和执行契约。

受影响通过项：`IMP-001/002/003/004/005/006/007/008`，`ACC-001/002/003/009/010`，`RACC-003/009`。未受影响继承项：P01 权威会话/VM/调试门禁、五产品工具边界、P02/P03 分工、`ACC/RACC-026`、NON 与 fallback。全局不变量保持：P02 不提供无检查点产品提交，不引入原始编辑、磁盘副本、自动端口探测或后序 P03-P09 依赖。

## 3. 验证与身份

- generator：退出 0；contract source SHA-256 `6a6cd996c846bc9b81a19719cccd7199c9d77a4ec3e8e17bb4c35d069bff6826`。
- independent validator：退出 0，122/122，错误 0，六个 RACC 全 true，需求保真 `6/6/0/0/0`。
- mutation validator：退出 0，19/19 PASS；报告 SHA-256 `f1ca8214b808fbda8867a6b4dd52c0f643b9f70ae1886e8fb8c8a7b15cc5450f`。
- dnlib facts：固定 4.5.0 DLL 两次提取 `cmp` 退出 0；事实 SHA-256 `7ee0846ce7a51bc6bc13e03a28f399180099a53ad2feb98e7b4cac90618ded8d`。
- evidence index：SHA-256 `9388e174f46eadaf1000a68a59026732e4a5a7eea8eaaaf0bbab447a7e1aec8a`。
- `git diff --check -- PLAN/2026.09.03`：退出 0。

## 4. 再次审核判定

本轮修改没有跨项目或多业务共享契约，没有不可逆迁移、权限/安全/隐私变化，没有改变总纲架构、Pxx 顺序、核心数据流或恢复策略；全部变化被限制在 P02 待实施契约和隔离生成证据，可由删改这些未提交文件完整回滚。不存在多个仍待选择的方向。

但原审计已因同根因进入审核循环熔断。为解除熔断并取得可实施资格，不能仅以作者自评替代独立判断：请求一个全新隔离第三方审核方对 P02 v8、上述四项闭合证据和全部继承不变量执行全面复审。复审范围为 P02 v8、完整 P02-contract、审计 24 与本文；通过条件为需求保真 `6/6/0/0/0`、四项破坏性反例均 fail closed、真实 dnlib API 证据成立、无新中高风险缺口。

当前状态保持：**未审核, 不得实施**。实施授权由用户此前明确授予并保持进行中，但在复审闭环前不得进入 IMPLEMENT。
