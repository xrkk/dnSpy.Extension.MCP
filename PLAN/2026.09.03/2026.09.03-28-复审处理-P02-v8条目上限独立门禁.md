# P02 v8 条目上限独立门禁复审处理

- 记录类型：复审处理（创建后不可修改）
- 记录版本：v0
- 日期：2026-09-03
- 会话角色：作者/执行方
- 原审计：`2026.09.03-27-复审-P02-v8复审熔断解除.md`，blob `090e63f5e75f624c735c4a01f923d44c8151ebaa`
- 处理前方案：P02 v8，blob `e1ff1f118c0f985df99b20738d68badad17ce448`
- 处理后方案：P02 v9，blob `dd606e77149fb138839bf46ae382890045ade787`
- 总纲/需求：v1 `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a` / v0 `22574a9054a5016e8a2899f0508d0069e7742843`
- 本地基线：`feature/p01-public-contract-vm-gate` / `236d023c15990bd8240f0f8af98c8b8f8cad3c04`

## 处理

同意 `AUD-005`。复审 27 的 entries-only 反例证明 v8 仅验证“当前 limit 与 lifecycle 自洽”，没有独立守住已冻结的 64 条/第 66 次拒绝。v9 作最小修改：

1. `validate_contract.py` 独立断言 `review_tombstone_entries == 64`，稳定错误为 `capacity:review-tombstone-entries`；原有 schema/byte/lifecycle/rollback 上界检查保持。
2. `test_validator_mutations.py` 新增 `review-tombstone-entries-one`，只把生成契约的 entries 改为 1，不改 256 KiB byte slot 或 1 MiB rollback slot；validator 退出 1 并命中上述稳定错误。
3. 重新运行 validator 为 123/123 PASS、错误 0；mutation suite 为 20/20 PASS；evidence index SHA-256 `929e4058687dbb6af95553da59e1a793466a58f759a184e8204d07b6cdea3912`。

`AUD-001/003/006` 按复审 27 已闭合；`AUD-002/004/007/008` 继承闭合。实际 diff 仅修改独立 validator、变异测试、两份报告、evidence index 和 P02 方案身份；规范源的 64、容量 schema、生命周期、总纲架构、Pxx、IMP/ACC、产品接口和 P02/P03 边界未变。

## 影响与复审请求

受影响项仅为 `IMP-001/003/005/008`、`ACC/RACC-009` 的验收门禁；其他通过项和全局不变量继承。没有跨项目、权限、安全、迁移、架构、核心数据流或恢复策略变化，修改可由移除单一断言与变异完整回滚。

由于同一 `AUD-005` 已再次被判审核循环熔断，作者不自行宣布解除。请求全新隔离审核方作定向复审：基准为审计 27；纳入 P02 v9 的 §3.3、`validate_contract.py`、`test_validator_mutations.py`、validation/mutation reports、evidence index；待回答问题仅为“规范源 entries=1 且其余槽位不变，经正常重生成后是否稳定失败并令 RACC-009 false”。排除并继承审计 27 已闭合的 `AUD-001/002/003/004/006/007/008`、总纲/需求/P01 和其余生成契约。通过条件为该 source-level 反例命中 `capacity:review-tombstone-entries`，正向 123/123、20/20，需求保真 `6/6/0/0/0`，且无由本次修改引入的新缺口。

当前状态：**未审核, 不得实施**。实施授权保持进行中；未进入 IMPLEMENT。
