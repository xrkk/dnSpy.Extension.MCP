# P02 单一可执行契约源与引用式方案复审结果处理

- 记录版本：v0（创建后不可修改）
- 日期：2026-09-03
- 会话角色：作者/执行方
- 处理轮次：审核循环熔断解除尝试后的首次引入
- 原复审：`2026.09.03-12-复审-P02单一可执行契约源与引用式方案.md`，blob `4ee470f563934adabd7679dc23f4c49502631535`
- 处理前方案：`2026.09.03-01-实施子方案-P02-事务内核私有副本统一审查常用结构化编辑.md` v4，blob `f230a60eeed58ab71c97fa86be2088fc5fc2746d`
- 处理后方案：同一 v4、同一 blob；本轮未修改方案或契约
- 总纲/需求：blob `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a` / `22574a9054a5016e8a2899f0508d0069e7742843`
- 本地基线：分支 `feature/p01-public-contract-vm-gate`，HEAD `236d023c15990bd8240f0f8af98c8b8f8cad3c04`
- 审核性质：独立全面复审结果引入
- 引入结论：**审核循环熔断，方案仍为“未审核, 不得实施”**

## 1. 身份、漂移与边界核对

原复审绑定的方案、总纲、需求、v3 Git blob、09/10 熔断链、11 作者自评、单一源、validator 和七个生成物身份均与当前磁盘对象一致。复审记录实测 blob 与记录值一致。决策关键路径没有复审后的漂移。

受保护现场仍为 `PLAN/dnSpy-MCP.md`、`PLAN/2026.08.26/独立核验/round-8/`、`tests/spikes/`；本轮未修改、暂存、回退或清理它们。P02/P03 边界、P01 前置、非目标和 fallback 均未改变。

## 2. 逐项处理

| AUD | 作者判断 | 理由与证据 | 实际修改 | 最小闭合条件 |
|---|---|---|---|---|
| AUD-001 | 同意，未闭合 | v3 的 TypeSig grammar、attributes 合法掩码/default、char 单 scalar、accessor null 规则未等价迁入单一源；当前 schema/validator 允许语义不同的实现。 | 无；熔断状态下不继续对同一方案堆叠文字补丁。 | 单一源可执行表达上述合法域和拒绝域；validator 用固定反例逐项证明，而非只核对名字/数量。 |
| AUD-002 | 同意，已闭合 | parameter update 的 private materialize、live insert 和 reverse remove 同源；return ParamDef 拒绝及 referenced/attached delete hard-fail 边界一致。 | 无。 | 保持现有生成契约和无/部分/完整 ParamDef 验收，不得回退。 |
| AUD-003 | 同意，未闭合 | mutation corpus 只有 component 标签、mode 和布尔预期；同一 TypeDef mutation 加 component 回显、以及 reorder no-op 可让全矩阵伪通过。 | 无。 | 每个 component 具有可执行 target、component-specific mutation、readback、restore 和最终指纹证据；独立 validator 能拒绝无关 mutation/no-op。 |
| AUD-005 | 同意，未闭合 | 独立构造的 schema 合法 review 为 4,435,095 字节，超过 2 MiB slot；`review_lifecycle` 未进入 capacity golden，现有 655 检查未覆盖两者。 | 无。 | 从 schema 机械推导或枚举最坏 UTF-8 序列化上限并使其小于 slot，或收紧 schema/调整容量；把连续 review 生命周期写入生成物并验证。 |
| AUD-006 | 同意，未闭合 | 240/240 fault golden row 缺 evidence schema 必填 `target`；manifest/trace/covered 的等价投影未冻结；动态五态 per-state cleanup/events 不变量未进入可执行契约。 | 无。 | fault golden 与公开 row schema逐条互验并冻结同一比较投影；五态分别冻结事件、cleanup、artifact/residual 不变量并由反例验证。 |

需求保真按第三方独立证据接受为：应承接 6、完整 4、未落点 0、语义弱化 1、不可验收 1；异常为 `RACC-003`、`RACC-009`。作者侧 11 记录中的 6/6/0/0/0 被反例推翻，不再作为门禁证据。

## 3. 实际 diff、影响与继承

本轮没有修改方案、契约源、validator、生成物或业务代码，实际契约 diff 为空。

受影响项：`IMP-001/003/004/005/006/008`，`ACC-003/009/010`；其中 `ACC-009` 当前不可验收。未受影响继承项：P01 前置、P02/P03 唯一边界、AUD-002、AUD-007、AUD-008、稳定 IMP/ACC ID、五产品/四测试入口边界、NON 和 fallback。

全局不变量保持：不发布 commit/checkpoint/export/Undo/Redo；不引入原始编辑、AsmEditor、自动端口猜测、隐藏磁盘副本或无审查实时提交；不把 P03-P09 当作 P02 验收前置。

## 4. 审核循环熔断与唯一上游解除路径

这是 AUD-001/003/005/006 在恢复尝试后继续以同一“契约各域之间缺少可执行交叉约束和独立反例验证”根因复开。根因分类：**方案抽象错误及证据不足**。不能靠再次改写方案正文或把检查计数增加到更大值解除。

解除熔断必须先在方案审核流程之外完成一个新的最小上游契约 Spike，并一次性产出以下可机械核对证据：

1. 完整操作合法域：TypeSig grammar、flags mask/default、constant/accessor 规则及拒绝反例；
2. component-specific fingerprint mutation/readback/restore recipe，而不是标签清单；
3. schema 驱动的最坏 UTF-8 大小证明、连续 review 状态生成物和 slot 相容性；
4. fault golden 与 evidence schema 的同一 row/projection，以及动态五态各自不变量；
5. 不先覆盖待验生成物、能跨 schema/golden/corpus 做反例检查、并从稳定 RACC 映射计算保真结果的独立 validator。

上述证据全部通过后，才允许据此升版方案并发起新的独立全面复审；任何一项仍依赖自然语言补足或作者硬编码 PASS 均不解除熔断。

## 5. 作者引入后复评与状态

- 来源、ID、Pxx 边界、前后序、非目标和 fallback：未漂移。
- 独立实施性：不通过；`ACC-009` 无一致 oracle，`RACC-003` 存在语义弱化。
- 需求保真门禁：不通过，后三项存在非零值。
- 再次审核判定：不执行。当前不是已闭合方案的复审路线选择，而是同根因审核循环熔断；必须先完成上游契约 Spike。
- 方案状态：保持 **未审核, 不得实施**。
- 实施授权：仍为进行中，但没有实施资格，未进入 `IMPLEMENT`。

最终结论：**审核循环熔断, 状态为审核未通过, 不得实施**。
