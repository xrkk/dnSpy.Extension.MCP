# P02 v9 条目上限独立门禁第三方定向复审记录

- 记录类型：全新隔离第三方定向复审（创建后不可修改）
- 记录版本：v0
- 复审日期：2026-09-03
- 运行平台：Codex
- 会话角色：全新隔离第三方审核方
- 复审基准：`2026.09.03-27-复审-P02-v8复审熔断解除.md`，blob `090e63f5e75f624c735c4a01f923d44c8151ebaa`
- 处理请求：`2026.09.03-28-复审处理-P02-v8条目上限独立门禁.md`，blob `e4981286feaafcdcc40ea29146fcd7e1f7bbd97f`
- 作者自评：`2026.09.03-29-自评-P02-v9条目上限独立门禁.md`，blob `c4a6f898b40a64b26ef133c3bfdcd5d797b53793`；只用于定位主张，不作为通过证据
- 精确结论：**审计通过**

## 1. 对象身份、流水号与现场

| 对象 | 独立机械核验 |
|---|---|
| P02 v9 | `PLAN/2026.09.03/2026.09.03-01-实施子方案-P02-事务内核私有副本统一审查常用结构化编辑.md`；版本 v9；状态“未审核, 不得实施”；Git blob `dd606e77149fb138839bf46ae382890045ade787`，与指定身份一致 |
| 总纲 | `PLAN/2026.08.31/2026.08.31-02-总纲-dnSpy-MCP结构化程序集编辑.md` v1；blob `bcc7b2c1c84abb619b5d0c6dc64547472b7c486a`，一致 |
| 需求 | `PLAN/2026.08.31/2026.08.31-01-需求提炼-dnSpy-MCP结构化程序集编辑.md` v0；blob `22574a9054a5016e8a2899f0508d0069e7742843`，一致 |
| P01 前置 | 分支 `feature/p01-public-contract-vm-gate`；HEAD `236d023c15990bd8240f0f8af98c8b8f8cad3c04`，与指定身份一致 |
| 流水号 | 创建前扫描 `PLAN/2026.09.03` 当日 Markdown，最大流水号为 29，目标文件不存在，故本文使用 30 |

审核开始、反例结束和创建本文前 HEAD 均为 `236d023c15990bd8240f0f8af98c8b8f8cad3c04`。创建本文前 `git status --short` 与审计 27 记录的既有现场相同：

```text
 M PLAN/dnSpy-MCP.md
?? "PLAN/2026.08.26/独立核验/round-8/"
?? PLAN/2026.09.03/
?? tests/spikes/
```

本轮没有修改、暂存、回退、清理或提交方案、契约、生成物、业务代码或既有审计记录。全部生成、报告写入和反例只发生在 `/tmp/p02-v9-audit.GGFXru/`。题面指定的 `PLAN/dnSpy-MCP.md`、`PLAN/2026.08.26/独立核验/round-8/`、`tests/spikes/` 未被本轮命令写入。

## 2. 实际定向范围与排除

纳入范围严格为：P02 v9 §3.3；`P02-contract/validate_contract.py`；`test_validator_mutations.py`；`generated/validation-report.json`；`generated/mutation-validation-report.json`；`generated/evidence-index.json`；以及这些对象引用的 `contract_source.py`、`capacity-golden.json`、`contract-index.json`、固定 dnlib facts 和 P01 `TransportLimits.cs`。

唯一待答问题为：规范源只把 `review_tombstone_entries` 从 64 攑为 1，保持 `review_tombstone_bytes=262144` 与 `rollback_slot_bytes=1048576` 不变并正常重生成后，独立 validator 是否稳定非零、包含 `capacity:review-tombstone-entries` 并令 `RACC-009=false`；官方 entries-only mutation 是否不被 byte bound 掩盖。

审计 27 已闭合的 `AUD-001/002/003/004/006/007/008`，以及总纲、需求、P01、P02 其余章节、其余生成契约、P03-P09、产品实现和既有 VM/业务测试均排除并继承。只检查这些继承对象是否漂移，不重复扩大为全面复审。

## 3. v8→v9 差异和身份复核

审计 27 保留的 v8 独立副本与其记录指纹一致。对该副本和当前 `P02-contract/` 作逐文件比较，只有以下五个文件不同：

1. `validate_contract.py`：只新增一条 `limits["review_tombstone_entries"] == 64` 的独立断言，稳定错误名为 `capacity:review-tombstone-entries`。
2. `test_validator_mutations.py`：只新增 `review-tombstone-entries-one`，只修改生成契约中的 entries，预期上述稳定错误。
3. `generated/validation-report.json`：122/122 更新为 123/123。
4. `generated/mutation-validation-report.json`：19/19 更新为 20/20，并记录新增反例。
5. `generated/evidence-index.json`：只更新上述脚本、报告身份和 123/123、20/20 计数。

关键 v8→v9 指纹为：validator `539a318e...`→`a129d264548530f9b7bef943ce6d0cf343dbdc48e411c81f475b231cd54071d4`；mutation test `26b22703...`→`f4d93650980c6294c6f72d2ebcf4bea5091c7af65c9f986ca5ad670b69a8b50f`；validation report `00ff90e6...`→`e6294c352103d0be4a545c6bb8fb13ca38ef4cf8159e860e2090b7df941f6c5b`；mutation report `f1ca8214...`→`016ad0790199a225ab62bae5145607f8f1401f38ff87f2b4c9bd822b4425492b`；evidence index `9388e174...`→`929e4058687dbb6af95553da59e1a793466a58f759a184e8204d07b6cdea3912`。

P02 文档升为 v9，§3.3 写明独立固定 64 条、123/123、20/20 和 entries-only 失败控制；规范源、容量产品值、生命周期契约、IMP/ACC、产品接口与 P02/P03 边界没有改变。新增 validator 断言是独立先验，不从生成器导入 64；因此不会随 source 和正常重生成共同降级。

## 4. 正向独立重跑与 evidence identity

在 `/tmp/p02-v9-audit.GGFXru/positive/` 中复制 P02 契约目录和 P01 `Transport/TransportLimits.cs`，保留仓库相对布局后执行：

```text
PYTHONDONTWRITEBYTECODE=1 python3 contract_source.py --output-dir generated
PYTHONDONTWRITEBYTECODE=1 python3 validate_contract.py
PYTHONDONTWRITEBYTECODE=1 python3 test_validator_mutations.py
PYTHONDONTWRITEBYTECODE=1 python3 write_evidence_index.py
diff -qr --exclude='__pycache__' /tmp/.../positive/PLAN/2026.09.03/P02-contract PLAN/2026.09.03/P02-contract
```

独立结果：

- generator 退出 0；六个规范生成物和 contract index 与仓库逐字节一致。
- validator 退出 0；123/123、errors 0、六个 RACC 全 true、`PASS`，需求保真 6/6/0/0/0。
- mutation suite 退出 0；20/20、`PASS`。
- evidence writer 退出 0；生成 SHA-256 `929e4058687dbb6af95553da59e1a793466a58f759a184e8204d07b6cdea3912`。
- `diff -qr` 无输出；重生成的脚本、依赖、全部生成物、两份报告及 evidence index 与工作区逐字节一致。

当前关键不变身份为：`contract_source.py` `6a6cd996c846bc9b81a19719cccd7199c9d77a4ec3e8e17bb4c35d069bff6826`；`capacity-golden.json` `f0b74ae4a421b3cf759c37910d87fef29ce41e68c0c86010f24e407c60cb5b54`；`contract-index.json` `af79ad25332615d95eee7f7a83e796575e853016f573d54d5489e3efe8f58bd4`；固定 dnlib facts `7ee0846ce7a51bc6bc13e03a28f399180099a53ad2feb98e7b4cac90618ded8d`。均与审计 27 相同。

## 5. source-level entries-only 独立反例

在 `/tmp/p02-v9-audit.GGFXru/entries-one/` 的另一完整相对布局副本中，只对规范源作以下改写：

```text
review_tombstone_entries: 64 -> 1
review_tombstone_bytes: 262144（不变）
rollback_slot_bytes: 1048576（不变）
```

随后正常执行 `contract_source.py --output-dir generated` 和 `validate_contract.py`。为验证稳定性，又完整重生成并重跑 validator 两次。三次报告 SHA-256 均为 `b1b43c873869ec723b94e618e4ab9a3936cefdf7afc9a047d7a0aa7657a32a97`，结果均为：

```text
generator_exit=0
validator_exit=1
checks=122/123
errors=["capacity:review-tombstone-entries"]
RACC-009=false
result=FAIL
```

正常重生成后的 capacity golden 为：entry limit 1；tombstone item 3,168 bytes；计算总量 3,168 bytes，小于保持不变的 262,144-byte 槽；rollback payload 517,179 bytes，小于保持不变的 1,048,576-byte slot；lifecycle 也与改写后的 entry limit 自洽。故 `capacity:review-tombstones`、`capacity:rollback-slot` 和 `review-lifecycle:bounded-closure` 均未失败，唯一失败来自独立固定的 entries 产品决定。这直接证明 byte bound 没有掩盖 entries 门禁，并令 `RACC-009` 确定为 false。

## 6. 官方 entries-only mutation

官方 `review-tombstone-entries-one` 只更新 `capacity-golden.json` 的 `review_tombstone_entries` 为 1，不改 byte 或 rollback slot。独立 20-case suite 中该 case 的 validator 退出 1、`passed=true`，且 `observed_errors` 明确包含 `capacity:review-tombstone-entries`。

该 generated-artifact 反例还记录 regeneration、`capacity:review-tombstones` 和 lifecycle 的跨生成物不一致；这不是 1-byte 下界，因为配置 byte slot 从未改为 1。真正排除掩盖可能性的决定性证据是上一节正常 source-level 重生成后，byte/lifecycle 检查通过而 entries 稳定单独失败。官方 mutation 与 source-level 反例相互补足。

## 7. 继承项、漂移和新增缺口检查

- `AUD-001/002/003/004/006/007/008`：按审计 27 继承闭合。除本次五个目标文件外，v8 审计副本与当前 P02 contract 逐文件相同；未发现使既有结论失效的契约漂移。
- 其余六个规范生成物及 contract index 指纹与审计 27 逐项相同：schemas `3fd48556...`、lowering `06266236...`、fault `d670a933...`、capacity `f0b74ae4...`、corpus `9b457b0a...`、requirements map `9664035f...`、contract index `af79ad25...`。
- P01 直接依赖逐项重算并与审计 24/27 一致：`McpServer.cs ccf1ba49...`、provider/registry/static provider `5849642e.../88160449.../72f1391b...`、call context/lifecycle/limits `7681698e.../6dfe5a70.../3021891f...`、debug gate/service/coordinator/cache `4277d527.../4f6a3409.../1f461bcf.../8cd19299...`、protocol/settings/IL `ed079152.../44f58fe6.../4d414088...`、项目文件/client/P01 runner `da391d58.../980ff166.../a2d83df9...`。
- 总纲、需求和 HEAD 身份均一致；未发现后序依赖、范围扩张、fallback、接口、容量数值或生命周期语义变化。
- 新断言只加强已有 `ACC/RACC-009` 验收门禁；新增反例精确覆盖审计 27 的绕过路径。报告和 evidence index 可由当前脚本确定重现，未发现本次 diff 引入的新缺口或新 AUD 根因。

## 8. 独立需求保真五项计数

从需求、总纲、P02 映射、未漂移生成契约、正向重跑和 source-level 反例重新计数：

| 项 | 计数 | 独立判定 |
|---|---:|---|
| 应承接 | 6 | `RACC-001/002/003/009/010/026` |
| 完整 | 6 | 原五项完整结论继承且无漂移；`RACC-009` 的唯一 entries 降级路径现由独立 64 常量 fail closed |
| 未落点 | 0 | 无 |
| 语义弱化 | 0 | entries=1 正常重生成后不能通过 |
| 不可验收 | 0 | generator、validator、mutation suite 与 identity writer 均可独立执行 |

后三项均为零，需求保真门禁通过。作为失败控制，entries-only source-level 反例自身报告为 6/5/0/1/0 且异常 ID 仅 `RACC-009`，证明需求级传播不是只改局部错误码。

## 9. AUD-005 判定与结论

`AUD-005` **已闭合**：独立 validator 固定 `review_tombstone_entries == 64`；只降 entries、保持两个 byte slot 不变并正常重生成后，validator 连续稳定退出 1，唯一错误为 `capacity:review-tombstone-entries`，`RACC-009=false`；当前正向证据为 123/123、20/20，evidence identity 一致。

没有外部阻碍，所以结论不是“审计被阻断”；没有不同根因或新增意见，所以不是“存在不同意见”；原 `AUD-005` 的同根因已由独立反例闭合，所以不再维持“审核循环熔断”。

因此：

- 独立需求保真计数为 `6/6/0/0/0`；
- `AUD-005` 闭合，审计 27 的其余闭合项保持；
- 新增意见：无；
- 精确结论：**审计通过**。

本文创建后不可修改；本文不修改 P02 v9、需求、总纲、契约、生成物、业务代码、既有审计记录或 Git 提交。
