# P02 v6 第三方复审结果处理

- 记录类型：复审处理（创建后不可修改）
- 记录版本：v0
- 日期：2026-09-03
- 会话角色：作者/执行方
- 原复审：`2026.09.03-20-复审-P02复审熔断解除与可执行契约闭环.md`，blob `fe23a0fba78ca7d44e27b7cbc2de92c375c69aa5`
- 处理前方案：P02 v6，blob `7e0e8600e03b4c0641695374af1ccd7aafe903f8`
- 处理后方案：P02 v7，blob `f6c1a6ec074348c6ce8cb900e1cf97ce0157772f`
- 总纲/需求：v1 `bcc7b2c...` / v0 `22574a9...`
- 分支/HEAD：`feature/p01-public-contract-vm-gate` / `236d023c15990bd8240f0f8af98c8b8f8cad3c04`
- 实施授权：进行中；`IMPLEMENT` 未进入

## 1. 逐 AUD 处理

| AUD | 处理 | 理由、实际修改和闭合条件 |
|---|---|---|
| AUD-001 | 同意 | TypeSig parser/vector 现以 `context + owner_type_arity + owner_method_arity` 为机器输入；`!n/!!n` 执行 `< arity`，新增零 arity、边界越一、Void generic argument、byref 非终结等向量。validator 真实计算，不再只检查规则字符串。 |
| AUD-003 | 同意，并删除过度设计 | 删除 61 个表/堆 raw-byte XOR 和每组件 reorder 的不可执行声明。保留需求真正要求的五条 fingerprint channel：module metadata、dnlib object、IL、managed resource、embedded PDB；每条给出同一 live `ModuleDef` 的公共 dnlib target/action/readback/restore。规范顺序只保留一个覆盖五 channel 的全局 reorder 负例。代价是“不逐个声明所有 ECMA/PDB 表和堆的人工测试 recipe”，但完整 writer image fingerprint 与五类需求风险仍保留，且不再伪造原始编辑能力。 |
| AUD-005 | 同意 | 新增 begin/apply 1/2 MiB per-response limit；独立从 schema 推导 517,179/1,712,099 bytes 并同时校验 64/4 MiB、1024/32 MiB 总缓存。review lifecycle 变为单 full slot + 最多 64 个轻量 tombstone，第 66 次新 review 零副作用 capacity error，apply/rollback/close/timeout 清零；同旧 request ID 可从同 revision 当前结果+tombstone 重建逐字节响应。新增把 apply limit 改成 1 的失败控制。 |
| AUD-006 | 同意 | actual trace 改为整次 forward→reverse 的 before/after 有序前缀，上限从 22-kind lowering 独立推导为 `2*(6+6)=24`。manifest/oracle schema 强制 `minItems=maxItems=240` 且 unique；covered unique 0/1。新增 trace cardinality、空 manifest 失败控制。 |
| AUD-002/004/007/008 | 同意继承 | 既有已闭合结论没有被上述修改改变。 |

## 2. 实际 diff、影响分类与不变量

- `contract_source.py`：契约变化；TypeSig context、五 channel corpus、cache/review closure、fault cardinality。
- `validate_contract.py`：独立 oracle 同步扩展；不导入 generator。
- `test_validator_mutations.py`：从 11 扩为 14 个失败控制。
- 六个规范 generated JSON、两报告和两个 index：按源重新生成；dnlib 固定依赖未变。
- P02 方案：v6→v7，只更新上述契约身份和实施/验收解释；稳定 `IMP/ACC/Pxx/OUT`、工具数、22-kind、错误码和顺序不变。

受影响通过项：`RACC/ACC-003/009`、`IMP-001/002/003/004/005/006/007/008` 的契约证据重新验证。继承项：RACC-001/002/010/026 的需求语义、P01 前置、P02/P03 边界、动态互斥、8 MiB 传输、dnlib opcode/mask/u8。全局不变量：五产品工具、四个仅测试入口、单事务、私有先行、无检查点产品提交、无原始编辑、无默认磁盘副本、无端口猜测或隐藏服务器 suite 状态均未改变。

## 3. 验证与需求保真

- generator：退出 0。
- independent validator：退出 0，117/117，错误 0，PASS。
- mutation validator：退出 0，14/14 PASS。
- evidence index：SHA `2a52eb297febc911a746ba24c377888c6443ff3b35cb0cfa4942226078cf352c`。
- 需求保真：应承接 6，完整 6，未落点 0，语义弱化 0，不可验收 0，异常 ID 无。

## 4. 再次审核判定

本轮修改不跨独立项目、不改变外部或多业务共享契约，不涉及不可逆迁移、权限、隐私或生产安全策略，不改变总纲/Pxx/依赖图，也可整体回滚到固定 v6 blob。但原审计结论为“审核循环熔断”，且四个意见都是当前 P02 核心可执行契约；解除必须由独立第三方对实际 v7 重新反例验证，不能由作者自评直接宣布解锁。

判定：保持 `未审核, 不得实施`，进入全新第三方全面复审。复审范围为完整 P02 v7 和 P02-contract，重点是 AUD-001/003/005/006 的最小闭合条件，同时确认继承项和全局不变量未回退。没有改变总纲或需求，不回流上层。
