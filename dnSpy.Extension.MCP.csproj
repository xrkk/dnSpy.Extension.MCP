<Project Sdk="Microsoft.NET.Sdk.WindowsDesktop">

  <Import Project="..\..\DnSpyCommon.props" />

  <PropertyGroup>
    <AssemblyName>dnSpy.Extension.MCP.x</AssemblyName>
    <Nullable>enable</Nullable>
    <UseWPF>true</UseWPF>
    <!-- The SDK implicitly globs every .cs file under the project root. Keep the
         test fixture (tests/fixtures/) and its generated obj/ out of our compile
         so building the extension doesn't pull in TestIL.cs / AssemblyInfo.cs. -->
    <DefaultItemExcludes>$(DefaultItemExcludes);tests/**</DefaultItemExcludes>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\..\dnSpy\dnSpy.Contracts.DnSpy\dnSpy.Contracts.DnSpy.csproj" />
    <ProjectReference Include="..\..\dnSpy\dnSpy.Contracts.Logic\dnSpy.Contracts.Logic.csproj" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.VisualStudio.Text.UI.Wpf" Version="$(MSVSTextVersion)" />
  </ItemGroup>

  <!--
    We use System.Net.HttpListener on both TFMs. Referencing ASP.NET Core (Kestrel) would
    compile fine, but loading the resulting extension into dnSpy at runtime fails silently:
    dnSpy's self-contained .NET bundle does not include ASP.NET Core, so TypeLoad faults
    during MEF composition and the extension's IExtension part is silently skipped.
  -->

  <!--
    System.Text.Json is in the BCL for .NET 10; on .NET Framework 4.8 it needs a package.

    The version MUST match the one dnSpy's own net48 build resolves (it arrives there
    transitively, so dnSpy declares no explicit reference we could inherit). .NET Framework uses
    strict assembly binding and dnSpy ships no binding redirect for System.Text.Json, so a
    mismatch is not a build error — the extension loads, the HTTP server starts, and then the
    first MCP request dies inside HandleHttpRequest with FileNotFoundException. (.NET 10 rolls
    forward, which is why this only ever bites net48.) See issue #21.

    tests/check-host-deps.ps1 asserts this stays in sync; CI runs it on every build.
  -->
  <ItemGroup Condition=" '$(IsDotNetFramework)' == 'true' ">
    <PackageReference Include="System.Text.Json" Version="9.0.0" />
  </ItemGroup>

  <!--
    The frozen debug contract schema (tests/debug/contracts/dnspy.debug.v1.schema.json, frozen by
    the audited plan) is embedded verbatim so the advertised debug tool inputSchemas come from the
    same single source as the canonical fixtures and can never drift. Explicit Include overrides
    the tests/** DefaultItemExcludes, which only affects default globs.
  -->
  <ItemGroup>
    <EmbeddedResource Include="tests/debug/contracts/dnspy.debug.v1.schema.json" />
  </ItemGroup>

</Project>
