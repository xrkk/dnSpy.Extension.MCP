{
 "suite": "structured-edit-p03",
 "case_id": "EDIT-ACC-005",
 "acc_id": "ACC-005",
 "target_architecture": "x64-and-x86",
 "tfm": "net48",
 "fixture": "C:\\Tools\\mcp-repo\\tests\\fixtures\\bin\\ImportHost\\ImportHost.exe (net48 console host; Machines class with iterator+async state machines; rebuilt from tests/fixtures/ImportHost.cs)",
 "driver": "p03_vm_acc005full.py",
 "precondition": "DNMCP_TEST=1; dnSpy with deployed P06 plugin (edit_import advertised); loopback 15378 healthy; ImportHost fixture built on the VM; ArtifactRoot edit dirs cleaned and dnSpy freshly started",
 "actions": [
  "tools/list advertises edit_import (P06 importer face; edit_compile proven by the p05-compile- evidence)",
  "edit_begin opens a transaction on ImportHost; edit_compile (edit_class, edited Machines with new bodies/members and a ghost System.Random sample) registers two artifacts",
  "cell1: replace_body with an explicit target whose identity mismatches (DoAsync reference, DoCoroutine token) rejects with EDIT_HISTORY_CONFLICT and leaves the private fingerprint unchanged",
  "unresolved reference: adding Dice() whose body needs System.Random rows the host never had rejects with EDIT_VALIDATION_FAILED and zero side effects",
  "cell2: replace_body with omitted target auto-matches DoCoroutine and DoAsync kickoffs exactly once; the generated async/iterator state machine subtrees land whole (missing members added, MoveNext bodies replaced)",
  "cell3/cell4: add AddedTag`1 (generic method, class constraint) on the nesting chain and get_Tag() with the explicit Machines container token",
  "edit_review returns risks; edit_commit confirms and writes the checkpoint lineage",
  "live IL asserts the edited async constant (200) and the new members (AddedTag, get_Tag)",
  "edit_export writes edit-output\\acc005full\\ImportHost-edited.exe below ArtifactRoot; the directory holds no .pdb sidecar",
  "open_files reloads the exported image (embedded symbol proof at byte level)",
  "debug_launch (net48-exe, entry) starts the exported exe; the module is found by sha256; a breakpoint on the imported DoAsync token at IL 0 is set and hit: breakpoint_hit event plus a stack frame at that token and IL offset 0",
  "run-id prefix p06-import- distinguishes this final-judgment evidence; p05-compile- evidence covers the compile-only clauses"
 ],
 "expected": "per OUT-006: stable-identity import into the private copy through frozen operations, generated subtrees whole, sequence points/scope/local/CDI transfer with embedded-only symbol output, all-or-nothing rejection for ambiguous/unresolved/mismatched targets, and a real breakpoint hit at the imported method IL 0"
}