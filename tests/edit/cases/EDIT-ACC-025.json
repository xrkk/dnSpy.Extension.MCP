{
  "suite": "structured-edit-p03",
  "case_id": "EDIT-ACC-025",
  "acc_id": "ACC-025",
  "target_architecture": "x64-and-x86",
  "tfm": "net48",
  "fixture": "C:\\Tools\\mcp-repo\\tests\\fixtures\\bin\\TestIL.dll (SHA-256 5F4B6D832A741E6E402684B4793006DCBBABD1783D64C17AF6B9B29ECBE997EE)",
  "driver": "p03_vm_acc025.py",
  "precondition": "DNMCP_TEST=1; isolated dnSpy UI instance running with deployed plugin net48; dedicated loopback/settings/fixture/ArtifactRoot/checkpoint store; driver seeds its own active lineage via a real commit when the store is empty",
  "actions": [
    "with no active transaction, select TestIL.Simple in dnSpy's native assembly tree, invoke Edit Type with Alt+Enter, change the focused Name field, and press the dialog's default OK button while recording UIA and screenshots",
    "prove the native in-memory rename with list_types, an unchanged fixture-file SHA-256, and edit_begin divergence before any test probe; use a mutate/restore test pair only to read back the already-diverged fingerprint and require exact restoration before accept",
    "edit_begin must reject with EDIT_LINEAGE_DIVERGED carrying family_id/lineage_id and leave state idle with zero side effects",
    "edit_accept_live with a wrong 64-hex fingerprint must be rejected with EDIT_HISTORY_CONFLICT and create no lineage",
    "edit_accept_live with the exact diverged fingerprint and acknowledge_new_baseline=true",
    "browse the superseded lineage (old checkpoints still readable) and the new lineage (single root, no parent, no forged operations), then begin/rollback a new transaction to prove usability"
  ],
  "expected": "the native UI change alone causes divergence without changing fixture bytes; no silent absorption on begin; wrong fingerprint never accepted; explicit accept creates a new baseline root containing the UI rename and linked to the superseded lineage; old package remains readable; the accepted root operation envelope is empty; coordinator usable after accept"
}
